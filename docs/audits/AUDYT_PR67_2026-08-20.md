# Audyt PR #67 (squash `015f715`, diff `238ff70..015f715`) — 2026-08-20 (M161)

Zakres squasha: M159 (audyt PR #66 + fixy F1–F4, pętla jakości Żywym Testerem
g1–g8: Z1 fingerprint, Z2/Z3/Z4 render, Z5+Z5b materialize-saga) oraz **M160**
(uwagi właściciela z testów: A — Selhoff Occultist i jednoczesne zgony,
B1/B2 — Seismic Monstrosaur: grupowanie ofert i etykieta poświęcanego lądu).
24 pliki (dokumentacja, 8 plików src/, 3 pliki testów, transkrypty audyt-m159).

Metoda: przegląd każdego pliku src/ hunk po hunku, weryfikacja mutacyjna
M160/A, reprodukcje w Node, `npm test` (fast) oraz `npm run build` na `main`.

## Zweryfikowane poprawne

- **F1 (madness timing, CR 702.34e):** wyjątek `madnessCast` w OBU bramkach
  castPermanent (main phase + pusty stos) — wzorzec suspend/rebound. Testy
  F1a/F1b (cleanup, tura przeciwnika) przechodzą; przegląd diff potwierdza,
  że wyjątek nie osłabia bramek dla zwykłego rzutu (`!hasFlash && !madnessCast`).
- **F2 (oferta=walidacja, L48):** `canPayMadnessCost` jest wiernym lustrem
  bramek płatności castPermanent(madnessCast) — redukcje generyczne,
  producibleMana (z phyrexian), pipy kolorowe. Oferta `cast:true` warunkowa;
  strażnik F2c „każda oferowana komenda przechodzi" obecny.
- **F3:** Revolutionist — cel ETB obowiązkowy (usunięte `optional:true`),
  komentarz wyjaśnia kontrast z Mystic Sanctuary (ADR 0022).
- **F4:** oferty madness niosą objectId/cardId; etykieta nazywa kartę w OBU
  wariantach.
- **Z1:** fingerprint uzupełniony o `regenerationShields`,
  `cantBeRegeneratedThisTurn`, `lostKeywordsUntilEOT`,
  `subtypesBeforeOverride`, `madnessReady` — weryfikacja mutacyjna Z1a–d
  w testach; bez zmiany formatu (addytywne pola obiektów i stanu).
- **Z5/Z5b:** `gameObjectDataOf` kopiuje `saga` także w gałęzi
  czystego enchantmentu; strażnik Z5b (wszystkie deskryptory mechanik przez
  materialize, weryfikacja mutacyjna) — generyczny, po polach, bez nazw kart.
- **M160/A (Selhoff Occultist, CR 603.10a):** SBA zbiera ofiary jednego
  przebiegu do listy `dying`, zdarzenia śmierci niosą `simultaneousIds`,
  a skan triggerów czyta LKI współzgonych (z fallbackiem na LKI zdarzenia
  dla tokenów — CR 704.5e). **Weryfikacja mutacyjna w tym audycie:**
  zamiana pętli fellows na pustą → 2 testy czerwone (A1/A2), przywrócenie →
  5/5 zielone. Brak podwójnego odpalania: pętla fellows odpala zdolność
  współzgonu za śmierć `died` (każdy zgon przetwarzany raz); własna śmierć
  źródła odpala wyłącznie w pętli głównej.
- **M160/B1/B2 (Seismic Monstrosaur):** `sacrificeLandId` w kluczu grupowania
  i w etykiecie (`poświęć: <ląd>`) — pola generyczne komendy, żadnych
  przypadków specjalnych po nazwie karty (ADR 0002).
- **Z2/Z3/Z4 (render):** warunek `anotherOpponentExists` → jawna etykieta
  „nieaktywny w 1v1"; strażnik katalogowy pustych opisów „: ."; rozdziały
  Sagi (I/II/III) z deskryptora w kafel i podglądzie — wszystko z danych,
  nie z nazw kart.
- **FoW/ADR 0017:** zmiany nie dotykają widoku ukrytych informacji; pola jawne w eventach (simultaneousIds, cardId ofert madness) to informacje jawne.

## Znaleziska

1. **D1 — dokumentacyjne (naprawiane w tej sesji): praca M160 nie istnieje
   w `docs/PROJECT_STATE.md`.** Squash PR #67 zawiera commit(y) M160 (5 testów
   w `test/m160-uwagi-wlasciciela.test.js`, zmiany w `state-based.js`,
   `triggers.js`, `render.js`), a najnowsza sekcja PROJECT_STATE to M159
   („2491/2491, 2127.4 kB"). Stan faktyczny na `main`: **2496 testów (fast),
   build 2130.5 kB**. Sekcję M161 tej sesji rozpoczynamy backfillem M160,
   żeby licznik testów i opis stanu były prawdziwe (ADR 0013 §4).
   Brak wpisu w HANDOFF M159 także o uwagach A/B — kolejność pracy po
   crashu GH_TOKEN prawdopodobnie przerwała dokumentację; kod i testy są
   kompletne, więc to wyłącznie luka dokumentacyjna.

## Obserwacje podejmowane w tej sesji (zlecenie właściciela)

Zasada właściciela (2026-08-20, po audycie PR #66): **nie zostawiamy
nieobsłużonych sytuacji zależnych od przyszłych kart — kod mechaniki ma być
gotowy, gdy taka karta się pojawi; martwa dziś ścieżka musi być zasygnalizowana
(test + strażnik), żeby o niej nie zapomnieć.** Konkretnie (obie z audytu
PR #66, sekcja „Obserwacje bez zmian kodu"):

- **O1** — `resolve_madness_cast` woła bezwarunkowo `castPermanent`;
  pierwsza karta instant/sorcery z madness dostanie reject
  „nie jest zagrywalnym permanentem". Wymagany routing po `kind` do ścieżki
  czarów (oferta per cele — wzorzec suspend/epic) + płatność kosztu madness.
- **O2** — `hasColorManaForObject` w `castPermanent` (i lustrze
  `canPayMadnessCost`) sprawdza przy madness pipy KARTY zamiast kolorów
  kosztu madness; dla Revolutionista ({5}{R} vs {3}{R}) wynik identyczny.
  Ta sama klasa latentna dotyczy `warpCast` (warp.colors vs pipy karty;
  Weftblade {5}{W} vs {2}{W} — dziś również identycznie). Bramkę zawężamy
  do kolorów AKTYWNEGO kosztu alternatywnego (madness/warp), bez zmiany
  zachowania dla dzisiejszego katalogu.

## Stan bazowy (potwierdzenie)

- `npm test` (fast): **2496/2496** zielone.
- `npm run build`: **51 modułów / 2130.5 kB**.
- `node --test test/pr66-audit-fixes.test.js`: 7/7;
  `test/m160-uwagi-wlasciciela.test.js`: 5/5;
  `test/m159-zywy-tester.test.js`: 9/9.
