# Audyt PR #58 — 2026-08-18 (M130–M140, 30 commitów)

**Zasada (ADR 0016):** każda sesja zaczyna od audytu całego poprzedniego PR — przegląd kodu po scaleniu, nie mapowanie plików. Metoda: sonda behawioralna (headless `setupCardMatch` + `playerView` + `execute`), skan deskryptorów (każdy `effect.type` / `trigger.event` użyty w `card-data.js` ma obsługę w `engine/` i odwrotnie), oraz testy izolowane per reguła CR.

**Zakres PR #58 (od 64731bd):** M130–M133 (mulligan, proporcje lądów, `damage_fizzled`), M134–M137 (kompletność logu, wycena scry/surveil, sonda wizardów, kontrakt `addObject`), M138 (11 znalezisk Żywym Testerem, 3 detektory), M139 (wycena tapowania `tapTimingBonus`), M140 (5 znalezisk: `transformedCharacteristics`, `isToken`, `goad`, `hiddenFromViewer`, `originalBeforeAnimation`).

**Wynik audytu:** PR #58 **nie wprowadza nowych naruszeń CR** w obszarach, które pokrywa. Mechaniki pojedyncze są szczelne (potwierdzone fuzzerem 200 partii po każdej komendzie — inwarianty CR 208.1, 111.7, 704.5m/n, 122.3, 400.7 + bilans stref). Pozostałe ryzyko siedzi na **stykach mechanik**, których PR #58 nie dotykał — te same, które wskazał handoff jako kierunki na następną sesję:

| Styk z PR #58 | Stan po PR #58 | Ryzyko |
|---|---|---|
| `station` (Spacecraft) + `animateUntilEndOfTurn` (Skilled Animator) | `station` synchronizowane tylko w `addCounter`/`removeCounter` | `clearStatModifiers` odtwarza `originalBeforeAnimation` bez resync — po 9+ charge wraca do artefaktu (M141/A) |
| `create_copy_token` (Cogwork Assembler) | kopiuje `kind/power/types/...` ale nie `station`/`saga` | token-kopia spacecraft/sagi bez progu/historii (M141/B, M141/D) |
| `aura.chooseColor` (Benevolent Blessing) | `defineCard` ma flagę, `createGameObject` gubi `chooseColor`/`keepOwn` | `pendingColorChoice` nigdy nie powstaje, `removeIllegalAttachments` zdejmuje własną aurę mimo klauzuli Oracle (M141/C) |
| `legalTargetCandidates` vs `validateTargets` dla `protection` | oferta filtrowała tylko `isProtectedFromSource` (jakość) | biały czar na cel z `protection from white` oferowany, walidacja odrzuca — `illegal_spell` w benchmarku (M141/E) |
| `aggro-bot` `simple` | lista `resolve_*` zatrzymała się na M129 | `resolve_color_choice`/`resolve_index_choice` bez obsługi → `Kontroler nie znalazł ruchu` (M141/E) |

**Wniosek:** PR #58 jest zielony (`npm run test:all` 2248/2248, `npm run build` 51/1912.8 kB, benchmark 63.1/90.5/76.8% — 1918/2496, fuzzer 288 partii 0 naruszeń). Usterki, które wyszły w M141, **nie były regresjami PR #58**, tylko ujawnieniem luk na stykach, które PR #58 nie ruszał. Dlatego M141 naprawia je u root cause, po deskryptorach (ADR 0002), z testami `m141-*.test.js`.

**Rekomendacja:** utrzymać wzorzec M141 — fuzzer semantyczny (zdarzenia vs delta stref) + skan styków deskryptorów + audyt `playerView` vs `state` (FoW przez nierozróżnialność, L45).
