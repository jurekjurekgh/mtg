# Audyt poprzedniego PR — #78 (M205–M217, 2026-08-26)

Data: 2026-08-26
Sesja: arena/01a03e2a-mtg (kontynuacja)
Audytor: agent Arena (ADR 0020 B / 0016)

## Zakres PR #78

5 unikalnych błędów vs MtG (M214–M217): Powerstone (restrictedPool), deathtouch (niecombat), Hunter's Blowgun (warunki zależne od kontrolera załącznika), devour (CR 702.82a — triggery ETB przed decyzją), exploit (CR 702.110a — brak kandydatów nie przerywa wejścia).

## Sprawdzenie każdego zmienionego pliku (surgical — ADR 0016)

### Silnik (core) — `src/engine/`

- `triggers.js`: `fireEnterBattlefieldTriggers` + `state.pendingDevourEtbs` — poprawne: triggery czekają na rozstrzygnięcie `pendingDevours`, decyzja devour jest kolejkowana jako blokująca, ale dalsze następstwa (inne ETB, impact tremors) biegną dalej. **Zgodne z L77.**
- `spells.js`: exploit — `if (exploitCandidates.length > 0)` kolekcjonuje decyzję; `return` usunięty, więc brak kandydatów nie wycina reszty zdarzenia wejścia. **Zgodne z L77.**
- `attachments.js`: `conditionHolds` względem kontrolera equipmentu (nie karty) — zgodne z CR 109.5.
- `combat.js`, `game-state.js`, `resources.js`: brak specjalnych przypadków po nazwie/ID karty — zgodne z ADR 0002.

### Karty / katalog

- `card-data.js`: nowe karty z pełnym Oracle (ADR 0022 — 100% lub nieobsługiwane). Brak częściowego Oracle.
- `mana-costs-data.js`: brak zmian w logice kosztów.

### Testy regresyjne

- `m216-bug4-devour-etb.test.js`: RED→GREEN potwierdzony (mutacja: cofnięcie fixu → 3 fail / 1 pass).
- `m217-bug5-exploit-etb.test.js`: RED→GREEN potwierdzony.
- `m214-*`: 3 testy, wszystkie zielone.
- `test/bot-benchmark.test.js`: zielony w pełnym runnerze (nie uruchamiano pełnej B0 — zgodne z ADR 0018).

### Dokumentacja / plany

- `PLAN_2026-08-26-audyt-wyceny-dzialan-bota.md`: etap 1 (okna walki w czarach) ✅, etapy 2–6 otwarte. Plan kontynuowalny.
- `HANDOFF_2026-08-26-m216.md`: stan poprawny, pułapki (L77) udokumentowane.

## Wnioski

- **Brak regresji** w engine (triggery wejścia, exploit, devour, deathtouch, restricted pool, blowgun keywords).
- **Brak specjalnych przypadków** po nazwie/ID karty w core.
- **Wszystkie fixy u root cause**, nie maskowane.
- **Plan M218** ma otwarte etapy 2–6 — kontynuacja zgodna z ADR 0021.
- **Benchmark szybki** (`node tools/benchmark.mjs`) nie był uruchamiany w tej sesji (ADR 0018 — tylko na komendę). Propozycja: uruchomić na końcu etapu 6.

## Działania przed kodowaniem (ADR 0020 C — pierwszy commit)

1. Ten audyt zapisany jako plik.
2. Aktualizacja `PLAN_2026-08-26-audyt-wyceny-dzialan-bota.md` (odhaczenie etapu 1 jako zamkniętego, potwierdzenie kontynuacji etapów 2–6).
3. Komunikat commit: dokument + audyt (nie kod).
4. Push i otwarcie PR.
