# Audyt scalonego PR #135 — 2026-09-24b

**Zakres:** `main` `f9bc44b` (squash PR #135, sesja 2026-09-24; 227 plików, +8645/−1850)
**Baza porównania:** `4f75e22` (squash PR #134)
**Metoda:** przegląd diffu `4f75e22..f9bc44b` + **weryfikacja mechaniczna** cytatów CR
wobec dosłownego tekstu CR i dowody mutacyjne na pinach (ADR 0030, L13/L34/L159).
**Decyzje:** ADR 0020 B (audyt poprzedniego scalonego PR), ADR 0016 (docs = archiwum).

## 0. Źródło reguł (ADR 0030)

Dosłowny tekst Comprehensive Rules, wydanie **2026-09-25** (Reality Fracture), pobrany
2026-09-24 z mirrora `nwgarne/mtg-data` (plik `rules/cr-raw.txt`, serwowany z
`raw.githubusercontent.com`; źródło pierwotne: `media.wizards.com/2026/downloads/MagicCompRules%2020260925.txt`).

- SHA-256: `8d860e451f20f38865b725b42d82feb714c725373dd8f3b32b8652b3eeb070ca` (zgodny
  z `rules/manifest.json` mirrora: `rule_count` 3165, `glossary_count` 741);
- plik trzymany **poza repo** (`/tmp/cr.txt`, 977 752 B) — pobranie:
  `curl -H "Accept: application/vnd.github.raw" -o /tmp/cr.txt https://api.github.com/repos/nwgarne/mtg-data/contents/rules/cr-raw.txt`
  (sandbox: `raw.githubusercontent.com` i `media.wizards.com` są blokowane; `api.github.com` działa);
- **Uwaga o wydaniu:** poprzednia sesja (PR #134/#135) pracowała na tym samym wydaniu
  (komentarze strażników podają „2026-09-25” i link do pliku 2026-09-25) — audyt nie
  wykrył rozjazdu wydań, więc numery weryfikowane w tej sesji są porównywalne.

## 1. Bramy i reprodukcja

| brama | `4f75e22` (baza) | `f9bc44b` (PR #135) | w tym audycie |
| --- | --- | --- | --- |
| `npm test` | 6304/6304 | **6455/6455**, 0 fail, ~226,5 s | odtworzone, zielone |
| `npm run build` | 59 modułów / 4124,0 kB | **60 modułów / 4199,5 kB** | odtworzone |

Nowe moduły w PR: `src/engine/timestamps.js` (+41 linii) i `tools/table-tester/x-wizard.mjs` (+65).
Testy: 177 plików, +4226/−393; nowe pliki m.in. `test/audyt-d4b-2026-09-24-warstwa-{4-i-nowy-obiekt,6,7}.test.js`
oraz cztery strażniki cytatów CR.

## 2. Narzędzia audytu (poza repo; Etap C przenosi jeden z nich do repo)

| narzędzie | co robi | wynik |
| --- | --- | --- |
| `cr-check.mjs` | czy cytowany numer istnieje + mapa „numer → tytuł sekcji” dla 701/702/122/712 | 2533 cytaty / 438 numerów w `src/` |
| `cr-strict.mjs` | to samo, ale **ścisłe**: cytat z literą musi istnieć dokładnie | 57 wystąpień / 18 numerów (w tym 2 celowe fixture'y strażników) |
| `cr-pairs.mjs` | pary „nazwa mechaniki w oknie ↔ numer” | 816 par, 14 flag — **1 trafienie** (F-2) |
| `cr-name-pair.mjs` / `cr-name-all.mjs` | pary z wzorca „Nazwa (CR n)” | 346 i 394 pary, 14 i 62 flagi — **3 trafienia** (F-2) |

Wszystkie flagi przejrzane ręcznie; reszta to false-positives heurystyki (cytat dotyczy
innej reguły obecnej w tej samej linii, np. `z exile (CR 400.7)`, `destroy + indestructible (CR 702.12)`).

## 3. Znaleziska

### F-1 (średnie, **sprzed PR #135**) — 16 cytatów wskazuje reguły, których nie ma w CR 2026-09-25

Cytat nieistniejącego numeru to fałszywe „zweryfikowane u źródła” (ADR 0030): następna
sesja czyta nie tę regułę i „potwierdza” bzdurę (ta sama klasa co L164/L165).

| cytowany numer | wystąpień | pliki (wybór) | co CR ma dziś | poprawny cytat |
| --- | --- | --- | --- | --- |
| `103.7a` | 26 | `game-state.js` ×4, `test/*` ×22 (auto-dobieranie, draw, full-turn, library-loss, m257r5b, mtg-rules-fixes, table-ui…) | 103.7 = Planechase (start), 103.8a = „the player who plays first skips the draw step of their first turn” | **`103.8a`** |
| `110.6b` | 5 | `objects.js:218`, `test/bug-hunt-2026-08-14-sherlock.test.js` ×4 | 110.5b = „Permanents enter the battlefield untapped, unflipped, face up, and phased in unless…” | **`110.5b`** |
| `117.11` | 4 | `abilities.js:1949/1994`, `render.js:3306`, `test/batch51-kart.test.js:112` | 117.1–117.7; proces aktywacji zdolności = 602.2b („identical to the process for casting a spell listed in 601.2b–i”) | **`602.2b`** (`601.2h` zostaje) |
| `110.6` | 3 | `objects.js:216`, `test/bug-hunt-2026-08-14-sherlock.test.js` ×2 | 110.5d = „Only permanents have status. Cards not on the battlefield do not.” | **`110.5d`** |
| `700.4c` | 3 | `triggers.js:2481`, `test/m200-uwagi-wlasciciela.test.js` ×2 | 700.4 = „The term dies means ‘is put into a graveyard from the battlefield’” (bez podreguł) | **`700.4`** |
| `510.5` | 2 | `combat.js:391`, `test/bug-hunt-2026-08-11.test.js:21` | 510.4 = pierwszy przebieg obrażeń (first/double strike); 510 kończy się na 510.4 | **`510.4`** |
| `603.4b` | 2 | `heuristic-bot.js:4452`, `test/m257-uwagi-z-testow.test.js:5` | 603.4 bez podreguł; „wszystkie cele nielegalne → efekt nic nie robi” = 608.2b | **`608.2b`** |
| `116.3c` | 2 | `test/m314-veteran-okna-walki.test.js:25/182` | 117.3b = „The active player receives priority after a spell or ability resolves” | **`117.3b`** |
| `706.10c` | 1 | `game-state.js:4024` | 706 = rzut kością; kopiowanie czaru = 707.10c („the new targets must be legal”) | **`707.10c`** |
| `708.9f` | 1 | `game-state.js:5441` | 708.9 = odsłanianie zakrytych przy zmianie strefy; day/night = 731.2a/b („as the second part of the untap step”) | **`731.2a/b`** |
| `708.9c` | 1 | `test/daybound-nightbound.test.js:82` | 702.145d = „if it’s neither day nor night, it becomes day” | **`702.145d`** |
| `106.2b` | 1 | `mana-sources.js:276` | 106.2 bez podreguł; pięć kolorów = 105.1 | **`105.1`** |
| `704.10` | 1 | `render.js:5513` (komunikat dla gracza) | 704.5c = „If a player has ten or more poison counters, that player loses the game” | **`704.5c`** |
| `602.2c` | 1 | `test/batch39-kart.test.js:126` | 602.2a = zdolność powstaje na stosie; rozstrzygnięcie po pasach = 117.4 | **`602.2a`** |
| `116.3b` | 1 | `test/m314-veteran-okna-walki.test.js:70` | jak wyżej — 117.4 | **`117.4`** |
| `720.4a` | 1 | `test/m337-akcje-opcjonalne-blokada.test.js:107` | 720.4 bez podreguł (omen cards); koncesja = 104.3a | **`104.3a`** |

**Dowód, że to nie regresja #135:** liczby wystąpień są identyczne na `4f75e22` i `f9bc44b`
(`git show <commit>:<plik> | grep -c`), a `git diff 4f75e22..f9bc44b` nie dotyka tych linii:
```
src/engine/objects.js:216/218  110.6 / 110.6b  → 2 / 2 wystąpień na obu commitach
src/engine/combat.js:391       510.5           → 1 / 1
src/engine/abilities.js        117.11          → 2 / 2
src/table/render.js            704.10          → 1 / 1
src/engine/game-state.js       706.10c         → 1 / 1
```
Kampania renumberingu z #135 (M425: „553 rozjazdy numeracji 701 naprawione”) objęła
sekcje **701/702/712** — poza nimi nie było strażnika, więc liczby z innych sekcji zostały.
Dwa dodatkowe „braki” raportowane przez skaner to **celowe fixture'y** strażników
(`CR 701.99` w `test/cr-numery-701-tabela-straznik.test.js:217`, `CR 702.404` w
`test/cr-numery-702-tabela-straznik.test.js:269`) — nie są znaleziskami.

### F-2 (średnie) — 3 rozjazdy pary „mechanika ↔ numer”, przepuszczone przez strażniki okna

| miejsce | cytat | poprawnie | dlaczego strażnik przepuścił |
| --- | --- | --- | --- |
| `src/engine/effects.js:959` | „…wraca do grobu właściciela (jak counter_spell, **CR 701.2a**)” — chodzi o kontrowanie czaru | **`701.6a`** („To counter a spell or ability means to cancel it… A countered spell is put into its owner’s graveyard”) | alias `activat` dla 701.2 trafia w słowo „activated” w linii obok (±4 linie) |
| `test/batch39-kart.test.js:165` | „// **Destroy (CR 701.19)**” | **`701.8`** (Destroy) — 701.19 to Regenerate, i o regeneracji jest ten test | w oknie ±4 linie jest słowo „Regenerate”, a to alias *cytowanego* numeru |
| `src/engine/resources.js:1808` | „// Surge/**Cleave** (CR 702.117…)” | **`702.117` (Surge) / `702.148` (Cleave)** — jedna liczba na dwie mechaniki | nazwa „Cleave” nie występuje w tabeli 702 w tej samej roli |

Pozostałe flagi heurystyk (13 z `cr-pairs.mjs`, 12 z `cr-name-pair.mjs`, 60 z `cr-name-all.mjs`)
przejrzane ręcznie — poprawne (np. `Saddle (CR 702.122a/702.171a)`, `Flying/reach (CR 702.9/702.17)`,
`poison counter (CR 702.164)`, `Lethal-first (CR 510.1)`).

### F-3 (procesowa) — brak strażnika ISTNIENIA numeru; strażniki par są heurystyczne

- `test/cr-numery-*-tabela-straznik.test.js` pokrywają **tylko 701 i 702**, a
  `test/audyt-pr134-2026-09-24-cytaty-cr.test.js` pilnuje listy **konkretnych** przestarzałych
  numerów. Numer z sekcji 1xx/2xx/5xx/6xx/7xx nie jest sprawdzany przez nic (stąd F-1).
- Detektory okna ±N linii sprawdzają, czy **jakakolwiek** nazwa/alias z tabeli jest w oknie —
  przypadkowe słowo w komentarzu obok wystarcza (dowody w F-2). To nie jest wada implementacji
  (detektor robi dokładnie to, co deklaruje), tylko **granica metody**: nazwa w oknie ≠ para.

**Rekomendacja (Etap C):** (a) strażnik istnienia numeru dla wszystkich cytatów + generator
tabeli z pliku CR; (b) dwa znalezione rozjazdy dopisać do `test/cr-numery-mechanik-straznik.test.js`
jako pary „mechanika ↔ zakazany numer”, żeby nie wróciły.

## 4. Weryfikacja twierdzeń regułowych #135 wobec dosłownego CR

Każde twierdzenie czytane w kodzie/diffie i porównane z **dosłownym** tekstem CR 2026-09-25:

| twierdzenie kodu | dosłowny CR | werdykt |
| --- | --- | --- |
| crew **nie** ustawia P/T (W-6) | 702.122a: „Crew N” means „Tap any number of other untapped creatures… **This permanent becomes an artifact creature until end of turn.**” | ✔ potwierdzone |
| późniejsze nadanie wygrywa z utratą (W-4) | 613.9 + przykład: „Applying them in timestamp order means the one that was generated last ‘wins.’” | ✔ |
| załącznik dostaje nowy znacznik przy przypięciu (613.7e) | 613.7e: „An Aura, Equipment, or Fortification receives a new timestamp each time it becomes attached to an object or player.” | ✔ (cytat dosłowny) |
| obrót twarzą do góry = nowy znacznik (613.7f) | 613.7f: „A permanent receives a new timestamp each time it turns face up or face down.” | ✔ |
| CDA to warstwa 7a, nadpisywana przez 7b (W-1) | 613.4a: „Layer 7a: Effects from characteristic-defining abilities that define power and/or toughness are applied.” + 613.4b | ✔ |
| zakryty 2/2 to wartości kopiowalne, późniejsze warstwy działają (W-2/W-3) | 708.2: „…have no characteristics other than those listed by the ability or rules that allowed the spell or permanent to be face down. Any listed characteristics are the copiable values…” | ✔ |
| hymn działa na zakrytego stwora | 708.2 (zakrycie tłumi zdolności WŁASNE) + 202.2 | ✔ |
| regenerate: tarcza zamiast zniszczenia | 701.19 (Regenerate) | ✔ numer z tabeli |
| stun = 122.1d, finality = 122.1h | 122.1d / 122.1h dosłownie | ✔ |
| poison → przegrana = SBA | 704.5c (i 104.3d / 122.1f) | ✔ |
| untapped wymagane do blokowania = 509.1a (nie 509.1b) | 509.1a: „The chosen creatures must be untapped…” | ✔ (poprawna korekta #135) |
| cloak: 2/2 z ward {2}, obrót za koszt many karty | 701.58, 702.21 | ✔ numer z tabeli |
| explore przy pustej bibliotece nic nie robi | 701.44a/b | ✔ (zgodne z M425) |
| dobranie z pustej biblioteki → przegrana | 121.4 + 704.5b | ✔ |
| kopia czaru: nowy cel musi być legalny | 707.10c: „…the new targets must be legal.” | ✔ |
| transform w miejscu nie tworzy nowego obiektu | 712.18: „When a double-faced permanent transforms or converts, it doesn’t become a new object. Any effects that applied to that permanent will continue to apply to it.” | ✔ |
| DFC: cechy twarzy / rzut przodem | 712.8 / 712.8a / 712.11 / 712.13 | ✔ (zgodne z tabelą strażnika) |
| bestow: odłączenie kończy bestow (wyjątek od 704.5m) | 702.103f | ✔ |
| zakryty permanent wchodzi nietapnięty | 110.5b (nie „110.6b”) | ✘ cytat → F-1 |

Wniosek §4: **żadne twierdzenie regułowe z #135 nie okazało się błędne** — błędy dotyczą
wyłącznie *adresu* reguły (numeru), nie jej treści.

## 5. Ścieżki wejścia na pole bitwy i znaczniki czasu (oś B1)

Sprawdzone wszystkie miejsca nadania `timestamp` (objects.js:207 przez `moveObjectDirectly`,
tokens.js:192, permanents.js:1198 `turnFaceUp`, effects.js 1165/3547/5194, game-state.js:610)
oraz **125** wywołań `state.objects.set(` w `src/`: każda bezpośrednia konstrukcja obiektu
na polu bitwy spreaduje `...moved` (z `moveObjectDirectly`) albo `...object`/`...copyBase`:

- `effects.js:212` (manifest) i `effects.js:2377` (cloak) — `...moved` ✔ (timestamp z wejścia, 613.7d;
  obrót twarzą do góry odświeża, 613.7f);
- `effects.js:124` (Throne of the Dead Three) — `...moved` ✔;
- `effects.js:4790` (powrót z wygnania) — `...moved` ✔;
- `triggers.js:3316` — nie jest permanentem, tylko źródłem LKI dla triggera (świadomy fallback) ✔.

Pola ze znacznikami mają domyślne `null` (`identity.js:270`, `objects.js:234`) i są zerowane
przy zmianie strefy (`objects.js:231–234`, W-9 / CR 400.7), a wszystkie piszące je ścieżki
nadają znacznik razem z polem (`keywordGrantTs` w effects.js:1590 i permanents.js:1629-1632,
`lostKeywordTs` w effects.js:2169, `subtypeOverrideTs` w effects.js:2178, `tempBasePT.ts`
w effects.js:6448/6480, `ts` we **wszystkich pięciu** zapisach `state.untilEndOfTurnBuffs`).
**Brak znaleziska** — warstwa znaczników jest spójna; pozostają `?? 0` jako zabezpieczenie
na obiekty sprzed migracji (np. `buff.ts ?? 0`, `lostKeywordTs?.[kw] ?? 0`) — bez skutku,
bo każdy zapis nadaje znacznik.

## 6. Jakość pinów (dowody mutacyjne)

Mutacje na kopii pliku (przywrócone przez `cp`; **nie** `git checkout` — L136), uruchomione
`node --test` na pinach z #135:

| mutacja | wynik | wniosek |
| --- | --- | --- |
| `effectiveKeywords`: „utrata wygrywa zawsze” (wyłączony warunek `e.ts > loss`) | **RED 4/10** (W-4/1, W-4/3, W-4/4, W-4/5) | piny warstwy 6 łapią regresję w kierunku PRZED naprawą (L13/L34) |
| `layeredBaseStat`: `tempBasePT` wygrywa zawsze (bez znaczników) | **RED 1/10** (W-5/1) | pin porządku 7b działa |

## 7. Werdykt

**APPROVE z zastrzeżeniami.** Rdzeń PR #135 (warstwy D4b, itd.) jest poprawny regułowo:
wszystkie sprawdzone twierdzenia zgadzają się z dosłownym CR, a ścieżki wejścia/znaczniki
nie mają luki. Zastrzeżenia dotyczą warstwy **dokumentacji reguł** (F-1: 16 cytatów do
nieistniejących reguł, F-2: 3 rozjazdy pary) i **procesu** (F-3: brak strażnika istnienia,
heurystyka okna przepuszcza pary) — żadne nie pochodzi z #135, ale #135 je *zostawił*
i jego własne strażniki ich nie widzą.

Zalecane naprawy (Etap C tej sesji, po jednym zielonym commicie):
1. **C1** — poprawić 16 cytatów z F-1 (tabela w §3) + piny.
2. **C2** — poprawić 3 pary z F-2 i dopisać je do `test/cr-numery-mechanik-straznik.test.js`
   jako pary „mechanika ↔ zakazany numer”.
3. **C3** — `tools/cr-numery.mjs` (generator/verifier wobec pliku CR) +
   `test/cr-numery-istnienie-straznik.test.js` (istnienie numeru na podstawie tabeli
   w repo, bez sieci) — domyka klasę F-3 bez sięgania do sieci w CI.

## 8. Czego audyt NIE objął (ograniczenia)

- **`src/table/*` i `src/controllers/*`**: przejrzane różnice komunikatów/etykiet i etykiet
  komend (m.in. `freeCastChoiceSuffix`, `abilityDrawsFromEmptyLibrary`, `CARD_TYPES`),
  bez pełnego czytania heurystyk bota (nowy widok `manaSource` — zmiana danych, nie reguł).
- **`src/cards/card-data.js`** (±175 linii): przegląd wybiórczy; pełna weryfikacja danych
  wobec Scryfall to osobne narzędzie (`tools/family-audit.mjs`), poza zakresem tego raportu.
- Cytaty w `docs/` **nie są** skanowane (ADR 0016 — zapis historyczny); mapowanie dla nich
  opisuje `AUDYT_PR134_2026-09-24.md` §4/F-3.
- Heurystyki par („nazwa w oknie”, „Nazwa (CR n)”) nie dowodzą poprawności **treści**
  twierdzenia — tam, gdzie treść była wątpliwa, czytano dosłowny CR (§4).

## 9. Etap D — pętla jakości (wykonanie)

### D1 — Żywy Tester (`tools/table-tester`, jsdom, artefakt `dist/`)

Po `npm run build` (60 modułów / 4199,5 kB, artefakt z kodem po #135) rozegrano
6 partii człowiek-vs-bot (profile `greedy` i `impatient`):

| talie (gracz vs bot) | seed | profil | wynik | detektory |
| --- | --- | --- | --- | --- |
| dominaria-brg vs ravnica | 7 | greedy | gracz (bot −5 ż.) | 0 zgłoszeń |
| wiedzmin-bg vs ravnica | 11 | greedy | bot (gracz −9 ż.) | 0 zgłoszeń |
| zendikar vs tarkir-bg | 23 | greedy | bot (gracz −8 ż.) | 0 zgłoszeń |
| final-fantasy vs innistrad-wu | 5 | greedy | bot (gracz: biblioteka) | 0 zgłoszeń |
| worek-legend vs mirrodin-brg | 3 | greedy | bot (gracz −4 ż.) | 0 zgłoszeń |
| tarkir-wur vs theros | 31 | impatient | bot (gracz 0 ż.) | 0 zgłoszeń |

Każda partia dobiegła końca (brak zawieszeń `[STOP]`, brak wyjątków runtime),
`NIEWYCENIONE: brak` (każdy ruch bota miał dedykowaną wycenę). Oś „kompletność
logu/etykiet”: 2214 linie transkryptów przeszukane pod `undefined`/`NaN`/`null`/
`błąd` — 0 trafień. Żadnych znalezisk do naprawy.

### D2 — łowy CR inną ścieżką niż poprzednia sesja

1. **Istnienie numerów** (nowe narzędzie z C3): 482 unikalne numery w 4800 cytatach
   `src/`+`test/`+`tools/` — każdy istnieje w CR 2026-09-25; po naprawie F-1
   w drzewie nie ma numeru-widma (poza 2 udokumentowanymi fixture'ami strażników).
2. **Semantyka nowych cytatów #135**: 175 unikalnych numerów dodanych w `src/`
   (diff `4f75e22..f9bc44b`), z pełnym przeglądem sekcji **6xx/7xx** (procedura
   rzutu/rozstrzygania, warstwy 613, akcje 701, mechaniki 702) wobec dosłownego
   tekstu CR — **0 rozjazdów**.
3. **Kandydaci i pozycje odroczone**: `205.1a` (permanents.js) — cytat DOSŁOWNY
   („the new subtype(s) replaces any existing subtypes from the appropriate set”,
   CR 205.1a); `205.1b` dotyczy „in addition to its other types” (kandydat
   rozstrzygnięty: bez zmian). Potwierdzone też: `701.19c` („can’t be regenerated”),
   `613.9` (przykład flying / loses flying), `115.6` (zero celów), `704.3`
   (SBA przed przyznaniem priorytetu).

### D3 — sondy własne (warstwy i znaczniki)

Wątek znaczników #135 domyka się bez nowych pinów: wejście na pole bitwy ma
znacznik w każdej ścieżce (125 wywołań `state.objects.set(`, raport §5),
`613.7e` (przypięcie) i `613.7f/g` (obrót/transform) mają piny z #135
(`test/audyt-d4b-2026-09-24-warstwa-4-i-nowy-obiekt.test.js` W-7/2,
`test/audyt-d4b-2026-09-24-warstwa-6.test.js` W-4/5). Brak luki → brak pinu;
„piny dla samych pinów” to szum (L39).

### Wniosek Etapu D

Pętla jakości nie znalazła nowych błędów reguł. Wszystkie znaleziska sesji
(F-1, F-2, F-3) dotyczyły warstwy adresowania reguł i zostały naprawione
commitami C1–C3 z pinami klasy.
