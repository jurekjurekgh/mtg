# AUDYT PR #75 (M203) — 2026-08-25

- **Audytowany PR:** #75 „M203: audyt PR #74 + pętla jakości" (scalony jako
  squash `9187fea`, 31 plików / ~3210 linii diffa).
- **Audytujący:** sesja M204 (`arena/01a0384b-mtg`).
- **Baza:** `npm test` **3198/3198**, `npm run build` **54 moduły / 2637,3 kB**.
- **Tryb:** ADR 0016/0020 B (przegląd każdego zmienionego pliku, bez pełnego B0
  — ADR 0018).

## Zakres PR #75

1. **Halo Forager (MOM)** — pełny fix: X jest wyborem gracza i musi równać się
   MV rzucanej karty; jedyną wydaną maną jest zapłata {X} (koszt many czaru
   wynosi {0}, CR 118.9a); zapłata za czar nie-artefaktowy nie może pochodzić
   z many ograniczonej drukiem (M202/N1).
2. **Konwencja `prezentacja = enumeracja`** — 128× `unshift` → `push` w
   `playerView` i `legalSpellCasts`; usunięte wszystkie kompensacje odwrócenia;
   `concede`/`pass_priority` dokładane NA KONIEC; klasyfikacja intencji celu
   (`effect-intent.js`) wydzielona ze `game-state.js` (usunięcie cyklu importów).
3. **Układ stołu** — Gracz z lewej/góra, Bot z prawej/dół; etykiety bez liczby
   kart; mierzony odstęp panelu `#turn-indicator`.
4. **Poprawki testera** — kreator celów wielokrotnych (`.multi-target-confirm`),
   walidacja nazw talii (jawny błąd zamiast cichego fallbacku), `--list-decks`,
   leniwy import jsdom, względne `--out` liczone od katalogu narzędzia.
5. **Wygnanie zakryte** (Pyxis of Pandemonium) — log nazywa go wprost, bez `?`.
6. **Detektor powtórzeń bota** — pomija przedruki IDENTYCZNEGO bloku modala
   (M203/#3).

## Weryfikacja ADR 0002 (brak specjalnych przypadków po nazwie/ID w core)

- `src/engine/effect-intent.js` — klasyfikacja wprost po deskryptorach efektów
  (`pump`, `add_counter`, `grant_keywords_until_end_of_turn`…), **zero nazw/ID**.
- Halo Forager: gałąź `pay_x_cast_from_graveyard` w `execute` jest generyczna,
  czyta `card.manaCost` i `cmd.xValue`; karta ma dane w `card-data.js`.
- Liliana's Triumph: `targets: [{ type: 'player', opponent: true }]` —
  deskryptor, a nie warunek po nazwie; `targetCandidatesBySpec` obsługuje
  `spec.opponent` od M69 (Dreams of Steel and Oil), spójnie z `validateTargets`.

Brak naruszeń ADR 0002.

## Weryfikacja L58 (globale Node w kodzie przeglądarkowym)

- Dodany kod w `src/table/main.js` (IIFE `syncTurnIndicatorClearance`) używa
  `document`, `window`, `ResizeObserver` — wszystkie bezpieczne w przeglądarce.
  Brak `process`/`require`/`__dirname` w ścieżce artefaktu.
- `src/engine/**` i `src/controllers/**` bez `process.`/`console.log`
  wprowadzonych przez PR.

## Weryfikacja ADR 0022 (pełny Oracle u `supported`)

- Halo Forager: `support.status = 'supported'`, `limitations: []`, `notes`
  opisują politykę (X = MV, czar z kosztem dodatkowym/X/Fireball poza zakresem
  tej ścieżki). Zakres wyklucza KSZTAŁT czaru (dodatkowy koszt/X/Fireball),
  nie odchyłkę zachowania — testy `m203-halo-forager-pay-x.test.js` pinują:
  X = MV, brak pobrania kosztu many czaru, ograniczenie many Powerstone,
  odrzucenie X ≠ MV oraz kart z kosztem dodatkowym.

## Znalezisko M204/1 (kosmetyczne) — naprawione w tej sesji

**Objaw:** w bloku enumeracji buyback-phyrexian w `src/engine/spells.js`
(funkcja `legalSpellCasts`) M203 zmienił wcięcie pętli `for (const k of
spellPhyrexianVariants)` z 8 na 6 spacji, a nad blokiem pozostał
**zdublowany komentarz** z M202 (dwa identyczne akapity „Buyback (CR 702.26)").

**Wpływ:** zerowy logicznie (wcięcie nie zmienia semantyki), ale kod
produkcyjny z regresją stylu i duplikatem — utrudnia czytanie i wypada złapać
w audycie (ADR 0016: chirurgiczna poprawka, nie maskowanie).

**Fix:** `9b0f0f0` — usunięty duplikat komentarza, przywrócone wcięcie pętli.
Zachowana cała logika (warunki `manaAvailable`, `phyrexianPayWithLife`,
`casts.push(cast2)`). Build zielony, `npm test` 3198/3198.

## Znalezisko M204/2 — test regresyjny dla M203/#3

**Objaw:** fix deduplikacji identycznych bloków modala w
`tools/table-tester/detectors.mjs` (M203/#3, Unstable Frontier seed 61) nie
miał testu jednostkowego — tylko pomiar na żywym przebiegu. Gdyby ktoś
go cofnął, regresja wyszłaby dopiero przy ręcznym czytaniu transkryptu.

**Fix:** `M204/2` — dwa testy w `test/table-tester-detectors.test.js`:
- identyczny blok modala powtórzony 4× → 0 zgłoszeń (fałszywy alarm z seeda 61);
- kontrola: rosnące bloki (1→5 wpisów) w tej samej turze → zgłoszenie z `5×`.

Weryfikacja żywa: `--human forgotten-realms --bot alara --seed 61 --steps 300`
→ 0 zgłoszeń detektorów, partia kończy się poprawnie (Bot wygrywa w 90. kroku).
`npm test` **3200/3200**, build 54/2637,2 kB.

## Weryfikacja wyniku

- `npm test`: 3200/3200 (przed audytem 3198; +2 nowe testy detektora).
- `npm run build`: 54 moduły / 2637,2 kB (przed: 2637,3 kB; kosmetyka wcięcia).
- Żywy przebieg seeda 61: 0 crashy, 0 zgłoszeń detektorów.

## Wniosek

PR #75 jest **poprawny merytorycznie**: konwencja kolejności ofert spójna,
Halo Forager zgodny z Oracle/CR 118.9a i M202/N1, brak naruszeń ADR 0002/0022.
Dwie drobne usterki kosmetyczne/utrzymaniowe znalezione w audycie zostały
naprawione od razu (M204/1) i przypięte testem (M204/2). Temat #3 z HANDOFF
M203b jest zamknięty (to był artefakt testera, nie błąd reguł/UI) i ma
teraz test regresyjny.
