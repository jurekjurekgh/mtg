# AUDYT PR #97 (audyt PR #96 + gruntowy audyt Batcha 53) — 2026-09-05

**Zakres:** `d67b684..5fb7994` (squash PR #96 → squash PR #97), **21 plików**,
`+1256/−32`. Status PR #97: **zmergowany** 2026-09-05T10:46:46Z (squash).
Audyt wykonany w sesji arena/01a0712d (PR #98) na świeżym `main`.

**Metoda:** pełny diff per-plik (wszystkie 21), weryfikacja tez regułowych
wobec Oracle/rulingów ze snapshotów `docs/cards/scryfall-*.json`,
niezależne pomiary bram, kontrola mutacyjna trzech napraw
(REPRO→RED→cofnięcie→GREEN), śledzenie konsumentów zdarzeń (L48).

## Bramy zmierzone niezależnie (main = 5fb7994)

| Bramka | Wynik | Handoff PR #97 |
|---|---|---|
| `npm test` | **4447/4447** | 4447 ✔ |
| `npm run build` | **59 modułów / 3265,2 kB** | 59/3265,2 ✔ |
| `node --test test/bot-benchmark.test.js` | **10/10** | 10/10 ✔ |

`test:all` nie powtarzany w audycie (ADR 0016: wystarcza fast rdzeń +
próbka bota; pełny pakiet bramą PR w CI).

## Przegląd per obszar (21/21 plików)

### Silnik — reguły

- **`src/engine/triggers.js`** (F3+A1). (1) `printLki` w
  `queueTriggerToStack` — migawka druku (kind/colors/types/subtypes/
  keywords/abilities/manaCost + warunkowe entersWithCounters/station/saga/
  transformTo/frontFaceId); konsument `create_offspring_token` czyta
  `lkiPrint` na stubie `resolveTriggerEntry`. Zgodne z rulingiem BLB
  (snapshot Rampagera: „If the creature leaves the battlefield before the
  offspring ability resolves, you'll still create a token copy of it") i
  „copies exactly what was printed" — `printLki.abilities` bierze DRUK
  (`source.abilities`), nie efektywne. ✔ (2) `triggerSourceZoneLegal`
  dopuszcza `reflexive_sacrifice` z dowolnej strefy + bramki
  `resolve_trigger_target/choice` analogicznie — zgodne z rulingiem
  Glorifiera („second reflexive ability triggers when you sacrifice […]
  as it goes on the stack") i niezależnością zdolności na stosie od
  źródła; `triggerConditionHolds` pozostaje wymagany. ✔
- **`src/engine/effects.js`** (A1+F3). `markTemporaryExile` ustala
  `reflexiveAbility` JUŻ przy rozstrzyganiu ETB (żywe źródło:
  `effectiveAbilities`; stub: druk z `lkiPrint.abilities`) i zamraża kopię
  do `pendingSacrifice` — refleks odpala nawet po zejściu źródła.
  `create_offspring_token`: live → LKI (`lkiPrint`), bramka `kind ===
  'creature'` zachowana na obu ścieżkach. Generyczne (klucze po
  deskryptorach zdolności, nie po nazwie karty — ADR 0002). ✔
- **`src/engine/game-state.js`** (F4+A1+widok). `take` emituje
  `reflexive_sacrifice_resolved {sacrificed:true}` (gałąź session.js:785
  ożywa; `skip` nadal `{sacrificed:false}` — bez dubli, L79 sprawdzone).
  Zdarzenie `reflexive_sacrifice` niesie `reflexiveAbility`. `playerView`
  wystawia `cantBeBlockedByPower` (eksport `attackerBlockPowerRestriction`
  z combat.js) — informacja publiczna (skutek statyki), gate
  `!hiddenFromViewer`, zgodne z ADR 0017. ✔
- **`src/engine/combat.js`** — tylko eksport istniejącej funkcji
  (`attackerBlockPowerRestriction`), logika nietknięta. ✔

### Karta

- **`src/cards/card-data.js`** (F1). Óin: `colors: ['R']` zdjęte z kosztu
  `{1}, {T}, Discard a card` — Oracle potwierdza koszt generyczny (snapshot
  `scryfall-oin-the-brave.json`). Goblin Picker ({R}) i Fledgling Imp ({B})
  nietknięte. ✔

### Bot

- **`src/controllers/heuristic-bot.js`** (C-FIX-1+C-FIX-2). `attackerCanBe
  Blocked`: bloker o efektywnej mocy ≤ `cantBeBlockedByPower` (z widoku,
  ADR 0017) nie liczy się do obrony; remis plain/offspring rozróżnialny w
  projekcji (`offspring: cmd.offspring ? 1 : 0`, wzorzec kicker). Golden-
  master (`bot-scoring-snapshot.json`) zregenerowany — zgodnie z regułą
  (zmiana wyceny wymaga świeżego fixture; L124: zapis na gotowym drzewie).
  Benchmark 10/10 utrzymany. ✔

### Stół

- **`src/table/render.js`** (B1+B4+B5+B6). `plotLine` przez `equipPips`
  (domyślne `colors = []` — brak crashu dla plotu bezbarwnego, test
  „Plot {2}" to pinuje); „kopia"→„kopię"; `describeStatic` opisuje ewazję
  mocową; `commandLabel` wariantu Offspring (format jak kicker — klasa
  M101/B zamknięta). ✔
- **`src/table/session.js`** (B3). `trigger_target_resolved`: `targetIds`
  (wielocełowy Glorifier) → „cele: A, B"; kompatybilność wstecz po
  pojedynczym `targetId` zachowana. ✔

### Testy (6 plików)

- `ability-cost-pips` (nowy strażnik klasowy F1, ograniczenia metody
  udokumentowane w nagłówku), `batch53-bot` (C-FIX-1/2 + 4 sondy C),
  `batch53-tiles` (B1/B4/B5/B6, przez harness DOM), `real-cards-batch53`
  (+4: F3, Óin mana bezbarwna, A1, Sheriff plot+exile),
  `table-owner-notes-2026-08-10` (+pin B3), fixture snapshot. Testy
  deklarują to, co naprawiają — potwierdzone mutacyjnie (§ niżej). ✔

### Dokumentacja (7 plików)

- README liczby na 4447/4457/3265,2 — zgodne z moim pomiarem (L92 domknięte
  w tym PR ✔). Raporty audytów, plany, handoff, PROJECT_HISTORY — spójne
  z commitami.

## Kontrola mutacyjna (niezależna, ta sesja)

| Mutacja | Oczekiwanie | Wynik |
|---|---|---|
| F1: przywrócenie `colors:['R']` Óinowi | `ability-cost-pips` RED | **RED** (`oin-the-brave#2 [R]`, 0/1) ✔ |
| A1: usunięcie `'reflexive_sacrifice'` z `triggerSourceZoneLegal` | `real-cards-batch53` RED | **RED 3/33** ✔ |
| C-FIX-2: wykomentowanie progu mocy w `attackerCanBeBlocked` | `batch53-bot` RED | **RED 1/6** ✔ |

Po cofnięciu mutacji pełny `npm test` 4447/4447.

**Incydent procesowy audytu (bez szkód w repo):** przy mutacji F1
`replace(…, 1)` na zduplikowanym stringu kosztu trafił w Goblin Picker
(`card-data.js:1916`) zamiast w Óina (`:10574`) — wykryte natychmiast
czerwonymi testami (B2/M138/B53), cofnięte kotwicą kontekstu. Klasa L8/L34:
mutacja zawsze z kotwicą kontekstu (komentarz nad linią), nigdy goły
string powtarzalny w pliku.

## Znaleziska

**Brak nowych znalezisk F** — wszystkie naprawy PR #97 potwierdzone
(reguły, rulingi, mutacje, bramki).

## Obserwacje (bez akcji w audycie)

- **O1 (handoff nieaktualny):** „Gdzie szukać dalej #5: Sheriff z plotu
  niećwiczony" — **zamknięte już w PR #97** testem „B53: Sheriff — plot
  {1}{W} i rzut z exile w późniejszej turze" (`real-cards-batch53.test.js`,
  dodany w tym PR). Nie dublować.
- **O2 (preatomatyczne):** stub martwego źródła refleksu w
  `processTriggersScan` bierze `controllerId: ev.playerId ?? live?…` —
  kontroler z chwili decyzji ofiary; dla skrajnych zmian kontroli między
  ETB a refleksem wartości zgodne z duchem CR (kontroler w momencie
  wykonania czynności), brak ścieżki gry, która to rozróżnia w 1v1 z
  obecnym katalogiem.
- **O3 (otwarte, kolejka):** `wasKicked` LKI (raport PR #96 O6) — potwierdzony
  przez poprzednią sesję kandydat; plan E3.1 tej sesji.
- **O4 (otwarte, doc):** L47 obiecuje `copyableDescriptorKeys`/„lista w
  jednym miejscu", a kod trzyma listy pól per-ścieżka kopiowania — korekta
  zaplanowana E3.2 (bez renumeracji — kotwice w kodzie).
- **O5 (strażnik):** `ability-cost-pips` dopasowuje pipy na poziomie KARTY
  (dwie zdolności o różnych pipach przejdą przy obu multizbiorach w
  Oracle) — ograniczenie jawne w nagłówku testu; do podniesienia, gdy
  pojawi się pierwsza karta z dwoma kolorowymi aktywacjami.

## Werdykt

**PR #97 dobry** — naprawy F1–F4/A1/B/C zgodne z Oracle/CR, generyczne,
chirurgiczne, z mutacyjnie zweryfikowanymi strażnikami; liczby w README
świeże. Kolejność dalszej pracy: O3 (`wasKicked`), O4 (L47), pętla jakości
Żywym Testerem (plan sesji E3).
