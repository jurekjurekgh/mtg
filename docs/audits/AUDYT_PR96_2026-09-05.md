# AUDYT PR #96 (batch 53, 589–598) — 2026-09-05

**Zakres:** `bf615b1..d67b684` (squash PR #95 → squash PR #96), **114 plików**,
`+2799/−382`. Metoda: pełny diff per-plik, rulingi WotC z `docs/cards/`,
reprodukcje headless (`/tmp/probe-*.mjs`, `/tmp/scan-*.mjs`), live Oracle
Scryfall dla Óina. Status PR #96: **zmergowany** 2026-09-05T07:22:58Z —
znaleziska naprawiane w PR #97 (ten PR).

**Wynik:** 4 znaleziska (F1 reguła, F2 docs, F3 reguła-corner, F4 log-minor)
+ 7 obserwacji. Bramy PR #96: fast 4432/4432, all 4442/4442,
build 59 modułów / 3257,4 kB (zmierzone niezależnie w tej sesji — zgodne
z M305; README w PR #96 zostawiono na liczbach etapu audytu — F2).

## F1 (reguła, krytyczna dla karty): Óin the Brave — {R} zamiast {1}

`src/cards/card-data.js` — aktywowana zdolność Óina ma
`cost: { mana: 1, tap: true, colors: ['R'], discardCard: true }`, a Oracle
mówi **`{1}, {T}, Discard a card: Draw a card.`** (snapshot
`docs/cards/scryfall-oin-the-brave.json` + live
`api.scryfall.com/cards/hob/106`). W silniku `cost.colors` to PIPY do
zapłaty (`abilities.js` `colorRequirementsOf` + egzekucja na ścieżce oferty;
por. Goblin Picker `{R}`, Fledgling Imp `{B}`) — karta żąda czerwonej many
tam, gdzie wystarczy bezbarwna. Repro: przy 1 manie bezbarwnej oferta
aktywacji **nie istnieje** (`BRAK` w probie). Test batch53 **pinnuje błąd**
(`deepEqual` kosztu + płatność tylko czerwoną; brak testu many generycznej).
**Naprawa (ten PR):** zdjąć `colors` (konwencja: Goblin Boarders `:4719`,
Kher `:1937`), poprawić pin, dodać test płatności bezbarwnej.
**Klasa:** pipy kosztów AKTYWOWANYCH nie mają strażnika katalogowego (L23
kryje tylko koszty zaklęć) — ten PR dodaje strażnik
`test/ability-cost-pips.test.js` (53 zdolności z pipami, po naprawie 0 miss).

## F2 (docs, L92): README „Bieżący stan” na liczbach etapu audytu

README zostawia `4412/4412` / fast `4402` / `3226,2 kB`, mimo że batch53
domknął się na **4442/4442** / fast **4432** / **3257,4 kB** (M305, handoff
i niezależny pomiar w tej sesji zgodne). Ta sama klasa co F1/F2 z audytu
PR #95 — procedura ENVIRONMENT §7 znowu nie dopięta w tym samym PR, który
ją raportował. **Naprawa (ten PR):** odświeżyć liczby w domknięciu.

## F3 (reguła, corner): Offspring — brak tokenu po zejściu źródła

Ruling BLB: „If the creature leaves the battlefield before the triggered
ability resolves, the token is still created” (LKI / CR 608.2b). Silnik
gubi token dwiema bramkami: (1) re-check intervening-if (CR 603.4,
`triggers.js:1144`) na stubie LKI bez `wasOffspring` → warunek upada;
(2) `create_offspring_token` wymaga `src.zone === 'battlefield'`.
Repro: rzut z offspring → trigger na stosie → źródło na cmentarz → drain
→ **brak tokenu**. **Naprawa (ten PR):** `wasOffspring` do `sourceLki`
(`queueTriggerToStack` + stub `resolveTriggerEntry` — kanał LKI na fakty
historyczne, CR 603.10) + bramka efektu na rekord w dowolnej strefie;
test: token 1/1 powstaje mimo zejścia źródła. `wasKicked` ma tę samą lukę
(pre-existing, poza zakresem — obserwacja O6).

## F4 (log, minor): gałąź `sacrificed:true` martwa

Ścieżka `take` w `resolve_sacrifice_choice` nie emituje
`reflexive_sacrifice_resolved` (w przeciwieństwie do `skip` i do wzorca
`food_choice`), więc gałąź `sacrificed:true` w `session.js` nigdy nie żyje.
**Naprawa (ten PR):** emisja `_resolved {sacrificed:true}` przy `take`
z `pending.reflexiveEvent` + test tekstu logu.

## Werdykty per-plik (skrót)

- `card-data.js` (10 kart + token): Oracle/koszt/typ/P-T zgodne ze snapshotami
  poza F1; `rulings` przy wszystkich 10 (ADR 0028) ✓.
- `mana-costs-data.js`, `registry.js`, `materialize.js`, `types.js`,
  `mana-sources.js`: koszty OK; `offspring` generyczny (nie po nazwie,
  ADR 0002); `EVENT_TYPES` +3 ✓.
- `effects.js`: formuła liczników Sheriffa (CR 121.6/614.1c) ✓; offspring
  kopiuje wartości druku bez liczników/aur (ruling) ✓ poza F3; reflexive
  dwa etapy (ruling LCI) ✓; `grant_keywords_until_end_of_turn` deleguje
  do `grantKeywordsUntilEndOfTurn` (print czysty) ✓.
- `triggers.js`: `wasOffspring`, 2 filtry celów, `becomes_blocked` raz na
  atakującego (ruling SOM) ✓ poza F3; Ironclad filtr lustrzany do lądowego
  (kontroler + `name != null`) ✓.
- `game-state.js`/`resources.js`/`combat.js`/`abilities.js`: offspring rzut
  jak kicker ✓; bramka bloku po mocy ✓; pola zdolności generyczne ✓.
- `state-based-actions.js` (`enduringStory` na GRACZU, sticky, liczy raz per
  permanent — rulingi HOB) ✓; `tokens.js`, `identity.js`, `fingerprint.js`
  (pełni gracze → brak ślepoty replay), `bot` (projekcja bez zmiany decyzji —
  para ravnica w fixturze nietknięta) ✓; `render.js`/`session.js` etykiety
  poza F4 ✓.
- `test/real-cards-batch53.test.js` (30 testów): każdy pin danych + real-cast
  z obiema gałęziami ✓; luki: brak testu Óina bez czerwieni, brak ścieżki
  plot-rzutu Sheriffa, brak asercji martwej gałęzi (→ F1/F4) — por. niżej.
- Talie: wstawki batch53 + re-split Warhammera (`generate-plan-decks`,
  ADR 0023/0024) zweryfikowane ogonami; migracja `brg/wu→ubr/wg` kompletna
  (zero wiszących odwołań poza historią); próbka regresji (6 pierwszych
  alfabetycznie) bez Warhammera → progi bez zmian słusznie ✓.
- Fixtura `bot-scoring-snapshot.json`: regen `--write` na gotowym drzewie ✓;
  zmiany w pełni wyjaśnione składem talii (mirrodin-wu, warhammer-ubr).
- `b1-final-2026-08-29.*`: tylko renamy (plik historyczny) — O4.
- `AUDYT_PR95_2026-09-04.md`, `PROJECT_HISTORY.md`, `ENGINE_MILESTONES.md`
  (M305), `collection-art-ids.csv` (589–598), plan batch53: zgodne ✓
  (handoff „hash się nie zmienił” nieścisły — O5).

## Obserwacje (bez akcji)

- **O1:** `enduringStory` poza PlayerView (ADR 0017 Q — odtwarzalne ze stołu
  poza sticky-historią; bot widzi efektywne P/T+haste) — akceptowalne.
- **O2:** bot nigdy nie rezygnuje z opcjonalnej ofiary (skip=0) — heurystyka
  v1, akceptowalna.
- **O3:** bot nigdy nie dopłaca offspring (remis → pierwsza oferta = zwykły
  rzut) — ten sam wzorzec co kicker, pre-existing.
- **O4:** historyczny `b1-final-2026-08-29` przemianowany (liczby nominalnie
  przy nowych składach) — kosmetyka, akceptowalna.
- **O5:** handoff batch53 „hash się nie zmienił” vs faktyczny regen fixtury —
  nieścisłość handoffu; fixtura poprawna.
- **O6:** `wasKicked` ma lukę LKI jak F3 (pre-existing, follow-up).
- **O7:** offspring/`create_copy_token` duplikują listy pól; `copyableKeys`
  z L47 nie istnieje w kodzie (dryf lekcji) — listy per-ścieżka to wzorzec.

## Luki testowe ( Martin do napraw)

- Óin: płatność maną bezbarwną / nielimitowanie do czerwonej (→ F1).
- Sheriff: rzut z plotu (`cast from exile plotting`) — ścieżka niećwiczona.
- Glorifier: asercja logu `sacrificed:true` (→ F4).
- Offspring: LKI po zejściu źródła (→ F3).

## Status napraw w PR #97

- [ ] F1 + strażnik klasowy `test/ability-cost-pips.test.js`.
- [ ] F3 + test LKI.
- [ ] F4 + test logu.
- [ ] F2 (README) w domknięciu PR.
