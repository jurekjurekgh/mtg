# AUDYT PR #87 (2026-08-29) — sesja arena/01a04e98

**Zakres:** audyt poprzedniego scalonego PR (`#87`, squash `15a2be5`) wg
[ADR 0020 B](../decisions/0020-mandatory-session-workflow-pr-audit-incremental.md)
i [ADR 0016](../decisions/0016-session-audit-and-surgical-patching.md).

PR #87: **303 pliki, +13 909/−98 250** (209 usuniętych = artefakty audytu,
porządek M253). Sesja `arena/01a049c7` (2026-08-28, kontynuowana 2026-08-29):
M253 (porządki repo + strażnik), A1 (strażnik L16), M254 (showcase sztuki +
kolejność pendingów), M255 (pętla jakości: F/G/A/B/C/D/E), M256 (Żywy Tester
runda 2: H/J + wycena bota), **Batch 51** (8 kart), ADR 0025 + przepisane
ADR-y 0001–0024.

**Baza audytu:** `main` @ `6d04551` (squash PR #86). Pomiar startowy sesji:
`npm test` **3725/3725**, `npm run build` **56 modułów / 2894.7 kB** — zgodne
z ENGINE_MILESTONES (Batch 51: 3661 → +64 testy z M255/M256/pinów).

## Metoda

- Pełne czytanie całego diffu kodu (6 zrzutów: combat+triggers /
  abilities+effects+spells / game-state+deck+identity+types / `src/cards` /
  controllers / table) + otoczenie zmian w źródłach.
- Weryfikacja **Oracle wszystkich 8 kart Batchu 51** przeciw JSON-om
  Scryfall w repo (`docs/cards/scryfall-*.json`) i CR (renown 702.112b,
  bloodrush, deathtouch, first strike, prewencja obrażeń bojowych 615.1a,
  mana value CR 702.13).
- **Weryfikacja mutacyjna (L61/L34) każdej deklarowanej naprawy** — kopia
  bazowa pliku przed edycją, jedna edycja na mutację, wynik przed i po.
- Mutacje strażników: `repo-artefakty-audytu` (2 ścieżki naruszeń) i pin A1
  (wykryła nową lukę → **A2**, patrz niżej).
- Fakt-check ADR-ów: normalizacja charakterowa + multizestawy liczb, odnośników
  CR i statusów (`6d04551` vs `15a2be5`, 26 plików) — porównanie maszynowe.
- `npm run test:slow` **10/10** (w tym strażnik budżetu benchmarku ADR 0025),
  `npm run test:all` **3735/3735** (brama PR).

## Znaleziska

| ID | Klasa | Ciężar | Status |
|---|---|---|---|
| **A2** | strażnik L16 (kod testu) | średnia | **naprawione** w tej sesji (`e596078`) |
| **D1** | README — dane przestarzałe | niska | **naprawione** w tej sesji (`04c3c85`) |
| **D5** | JSDoc w `effects.js` w złym miejscu | niska (kosmetyka) | **naprawione** w tej sesji (`a1800f1`) |
| **D2** | brak końcowego handoffu sesji PR #87 | niska | **naprawione** w tej sesji (`HANDOFF_2026-08-29.md`) |
| **D3** | ADR-y 0001–0024 przepisane bez deklaracji zakresu | niska (proces) | zgłoszone właścicielowi |
| **D4** | opis PR #87 niezaktualizowany przy domykaniu | niska (proces) | zgłoszone właścicielowi |

### A2 — strażnik klasy L16 liczył pokrycie z TREŚCI CIĄGÓW ZNAKOWYCH

Punkt (a) handoffu 2026-08-28c („czy `stripComments`/pin A1 są odporne na
inne kształty komentarzy, np. `state.pendingX` w ciągu znakowym") — **luka
potwierdzona sondą**: `stripComments` maskował tylko komentarze, więc
`const note = 'state.pendingZzz nie jest pokryte'` w `fingerprint.js` zamykało
klasę L16 dokładnie tak samo jak komentarz (L83: strażnik liczy KONSTRUKTY —
wzmianka w tekście nie jest pokryciem).

Naprawa (test-only, `test/fingerprint-pending-decisions.test.js`): nowe
`maskNonCode` (komentarze + treści `'…'`/`"…"`/`` `…` `` z zachowaniem kodu w
interpolacjach `${…}`, łącznie z zagnieżdżonymi szablonami); dwa skany po dwóch
konstruktach (literal `PENDING_DECISION_FIELDS` — tam treść ciągów jest danymi;
odczyty `state.<pole>` — skan po `maskNonCode`). Pin A1 rozszerzony o
przypadki A2 (ciąg + szablon). Mutacja omijająca `maskNonCode` → pin RED;
po przywróceniu GREEN. `npm test` 3725/3725.

### D1 — README opisywał stan sprzed ~30 batchy

`2445/2445` → **3735/3735** (`test:all`), `51 modułów / 2072 kB` →
**56 modułów / 2894.7 kB**, `138 kart` → **436 kart realnych + 42 tokeny**,
`Batch 22` → **Batch 52**; pozycja 2 listy luk (`any target` Reliquary
Dragon, `Mesmerize`) — obie zaimplementowane (triggers.js:290; katalog),
zastąpiona zasadą ADR 0022/0002; progi `B0 0.78/0.57` zastąpione ADR 0018.

### D5 — pięć JSDoc-ów nad funkcją, której nie opisują

Efekt wcześniejszych chirurgicznych patchy (kod przesuwał się, komentarze nie):
„Wspólny interpreter efektów…" nad `dealNonCombatDamage` (należy nad
`applyEffect`), „Temat 6 — search library" nad `revealTopGainLife` (należy nad
`queueSearchChoice`), „Czy karta pasuje do kwalifikatora" (nad
`librarySearchMatches`), „każdy zakryty stwór" (Veiled Ascension, nad
`faceDownCreaturesYouControl`) i „Wykonuje WYBRANY cel pokoju" (nad
`applyRoomTargetChoice`). Czysty przenos (git diff: 31−/31+, treść identyczna),
skan: brak podwójnych bloków.

### D2 — brak końcowego handoffu sesji PR #87

Najnowszy handoff przed audytem (`HANDOFF_2026-08-28c.md`) jest **środkowy**
(„trzecia tego dnia", sesja w trakcie — kończy się na M253/A1). Sesja PR #87
kontynuowała się 2026-08-29 (M254/M255/M256, Batch 51, ADR 0025) i domknęła
PR bez handoffu końcowego. Naprawa: `docs/setup/HANDOFF_2026-08-29.md`
(domyka stan prac PR #87 i przekazuje tę sesję).

### D3 — ADR-y 0001–0024 + README ADR-ów przepisane bez deklaracji zakresu

Charakterowa normalizacja wykazała, że **wszystkie 26 plików** różnią się
więcej niż whitespace — to **parafraza/skrócenie (−10–25% objętości)**, nie
reflow. Fakt-check (multizestawy liczb, odnośników CR, statusów, progów):
**zmysł zachowany** — zero zmian semantycznych; delta to:

- zmiany redakcyjne (link → zwykły tekst, „oraz" → „i", numeracja list);
- legitymacyjne dopiski: nota 2026-08-29 w ADR 0018 (odniesienie do
  ADR 0024/0025) i nowy ADR 0025 (Zaakceptowana; w rejestrze README status
  zgodny z plikiem — zweryfikowane wprost);
- **regresja typograficzna**: polskie zamykające cudzysłów `”` zamienione na
  prosty `"` (ADR 0021: 10 → 0 wystąpień; podobnie w pozostałych plikach).

Prawdopodobny motyw: budżet lektury (L66 — dokumenty muszą mieścić się w
budżecie; `dokumentacja-budzet-lektury` 2/2). Proces: zakres przepisywania
zaakceptowanych ADR-ów **nie był zadeklarowany w opisie PR** — zgłoszenie
właścicielowi (przyjąć albo przywrócić typografię; zasadę deklaratywności
zakresu uwzględnić w checklistce domykania PR).

### D4 — opis PR #87 niezaktualizowany przy domykaniu

Opis kończył się na stanie z 2026-08-28 (przed M256, bez ADR 0025, liczba
testów 3687 vs 3725 w scalonym drzewie). PR domknięty przez właściciela w tym
stanie — zgłoszenie (reguła: opis PR = aktualne podsumowanie przy każdym
domykaniu sesji).

## Zweryfikowane poprawnie (brak znalezisk)

- **Batch 51 — 8/8 kart zgodne z Oracle** (Thunderstaff, Invasive Species,
  Skinbrand Goblin, Akroan Sergeant, Kulrath Mystic, Savage Surge,
  Typhoid Rats, Dromoka Warrior): koszty, PT, klauzule, warianty
  (bloodrush z ręki, MV z obiektu czaru, renown 1, deathtouch, first strike,
  „attack only as a sorcery" — brak oferty aktywacji na polu bitwy).
- **Renown (CR 702.112b)**: `renowned` trwa do opuszczenia pola bitwy, drugi
  trigger nie odpala, atak zablokowany (obrażenia w stwora) nie daje renown —
  implementacja w `dealCombatDamageToPlayer` (combat.js) zgodna z CR i
  wyrokami.
- **Invasive Species**: hexproof blokuje tylko kontrolę PRZECIWNIKA
  (triggers.js:282) — nie ma nadmiernego blokowania przez engine.
- **M255/A (modal)**: wyjątek `untilEndOfTurn:true` z szumu jest bezpieczny —
  flagę niosą wyłącznie effects.js:4998 (set_base_pt) i :1831 (buff_creature);
  `modifyStats` (permanents.js:748) nie niesie flagi; masy używają
  `mass_stats_modified` (poza zestawem szumu — celowo, L87). Brak wycieku
  szumu w modalu i braku utraty szumu poza nim (testy A3/A4).
- **`addCounter`**: rzuca dla obiektu spoza pola bitwy, ale wszystkie
  wywołania pre-guardowane (sprawdzone w otoczeniu).
- **M254**: kolejka sztuki FIFO + `awaitingArtAck`/`continueArtPlay`,
  podgląd `hiddenArt`, `scryfallCardUrl`, badge `temporaryExile` — spójne
  z konwencjami stołu.
- **M255/B/C/D/E**: etykiety logowe (bloodrush po wzorcu cycling),
  odrzucenie jako KOSZT, tabela opisów zdolności (C1 strażnik), dynamiczne
  +X/+X (D1–D3), bramka bota (E1/E2) — spójne z L84/L85/L86/L87/L88.
- **M256 (H/J)**: `emptyReceiverReason` w `resolveTriggerEntry` — powody
  per-receiver + kontrolne pozytywne/ant-over-fix; wycena bota
  (`TEMPORARY_PUMP_EFFECTS`/`pumpDelta`) spójna z L88.
- **L41/48/57/73/84/85/86/87/88/90/91** — bez naruszeń w diffie.
- **Kolejność pendingów (M254/A)**: 3 gałęzie oferty permutowane — 2/3 RED
  (sprawdzone w poprzednim etapie audytu), 1/3 GREEN (intencjonalne: pass
  nie zależy od kolejki — ADR 0017).
- **Punkt (b) handoffu 2026-08-28c**: usunięcie 205 transkryptów nie
  odebrało dowodów — raporty `docs/audits/*` nie cytują usuniętych plików
  per plik (sprawdzone w poprzednim etapie, potwierdzone spot-checkiem);
  strażnik `repo-artefakty-audytu` ma 3 żywe nogi (mutacje R1/R2 + kontrola
  pozytywna).

## Wyniki weryfikacji mutacyjnej

| # | Mutacja (cofnięcie fixu) | Oczekiwany | Wynik |
|---|---|---|---|
| M1 | bloodrush: usunięcie bramki „atakuje" (validateTargets) | RED | **RED** (`cel nieatakujący odrzucony`) |
| M2 | renown: usunięcie `!source.renowned` | RED | **RED** (`renown NIE powtarza się`) |
| M3 | warunek MV: `return true` | RED | **RED** (`czar MV 1 triggera NIE odpala`) |
| M4 | M255/F: bramka blokuje KAŻDEGO (regresja) | RED | **RED** (F1, F3, F4) |
| M5 | M255/G: exploit blokuje bezwarunkowo (regresja) | RED | **RED** (G1) |
| M6 | M256: powód zawsze `no_result` | RED | **RED** (H1, H1c, H2…) |
| A2 | ominięcie `maskNonCode` w strażniku L16 | RED | **RED** (pin A1+A2) |
| R1 | śledzony plik `tmp-audyt-*` (strażnik M253, noga 1) | RED | **RED** |
| R2 | usunięcie reguły `tmp-audyt-*/` z `.gitignore` (noga 2) | RED | **RED** |

Po każdej mutacji: odtworzenie kopii bazowej + GREEN.

## Wnioski

Silnikowo PR #87 jest **poprawny**: 8 kart Oracle, CR-renown, bramki M255
(F/G) i M256 (H) przypięte mutacyjnie, strażnik ADR 0025 działa (test:slow
10/10). Znaleziska to jedna luka strażnika (A2 — naprawiona i przypięta),
trzy drobne usterki dokumentacji (D1/D2/D5 — naprawione) i dwa
zgłoszenia procesowe dla właściciela (D3 zakres przepisywania ADR-ów +
typografia; D4 świeżość opisu PR przy domykaniu).

**Rekomendacje (bez zmian kodu):**
1. D3: decyzja — przyjąć przepisane ADR-y (przywracając cudzysłowy `”`) albo
   przywrócić poprzednią wersję; dodać do checklisty domykania PR pozycję
   „zakres zmian w dokumentach zaakceptowanych jest zadeklarowany w opisie".
2. D4: domykanie sesji = aktualizacja opisu PR do stanu scalonego drzewa.
