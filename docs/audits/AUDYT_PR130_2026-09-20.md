# Audyt PR #130 (squash `0b49b12`) — 2026-09-20

Audyt poprzedniego scalonego PR (ADR 0020 B / ADR 0016), wykonany w sesji
`arena/01a0bf64-mtg` (PR #131 — „Sesja 2026-09-20b: audyt PR #130 + pętla
jakości”). Zakres: **wszystkie 134 pliki** zmiany `8af0c7c…0b49b12` (PR #130 —
„Sesja 2026-09-19: audyt PR #129 + pętla jakości (ADR 0020)", scalony
2026-09-20T15:17Z; 28 plików `src/` = +2807/−250, 52 pliki testowe, 28 z nich
nowych, 34 dokumenty, 13 talii, 6 narzędzi), pod kątem logiki, zgodności z CR,
generyczności (ADR 0002), kompletności widoku (ADR 0017), determinizmu
(ADR 0005), pochodzenia kart (ADR 0029/0022) i **ważności pinów** (RED→GREEN
przez mutacje, L13).

Metoda: `git diff 8af0c7c..0b49b12` per plik (klon sesji jest płytki — po
`git fetch --unshallow`), lektura kodu na HEAD `0b49b12`, lektura wszystkich 28
nowych i 24 zmodyfikowanych plików testowych, **20 mutacji** w trzech harnessach
poza repo (`/tmp/probe.mjs` P1–P7, `/tmp/probe130.mjs` PA–PH,
`/tmp/probe-d.mjs` Q1–Q5; każda: podmiana fragmentu źródła → pomiar →
przywrócenie z kopii, L34/L136), pomiar bram (`node tools/run-tests.mjs all`,
`npm run build`, `test/dokumentacja-budzet-lektury.test.js`). Bez pełnego B0
(ADR 0018).

## 1. Bramy zmierzone na HEAD `0b49b12` (przed zmianami tej sesji)

| Pomiar | Wynik | Deklaracja w `HANDOFF_2026-09-20` | Ocena |
|---|---|---|---|
| `node tools/run-tests.mjs all` | **6027/6027, 0 fail** (~350 s) | 6027/6027 | zgodne |
| `npm run build` | **59 modułów / 3950,9 kB** | 59 modułów / 3950,9 kB | zgodne |
| budżet lektury startowej | **99 985 / 100 000 tokenów** (zapas 15 ≈ 42 znaki) | 99 985 / 100 000 | zgodne, ale na granicy — patrz §5 |

## 2. Przegląd plików (wszystkie 28 w `src/`)

| Plik | Zmiana | Ocena |
|---|---|---|
| `src/cards/card-data.js` | +317: karty batcha 57 (Delve: Hooting Mandrills, Treasure Cruise…), Annie Flash the Veteran, Zoraline; deskryptory `delve`, `craft`, `permanent_card_in_graveyard` | poprawne; druki zweryfikowane ze Scryfall (`docs/cards/scryfall-*.json`), karty z kolekcji właściciela (ADR 0029); reguły w deskryptorach, nie w nazwach |
| `src/cards/mana-costs-data.js` | +13: koszty nowych kart | bez zastrzeżeń |
| `src/cards/materialize.js` | +10: przeniesienie deskryptora `delve` na obiekt gry | bez zastrzeżeń |
| `src/cards/registry.js` | +5: wpisy rejestru | bez zastrzeżeń |
| `src/controllers/aggro-bot.js` | 1 linia: nowy typ decyzji na liście prostych | bez zastrzeżeń (uzupełnione w D o `resolve_aura_host`) |
| `src/controllers/heuristic-bot.js` | +420: wyceny Delve, landcyclingu, Skarbu (`treasureRefundLead`), oddanej gardy, okna impulse/menace | poprawne i deskryptorowe; mutanty PD/PF/PE czerwone na pinach PR #130. **Otwarte (niskie)** — §6 |
| `src/controllers/heuristic-params.js` | +6: wagi nowych wycen | bez zastrzeżeń |
| `src/engine/combat.js` | +30: `cantBeBlockedFromEquipment`, sloty bloku | poprawne; mutant PB czerwony. **Otwarte (niskie)**: fallback `legalBlockerOptions` powyżej capu 32 — §6 |
| `src/engine/deck.js` | +5 | bez zastrzeżeń |
| `src/engine/effects.js` | +263: `resolveCraftExileOutcome`, `return_permanent_from_graveyard`, `isLandCard` | craft poprawny (wzorzec L41); **znalezisko D** — automatyczny gospodarz aury (CR 303.4f), naprawione w `19f47fa` |
| `src/engine/fingerprint.js` | +9: `pendingDelveExile` / `pendingCraftExile` w odcisku | poprawne (klasa L16); strażnik B2/2 zielony (uzupełnione w D o `pendingAuraHost`) |
| `src/engine/game-state.js` | +452: stany pending (delve, craft), bramki `firstPendingDecision`, oferta po wariancie, widoki | poprawne strukturalnie; ADR 0017 spełniony (`pendingDelveExile` mapowany do widoku). **Znaleziska A, B, C, D** przechodziły przez ten plik — naprawione |
| `src/engine/identity.js` | +8 | bez zastrzeżeń |
| `src/engine/impulse-window.js` | +19: `castsWithoutPayingMana` | poprawne; mutant PC czerwony (brak stempla impulsu) |
| `src/engine/mana-sources.js` | +38: `MANA_ABILITY_PAYLOAD_KEYS`, `isPureManaAbilityCommand` | poprawne i generyczne (rozpoznanie po kluczach payloadu, nie po kartach) |
| `src/engine/permanents.js` | +38: `tapObject` | bez zastrzeżeń |
| `src/engine/resources.js` | +33: `castPermanent` | **znaleziska A i B** (bramka `delveLimit > 0`; limit z kosztu wydrukowanego) — naprawione w `b1c66e1` |
| `src/engine/spells.js` | +215: Delve (`delveExileLimit`, `delveManaAfter`, `declareDelveCast`, `resolveDelveExile`, `DELVE_OPTION_CAP`), `castModalSpell` | **znalezisko B** (limit z wydruku zamiast z kosztu całkowitego, CR 702.66a/b) — naprawione; po naprawie jedno źródło limitu (`delveGenericMana`) |
| `src/engine/tokens.js` | +23: `elseEffectSummary` | bez zastrzeżeń |
| `src/engine/triggers.js` | +41: trigger Barala, licznik `instantSorceryCastThisTurnByPlayer` | **znalezisko C** (brak resetu licznika przy zmianie tury) — naprawione; mutant PA czerwony |
| `src/protocol/types.js` | +13: `resolve_delve_exile`, `resolve_craft_exile` + zdarzenia | poprawne (uzupełnione w D o `resolve_aura_host` i 2 zdarzenia) |
| `src/table/choice-request.js` | +113: kreator wygnania Delve (`renderDelveExileWizard`), `previewCardIdOfOption`, `controllerSuffix` | poprawne; licznik i podgląd kart spójne z limitem silnika |
| `src/table/index.html` | +35: style kreatora | bez zastrzeżeń |
| `src/table/main.js` | +135: obsługa kreatorów, wyłączenie deck-buildera | poprawne; wyłączenie deck-buildera zgodne z ADR 0029 (tylko talie z kolekcji) |
| `src/table/mana-wizard.js` | +65: `guideManaSources` | poprawne; mutant PG czerwony. **Otwarte (niskie)**: założenie o prefiksach identyfikatorów źródeł — §6 |
| `src/table/multi-target.js` | +70: `castModePlanOf`, `modeFollowUpPlanOf` | logika poprawna. **Otwarte (niskie)**: `modeFollowUpPlanOf` (l. 644) eksportowany, ale bez użytkownika — §6 |
| `src/table/render.js` | +381: `STATIC_CONDITION_MECHANIC_LABELS`, `selectedLogTurn`, `renderLogPanel`, pipy zdolności | poprawne (C3: log chronologiczny, wybór tury); mutanty M389 (pip hybrydowy) zielone. **Otwarte (niskie)**: dedup wpisów tur tylko po numerze — §6 |
| `src/table/session.js` | +298: `hasMeaningfulDecisionOf`, `manaSourceLogText`, auto-pass | poprawne; mutant PH czerwony (auto-pass zatrzymuje się na czystej zdolności many) |

Pozostałe pliki zmiany: 52 testowe (28 nowych — wszystkie przeczytane; piny
nazwane po znaleziskach i regułach CR, bez asercji „na wiarę"), 34 dokumenty
(plan sesji, ADR-y 0029/0030, handoff, `docs/cards/scryfall-*.json`), 13 talii
(`decks/*.txt` — zostają w repo, strażnik `test/repo-artefakty-audytu.test.js`),
6 narzędzi (`tools/table-tester`, `tools/run-tests.mjs`), README — bez
zastrzeżeń merytorycznych. W planie CSV skorygowano błąd przypisania edycji
(Theros → Wiedźmin) — zgodne z drukiem właściciela.

**ADR 0002 (generyczność):** w całym dodanym kodzie `src/` nie ma ani jednego
porównania po `cardId`/nazwie karty
(`git diff 8af0c7c..0b49b12 -- src/ | grep -E "^+" | grep -cE "cardId === ['\"]|\.name === ['\"]"` → **0**).
Reguły czytane są z deskryptorów (`cost.delve`, `effect.type`,
`aura.doesntUntap`, `MANA_ABILITY_PAYLOAD_KEYS`). **Bez zastrzeżeń.**

**ADR 0005 (determinizm):** zero nowych wywołań RNG w dodanych liniach `src/`
(grep `Math.random` → **0**); oferty i wyceny to deterministyczne funkcje stanu.
**Bez zastrzeżeń.**

**ADR 0017 (kompletność widoku):** `pendingDelveExile` i `pendingCraftExile` są
mapowane do widoku gracza razem z listą kandydatów — interfejs ma czym
podejmować decyzję. **Bez zastrzeżeń** (lukę tego typu miało dopiero znalezisko
D — nowa decyzja `pendingAuraHost` weszła do widoku razem z naprawą).

**ADR 0029/0022 (karty):** wszystkie nowe karty mają druk w kolekcji
właściciela i snapshot ze Scryfall w `docs/cards/`; deck-builder pozostaje
wyłączony. **Bez zastrzeżeń.**

## 3. Weryfikacja mutacyjna pinów PR #130 (L13)

20 mutantów, każdy przywrócony po pomiarze. Wynik: **20/20 wykrytych**.

| Mutant | Podmiana | Cel | Wynik |
|---|---|---|---|
| PA | trigger Barala co każdy czar (nie pierwszy) | piny PR #130 (Baral) | wykryta |
| PB | sloty bloku `min(slots, 2)` | piny PR #130 (combat) | wykryta |
| PC | `castsWithoutPayingMana` bez stempla impulsu | piny PR #130 (impulse) | wykryta |
| PD | landcycling bez kary za brak celu | piny wyceny bota | wykryta |
| PE | Skarb nie wyprzedza rzutu (`treasureRefundLead`) | piny wyceny bota | wykryta |
| PF | brak kary za oddaną gardę | piny wyceny bota | wykryta |
| PG | kreator many bez filtra brakującego koloru | piny UI/wizarda | wykryta |
| PH | auto-pass zatrzymuje się na czystej zdolności many | piny auto-passa | wykryta |

(Wniosek: testy dodane w PR #130 są realnymi strażnikami, nie dekoracją.)

Mutanty dla **własnych** napraw tej sesji:

| Mutant | Podmiana | Wykryta przez |
|---|---|---|
| P1 | bramka oferty permanentu z `limit > 0` | `audyt-pr130-delve-i-licznik-tury` (A) |
| P2 | `delveGenericMana` z wydruku (bez obniżek) | j.w. (B, B2, B3) |
| P3 | usunięty reset licznika Barala | j.w. (C) |
| P4 | strażnik sumy w `castPermanent` | j.w. (B4) |
| P5 | strażnik sumy w `castSpell` | j.w. (B5) |
| P6 | bramka many przed wygnaniem w `castSpell` | j.w. (B6) |
| P7 | routing decyzji bez `limit > 0` | j.w. (A) |
| Q1 | `hosts.length > 1` → `false` (automat zamiast decyzji) | `audyt-pr130-gospodarz-aury` (D) |
| Q2 | brak re-walidacji gospodarza przy wykonaniu (`isLegalAuraHost`) | j.w. (D) |
| Q3 | bramka execute nie odrzuca cudzej decyzji | j.w. (D) |
| Q4 | brak `pendingAuraHost` w `firstPendingDecision` | j.w. (D) |
| Q5 | wycena bota: zawsze pierwszy kandydat (`hostile = false`) | j.w. (D) |

Q3/Q4/Q5 **przeżywały pierwszą wersję pinów** — piny pokrywały tylko ścieżkę
„szczęśliwą". Domknięte trzema dodatkowymi testami (obcy decydent, brak passa
u właściciela decyzji, bot wybiera po znaku aury) w `69b69e2`. To dokładnie
przypadek, przed którym ostrzega L13: zielony test bez mutacji nie jest
dowodem.

## 4. Znaleziska (naprawione w tej sesji)

### A (wysokie) — Delve na permanentie: pusty grób odbierał rzut

Bramka oferty w `resources.js` wymagała `delveLimit > 0`, więc przy pustym
grobie Hooting Mandrills nie miał **żadnej** oferty rzutu — gracz nie mógł
zapłacić pełnego {5}{G}. CR 702.66a: „For each generic mana in this spell's
total cost, you **may** exile a card from your graveyard rather than pay that
mana" — wygnanie jest opcjonalne. Ścieżka czarów (`spells.js`) miała to
poprawnie: dwie bliźniacze bramki się rozjechały (klasa L107).

Naprawa (`b1c66e1`): reguła w JEDNEJ funkcji `affordableDelveCounts`
(`spells.js`), czytanej przez deklarację i obie bramki; przy limicie 0 decydent
nie dostaje decyzji bez alternatywy — rzut idzie zwykłą ścieżką (wzorzec
`resolveCraftExileOutcome`, L41).

### B (wysokie) — limit Delve liczony z kosztu wydrukowanego, nie całkowitego

CR 702.66a/66b i ruling KTK 2021-03-19: limit to część generyczna kosztu
**całkowitego** („applies only after the total cost of the spell with delve is
determined"; nie wolno wygnać więcej kart niż generic mana requirement „unless
an effect has increased its cost"). Przy reduktorze kosztu gracz mógł wygnać
więcej, niż wynosił koszt: `manaSpent`/`totalMana` stawały się ujemne,
`spendMana` rzucała `RangeError` — a ponieważ wygnanie następowało **przed**
płatnością, odrzucona komenda zostawiała karty grobu w exile (mutacja stanu
komendą nielegalną).

Naprawa (`b1c66e1`): `delveGenericMana` (`mana-cost.js`) jako jedno źródło
limitu dla `delveExileLimit`, `castSpell` i `castPermanent`; twardy strażnik
sumy przed pierwszą mutacją kosztu; bramka `producibleMana` w `castSpell`
(lustro tej z `castPermanent`); `delveManaAfter` bez dwóch gałęzi liczących to
samo.

### C (średnie) — licznik „pierwszy instant/sorcery w turze" bez resetu

`instantSorceryCastThisTurnByPlayer` (Baral and Kari Zev, ruling TDC
2023-04-14) nie był zerowany w bloku resetów przy zmianie tury — blok zeruje
`spellsCastThisTurnByPlayer`, `cardsDrawnThisTurn`, `landEnteredThisTurn`,
`abilityActivatedThisTurn` i resztę. Trigger odpalał **raz na partię** zamiast
raz na turę. Naprawa (`b1c66e1`): reset obok pozostałych liczników tur.

### D (wysokie) — gospodarza aury wracającej z grobu wybierał automat

`return_permanent_from_graveyard` (`effects.js`) wybierał gospodarza przez
`state.zones.battlefield.find(hostId => isLegalAuraHost(...))` — czyli
PIERWSZEGO legalnego w kolejności strefy. CR 303.4f i ruling OTJ 2024-04-12
(Annie Flash, the Veteran): gracz „chooses what it will enchant" w chwili
wejścia aury. To nie jest celowanie (hexproof/protection nie blokują), ale
**jest** decyzją gracza, gdy legalnych gospodarzy jest więcej niż jeden —
silnik ten wybór odbierał. Osiągalne kartami z kolekcji: Zoraline i Annie Flash
(`permanent_card_in_graveyard`, MV ≤ 3) mogą wrócić aurę; w katalogu są 23 aury
o MV ≤ 3 (Curiosity, Glaring Aegis, Containment Membrane, Chronic Flooding…).

Naprawa (`19f47fa` + piny `69b69e2`, wzorzec L41 — jedno źródło reguły):
eksportowany `returnPermanentFromGraveyardOutcome(state, targetId, effect,
auraHostId)` w `effects.js`; trzy gałęzie wyboru gospodarza — **0** legalnych →
karta zostaje w grobie + `aura_returned_without_host` (bez zmian), **dokładnie
1** → domknięcie automatyczne (wybór bez alternatywy nie jest decyzją), **≥2** →
blokująca decyzja `pendingAuraHost` + `aura_host_choice_required` (priorytet
przechodzi na decydenta, `restorePriorityTo` jak w craft exile). Do tego: pole
stanu, `firstPendingDecision` (kind `auraHost`), bramka execute (swój decydent,
kandydat z listy, gospodarz nadal legalny w chwili wejścia — CR 608.2b/LKI),
oferta po wariancie na kandydata (L48), widok `pendingAuraHost` (ADR 0017),
wpis w odcisku (`fingerprint.js`, klasa L16/B2), `resolve_aura_host` +
2 zdarzenia w `protocol/types.js`, log (`session.js`), tytuł grupy i etykieta
opcji (`render.js`), wycena bota TĄ SAMĄ regułą polaryzacji aury co wycena
rzutu (`auraIsHostile`) i typ na liście decyzji prostych (`aggro-bot.js`).

## 5. Skutki uboczne napraw (strażniki, które zapaliły się same)

Trzy istniejące strażniki wykryły nowe pole/decyzję bez ręcznego szukania:
`test/b2-odcisk-straznik-pokrycia.test.js` B2/2 („odcisk nie pokazuje kluczy
stanu: pendingAuraHost"), `test/fingerprint-pending-decisions.test.js` i guardy
renderu (`m163` A3: każda nowa komenda `resolve_*` potrzebuje etykiety;
`m201`: każda potrzebuje klucza grupowania). To najlepszy dowód, że sieć
strażników działa — **koszt naprawy D byłby znacznie wyższy bez nich**.

Budżet lektury startowej ma 15 tokenów zapasu (≈42 znaki): każdy nowy wpis do
`docs/LESSONS.md` wymaga kondensacji istniejących. Kondensacja wykonana w tej
sesji (commit opisany w §7).

## 6. Otwarte, nienaprawiane (świadomie — niskie ryzyko, brak reguły CR)

1. `modeFollowUpPlanOf` (`src/table/multi-target.js:644`) — eksport bez
   użytkownika (martwy kod UI); usunięcie to kosmetyka, nie reguła.
2. `guideManaSources` (`src/table/mana-wizard.js`) — założenie o prefiksach
   identyfikatorów źródeł many; dziś prefiksy są stabilne, błąd objawiłby się
   wyłącznie w podpowiedzi UI (nie w legalności).
3. `logTurnEntries` (`src/table/render.js`) — dedup wpisów tur tylko po
   numerze; przy partiach wieloturowych o tym samym numerze (nie występuje w
   silniku) dałby scalenie wpisów.
4. Okno menace/haste w wycenie bota (`heuristic-bot.js`) — wąskie: liczy
   najbliższą turę, nie perspektywę kilku tur. Jakość gry, nie poprawność.
5. `treasureRefundLead` — przy wielu Skarbach może zaproponować nadmiarową
   aktywację; silnik ją odrzuci (oferta = walidacja, L48), więc skutek to
   zmarnowany ruch bota.
6. `legalBlockerOptions` (`src/engine/combat.js`) — fallback powyżej capu 32:
   zbiór blokerów jest poprawny, ale niekoniecznie najlepszy z możliwych.
7. Pipy zdolności na kartach kolorowych (`render.js`) — kosmetyka widoku.

## 7. Werdykt

PR #130 był merytorycznie solidny (generyczność, determinizm, widok, karty z
kolekcji — bez zastrzeżeń), a jego testy są realnymi strażnikami (8/8 mutantów
PA–PH wykrytych). Audyt znalazł jednak **cztery błędy reguł**: trzy w Delve
(A, B — CR 702.66a/b) i liczniku tury (C), oraz jeden w ścieżce powrotu aury z
grobu (D — CR 303.4f). Wszystkie cztery naprawione w tej sesji, każda naprawa
z pinem RED→GREEN i dowodem mutacyjnym (P1–P7, Q1–Q5):

| Commit | Zakres | Testy | Build |
|---|---|---|---|
| `b1c66e1` | A + B + C (8 pinów) | 6035/6035 | 59 / 3957,2 kB |
| `19f47fa` | D (3 piny) | 6038/6038 | 59 / 3965,2 kB |
| `69b69e2` | piny mutacyjne D (3 testy) | 6041/6041 | 59 / 3965,2 kB |

Bramki końcowe tej sesji: `node tools/run-tests.mjs all` → **6041/6041, 0
fail**; `npm run build` → **59 modułów / 3965,2 kB**; budżet lektury ≤ próg.
