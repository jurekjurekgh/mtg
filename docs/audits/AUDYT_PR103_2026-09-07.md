# Audyt PR #103 — 2026-09-07, sesja 01a07b98

Audytowany PR: [#103](https://github.com/jurekjurekgh/mtg/pull/103), squash
`c9b884c`, poprzednik `dfbc39f`. Praca naprawcza: [#104](https://github.com/jurekjurekgh/mtg/pull/104),
gałąź `arena/01a07b98-mtg`. Plan opublikowany osobnym commitem `d389d15`
przed kodowaniem. **Nie dodano kart ani nie uruchomiono pełnego B0.**

## 1. Metoda i punkt odniesienia

Przeczytano cały diff kodu/testów/dokumentów (59 plików), sprawdzono miejsca
wywołania nowych helperów i projekcję stanu. Raporty danych sprawdzono
programowo: wszystkie komórki i sumy oraz dokładną zgodność tekstu z formatterem
JSON. Samo przejście testów nie jest poniższym werdyktem.

Baza zmierzona: `npm test` **4636/4636**, `npm run build` **59 modułów /
3369,6 kB**, osobno `node --test test/bot-benchmark.test.js` **10/10**.
Mutacje wykonane na odizolowanej kopii `git archive c9b884c` (bez innej gałęzi);
plik zawsze odtwarzany kopią w `finally`, każda zmiana przechodzi `node --check`.
Sondy wykonują komendy przez `execute` z asercją `ok` (wyjątek: tworzenie
zakrycia helperem do izolowanego porównania widoku). Żaden wynik odrzuconej
komendy nie stanowi dowodu skutku gry.

Źródła reguł (odczyt 2026-09-07):
- snapshot `docs/cards/scryfall-veiled-ascension.json`: 14 rulingów WotC
  2024-02-02, szczególnie brak ETB przy obrocie i rozróżnialność zakryć;
- [CR / oficjalny indeks](https://magic.wizards.com/en/rules): udostępniony
  TXT `MagicCompRules%2020260819.txt` podaje obowiązywanie od 07.08.2026;
- [CR 701.40a–h, manifest](https://mtg.wiki/page/Manifest#Rules), zwłaszcza
  **701.40c**: „its controller may turn that card face up using either the
  procedure described in rule 702.37e […] or the procedure described above”;
- [CR 701.34a, proliferate](https://mtg.wiki/page/Proliferate#Rules): „give each
  one additional counter of each kind”; CR 704.5c/104.4b: obie przegrane
  jednocześnie to remis, nie wygrana pierwszego przetwarzanego gracza;
- CR 704.4/117.2e: SBA nie wykonują się w środku rozstrzygania czaru.

Numery w starych komentarzach bywają z innych wydań (np. proliferate 701.27);
nie stanowią podstawy automatycznej zamiany wszystkich cytowań w historii.

## 2. Potwierdzone ustalenia (stan PRZED naprawami)

### F1 — numer zakrycia ujawnia relację między ukrytymi kartami

`tokens.nextFaceDownCopyNumber` filtruje po `cardId`, mimo że `copyNumber`
idzie jawnie do przeciwnika. Porównanie dwóch stanów różniących się tylko
wierzchem biblioteki, dwa wykonania cloaka:

| Ukryte karty | Publiczne etykiety przeciwnika |
|---|---|
| Plains, Plains | `Cloak 1`, `Cloak 2` |
| Plains, Island | `Cloak 1`, `Cloak 1` |

To i przeciek FoW (pochodna tajnej informacji), i nierozróżnialne cele.
M331 naprawił tylko dwa TAKIE SAME lądy; testy m319/m331 nie zmieniają nazwy
pod drugim rewersem. Naprawa: numer z jawnej historii wejść, bez grupowania
po tożsamości karty; test różnicowy całego widoku + etykiety/ciągłość numeru.

### F2 — manifest nie daje procedury obrotu za morph

`manifestCardFaceDown` zostawia `abilities: []`, podczas gdy cloak i rzut
morphem używają `faceDownAbilities`. Monastery Flock zmanifestowany, pula
**{U}**: brak oferty dla tego permanentu, `abilities: []`. CR 701.40c pozwala
zapłacić morph {U}, nie tylko koszt karty {2}{U}. Naprawa przez istniejący
provider, nie nowy typ komendy; obie procedury, megamorph, cleanup i brak
ujawnienia zdolności przeciwnikowi wymagają osobnych asercji.

### F3 — proliferacja: score zależy od kolejności graczy w jednym wyborze

Bot p2, obaj gracze przy 9 truciznach; prawdziwe rozstrzygnięcie Courage in
Crisis, następnie wybór `resolve_proliferate` (każdy execute przyjęty):

| targetIds | Score bota | Rzeczywisty wynik |
|---|---:|---|
| p1, p2 | 1000 | remis (winnerId null) |
| p2, p1 | −Infinity | ten sam remis |
| p1 | 1000 | wygrana p2 |

Wczesny `return finish(1000)` omija pozostałe konsekwencje wyboru. Nie
zidentyfikowano straty partii w próbce bazowej; dowód dotyczy oceny legalnej
komendy i podatności wyboru na kolejność ofert. Naprawa: obejrzeć CAŁY wybór
przed premią za zwycięstwo; polityka unikania własnej dziesiątej trucizny
zostaje. Kontrola: obaj gracze jako bot, permutacje ID/ofert, zwykły buff.

### F4 — bonus ETB za obrót i test wymagający tego błędu

Zmanifestowany Wormfang Newt 2/2, 5 many: bot wybiera
`turn_manifest_face_up`, score **3**, płaci {1}{U}. Wynik zawiera tylko
`mana_changed`, `object_flipped`, `turned_face_up` — nie ma ETB. Jest to ten
sam permanent, a lokalny ruling WotC mówi wprost: „turning a permanent face
up doesn’t cause any enters-the-battlefield abilities to trigger”.

Mutacja USUWAJĄCA `etbBonus` robi RED w **m334/C**: test wymaga kupowania
nieistniejącego efektu. Naprawa: usunięcie premii ETB i wymiana przesłanki C
na rzeczywisty zysk ciała/utratę warda, bez wymyślania nowej karty katalogu.

### F5 — test m336/E2 utrwala SBA w środku rozstrzygania

Courage in Crisis daje +1/+1 stworowi z −1/−1, po czym czeka na proliferate.
Na bazie `pendingSpell = {stackId: "spell-0", effects: []}`, ale liczniki są
już `{}`. `accepted()` odpala SBA po komendzie mimo niedokończonego czaru.
Pusty ogon efektów NIE oznacza zakończenia (to ta sama pułapka co M335).
CR 704.4 każe poczekać do zakończenia rozstrzygania. Naprawa musi być w
punkcie kontroli SBA dla wstrzymanego czaru, nie w samej proliferacji ani
przez odtwarzanie usuniętych liczników. Testy: licznik obu rodzajów w oknie,
anihilacja po zakończeniu, skutek kolejnego efektu, zwykłe SBA nadal czynne.

### F6 — M337/C jest próżny dla gałęzi manifestu

Test rzuca Manifest Dread i dodaje scry PRZED wyborem karty. Żaden permanent
nie został jeszcze zmanifestowany. Mutacja wyłącznie bramki manifestu
`optionalActionsOpen → hasPriority`: **M337/C nadal GREEN**, cały plik
czerwienieje tylko źródłowym M337/E. Nie stwierdzono tu nowego błędu
produkcyjnej bramki; naprawić fixture: prawdziwy manifest, oferta przed,
nieobecność przy własnej/cudzej decyzji, powrót i przyjęte execute po niej.

### F7 — ożywione CLI rulingów kasuje dane po trzech HTTP 429

Hermetyczna sonda CLI (preload `fetch` zwraca 429, zegar bez czekania;
snapshot testowy z jednym rulingiem, nie karta kolekcji):

    zapisane scryfall-probe.json: 0 rulingów
    Snapshoty: 1, zaktualizowane: 1, błędy: 0.

W pliku `rulings: []`, `rulingsSource: ""`, nowa data. `fetchRulings` po trzech
`continue` zwraca fikcyjny sukces. M328 testuje tylko ścieżki PRZED fetch.
Naprawa: wyczerpany limit jest błędem, plik ma pozostać bajt w bajt bez zmian;
przypadki 429→200, pusta prawdziwa lista i błędna odpowiedź oddzielnie.

## 3. Próby mutacyjne istniejących strażników

19 uruchomień: **17 błędnych wariantów zatrzymanych**, jeden wariant manifestu
przepuszczony przez test C (F6), jedna POPRAWKA ETB odrzucona przez zły test
(F4). Brak błędów składni/importu jako „dowodu RED”. Najważniejsze pary:

| Mutacja | Asercja, która reaguje |
|---|---|
| Cloak bez procedury morph / bez originalAbilities / bez ward z migawki | m322 A1/A2 / B1 / C1, oddzielnie |
| Manifest P/T 2/2 zamiast migawki | m334 A |
| cloakReady lub ward wycięte z odcisku | m323 A lub B, oddzielnie |
| Stara ósemka typów wardu | m324 B/C (decyzja, nie sam pin) |
| Surowa toughness blokera | m325 A (tylko pin źródłowy) |
| Manifest bez bramki decyzji | m337 E RED, C GREEN — F6 |
| Jedna karta manifest dread: return true | m335 A/D |
| Usunięcie bonusu ETB | m334 C RED — F4 |
| Brak jawnego cause / stara etykieta logu / numeracja tylko creature | m326 A1/A2/C1 / m331 A–D / m331 B |
| Wyłączony kreator face-up | m327 A/A2/B |
| CLI bez deklaracji files / stary budżet help | m328 A/B / m338 1/4 |
| Proliferate wraca do zera | m336 A/B/C |

## 4. Inwentarz i werdykt plik po pliku

| Plik | Sprawdzenie semantyczne / wniosek |
|---|---|
| `docs/ENGINE_MILESTONES.md` | Pin długu 63→62 uzasadniony usunięciem warunku pasa; nie jest to migracja protokołu. |
| `docs/LESSONS.md` | L1–L135 zachowane; nowe L136–L140 i budżet 99 946. Reguły czytane z testami, nie jako dowód poprawności F1–F7. |
| `docs/LESSONS_PRZYPADKI.md` | Wyniesione narracje L123/124/128/129/130/131/132/136/137 + L138–L140; odsyłacze i numery zachowane. |
| `docs/PROJECT_HISTORY.md` | Wpis i dwie kontynuacje zgodne z commitami; historyczne zapewnienia o FoW/wycenie obalone w F1/F4. |
| `docs/audits/AUDYT_PR102_2026-09-06.md` | Przeczytane §1–8. Poprawne źródła cloak/Oracle, ale wniosek „nic nie przecieka” pomija pochodne cardId (F1); M334/C utrwala ETB (F4). |
| `docs/audits/AUDYT_PR92_2026-09-02.md` | Zmiana tabeli wystąpień 54/63→53/62 zgodna z M293; dług pozostaje. |
| `docs/backlog.md` | Tylko aktualizacja tego samego licznika; brak nowego zadania do samowolnego podjęcia. |
| `docs/cards/scryfall-veiled-ascension.json` | Porównanie JSON: zmieniono tylko rulings, rulingsSource, rulingsPobrano; 14 rulingów WotC. Ruling o braku ETB przeczy wycenie (F4). |
| `docs/decisions/0025-benchmark-match-budget-not-all-combinations.md` | Data i liczby pełnego biegu potwierdzone z JSON/formattera; decyzja/budżet bez zmian. Numer remisu należy czytać jako CR 104.4b, nie 104.3a. |
| `docs/plans/PLAN_2026-09-06c-audyt-pr102-petla-jakosci.md` | Etapy zamknięte, następne przyrosty w handoffach; stare liczby końcowe dotyczą wcześniejszego etapu, nie HEAD. |
| `docs/setup/HANDOFF_2026-09-07a.md` | Historyczny stan M322–M335; końcowe fakty zastąpione 07c. Teza o numeracji wszystkich cloaków wymaga F1. |
| `docs/setup/HANDOFF_2026-09-07b.md` | Historyczny stan M333–M335. Zamknięta wycena Wormfang Newt wraca jako nowy, mierzalny fakt F4. |
| `docs/setup/HANDOFF_2026-09-07c.md` | Zgodne bramki, ostatni commit/PR sprawdzone zdalnie; prawidłowe zakazy pełnego B0 i dodawania kart. |
| `docs/setup/TESTER_STOLU.md` | Opis artefaktu, chwilowych talii, bramek i trzy osie audytu zgodne z narzędziem; rozróżniać decyzję od okna priorytetu. |
| `src/cards/card-data.js` | Wyłącznie sprostowanie opisu CR manifest dread; definicje i support bez zmian. |
| `src/controllers/heuristic-bot.js` | Ward: nowe typy i rezerwacja; pump: dwa porównania; proliferate/face-up: F3/F4. Zależność od kolejności oraz bonus nieistniejącego zdarzenia potwierdzone skutkiem execute. |
| `src/engine/abilities.js` | Wyłącznie numery CR; procedura morph pozostaje specjalną akcją. Jej udostępnienie dla manifestu brakowało (F2). |
| `src/engine/effects.js` | Migawki cech, abilities i return bez decyzji poprawne mutacyjnie; manifest nadal omija faceDownAbilities (F2). |
| `src/engine/fingerprint.js` | cloakReady i kwota ward zmieniają fingerprint niezależnie; numer etykiety słusznie nie jest regułą legalności. |
| `src/engine/game-state.js` | Transport faceDownCause i wspólna bramka poprawne, ale C nie testuje gałęzi (F6); accepted uruchamia SBA przy pendingSpell (F5). |
| `src/engine/identity.js` | Wyłącznie sprostowanie komentarza manifestu; nie zmienia fabryki obiektów. |
| `src/engine/permanents.js` | Odtwarzanie cech i centralne czyszczenie potwierdzone dla dwóch dróg cloaka; ward i P/T nie giną. Dalsza droga manifestu: F2. |
| `src/engine/resources.js` | Morph zeruje drukowany ward i odkłada go w migawce, publiczna przyczyna trafia na stos; eksport faceDownAbilities niezużyty w manifeście (F2). |
| `src/engine/state-based.js` | POISON_LOSS_LIMIT wspólny, próg bez zmiany. Miejsce wywołania podczas rozstrzygania stanowi F5. |
| `src/engine/tokens.js` | Zmiana cloakReady→faceDownCause naprawia dwa takie same lądy, ale licznik nadal grupuje po ukrytym cardId (F1). |
| `src/table/choice-request.js` | Nazwa celu delegowana do faceDownLabel; FoW zależy także od numeru, nie tylko braku cardId (F1). |
| `src/table/main.js` | Przyczyna w pasku stosu, turnUpCost przekazane do deskryptora; brak nowych zapisów stanu/losowości. Błędny numer CR 701.55c w komentarzu nie określa reguły. |
| `src/table/mana-wizard.js` | Koszt karty, kolorowe pipy i brama rozbieżności; oba typy otwierają wizard. Komentarz 701.55c→701.40b przy najbliższej zmianie tego miejsca. |
| `src/table/multi-target.js` | Czyste targetSlotsOf liczone raz; min/max/optional nadal z tych samych komend, bez zmiany semantyki null. |
| `src/table/render.js` | Wspólna etykieta celów/kafli; null badge wygnania nie uzupełniany Morphem. Nie wydobywa ukrytej nazwy, ale dziedziczy przeciek numeru (F1). |
| `src/table/session.js` | Log deleguje nazwę zakrycia przy zachowanej bramce obserwatora; numer ujawniony obu stronom musi być niezależny od cardId (F1). |
| `test/batch50-kart.test.js` | Tylko numer CR; negatywna oferta dla nie-stwora pozostaje. |
| `test/bug-hunt-2026-08-16-bronze.test.js` | Tylko CR morpha; realny obrót i pusty stos nadal asertowane. |
| `test/fixtures/bot-scoring-snapshot.json` | Dokładnie dwa mecze: @1000 tylko hash; @1001 hash i +1006 scoreSum. Liczby decyzji nie zmieniły się; brama bazowa zielona bez zapisu. |
| `test/fow-facedown-names.test.js` | Dodatkowy zakaz nazwy/skanu zachowany; alternatywa Cloak lub Morph zbyt szeroka do dowodzenia konkretnej przyczyny, nie bada F1. |
| `test/m258-f3-ward-cloak.test.js` | Tylko CR; testy warda i tworzenia 2/2 nadal działają. |
| `test/m293-peek-jeden-wizard-chipy.test.js` | 63→62 ma atrybucję do skasowanej listy; dokumenty zaktualizowane w trzech miejscach. |
| `test/m315-veiled-cloak.test.js` | Poprawiono CR i realny kształt fixture faceDownCause; zachowane asercje warda, latania, oferty i ceny obrotu. |
| `test/m319-cloak-etykiety.test.js` | Fixture dopasowane do widoku; dwa identyczne cardId nie wykrywają F1. |
| `test/m321-bot-uncover.test.js` | Tylko CR. Komentarz bonusu ETB/main jest błędną przesłanką, nie regułą; F4. |
| `test/m322-cloak-zdolnosci.test.js` | A/B/C reagują na osobne mutacje procedury, migawki abilities, kwoty ward; test C jest izolowanym obiektem syntetycznym, nie całym torem materializacji. |
| `test/m323-cloak-odcisk.test.js` | Dwie mutacje fingerprint ubite oddzielnie; kontrola realnego execute i świadoma granica copyNumber. |
| `test/m324-bot-ward-rodzina.test.js` | Powrót starej ósemki ubija B/C na wyborze bota, nie tylko pin nazw. Brak nowego powodu do zmiany wag/progów. |
| `test/m325-bot-pump-rodzina.test.js` | Mutacja surowej toughness ubita przez A; wyłącznie dowód źródłowy, nie twierdzenie o lepszej decyzji w próbce. |
| `test/m326-cloak-przyczyna.test.js` | Brak cause ubija A1/A2/C1; maskuje cardId/cloakReady, lecz nie bada zależności numeru od ukrytej karty (F1). |
| `test/m327-wizard-odsloniecie.test.js` | Wyłączenie branch ubija A/A2/B. Deskryptor+otwarcie, nie pełna płatność przez DOM; negatywy kosztu/X/braku obiektu. |
| `test/m328-narzedzie-rulingi.test.js` | Usunięcie deklaracji files ubija A/B; wszystkie testy omijają realny fetch i nie wykrywają kasowania po HTTP 429 (F7). |
| `test/m331-log-przyczyna.test.js` | Stary helper logu i stary warunek cloakReady ubite. Oba lądy to Plains — F1 niewidoczny. |
| `test/m333-przyczyna-manifest.test.js` | Testuje cause/ward, nie procedurę morpha po manifeście. Część wejść przez helper, nie execute; F2 uzupełnia tę lukę. |
| `test/m334-bot-manifest-odsloniecie.test.js` | A pinuje P/T, B słabe ciało. C robi RED po USUNIĘCIU nielegalnego bonusu ETB — zła oczekiwana decyzja (F4). |
| `test/m335-manifest-bez-wybory.test.js` | Powrót return true ubija A/D; test sprawdza dokończenie czaru, nie sam powstały obiekt. |
| `test/m336-bot-proliferate.test.js` | Usunięcie wyceny ubija A/B/C. Brak wspólnej trucizny obu graczy (F3); E2 utrwala SBA w środku czaru (F5). |
| `test/m337-akcje-opcjonalne-blokada.test.js` | Gałąź manifestu bez blokady: C nadal GREEN, czerwony tylko E. W C nie ma zmanifestowanego permanentu (F6). |
| `test/m338-pomoc-benchmarku-liczby.test.js` | Mutacja budżetu ubija 1/4; kształt policzony z katalogu. Watchdog opisany poprawnie, ale osobnego pinu watchdog nie dodano. |
| `test/table-mana-wizard.test.js` | Tylko CR; bezbarwny koszt morpha nadal sprawdzony. |
| `tools/b1-final-2026-09-07.json` | Wszystkie 570 komórek sumują się do 3×1900; wins+unfinished=games, 5700 w budżecie, stalls=[]. |
| `tools/b1-final-2026-09-07.txt` | Zawiera dokładny formatBenchmarkReport(JSON), poprzedzony postępem i zakończony podsumowaniem — brak sprzeczności dwóch raportów. |
| `tools/benchmark.mjs` | Zmiany komentarzy/help i eksportów, nie obliczeń. Liczby 8/6000/19→5/8000 zgodne z kodem i raportem. |
| `tools/fetch-card-rulings.mjs` | Deklaracja files ożywia CLI, ale trzy HTTP 429 zamienia w sukces z pustymi rulingami i nadpisuje snapshot (F7). |

## 5. Tropy zamknięte i granice dowodu

- Raport pełnego B0 nie jest uszkodzony: 570 komórek, 5700 meczów,
  19 talii, 190 par, 5 seedów, sumy poprawne, tekst dokładnie odtwarza JSON.
  Jeden wynik bez zwycięzcy nie upoważnia do kolejnego `--full`.
- Refaktor `multiTargetPlanOf` nie zmienia zbiorów celów/null; chodzi o
  uniknięcie trzech identycznych skanów, nie naprawę nowej reguły.
- Nowe jawne pole `faceDownCause` samo nie ujawnia karty. F1 dotyczy
  innego pola i jego zależności od tajnych danych; nie cofamy publicznej
  przyczyny ani warda, nie otwieramy dostępu do pełnego stanu botowi.
- F6 jest luką strażnika, nie pretekstem do przepisania poprawnego
  `optionalActionsOpen` ani dodania czwartej listy decyzji.
- `npm test` + benchmark-regresja nie dowodzą pełnej zgodności z CR:
  F1–F7 przeszły bazę. jsdom nie sprawdza layoutu, obrazów ani gestów.
- Historia zawiera kolejne stany tej samej sesji; końcowe wyniki czytać
  z 07c. Nie przepisujemy dawnych pomiarów ani pełnego raportu B0 nową datą.

## 6. Realizacja i kolejne pomiary

Audyt zakończony przed implementacją; F1–F7 skierowano do napraw i walidacji.
Partie Żywego Testera na bazowym artefakcie w toku (seedy 10401–10403,
pary/profil w planie wykonania); wyniki i dalsze znaleziska będą dopisywane.


### M339 — F1 zamknięte: publiczna numeracja bez tajnej tożsamości

`nextFaceDownCopyNumber(state)` liczy żywe zakrycia z całego stołu, nie
cardId ani kontrolera. Wspólny zakres pozwala zachować numer także po
przejęciu permanentu, bez kolizji dwóch wcześniejszych „Cloak 1”. Nie ma
nowego pola protokołu ani zależności bota od pełnego stanu.
`test/m339-cloak-numeracja-fow.test.js`: **4 RED przed / 4 GREEN po**;
pełne widoki przeciwnika identyczne dla czterech wariantów ukrytych kart,
rozróżnialność etykiet obu graczy, przejęcie i ciągłość po obrocie.
Z m319/m326/m331: **20/20**. Kolejne F pozostają do wykonania.


### M340 — F2 zamknięte: manifest korzysta z procedury morpha

Jedna zmiana producenta: `manifestCardFaceDown` używa tego samego
`faceDownAbilities(card)` co cloak i rzut morphem. Nie powstaje kolejna kopia
reguły. Test m340 prowadzi przez rzeczywisty Manifest Dread i wybór karty:
6 RED przed poprawką, kontrola bez morpha GREEN; po poprawce **7/7**.
Sprawdza tańszy morph {U}, obie procedury megamorpha (licznik tylko za koszt
megamorph), drukowane zdolności, zapłatę/kolor/priorytet, cleanup i FoW.
Rodzina m322/m333/m334/m335/m340: **28/28**. M339 przed tym przyrostem:
pełny rdzeń **4640/4640**, build **59 / 3368,9 kB**, commit `5d81996`.


### M341 — F3 zamknięte: najpierw cały wybór, potem premia za wygraną

`lethalPoison` odkłada premię do końca pętli; własna dziesiąta trucizna
nadal odrzuca wariant, bez zmiany wag ani progu. M341: **4 RED mutacji
wczesnego return + 2 kontrole GREEN → 6/6 GREEN**. Bot p1 i p2 wybiera
wygraną nawet po umieszczeniu wariantu remisowego na początku oferty;
każdy deklarowany skutek potwierdzony przez przyjęty execute.
Z m336 i golden-masterem **18/18**, fixture bez regeneracji.
Pierwsza wersja sondy B zakładała `targetIds: []` w pustej ofercie; protokół
pomija to pole. Ten błąd harnessu poprawiono, a RED powtórzono mutacją
(cztery asercje, bez TypeError). M340 przed tym przyrostem: **4647/4647**,
build **59 / 3369,1 kB**, commit `3a12df7`.


### M342 — F4 zamknięte: odsłonięcie nie kupuje ETB

Usunięto bonus za trigger wejścia, bez zmiany wag ciała/keywordów/wardu.
M334/C ma teraz rzeczywistą przesłankę: Garruk's Companion 3/2 z trample
zyskuje 2 punkty, ale pod cloak traci ward. Wormfang Newt dostaje negatywny
strażnik m342/B. Syntetyczna para różniąca się TYLKO ETB sprawdza oba tory
obrotu i brak wywołania ETB w execute: **3 RED→3 GREEN**. Z m321/m334 i
golden-masterem **16/16**, fixture nietknięte.
Poprawiono fałszywy komentarz o „braku skutku obrotu w walce”: pozostaje
konserwatywna polityka bota tylko w mainie, nie zakaz regułowy ani dowód
optymalności. M341: **4653/4653**, build **59 / 3369,2 kB**, `3844ffb`.

### E3 — nowy fakt F8: tester pokazywał początek logu zamiast ogona

Pierwsze trzy partie ukończone: innistrad-wu|warhammer-ubr s10401 explorer
(74 kroki), mirrodin-brg|zendikar s10402 defensive (61),
warhammer-wg|wiedzmin s10403 impatient (78). Wszystkie z ptaszkami auto-pass,
wszystkie 0 zgłoszeń. Ręczny odczyt: w końcowym snapshocie nadal
`Obie ręce zatrzymane […] Nowa partia (seed 10401)`. Render loguje najnowsze
na początku DOM (`reverse()`), a snapshot testera wybiera `slice(-6)`.
Bliźniaczy kolektor odrzuceń czyta nowe wpisy po indeksie od końca i przy
drugim odrzuceniu powtarza stare. `collectMainLog` już zna poprawny kierunek,
lecz nie jest wspólnym źródłem. **F8 do naprawy w E3:** jeden ekstraktor
kolejności dla tych trzech konsumentów + testy i ponowne partie A/B,
bez zmiany produktu tylko po to, by dopasować go do narzędzia.


### M343 — F5 zamknięte: SBA po dokończeniu wstrzymanego czaru

Wspólne `stateBasedActionsOpen` osłania `runStateBasedActions` ORAZ późniejszą
fazę usuwania tokenów w `accepted`. `pendingSpell.effects: []` nie otwiera
bramki. Nie zmieniono listy kandydatów proliferacji, nie odtwarza się
przedwcześnie usuniętych obiektów/liczników i nie wyłączono inwariantów.
Poprawiono błędny m336/E2: oba liczniki istnieją przy wyborze, są zwiększane,
a SBA znosi pary po wyjściu czaru ze stosu.

M343 **5 RED→5 GREEN**: liczniki, 0 życia przez DWA kolejne scry → odzyskanie
życia, chwilowe 0 wytrzymałości z odzyskaniem i bez (śmierć na końcu), token
w wygnaniu (zanika dopiero po końcu). To dowód, że dawne zachowanie NIE było
„kosmetyką”: przegrywał gracz, który kończy rozstrzyganie z 2 życia.
Trzy osobne mutacje: stare SBA ubija A/B/C/D, pominięta bramka tokenów ubija D,
uznanie pustego ogona za koniec ubija A/C(false)/D. Z m335/m336 i fixture:
**21/21**, golden-master bez zapisu. M342: **4656/4656**, **59 / 3369,2 kB**,
commit `437cf27`.

Pierwsza pełna brama M343: **4660/4661**. Jedyna czerwień to stary
`real-cards-batch12` (Rage of Purphoros), wymagający śmierci 3/3 PRZED
końcowym scry. Snapshot Oracle: „deals 4 damage […] Scry 1”; CR 704.4
wymaga SBA dopiero po całości. Pin poprawiony DWUSTRONNIE: przed wyborem
stwór ma 4 obrażenia na polu, po wyborze ginie. To nie usunięcie asercji
śmierci ani rozluźnienie progu; przykład rzeczywistej karty tej samej klasy.


### M344 — F6 zamknięte: strażnik ma realny manifest i przechodzi całe okno

M337/C najpierw kończy Manifest Dread, potwierdza istniejącą ofertę obrotu,
a dopiero potem otwiera własne/cudze scry. Sprawdza brak ofert u OBU graczy,
odrzucenie starej komendy, powrót oferty i przyjęty obrót po decyzji.
Kod produkcyjny bramki nietknięty. Plik: **6/6**; ta sama mutacja wyłącznie
bramki manifestu, która przechodziła dawny C, teraz daje **1 RED / 1 kontrolę
GREEN w C** (bez uruchamiania źródłowego E).
M343 wypchnięte `a659024`: **4661/4661**, build **59 / 3369,7 kB**.
Szybki benchmark po wszystkich naprawach produkcyjnej rozgrywki:
**672 mecze / 173,3 s**, heuristic **570/672 (84,8%)**, aggro **89/336**,
random **13/336**, identycznie z bazą. Żadnego podnoszenia progów ani `--full`.


### M345 — F7 naprawione lokalnie: błędny fetch nie udaje pustych rulingów

Wyczerpany limit 429 rzuca błąd, odpowiedź 200 musi zawierać tablicę `data`,
a pierwsze pobranie pustej listy zapisuje proweniencję zamiast udawać „bez
zmian”. Prawdziwe [] nadal może zastąpić stare dane, --dry-run nie zapisuje,
wcześniej potwierdzone niezmienione dane nie są reformatowane. Nie pobierano
sieciowo ani nie przepisywano żadnego snapshotu kolekcji.
M345: **3 RED + 5 kontroli → 8 GREEN**; trzy mutacje osobno ubite przez
A (429), C (kształt i kontrolowany błąd, nie TypeError), F (proweniencja).
Z m328 **12/12**. Mock obejmuje tylko sieć/zegar, CLI i zapis pliku są realne.
M344 wypchnięte `af0226f`: **4662/4662**, build **59 / 3369,7 kB**.


**Blokada po zielonej bramce:** M345 `npm test` **4670/4670**, build
**59 / 3369,7 kB**. Pierwsze sprawdzenie stanu PR przed commitem zakończyło
się `HTTP 401: Bad credentials` z GitHub. M345 nie został jeszcze
zacommitowany ani wypchnięty; bieżącego stanu PR/CI nie udało się sprawdzić.
Kod i testy zachowane lokalnie, właściciel poproszony o ponowne połączenie
GitHub w Arena. F8 i brama końcowa `test:all` pozostają do wykonania.


**Wznowienie po odświeżeniu GitHub (2026-09-07):** połączenie działa,
PR #104 nadal OPEN/draft, gałąź zdalna i lokalna zgodne (`af0226f`, 0/0),
main nadal `c9b884c`. Brudne drzewo M345 zachowane bez resetu. Ponowna
bramka fast + build przed osobnym commitem/pushem M345; potem F8 i E4.


### M346 — F8 zamknięte: tester ogląda bieżący log, nie początek partii

Jeden `chronologicalLogEntries` dla snapshotu, odrzuceń i dowodów auto-pass.
Nowe powody odrzuceń są przypisywane do NOWEGO kliknięcia; identyczne teksty
nadal oznaczają dwa zdarzenia. Skrócenie logu zeruje indeks kolektora.
`captureLogWindow` jest wspólnym producentem dla snapshotu i rekordów okien;
najnowszy wpis DOM jest pobierany niezależnie od badanego ekstraktora.
Nowy `detectStaleLogTail` działa również bez linii snapshotów.

Test m346 wykonuje rzeczywiste lokalne funkcje sterownika w minimalnym DOM
przez `node:vm` (nie kopię algorytmu i bez jsdom w głównym pakiecie).
**6 RED + 3 kontrole → 9 GREEN**; A/B/C/D czerwieniały na zachowaniu starego
sterownika, G na braku nowego API, H na braku detektora. Cztery mutacje
osobno ubite: stary ogon A/B, stare odrzucenia C/D, brak chronologii
A/B/C/F/G, wyłączony detektor H. Rodzina z testami ekstraktorów/detektorów:
**116/116**. Kod stołu/engine nie był zmieniany, żeby dogodzić testerowi.

**Żywe A/B, ten sam artefakt i seed 10401:** stara kolejność zasymulowana
wyłącznie w producencie ogona, plik przywrócony kopią w `finally`.

| Tryb | Przed: flagi nowego detektora | Po: flagi nowego detektora | Wynik |
|---|---:|---:|---|
| quiet, explorer/policy13/tick0.2 | 14 | 0 | koniec w kroku 74, życia −2/20 |
| snapshot-every 1, ten sam profil | 14 | 0 | koniec w kroku 73, życia −2/20 |

Różnica kroków dotyczy próbkowania DOM (te same kroki przed i po dla danego
trybu); flagi F8 są identyczne w obu trybach. Przed ogon kończył się
`Nowa partia (seed 10401). Powodzenia!`, po:

    Revolutionist zadaje 3 obrażenia (Ty) ⏎ Ty: życie 5 → 2 ⏎
    Minotaur Abomination zadaje 4 obrażenia (Ty) ⏎ Ty: życie 2 → -2 ⏎
    Przegrywasz (brak życia)

Dalsze dwie partie po naprawie: s10402 (defensive) koniec 61, 0 flag;
s10403 (impatient) koniec 78, 0 flag. Profile/talie jak w tabeli bazowej.
Łącznie po poprawce trzy ukończone partie quiet bez flag + powtórka g1
ze snapshotami. Ta ostatnia ma JEDNĄ inną flagę — F9 poniżej, nie F8.
Transkrypty `tools/table-tester/audyt-pr104/m346-*` są gitignorowane.

### F9 — nowy fałszywy alarm: oznaczenie kosztu dodatkowego to nie pusta cena

W g1 verbose (przed i po M346): `Pusty deskryptor kosztu` dla
`Wygnaj stwora z grobu (koszt) — Makeshift Mauler`.
`choiceSourceTitle` w render.js generuje ten tekst dla `cast_permanent`
z `exileTargetId`: czynnością kosztową jest WYGNANIE STWORA, a nie brakująca
kwota many. Drugi wariant tej samej rodziny to `z pola bitwy`.
To fałszywy alarm detektora, nie błąd rozgrywki; regex utożsamia każde
`(koszt)` z pustym slotem liczbowym (analog kalibracji glosy Escape w M267).
Naprawa w kolejnym niezależnym kroku: rozróżnić ten znacznik od brakującej
ceny, zachować wykrywanie prawdziwych pustych cen/dopłat (także obok poprawnego
znacznika w jednym panelu), oprzeć odczyt etykiet panelu także na rejestrowanych
akcjach. Test przez prawdziwy widok/tytuł grupy + kontrola starych przypadków,
mutacja i ponowna partia. NIE zmieniać poprawnych etykiet produktu pod regex.

M345 wypchnięty jako `0f294d8` po wznowionej bramce **4670/4670** + build.
Blokada autoryzacji zamknięta; końcowa brama PR pozostaje w E4.


### M347 — F9 naprawione u detektora, bez zmiany poprawnej etykiety stołu

Klasyfikator wyłącza wyłącznie znacznik roli kosztu po wygnaniu stwora
(z grobu albo pola bitwy); reszta tego samego panelu nadal jest analizowana.
Puste `(koszt)`, `(cena )`, `+ kicker )` pozostają błędami. Gdy są rekordy
okien, pełne etykiety panelu pochodzą z `windowRecords.actions`, a nie z linii
snapshotu ani przyciętych nazw „pokrycia UI”. Podwójna obserwacja tej samej
etykiety nie daje podwójnego alarmu. Bez rekordów działa parser archiwalny;
linie ręki/modali są nadal osobnym źródłem.

M347: **4 RED + 2 kontrole → 6 GREEN**. A obejmuje dwie strefy przez
`playerView` → `choiceGroupLabel` i przyjęty execute płacący wygnaniem.
C sprawdza, że flagowany jest pusty kicker OBOK poprawnego znacznika, nie
sam znacznik (sama liczba flag byłaby próżnym pinem). D sprawdza jednakowe
wyniki na tych samych rekordach z liniami verbose i bez nich.
Cztery oddzielne mutacje: brak rozróżnienia A/C/E, pominięcie całego panelu C,
brak podpięcia rekordów D, rozluźnienie detekcji przez wymaganie spacji B/D.
Z m266/m346/detektorami **125/125**. Trwają powtórki czterech przebiegów
(g1 quiet/verbose oraz g2/g3 quiet); wyniki zbiorcze w zamknięciu poniżej.
M346 wypchnięte `528e76e`: **4679/4679**, build **59 / 3369,7 kB**.

**Powtórki M347:** g1 quiet/verbose (po 19 okien), g2 (15 okien), g3
(155 okien) zakończone, wszystkie 0 flag; w g1 zniknęło zgłoszenie F9,
a rekordy F8 nadal zgodne z DOM. Wyniki żyć: g1 −2/20, g2 −9/23,
g3 12/−1. Nie utożsamiamy tego z brakiem błędów: stderr g3 ujawnił F10.

### F10 — wyjątek JavaScript przy zastąpieniu otwartego kreatora many

W profilu impatient s10403, mimo ukończenia partii i 0 flag:
`TypeError: session.log is not a function` w `main.js:play`.
Sonda stderr potwierdza ten sam błąd także w dawnych logach g3. Kod
zamyka stary kreator, po czym woła `session.log('system', ...)`; `log`
w `createSession` jest TABLICĄ. Przez wyjątek nowa komenda nie dociera
ani do `playDirect`, ani do nowego kreatora. To nie jest oczekiwane
odrzucenie komendy przez silnik w profilu impatient.

Następny przyrost M348: jawny interfejs wpisu systemowego sesji (przez
istniejący sessionLog, bez zmiany tablicy dla czytelników), poprawne
wywołanie w play; test zastąpienia kreatora na prawdziwej sesji, zachowania
many i obsłużenia nowej komendy. Tester musi rejestrować runtime errors
od beforeParse, a nie polegać na tekście logu gry. Oddzielny detektor bez
wyjątku dla impatient, testy i żywe A/B na starym/poprawionym artefakcie.


### M348 — F10: poprawny zapis systemowy i obserwacja wyjątków runtime

`createSession` udostępnia `logSystem(text)` przez istniejące `sessionLog`;
`session.log` pozostaje tablicą. `play` zamyka stary kreator, zapisuje
anulowanie i normalnie przechodzi do nowej komendy/kreatora. Nie ma osłonki
`typeof log === function`, która tylko zamaskowałaby niedostarczenie wpisu.
Samo anulowanie nie cofa ani nie wydaje many (fingerprint bez zmian).

M348 UI: **3 RED + 1 kontrola → 4 GREEN** (realna sesja, rzeczywista funkcja
play w VM, stubs tylko dla DOM/kolejnego kroku). Dwie ścieżki nowej komendy,
typ wpisu, zachowanie tablicy, many i brak spamu bez starego kreatora.
Runtime: **6/6** (error, Promise, realne boot ze wstrzykniętym konstruktorem
JSDOM, profile/logowanie, odrzucenie ≠ wyjątek, transport do detektorów).
6 mutacji osobno: dawne wywołanie tablicy A; zgubiona nowa komenda A;
pusty zapis A/B; spóźniony obserwator runtime C; wyłączony detektor D/F;
zgubiony parametr F. Rodzina z m346/m347/mana-wizard: **50/50**.

**Dowód PRZED na starym artefakcie** (s10403, impatient/policy19/tick0.15):
quiet i verbose rejestrują po **6 runtimeErrors**, po jednym unikalnym
zgłoszeniu `Wyjątek JavaScript w stole`, mimo ukończenia partii 12/−1.
Hash starego HTML: `c374298d173718f29e1803f239b7ea39d7f13e587f5188b9d5af9bae7e35962d`.
Po przebudowie z poprawką hash `b635c7d731821953df4a3f597f802dd404a0e5b3f9d82b93326968a0bd31e8cc`,
**59 modułów / 3369,8 kB**. Powtórki PO oraz dwa inne profile w toku.
M347 wypchnięte `2976a45`: **4685/4685**, build **59 / 3369,7 kB**.


**M348 PO, twarda kontrola `runtimeErrors` i wszystkich flag:** s10403 quiet
oraz verbose: **0 wyjątków / 0 flag**, po 126 okien, naturalny koniec 14/−5.
Przed było po 6 wyjątków i 155 okien, koniec 12/−1. Wynik może się zmienić,
bo po poprawce nowe kliknięcia rzeczywiście przechodzą zamiast przepadać
w wyjątku; nie jest to dryf bota headless. Tryb verbose PRZED miał również
trzy informacje o odrzuconym `pay_or_sacrifice_unresolved`; PO już ich nie ma.
Nie zmieniano mapy odrzuceń ani nie wyciszano profilu/wiadomości.

Kontrole na nowym artefakcie z obserwatorem od startu: g1 explorer s10401
(19 okien, −2/20) i g2 defensive s10402 (15 okien, −9/23), obie **0 wyjątków /
0 flag**. Przeczytano transkrypty w osiach: zachowanie bota, informacja
log/modal, auto-pass. Własny Shatter w Ichorclaw Myr / counter w własny
Dream Twist to ruchy testowego GRACZA, nie heurystyki przeciwnika; seria
kliknięć equip w profilu impatient wymaga sprawdzenia odrzucenia, nie
przypisywania botowi pętli. Log obecnie pokazuje też końcowe obrażenia,
zmiany życia i śmierci; stan starego ogona nie maskuje końca partii.
Pozostaje granica narzędzia: jsdom nie weryfikuje wyglądu/gestów na telefonie
ani optymalności każdego ruchu bota. Bramki E4 jeszcze przed nami.
