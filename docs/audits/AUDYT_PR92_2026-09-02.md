# AUDYT PR #92 — M278–M279 + M280 (sesja arena/01a06193, 2026-09-02)

**Audytowany PR:** #92 „Sesja arena/01a05d4f: audyt PR #91 + pętla jakości + batch 52 kart”
(squash `db0c493`, scalony 2026-09-02 o 10:03, 1452 plików, +254 258 linii —
w tym snapshoty Scryfall i `docs/replays`, które stanowią większość objętości).
**Metoda (ADR 0020 B / ADR 0016):** pełne czytanie diffa `src/`, odtwarzanie
zachowania na żywych stanach (repro w `/home/user/scratch/*.mjs` uruchamiane na
`main` + gałęzi, nigdy tylko raport z testów), weryfikacja mutacyjna RED→GREEN
dla każdej naprawy, sprawdzenie każdej decyzji z CR/Oracle (L57 — zgłoszenie z
rozgrywki ≠ reguła), audyt kontraktów pól (ADR 0027) i odcisku stanu (ADR 0005).
**Pomiar startowy sesji:** `npm test` **4131/4131**, `npm run build`
**57 modułów / 3080,2 kB**, HEAD `db0c493` (= `main`).
**Naprawczo:** ten PR (#93) nie wprowadza nowych kart ani mechanik — pięć
commitów naprawczych zebranych z audytu + strażnicy klasy (punkt 2.2 planu) +
domknięcie punktu 2.3 planu (mechaniczny przegląd `pending*`).

## Werdykt

PR #92 **wnosi realną wartość** (9 kart batchu 52 zgodnie z Oracle, dwie luki
wyceny bota zamknięte u root cause, zawężenie oferty Discover, korekta
„nieograniczonej man'y” w `tools/table-tester/proxy.mjs`) i **nie pogorszył
żadnej bramy**. Znalazłem w nim **pięć defektów w trzech klasach**:

1. **utracone pokrycie odcisku stanu** — dwa `pending*` blokujące priorytet
   wypadły z `PENDING_DECISION_FIELDS`, a strażnik, który miał to wykryć,
   był vacuous (znaleziska 1+2, razem — to samo korzeń);
2. **stan zamiast zdarzenia** — wyzwalacz liczony „które to z rzędu dobranie”
   czytał licznik podnoszony przez trzy rozjechane ścieżki (znalezisko 3)
   oraz **grupowanie po złym wymiarze** — dedup wyzwalaczy po kontrolerze
   zamiast po instancji zdolności (znalezisko 4);
3. **luka między ofertą a walidacją** — M280/F naprawiło samą ofertę,
   zostawiając starszą, szerszą walidację i trzy kopie tego samego filtru
   (znalezisko 5).

Wszystkie pięć naprawione, każda z testem RED→GREEN i sprawdzeniem mutacyjnym
w obie strony (wycofanie naprawy czerwieni test; pozostawienie naprawy przy
osłabieniu strażnika też czerwieni). Żadna zmiana nie jest obejściem ani
maskowaniem (`return`, `try/catch`, warunek od nazwy karty — ADR 0002).

## 1. Znalezisko 1+2 — odcisk stanu traci dwa blokujące `pending*`, a strażnik milczy

**Objaw.** `pendingWardPay` (oddawany od M157) i `pendingExileCast`
(wniesiony przez Vaana w #92) są w `firstPendingDecision` (blokadzie
priorytetu ~64 pól), ale nie w `PENDING_DECISION_FIELDS` (projekcja odcisku).
Stan z takim pendingiem dawał identyczny odcisk jak stan bez niego — golden
replay i testy porównujące fingerprint nie odróżniały „czeka decyzja” od
„nic nie czeka”.

**Dlaczego nikt nie zobaczył (L26 + L112).** `test/fingerprint-pending-decisions.test.js`
wywodził ground truth z ciała `firstPendingDecisionPlayerId` **regexem** — a ta
funkcja jest delegatem (`return firstPendingDecision(state)?.playerId ?? null`),
więc zbiór oczekiwany był **pusty**, a test przechodził jako vacuous. Drugi test
„każde pole z ground truthu jest w odcisku” nie miał czego sprawdzać. Trzeci warunek
`found.length > 0` też jest spełniony przy jednym trafieniu.

**Naprawa (commit `fb92c01`).**
- strażnik skanuje **oba ciała** (`firstPendingDecision` + delegat) i assertuje
  **liczebność** ground truthu (`>= 50`, aktualnie 64 pola) — bez licznika
  każda przyszła podmiana delegata znów uciszy bramkę;
- pin „strażnik nie jest vacuous”: wylicza 4 pola znanego wystąpienia klasy
  (`pendingWardPay`, `pendingExileCast`, `pendingEscapeExile`, `pendingDiscover`)
  i assertuje, że ground truth jest ich nadzbiorem;
- `src/engine/fingerprint.js`: oba pola do `PENDING_DECISION_FIELDS` (projekcja
  gracza + projekcja globalna).

**Testy.** `test/fingerprint-pending-decisions.test.js` +2 przypadki
behawioralne: odcisk **musi drgnąć** po wejściu w `pendingWardPay` i
`pendingExileCast` (wcześniej oba dawały ten sam fingerprint = RED), pin
nie-vacuous, licznik pól. Mutacyjnie: usunięcie `pendingExileCast` z odcisku →
2 RED; przywrócenie skanu samego delegata → 2 RED (w tym pin vacuous).

## 2. Znalezisko 3 — Jolrael: „porządek dobrania” liczony ze STANU, nie ze zdarzenia

**Objaw (repro `/home/user/scratch/repro-jolrael.mjs`).** Jolrael, Mwonvuli
Recluse (MKC): „Whenever you draw your **second** card each turn, create a 2/2
green Cat creature token" (sprawdzone 1:1 z `docs/cards/scryfall-jolrael-mwonvuli-recluse.json`).
Silnik
czytał `state.players[ev.playerId].cardsDrawnThisTurn === 2` **po** komendzie,
a licznik podnoszą trzy rozjechane ścieżki (draw step w `game-state.js`,
`drawPlayerCards` w `effects.js`, cycling w `spells.js`). Skutki:
- „draw two” na starcie tury → licznik 0 → 2 → **dwa** wyzwalacze (ma być jeden:
  to samo dobranie, licznik rośnie przy pierwszym i drugim);
- dobranie w draw step + „draw two” → licznik 1 → 3 → **zero** wyzwalaczy
  (ma być jeden — drugie dobranie tego porządku to pierwsza karta z drugiego dobrania).

**Decyzja (nie „porządek względem czyjejś komendy”, a zdarzenie).** Naprawa
u korzenia, nie warunkiem specjalnym: wprowadzony **choke point**
`recordCardDrawn(state, playerId, extra)` w `src/engine/players.js` — wszystkie
trzy ścieżki dobierania przechodzą przez niego i stemplują w zdarzeniu
`card_drawn` pole `drawNumberThisTurn` (1 = pierwsza karta tego porządku).
Wyzwalacz czyta `ev.drawNumberThisTurn === 2` (porządek należy do zdarzenia, nie
do licznika, który ktoś może pominąć lub zresetować w trakcie komendy). Mulligan (CR 701.3b: karty
z mulligana **nie są dobraniem**) idzie jawnym `drawNumberThisTurn: null`, żeby
pole było w 100% pokryte (ADR 0027) bez wpisywania wyjątku w narzędzie.

**Testy.** `test/audyt-pr92-draw-ordinal.test.js` (4): batch 1+2 → jeden,
2+1 → jeden, 2 na starcie → jeden, mulligan nie startuje licznika. Istniejący
test batchu 52 (`test/batch52-kart.test.js:345–365`) dobierał 1+1 osobno i
**maskował** defekt — zostaje nietknięty (przechodzi), bo zachowanie per
wyzwalacz jest w nim prawidłowe.

**Obocznie:** zdarzenie `card_drawn` ma od teraz `drawNumberThisTurn` u
wszystkich emiterów (choke point + jawnie `null` przy mulliganie), a od
sekcji 4a pilnuje tego wymóg deklaratywny w analizatorze ADR 0027.

## 3. Znalezisko 4 — grupowe wyzwalacze „one or more” dedupują po kontrolerze, nie po instancji zdolności

**Objaw (repro `/home/user/scratch/repro-vaan.mjs`).** Klucz grupowania w bloku
`any_combat_damage_to_player` (`src/engine/triggers.js`) to
`${source.controllerId}|${filtry}` — jedna iteracja po `candidates` z
`return true` po pierwszym dopasowaniu. Dwie kopie Vaana (albo Vaan + Disa the
Restless, które dzielą zdarzenie i **brak** filtru): **jeden**
`ability_triggered`, bo druga instancja zdolności jest uznana za
„kontroler już odpalił”. To rażące naruszenie **CR 603.3** (każda instancja
wyzwala osobno); grupowanie **CR 603.2** scala ZDARZENIE (dwa stwory atakujące
razem = jeden wyzwalacz jednej instancji), nie sprawcę.

**Naprawa (commit `0b409fd`).** Klucz per **żywiciel + indeks zdolności +
filtr + poszkodowany**: `${candidate.id}#${abilityIndex}|${filtry}|${ev.target}`;
pętla przez `effectiveAbilities(candidate).entries()`, więc zdolność nadana
(grants) nie koliduje z nadrukiem karty; zbiór `anyCombatDamageControllers`
przemianowany na `groupedCombatDamageFires` (nazwa mówi, co liczy) z
komentarzem przyczyny. Zachowane: filtry podtypów źródła obrażeń,
`damagedPlayerId`, dedup w obrębie komendy, dwa odpalenia przy first strike +
normalne obrażenia.

**Testy.** `test/audyt-pr92-grupowe-trygery.test.js` (3, syntetyczne
permanenty — ADR 0002): dwie instancje → dwa `ability_triggered` (RED przed
naprawą), jeden permanent + dwaj atakujący → nadal jeden (grupowanie zdarzenia,
GREEN przed i po — dowodzi, że naprawa nie rozgrupowała), filtr podtypów nie
oznacza kontrolera „obsłużonego” przez stwór spoza listy.

**Nie ruszone:** siostrzana grupa `leftBattlefield` (też per kontroler) —
osobne rozpoznanie w `docs/backlog.md`, bo wymaga sprawdzenia, czy któraś karta
oczekuje dokładnie jednego triggeru na kontrolera.

## 4. Znalezisko 5 — darmowy rzut: zawężona OFERTA, starsza WALIDACJA, trzy kopie filtru

**Objaw.** M280/F (uwaga właściciela „noop Discover”) poprawiło tylko wystawianie
oferty. `resolve_discover_choice { castFree: true }` w `execute()` nadal przyjmuje
**dowolny** `kind === 'spell'`, kładzie czar na stosie z `targets: []` i pozwala
mu fizzlować (CR 608.2b). Gracz tego nie zobaczy (UI i boty czytają
`legalCommands`), ale dowolny inny sterownik komend — test, replay, driver
zewnętrzny — nadal mógł ją wysłać. Ten sam
filtr miał trzy kopie rozjechane w drugą stronę: bramka `resolve_exile_cast`
(Vaan) nie znała `modes`, więc opierała się o `throw` w głębi `castSpell`.

**Naprawa (commit `10f7a39`).** Jeden predykat `outsideHandCastScope(card,
{ allowTargets })` w `src/engine/game-state.js`, wołany w czterech miejscach:
oferta Discover, walidacja Discover (odrzucenie
`illegal_discover_free_cast`), oferta Vaana, bramka Vaana. Land → zawsze `false`
(CR 305.1); `allowTargets: false` dla Discovera (nie enumeruje celów), `true`
dla Vaana (warianty `epicCastOffers` per zestaw celów).

**Testy.** `test/audyt-pr92-darmowy-rzut-zakres.test.js` (4; 1–2 RED przed
naprawą). W trakcie pisania testów wyszła jeszcze jedna rzecz: fabryka
`addObject` **odrzuca pola spoza kontraktu** (L21) — stempel `playableUntilTurn`
nakłada sam efekt Vaana, więc test robi to samo; bez tego pozytywowana ścieżka
„Vaan rzuca instant TERAZ” jest odrzucana przez `requireSpell` komunikatem
o karcie z ręki (mylący powód, ale decyzja słuszna).

## 4a. Strażnik klasy dla znalezisk 3 i 4 (punkt 2.2 planu)

Sama naprawa nie domyka klasy (L112/L113: narzędzie ma tępić KLASĘ, nie
pojedynczy błąd), więc doszły dwa mechanizmy w `tools/`:

1. **Nowa rodzina pól `draws` w `tools/family-audit.mjs`** — każdy zapis
   `cardsDrawnThisTurn` poza `src/engine/players.js` (plik-właściciel choke
   pointu `recordCardDrawn`) jest naruszeniem. Wszczepiona ścieżka
   `state.players.find(…).cardsDrawnThisTurn = 1` w `effects.js` →
   `RAZEM naruszeń rodzin: 1`; drzewo bez skaleczenia → „brak naruszeń”
   (sprawdzone obie strony).
2. **Pola WYMAGANE w `tools/event-contract-audit.mjs`**
   (`CONTRACT_REQUIRED_FIELDS = { card_drawn: ['drawNumberThisTurn'] }`).
   Ten mechanizm powstał PRZY TESTOWANIU mojego własnego założenia: napisałem
   w nagłówku testu, że analizator ADR 0027 pilnuje stempla u wszystkich
   emiterów, a po usunięciu `drawNumberThisTurn` z emitera mulliganu
   analizator **milczał**. Przyczyna reguły: `CONTRACT_RATIO = 0.6` liczy
   pokrycie większościowe, a `card_drawn` ma DWÓCH emiterów — 1/2 = 50% < 60%,
   czyli pole przestaje być „częścią kontraktu” dokładnie w małych rodzinach.
   Wymóg deklaratywny omija próg i minimum liczby emiterów; usunięcie stempla
   z drzewa daje `[pole WYMAGANE] card_drawn.drawNumberThisTurn: niesie 1/2
   emiterów` i czerwieni `test/m273-kontrakty-zdarzen.test.js` (sprawdzone
   mutacją na żywym kodzie, potem przywrócone).

**Pin anty-vacuous dla obu strażników** (ta sama lekcja, co znalezisko 1):
`test/family-audit.test.js` wymaga teraz, by KAŻDA rodzina pól miała próbki
`bypass` (wzór MUSI pasować) i `legal` (wzór NIE może pasować) — bez nich
martwy wzór udawałby nadzór; `test/m273-kontrakty-zdarzen.test.js` sprawdza
mechanizm na syntetycznych emiterach i pilnuje, że pole wymagane nie jest
jednocześnie wyjęte wyjątkiem.

## 5. Mechaniczny przegląd `pending*` (punkt 2.3 planu)

Skrypt (L5: bez regexowych wniosków o zachowaniu — tylko enumeracja, potem
każdy kandydat czytany ręcznie) policzył **68** różnych nazw `pending*`
w `src/engine/`. Wynik:

| miara | wartość |
|---|---|
| nazwy `pending*` w silniku | 68 |
| w blokadzie priorytetu (`firstPendingDecision`) | 64 |
| w projekcji odcisku (`PENDING_DECISION_FIELDS`) | **68 (100%)** |
| poza blokadą — zweryfikowane jako nie-decyzje | 4 |

Cztery „poza blokadą” to **księgowość przejściowa, nie decyzja gracza** — każda
jest ustawiana w parze z prawdziwym pendingiem albo natychmiast konsumowana:
`pendingAbilityActivation` (parametry dokończenia aktywacji, czytane tuż po
`resolve_opponent_target`; parowany z `pendingOpponentTarget`/`pendingDiscardChoice`),
`pendingDevourEtbs` (odroczone ETB pożerania, konsumowane w `triggers.js`),
`pendingSpellDiscounts` (jednorazowe zniżki, konsumowane w `mana-cost.js`),
`pendingSpellReturnToHand` (flaga przeczytana w tej samej funkcji, która ją
ustawiła). **Klasa defektu zamknięta**: odcisk ma pokrycie 100%, a strażnik
pilnuje liczebności, więc nowy `pending*` bez projekcji czerwieni test.

## 6. Zweryfikowane i odrzucone (żeby następna sesja nie powtarzała rozpoznania)

| podejrzenie | werdykt |
|---|---|
| Vaan czyta wierzch biblioteki właściciela zamiast kontrolera (`ownerId`) | **odrzucone** — Oracle: „exile the top card of **an opponent's** library"; `libraryTopId` po `controllerId` jest prawidłowe, różnica ownerId/controllerId dotyczy tylko efektów „your library" |
| pokrycie warstw (koszt/timing/flagi) w `resolve_exile_cast` | **odrzucone** — bramka i `requireSpell`/`castPermanent` pokrywają się; testy 3–4 sekcji 4 to spinają |
| kontrakt `object_transformed` (4 emitera) | **odrzucone** — wszystkie niosą `fromCardId` + `controllerId`; `tools/event-contract-audit.mjs`: brak naruszeń |
| Merfolk Falconer: kicker na instant/sorcery | **odrzucone jako błąd karty** — silnik nie ogarnia kosztu dodanego przy tych timingach (sama reakcja jest gotowa: trigger czyta `ev.kicked` ∪ `object.wasKicked`); decyzja zakresu w §7 (L52: nie oznaczaj karty jako obsługiwanej bez testów) |
| Ulna/Infusion, Leonin `activePlayerIsController`, `sourceCardId` w `pendingLookTopN`/`pendingRevealExile` | **odrzucone** — po weryfikacji z deskryptorami i CR, bez rozjazdu |
| `return true` w pętli Vaana | **odrzucone jako objaw** — prawdziwy defekt to wymiar klucza grupowania (znalezisko 4); usunięcie `return` dałoby drugorzędny skutek |
| „Rulingi WotC dla Vaana” (claim planu) | **nie do potwierdzenia z repo** — snapshoty `docs/cards/scryfall-*.json` nie zawierają pola `rulings`; opis karty wzięty 1:1 z `text` snapshotu (9/9 kart batchu 52 zgodne z Oracle) |

## 7. Otwarte / rekomendacje

1. **`leftBattlefield` i inne grupowania per kontroler** — ta sama klasa co
   znalezisko 4; wymaga osobnego rozpoznania (karty, które mogą chcieć
   jednego triggeru na kontrolera). Wpis w `docs/backlog.md`.
   → **rozstrzygnięte w §9** (tag `trigger.groupPer` po stronie karty).
2. **Treasure w `resolve_exile_cast`** składany ręcznie przez
   → **rozstrzygnięte w §9** (`TREASURE_TOKEN_EFFECT` + katalog tokenów);
   `applyEffect({ type: 'create_token' })` zamiast z katalogu tokenów
   (klasa L107 — najbogatsza żyła błędów: ścieżka robiąca to samo co helper,
   ale ręcznie). Nie zdefektowane dzisiaj, ale to kopia opisu tokenu.
3. **`state.log === state.events`** (delegat) — świadoma redundancja; gdyby
   kiedyś rozjechać pola, odcisk już to łapie.
4. **Snapshoty Scryfall bez `rulings`** — audyt rulingów wymaga albo pobrania
   `rulings` do `docs/cards/`, albo zewnętrznego odczytu; przy braku egressu
   w sandboxie (ENVIRONMENT) to punkt dla właściciela.
   → **rozstrzygnięte w §9**: egress JEST (przez `fetch_page`), narzędzie
   `tools/fetch-card-rulings.mjs` ściągające rulingi leży w repo, snapshoty
   batchu 52 mają `rulings`, a `HOW_TO_ADD_CARD.md` ma punkt kontrolny.
5. **Merfolk Falconer** — karta wspierana, ale ścieżka kickera na
   instant/sorcery nie istnieje w silniku; decyzja (obsłużyć vs oznaczyć
   ograniczeniem) należy do właściciela (ADR 0022: `limitations` tylko za
   pozwolonymi powodami).

## 8. Bramy i liczby sesji

| brama | start | koniec |
|---|---|---|
| `npm test` (test:fast) | 4131/4131 | **4147/4147** (~109 s) |
| `npm run test:all` (brama PR, ADR 0019) | — | **4157/4157**, 0 fail (~188 s) |
| `npm run build` | 57 modułów / 3080,2 kB | 57 modułów / **3084,1 kB** |
| `node --test test/bot-benchmark.test.js` | 10/10 | **10/10** (po znalezisku 4 i 5) |
| `node tools/event-contract-audit.mjs` | brak naruszeń | **brak naruszeń** |
| `node tools/family-audit.mjs` | brak naruszeń | **brak naruszeń** |
| `node tools/oracle-coverage.mjs --only=…` (9 kart batchu 52) | 9/9 = 100% | **9/9 = 100%** |

Commity (każdy osobno zielony, push po każdym, bez force pusha):

| commit | czego dotyczy | rozmiar |
|---|---|---|
| `6c9a3b8` | plan sesji (ADR 0020/0021) | 1 plik, +130 |
| `fb92c01` | znaleziska 1+2: strażnik L16 + dwa pendingi w odcisku | 2 pliki, +50/−9 |
| `094a8c0` | znalezisko 3: porządek dobrania w zdarzeniu (choke point) | 6 plików, +195/−17 |
| `0b409fd` | znalezisko 4: dedup grupowych triggerów po instancji | 2 pliki, +114/−13 |
| `10f7a39` | znalezisko 5: jeden filtr oferty i walidacji darmowego rzutu | 2 pliki, +167/−23 |
| `d5eba18` | strażnicy klasy (punkt 2.2): rodzina `draws` + pola WYMAGANE w analizatorze | 5 plików, +163/−1 |

Razem `db0c493..d5eba18`: 15 plików, +815/−65 — `src/engine/` 6 plików
+126/−53, `test/` 6 plików +471/−10, `tools/` 2 pliki +88/−2. Zero zmian w
katalogu kart, zero zmian w UI (reguła: UI tylko wysyła intencje), zero zmian
w ADR-ach, zero zmian w wycenach bota (golden-master nietknięty).


## 9. Tura 2 (2026-09-02): cztery rozstrzygnięcia właściciela w kodzie

Tura 1 zostawiła cztery pytania (patrz §7). Właściciel odpowiedział, odpowiedzi
są wdrożone. Rulingi WotC, na których oparto decyzje, leżą teraz w snapshotach
(`docs/cards/scryfall-*.json`, pola `rulings` / `rulingsSource` /
`rulingsPobrano`) — ściągnęło je nowe `tools/fetch-card-rulings.mjs`; działa
przez `fetch_page`, bo `curl` z sandboxa nie ma egressu
(`docs/setup/ENVIRONMENT.md` §4).

| rozstrzygnięcie | co mówią rulingi | co zmieniono |
|---|---|---|
| (c) sprawdzić „zgodne z rulingami WotC" | 9 kart batchu 52, 14 rulingów w repo | narzędzie + dane w repo (commit `a072ae4`); odchylenia — pkt 1 i 2 poniżej |
| (a) „engine headless, name-agnostic" | Leonin: „Start your engines! isn't a triggered ability … happens as a state-based action" | zdolność `static` + `effect: [{type:'start_engines'}]` w danych karty, jeden pass w `runStateBasedActions`, choke pointy `setPlayerSpeed`/`startEnginesFor`, rodzina pól `speed` w `family-audit` (`81ebb2f`) |
| (b) kicker na instant/sorcery: „OCZYWIŚCIE OBSŁUŻYĆ" | — (w katalogu nie ma instanta z kickeriem) | `castSpell(..., kicked)`: pipy kickera w wymaganiach, koszt poza obniżkami (CR 601.2f), `wasKicked` na stosie, `kicked` w `spell_cast`; oferta i UI (`3d07dc0`) |
| (d) Treasure składany z katalogu tokenów | — | `TREASURE_TOKEN_EFFECT` w `src/engine/tokens.js` dla 3 miejsc w rdzeniu + zdolność w definicji `token_treasure` + test antydryfowy (`49bfe25`) |
| (§7.1 grupowanie per kontroler) | CR 603.2/603.3 + ruling M201/N2 | deklaratywny tag `trigger.groupPer` w danych kart, jeden `mayFireGrouped` w rdzeniu (`766ef89`) |

Odchylenia wobec rulingów — oba z naprawą:

1. **Leonin Surveyor i Glitch Ghost Surveyor.** Ruling WotC 2025-02-07 mówi,
   że wzrost prędkości jest akcją stanową (nie triggerem ETB), że działa także
   przy przejęciu permanentu z silnikami i przy zdolności NADANEJ, oraz że
   utrata źródła prędkości nie cofa. Model w silniku odpalał się z
   `enter_battlefield` i nie widział ani przejęcia, ani grantów. Po naprawie
   rdzeń nie zna żadnej nazwy karty: szuka na polu bitwy permanentu, którego
   `effectiveAbilities` zawierają efekt `start_engines`.
2. **Vaan, Street Thief.** Ruling WotC 2025-02-10: „You can't wait to cast it
   later in the turn and decide to pay its mana cost." Efekt zakładał wygnanej
   karcie stempel `playableUntilTurn` (mechanizm „impulse"), a stempel przeżywał
   rezygnację — karta zostawała w exile „rzucalna do końca tury". Naprawa nie
   „czyści stempla", tylko usuwa go z modelu: oknem jest sama decyzja
   `pendingExileCast`, a uprawnienie do rzutu z exile i poza timingiem daje
   flaga `abilityWindowCast` (dawna `vaanCast` — nazwa KARTY w rdzeniu,
   sprzeczna z ADR 0002).
3. Bez zarzutu: **Cemetery Recruitment** (guard `zone !== 'graveyard'` zwraca
   przed dobieraniem), **Fourth Bridge Prowler** (opcjonalny cel = „you may"),
   **Jolrael, Mwonvuli Recluse** (X liczone przy rozstrzyganiu, trigger na
   drugim dobraniu w turze), **Merfolk Stalwart** i pozostali — porównane z
   treścią rulingów.

Uwaga o odcisku stanu (klasa znalezisk 1+2): `speed` **nie** wymagał dopisania
do `src/engine/fingerprint.js`, bo ten serializuje całe `state.players`
(`players: state.players`) — pola gracza wchodzą z automatu. Sprawdzone
grepami i odczytem projekcji, nie założone.

Zostawione właścicielowi (nie wdrożone; ruszają rozliczanie many):

- `src/engine/mana-sources.js:46` — wpis `'token_treasure'` w mapie kolorów,
  którego regulaminowy komentarz w tym samym pliku sam zabrania: „karty z
  darmową zdolnością `{T}: Add` NIE wchodzą do mapy — kolory/ilość czytane są
  z deskryptora";
- `src/engine/resources.js:623,847` — `object.cardId !== 'token_treasure'`
  przy dostępności i wydawaniu many skarbowej (koszt alternatywny Security
  Rhox). Oba to ID KARTY w rdzeniu (klasa ADR 0002); czysta naprawa wymaga
  flagi na deskryptorze tokenu i migracji literali po stronie kart, więc idzie
  na osobny dzień;
- Skarb: **3 miejsca w rdzeniu** mają dziś jedną definicję, a **6 literali po
  stronie kart** zostaje opisami w danych (to warstwa danych, nie rdzeń) — ich
  zgodność z katalogiem pilnuje `test/audyt-treasure-katalog.test.js`.
  Liczby sprostowują opis w §7.2, który mówił o jednym miejscu.

### Budżet lektury startowej przepełniony — i co to ujawniło

`test/dokumentacja-budzet-lektury.test.js` (100k tokenów na AGENTS.md + ADR-y +
`docs/LESSONS.md` + `docs/setup/ENVIRONMENT.md`, AGENTS.md §0) **zczerwieniał**
po dopisaniu L114–L116: lektura ważyła ~101,2k. To nie jest wina jednego wpisu —
rejestr miał już 99,9% budżeta, więc przelewało go każde zdanie. Strażnik każe
*wtedy* przepisać/rozdzielić dokumenty (nie podnosić progu), więc:

1. **nowe wpisy** (L114–L116) skondensowane do formatu szablonu: nagłówek,
   przypadek w dwóch zdaniach, pełna reguła, pełny strażnik; narracja do tego
   raportu (§9);
2. **odsyłacze kotwic** w `docs/LESSONS.md` z linków `[L21](#l21-…-slug…)` na
   `[L21]` — 17× po ~90 B czystego slugu, zero utraconej treści (test wymaga
   `[LNN]` albo `(#lnn-`, wariant krótki przechodzi);
3. **narracja dwóch najdłuższych wpisów** (L91, L106) przeniesiona verbatim do
   nowego `docs/LESSONS_PRZYPADKI.md` — to archiwum przypadków, **nie** lektura
   startowa; w rejestrze zostały reguła i strażnik w całości;
4. przy okazji: **głowica wpisu L105 była sklejona z końcem L106** (brak
   podwójnej przerwy), więc `^## L105` nie istniało dla żadnego grepowania —
   wpis był niewidzialny dla skanów liczących lekcje. Naprawione.

Po: **279 545 B → ~99,84k tokenów**, zapas 455 B. **Decyzja dla właściciela:**
budżet jest wyczerpany strukturalnie, nie przypadkiem. Do wyboru: (a) podnieść
próg w AGENTS.md §0 (uma z testem: `BUDZET_TOKENOW = 100_000`), (b) rozpisać
trwały podział rejestr `LESSONS.md` (reguła+strażnik) / `LESSONS_PRZYPADKI.md`
(narracja) na wszystkie >2 kB wpisy, (c) przenieść klasy pilnowane przez
automaty (`tools/event-contract-audit.mjs`, `tools/family-audit.mjs`) do
podręczników obszarowych. Sesja nie podnosi progu samodzielnie — to zmiana
umowy (patrz komentarz w teście).

### Bramy tury 2

| brama | start (koniec tury 1) | koniec tury 2 |
|---|---|---|
| `npm test` | 4147/4147 | **4168/4168** (~146 s) |
| `npm run test:all` (brama PR, ADR 0019) | 4157/4157 | **4178/4178** (~250 s) |
| `npm run build` | 57 modułów / 3084,1 kB | 57 modułów / **3097,4 kB** |
| `node tools/family-audit.mjs`, `node tools/event-contract-audit.mjs` | brak naruszeń | **brak naruszeń** |

Commity tury 2 (każdy zielony osobno, push po każdym, bez force pusha):

| commit | czego dotyczy |
|---|---|
| `a072ae4` | `tools/fetch-card-rulings.mjs` + rulingi WotC w 9 snapshotach batchu 52 |
| `81ebb2f` | „Start your engines!" jako akcja stanowa (Leonin, Glitch Ghost Surveyor) + rodzina `speed` |
| `35e4ce5` | okno rzutu z exile = decyzja, nie stempel (Vaan) + `abilityWindowCast` zamiast `vaanCast` |
| `49bfe25` | jeden deskryptor Skarba w rdzeniu + zdolność w katalogu tokenów + test antydryfowy |
| `3d07dc0` | kicker na instant/sorcery (silnik, oferta, UI) |
| `766ef89` | deklaratywne grupowanie wyzwalaczy (`trigger.groupPer`) + CR 603.3 dla dwóch kopii |

## 10. Tura 3 (2026-09-02): cztery wątki z §9 zamknięte w kodzie

Tura 2 zostawiła w §9 listę „do zgłoszenia właścicielowi" (pkt 1–5) i w
`docs/setup/HANDOFF_2026-09-02-audyt-pr92-petla-jakosci.md` siedem wątków do
dalszego polowania. Sesja wzięła cztery z nich — kolejnością: najpierw długi
ogon API, potem fakt w danych karty, potem rodzina pól, na końcu mechanika,
której brak blokował resztę.

| krok | wątek (skąd) | co wyszło | commit |
|---|---|---|---|
| A | „12–13 argumentów pozycyjnych `castSpell`" | ogon `castSpell(state, playerId, objectId, targets, cleaved, modeIndex, stunTargetId, options = {})` — sześć flag (kicker, abilityWindowCast, madnessCast, warpCast, freeImpulse, phyrexianPayWithLife) pojedynczym obiektem; `castPermanent` miał ten kształt od początku, więc to wyrównanie bliźniaczych ścieżek (klasa L21: jedna rodzina, dwie implementacje) | `9d0ba7b` |
| B | „cień `token_treasure` w `mana-sources.js:623,847`" + decyzja właściciela „Skarby składamy z katalogu tokenów" | kolory Jednostki stały się DANĄ tokena (`effect.colors` w katalogu i w `TREASURE_TOKEN_ABILITY`, plus sześć efektów `create_token`, które ich nigdy nie miały); `treasureManaAbilityOf` czyta deskryptor zamiast porównywać `cardId`; `player.treasureManaColors` w puli zamiast literału pięciu kolorów; MANA_SOURCE_MAP bez wpisu dla Skarba | `5d7b3f4` |
| C | „rodziny pól `speed` i `draws` działają, `playableUntilTurn` nie" | nowy moduł-choke point `src/engine/impulse-window.js` (58. w bundlu): siedem zapisów i trzynaście odczytów dwóch pól impulsu przeszło przez niego, a `tools/family-audit.mjs` dostał rodziny `impulse-window` i `impulse-free-cast` z próbkami bypass/legal | `62e03e6` |
| D | „semantyka kontr-zaklęcia przy `pendingExileCast`" — odłożone w §9, bo nie było jak tego zmierzyć | Stifle (CNS #108, snapshot z trzema rulingami WotC) + typ celu `ability_on_stack` + efekt `counter_ability` idący przez wspólny `counterStackObject`; `playerView` niesie `abilityEffects` (ADR 0017), bot traktuje kontrę zdolności jak kontrczar (nigdy w siebie, trzyma na trywialnym celu) | `9f1c37c` |

### Co rozstrzygnęło się przy okazji (i dlaczego to nie jest „drobny porządek")

1. **Pytanie z §9 o Vaana ma odpowiedź w teście, nie w dyskusji.** Skontrowany
   trigger nie rozstrzyga się (CR 118.12/608.2a): brak wygnania wierzchu
   biblioteki, brak Skarbu, brak decyzji `pendingExileCast` — a obrażenia
   walki, które go uruchomiły, zostają. Dowód: test 2 w
   `test/audyt-kontrzenie-zdolnosci.test.js`, idący prawdziwymi komendami
   (`declare_attackers` → `resolve_combat` → priorytet przeciwnika →
   `cast_spell` → drenaż stosu).
2. **„Mana abilities can't be targeted" jest spełnione konstrukcją silnika**
   (zdolność many nie wchodzi na stos, CR 605.1a), nie warunkiem w kodzie.
   Test 4 pilnuje obu stron: koszyk celów Stifle jest pusty przy tapniętym
   Skarbie i oferta też nic nie wymyśla. To jest dokładnie to, czego brak
   blokowało kiedyś dyskusję o kontrzeniu: nie da się obronić reguły, której
   nie da się wywołać.
3. **Refaktor B był zachowawczy, i to jest udokumentowane kierunkiem mutacji.**
   Dwie mutacje (przywrócenie porównań `cardId === 'token_treasure'`; usunięcie
   `colors` z definicji tokena) RED-ują testy 2/3/4 i 1/4 — fakt faktycznie
   mieszka w danych, a nie w silniku (L114: mutacja ma zaciskać bramkę).
4. **Pułapki harnessu, które testy złapały po drodze** (opisane w komentarzach
   plików, żeby następna sesja nie płaciła za nie drugi raz):
   `gameObjectDataOf` nie przenosi podtypów — bez jawnych `subtypes` Vaan nie
   widzi Scouta i trigger w ogóle nie wpada na stos, więc test przechodziłby na
   pustym stosie; komendy z `playerView` nie niosą `cardId`, więc filtr
   `c.cardId === 'stifle'` dawałby pusty pin „brak oferty" (patrz L116).
5. **Dwie linie rejestru, których sesja NIE ruszyła:** budżet lektury startowej
   jest wyczerpany (455 B zapasu, §9), więc tura 3 nie dopisała żadnej lekcji do
   `docs/LESSONS.md`. Narracja tej sekcji jest miejscem, w którym zostaje;
   decyzja o progu (a/ b/ c z §9) nadal czeka na właściciela.

### Bramy tury 3

| brama | start (koniec tury 2) | koniec tury 3 |
|---|---|---|
| `npm test` | 4168/4168 | **4186/4186** (~152 s) |
| `npm run test:all` (brama PR, ADR 0019) | 4178/4178 | **4196/4196**, 0 fail (~250 s) |
| `npm run build` | 57 modułów / 3097,4 kB | **58 modułów / 3111,8 kB** |
| `node tools/family-audit.mjs`, `node tools/event-contract-audit.mjs` | brak naruszeń | **brak naruszeń** (plus dwie nowe rodziny pól) |
| `npm run benchmark` | heuristic 82,7% (556/672), aggro 28,9% (97/336) | **bez zmian** — nowa karta nie gra w BENCH_DECKS, więc progi regresji nietknięte |
| `node --test test/dokumentacja-budzet-lektury.test.js test/docs-decisions.test.js` | 23/23 | **23/23**, lektura 279 545 B / 280 000 |

Commity tury 3 (każdy zielony osobno, push po każdym, bez force pusha):
`9d0ba7b` (A), `5d7b3f4` (B), `62e03e6` (C), `9f1c37c` (D), ten dokumentacyjny.

## 11. Tura 4 (2026-09-02, noc): budżet lektury odzyskany kondensacją rejestru

**Decyzja właściciela:** „Zanim dopiszesz coś nowego, musisz zaoszczędzić miejsce
w dotychczasowej lekturze startowej robiąc odpowiednie streszczenia, zespolenia
lekcji, oszczędzając na prozie i rozwlekłości w plikach."

Żadne z trzech wyjść §9 (a: podnieść `BUDZET_TOKENOW`, b: trwały podział rejestr /
przypadki, c: wynieść klasy pilnowane przez automaty) nie zostało wybrane. Rejestr
miał już wpisaną umowę „proza do `docs/LESSONS_PRZYPADKI.md`", tylko zastosowaną
do dwóch wpisów — zastosowaliśmy ją do całości, a **próg 100k tokenów nie drgnął**.

### 11.1 Rozkład lektury startowej (limit 280 000 B = 100k tokenów)

| pozycja | przed | po | uwaga |
|---|---|---|---|
| `docs/LESSONS.md` | 151 441 B | **113 852 B** | 75 z 116 wpisów skróconych |
| `AGENTS.md` | 21 792 B | 22395 B | +603 B: przepis odzyskiwania miejsca w §0 |
| `docs/setup/ENVIRONMENT.md` | 8 775 B | bez zmian | |
| `docs/decisions/` (27 plików z indeksem) | 97 537 B | 97 537 B | bez zmian; uwaga: indeks `README.md` też jest w budżecie, bo §0 każe czytać WSZYSTKIE `.md` w katalogu |
| **razem** | **279 545 B** (zapas 455 B) | **242 559 B** (**zapas 37 441 B**) | 86 628 tokenów z 100 000 |
| `docs/LESSONS_PRZYPADKI.md` (POZA lekturą) | 2 272 B | 65885 B | 77 przypadków, grep po numerze lekcji |

### 11.2 Co zrobiła transformacja i dlaczego mechanicznie

Każdy wpis powyżej 700 B przeszedł do postaci: nagłówek nietknięty, jednozdaniowy
`**Przypadek:**` z konkretami (karta / test / numer CR), bloki `**Reguła:**`,
`**Zasada:**`, `**Wniosek:**`, `**Strażnik:**` i `**Wpis zbiorczy**` zachowane
słowo w słowo, plus odsyłacz `→ narracja:`. Reszta pojechała do archiwum **bez
przepisywania** — to nie jest streszczenie autora, tylko inny plik, więc
najtańsza forma oszczędności, jaką da się obronić.

Aby to było bezpieczne, transformacja miała asercję: każdy zakres w backtickach,
każdy `CR x.y`, każdy `Mnnn` i każda ścieżka `test/…` z oryginału musi zostać w
unii (rejestr ∪ archiwum). 23 wpisy, przy których coś by zginęło albo zabrakło
rdzenia reguły, zostało nietkniętych — automat woli zostawić gruby wpis niż
zwrócić chudy bez zasady. Naprawione przy okazji dwie wady rejestru z PR #92:
urwany cytat wskazujący archiwum w L91 i pospolite `.**` na końcu linii odsyłacza
w ośmiu kotwicach.

### 11.3 Kondensacja ma strażnika

`test/docs-decisions.test.js` (test „wyniesiona narracja ma adresata"):
(a) każdy `→ narracja:` musi mieć nagłówek w archiwum, (b) każdy nagłówek archiwum
musi mieć lekcję w rejestrze, (c) wpis z odsyłaczem musi zachować
`**Reguła|Zasada|Wniosek|Strażnik**`, (d) minimum 50 wyniesień i zakaz powrotu pola
`**Objaw:**` do wzorca wpisu. Kontrola mutacyjna w kopii drzewa w `/tmp/mut`:
usunięcie nagłówka przypadku, usunięcie wszystkich odsyłaczy, powrót starego
templatu i sprowadzenie wpisu do „Przypadek + odsyłacz" — cztery RED, po
przywróceniu 24/24. Dwie wcześniejsze mutacje były zielone nie przez słabość
strażnika, tylko przez wadę samej mutacji (źle wycięty zakres; alternacja
dopuszczała `**Wpis zbiorczy**`) — L114: sprawdzaj KIERUNEK i to, że mutacja na
pewno jest stanem sprzed naprawy.

### 11.4 Co jeszcze widać przy tej okazji

1. **Budżet liczy pliki, nie intencje**: `readdirSync('docs/decisions')` łapie też
   `README.md` rejestru (6 792 B) i każdy przyszły `.md` w tym katalogu — nowy
   dokument wrzucony obok ADR-ów automatycznie płaci za czytanie. `archive/` nie
   płaci, bo to katalog.
2. **Nie ma co archivesować**: statusy wszystkich 27 ADR-ów to `Zaakceptowana`, a
   reguła archiwizacji dopuszcza przeniesienie tylko wpisu `Zastąpiona`/`Wycofana`
   po wyniesieniu treści do następcy. Gdyby ktoś chciał stąd skubać 97 kB, najpierw
   jest praca merytoryczna, nie zmiana progów.
3. **Format wpisu jest interfejsem automatu**: transformacja nic nie wyciągnęła z
   wpisów, których reguła nie zaczyna się od `**Reguła:**` (albo `**Zasada**`/
   `**Wniosek**`). Rejestr, który każe coś w jednym formacie, daje potem darmowe
   narzędzia — za to płaci się raz, przy pisaniu.
4. **Liczba w message commita to też fakt dokumentacyjny**: `dbf5b16` podał
   240 265 / 39 735 B zmierzone przed ostatnią edycją nagłówka rejestru; stan
   końcowy jest w §11.1. Nawyk: liczby wstawia skrypt, nie autor.

### 11.5 Pytanie właściciela: czy dociągać rulingi dla ~500 kart?

Pomiary z tej tury:
- katalog ma **441 snapshotów** (439 unikalnych nazw); klucz `rulings` ma **10**,
  w tym 4 z pustą listą = „ściągnięto, WotC nie ma nic" (konwencja z `a072ae4`);
- **Scryfall nie ma ścieżki masowej na rulingi**: `/cards/search` (testowane dziś
  na karcie Stifle) zwraca w listcie wyłącznie `rulings_uri`, nigdy samych rulingów;
  `/cards/<id>/rulings` to jedno żądanie na kartę, limit ~10 rps;
- `bash` w sandboxie **nie ma egressu** (`curl` zwraca pusty wynik), więc jedyna
  realna droga w sesji to `fetch_page` — czyli ~429 wywołań narzędzia.

Stanowisko sesji: **nie hurtować 429 kart**. Koszt jest pewny (godziny sesji i
szum w `docs/cards/`), a korzyść koncentruje się tam, gdzie tekst Oracle jest
wieloznaczny — czyli na kartach, które i tak audytujemy. Proponowane zamiast
tego:
1. **„przy kartce" (już obowiązuje)** — każdy nowy lub odświeżany snapshot niesie
   `rulings`, choćby puste (ADR 0022 + punkt kontrolny w `HOW_TO_ADD_CARD.md`);
   pokrycie rośnie samo dokładnie tam, gdzie ktoś dotyka karty;
2. **kolejka priorytetu zamiast pełnego przejścia** — ściągnąć dla kart z
   niepustym `support.limitations`, kart spiętych nietypowym CR (SBA, efekt
   zastępczy, okna, grupowanie) i tych, które pojawiły się w ustaleniu Żywego
   Testera: `node tools/fetch-card-rulings.mjs --only=<slug>`;
3. **jeśli ma być gwarancja**: test pokrycia „każda karta z niepustym
   `support.limitations` ma `rulings` w snapshocie (nawet pustą listę)" — to ok.
   30 kart i zamyka realne ryzyko (audyt „zgodne z rulingami" bez danych).

Sesja NIE dodaje tego testu bez decyzji właściciela: każda liczba progowa w
rejestrze to zmiana umowy (por. ADR 0019). Decyzja do wzięcia w następnej turze;
wpis z rekomendacją stoi w `docs/backlog.md`.

### Bramy tury 4

| brama | start (koniec tury 3) | koniec tury 4 |
|---|---|---|
| `npm test` | 4186/4186 | **4187/4187** (+1 test: strażnik kondensacji) |
| `npm run test:all` (brama PR, ADR 0019) | 4196/4196 | **4197/4197**, 0 fail |
| `npm run build` | 58 modułów / 3111,8 kB | **bez zmian** (58 / 3111,8 kB) |
| `node tools/family-audit.mjs`, `event-contract-audit.mjs` | brak naruszeń | **brak naruszeń** (kod nietknięty) |
| `node --test test/dokumentacja-budzet-lektury.test.js test/docs-decisions.test.js` | 23/23, lektura 279 545 B | **24/24, lektura 242559 B / 280 000** |
| `npm run benchmark` | heuristic 82,7%, aggro 28,9% | **bez zmian** (zero zmian w `src/`) |

Commity tury 4 (każdy zielony osobno, push po każdym, bez force pusha):
`19ab3ed` (rejestr + archiwum), `dbf5b16` (strażnik kondensacji + wzorzec wpisu),
ten dokumentacyjny.

---

## Tura 5 (2026-09-02): audyt Żywym Testerem — działania niescoringowane bota

Polecenie właściciela: „proponuję audyt Żywym Testerem z akcentem na inteligentne
zachowanie bota i scoringowanie działań niescoringowanych". Sediem nie było więc
dociąganie kart, tylko pytanie: **które decyzje bota zapadają bez żadnej wyceny**
— i czy w ogóle da się je wycenić.

### 12.1 Pomiar (to jest źródło, nie grep)

Statyczna inwentaryzacja punktów decyzyjnych (`/home/user/scratch/inwentaryzacja-bota.mjs`,
84 funkcje w `createHeuristicBot`) wskazała 6 podejrzeń — i okazała się **zbyt
optymistyczna**: przy jednej wielkiej funkcji decyzyjnej regiony nachodzą na siebie.
Właściwym przyrządem jest `tools/bot-tie-audit.mjs`: 12 partii parami talii
**spoza** próbki benchmarkowej + `bot.trace()` per decyzja. Wynik: 5340 decyzji,
1025 z alternatywami, **312 remisów na maksimum = 30,4%**. Remis oznacza wybór
arbitralny (kolejność `legalCommands`, ewentualnie rng z puli top-3), czyli ten sam
skutek co brak wyceny — tyle że w źródle niewidoczny. Rozkład: `block` 188,
`play_land` 75, `attack` 35, `cast_permanent` 8, `activate_ability` 8, reszta ≤7.
Żywy Tester (8 gier, profile greedy/explorer/random, `--steps 300`) w tym samym
przebiegu: „brak zgłoszeń" w 8/8 transkryptach — oś mechaniczna czysta, cała
wartość audytu leży po stronie wyceny.

### 12.2 Naprawa #1: wybór lądu

`play_land` miał wycenę PŁASKĄ 90. Od dziś `90 + landPlayDelta` w zakresie
[-14, +16], a dane liczy `landAnaliza`: kolory kandydata z definicji karty przez
`manaSourceOfCardDefinition` (nowy eksport, CR 305.6 + deskryptor zdolności),
lądy na polu bitwy przez `getSourceForObject` (to samo rozwiązanie co silnik),
zapotrzebowanie ręki przez `coloredPipsOf`. Zero nazw kart — ADR 0002. Preferencje:
pokrycie nieopłaconych pipów (monotonicznie 1→10, 2→12, 3→14, 4→15, ≥5→16),
pierwszy kolor w manabazie (+3), wiele many z jednego tapnięcia (+4), zdolność
inna niż manowa przy gotowej bazie (+2), wejście tapnięte (−8), ląd bez koloru
przy brakach koloru (−3).

Trzy rzeczy, które wyszły przy okazji i są warte odnotowania:

1. **Klapsa jest pułapką.** Pierwsza wersja (10 + min(6, n−1) dla każdego koloru,
   potem wspólny sufit 16) zgrywała do jednego wyniku ląd pokrywający 2 i 3 pipów.
   Zobaczył to audyt, nie test jednostkowy — bo test sprawdzał „lepszy wygrywa",
   a tu oba były „lepsze".
2. **Ślad musi nazywać wariant.** `summarize('play_land')` zwracał sam typ komendy,
   więc audyt nie odróżniał dwóch lasów od lasu i góry. Dziś `play_land(id:cardId)`
   — ta sama lekcja co M195/B i M203/2, po raz trzeci.
3. **Remis uczciwy zostaje.** Dwa lasy w ręce mają nadal identyczny wynik i
   identyczną projekcję. Sztuczne rozbijanie zamienników (tie-breaker po id)
   wyglądałoby w metrykach jak „wycena działa", a byłoby kłamstwem — dlatego
   klasyfikacja idzie po **wejściach** delty, nie po tożsamości wariantów.

Po naprawie: `play_land` 37 remisów, wszystkie o równych wejściach, **0 groźb**;
ogólny odsetek remisów 30,3%. Benchmark quick: heuristic **83,6%** (przed: 82,7%),
aggro bez zmian — wycena lądów niczego nie popsuła, minimalnie pomogła.

### 12.3 Bramka i backlog

Bramka: `test/audyt-bot-wybior-landu.test.js` (5 testów, w tym `audytRemisow()` na
3 parach — spinaja, żeby żaden remis lądu nie miał różnych wejść przy równym wyniku).
Mutacje: płaska wycena ⇒ RED 1/3/5; usunięta kara `entersTapped` ⇒ RED 3/5; ślad bez
karty ⇒ RED 1/2/3/4. Narzędzie ma `--gate=<kind>` (exit 1) do zastosowań CI poza `npm test`.

Backlog z pomiaru — kolejność wg liczby remisów i realnej wagi sportowej:

| kind | remisy | ocena | krok |
|---|---|---|---|
| `block` | 186 | głównie benign: silnik wystawia `block[]` obok `pass_priority` (ten sam no-op); projekcja niezdefiniowana | dodać `tieProjection` dla `declare_blockers` i policzyć jeszcze raz; jeśli >0 — wyceniać przypisania blokujące |
| `attack` | 35 | do sprawdzenia: remisy zestawów różniących jednym stworem bywają uczciwe (lethal już zapewniony) | j.w. dla `declare_attackers`: wejścia = suma obrażeń, śmiertelność, ryzyko kontrzenia |
| `cast_permanent` / `cast_spell` | 8 / 5 | przy dwóch zamiennych kartach — OK; przy różnych — przeoczenie | projekcja per karta (cost, value) |
| `activate_ability` | 8 | zdolności bez widocznej róźnicy wartości | projekcja = efekt zdolności |
| `resolve_*` (discard, trigger_target, exploit, search, …) | po 1–7 | pojedyncze, zwykle wybórAmong równymi statystykami | doraźnie, przy okazji zgłoszeń |

Osobna, wciąż aktualna niespójność srodowiska: `docs/setup/ENVIRONMENT.md` twierdzi,
że egressu nie ma — tymczasem rejestr npm działa (`npm ping` ⇒ PONG), a
`api.scryfall.com` jest blokowany (`ECONNRESET`). Do poprawki przy najbliższym
tyknięciu setupu.

### 12.4 Tura 6: pomiar pomiaru — no-op y i saturacja na lethalu

Sama liczba 30,4% nie była użyteczna, bo mieszała dwa zjawiska. Rozkład po
12 partiach: **208 z 308 remisów** to para `block[]`/`attack[]` vs
`pass_priority` — silnik w tym samym kroku oferuje „nie deklaruj bloków" i
„spasuj". Zanim to obcięto, przyszła kolej na dowód, nie na wrażenie
(`test/audyt-bot-walka-remisy.test.js`, test 1): obie drogi kończą się
**identycznym stanem po obrażeniach** (życie 17, ten sam stół, ten sam krok),
różnią się tylko kolejnością pierwszego okna odpowiedzi (M172/C po jednej
stronie, pełna runda passów po drugiej). Dopiero z tym udokumentowanym
`no-op` liczba przestaje straszyć: **100 remisów między realnymi wariantami,
12,4% decyzji akcyjnych**.

Druga nauczka dotyczy metryki, nie bota. Reguła „jeśli którakolwiek opcja nie ma
projekcji ⇒ bez danych" **zacierała findingi**: przy `block[]` vs
`block[x<y]` obok `pass_priority`null projektu wciskał przypadek do koszyka.
Po odrzuceniu opcji bez projekcji wypłynęły 2 groźby `block` i 2 `attack` — a
przy nich wyszło, że surowa suma siły ataku jest złą osią porównania: przy
ataku śmiertelnym zestawy 16 i 17 obrażeń wygrywają tak samo, a obrona
zostawiona w domu nie znaczy nic, bo partia kończy się w tej turze. Projekcja
saturuje więc na lethalu (`sila = min(sila, życie wroga)`, `obronaWDomu` tylko
dla ataków nieśmiertelnych). Zasada: **metryka ma pytać o dane, które mogą
zmienić wynik partii, nie o wszystkie możliwe różnice** — w tę stronę działa to
tak samo uczulająco jak w tamtą.

### 12.5 Cztery groźby przejrzane: dwie polityką, dwie do benchmarku

| klasa | przykład | wyrok |
|---|---|---|
| `block` 2 | `srodziemie\|theros` s4010 t11: `block[]` (0) = `block[p13<t58]` (0), unik 4 obrażeń za 1 stwora | **polityka** — branch `declare_blockers` karze NIEblokowanie tylko pod presją śmiertelną; wymiana stworów za obrażenia nieśmiertelne jest świadomie neutralna |
| `attack` 2 | `kaladesh\|zendikar` s4008 t6: `attack[]` (0) = atak 1/1 w blokerów (0); `dominaria-wu\|worek-mroczny` s4012 t10: zestawy 3 vs 4 siły, obrona w domu 3 vs 2 (0 = 0) | **do benchmarku** — wycena jest płaska na drobnych różnicach; zaostrzenie wymaga testu win-rate, nie testu jednostkowego → `docs/backlog.md` |

Bramka jest więc **grzechotką** (`razem <= 4`, z wypisanymi przykładami w
momencie przekroczenia), nie zerem: udawanie, że projekt obwieścił ideał,
byłoby tym samym grzechem, przed którym ostrzega ADR 0019 (progi opisują stan,
nie aspiracje). Wzrost liczby łapie `npm test`; każda redukcja to świadoma
regresja progu w teście.

Bramy tury 6: `npm test` **4195/4195** · golden-master regenerowany (tylko
metadane śladu) · benchmark quick **83,6% / aggro 28,9%** — identycznie, bo
wagi się nie zmieniły. Commit: `cf978f0`.

### 12.6 Tura 7: `cast_permanent` widział ciało, ale nie cenę many

Krok planu brzmiał „zaostrzyć wycenę ataku, ale tylko przez benchmark". Zanim
cokolwiek zaostrzyłem, ta sama grzechotka pokazała nudniejszą i groźniejszą rzecz:
**4 na 8 remisów `cast_permanent` to pary stworów o tym samym korpusie i różnej
cenie, ex aequo po 73,8 / 74,7 / 71,1 punktu**. Gałąź wygląda na wycenioną
(jest `finish(score)`, są wagi `creaturePowerWeight`/`creatureToughnessWeight`),
ale w formule **nie ma kosztu**: 2/2 za {2} i identyczne 2/2 za {6} to dla bota
ta sama decyzja. Fix: nowy nazwany parametr `creatureManaCostWeight` (1 punkt za
punkt many — celowo mniej niż waga siły 2/pt, żeby większy korpus obronił swoją
cenę), wpięty w gałąź rzutu stwora. Waga rodziny `permanent` (0,9 z tunera B4)
skaluje różnicę, więc pin arytmetyczny testu wygląda na ułamek, a jest
dokładny: `4 × 1 × 0,9 = 3,6`.

Akceptacja liczbowa (plan wymagał benchmarku, nie testu jednostkowego). Baseline
zmierzony **na tej samej próbie i tym samym profilu** — z `git worktree` na `HEAD`,
żeby nie porównywać quicka z pełnym profilem:

| profil | baseline (kod sprzed zmiany) | po naprawie | Δ |
|---|---|---|---|
| quick, 672 mecze | heuristic 83,6% (562/672) | 83,8% (563/672) | +1 mecz |
| `--seeds 24`, 2016 meczów | heuristic **85,7%** (1727/2016) | 85,5% (1724/2016) | **−3 mecze** |

Werdykt, zapisany bez naciągania: **wynik jest neutralny** (różnica rzędu 0,2 pp
na 2016 meczach to szum, nie regresja ani poprawa). Przyjmuję change ze względu na
**lukę modelową**, nie na win-rate: formuła bez składnika ceny jest niekompletna
niezależnie od tego, czy na tym zbiorze talii trafiła w pozycję rozstrzygającą, a
parametr pozostaje nazwany i dostrajalny (B6). Kryterium planu brzmiało „brak
regresji" — jest spełnione; gdyby zamiast tego przyjąć „poprawa mierzalna", term
należałoby wycofać. Uczciwie odnotowane, bo pierwsza wersja tego akapitu podała
85,5% bez porównania z baseline'em i wyglądała jak poprawa.

Po zmianie wag — zgodnie z polityką z handoffu — partia Żywym Testerem: 12 gier
(4 pary talii × profile greedy/explorer/random, `--steps 300`), **DETEKTORY: brak
zgłoszeń w 12/12** transkryptów (transkrypty poza repo, decyzja właściciela).
Bramy tury 7: `npm test` **4199/4199** · `npm run test:all` **4209/4209** (0 fail)
· `npm run build` 58 modułów / **3126,4 kB** · strażnicy dokumentacji 24/24.

### 12.7 Metryka nie może być modelem gorszym od mierzonego kodu

Trzy korekty przyrządu, każda z nich zmieniła *wynik audytu*, nie tylko jego opis:

1. **`materialna = power + toughness`** było modelem gorszym niż wycena, którą
   mierzyło: Magic waży siłę i wytrzymałość inaczej (2/pt vs 1/pt), więc proste
   sumowanie flagowało pary słusznie uznane za zamienne. Zastąpione przez
   `waluta` — wartość korpusu minus koszt, czyli to, co gałąź realnie konsumuje.
2. **`obronaWDomu`** (suma wytrzymałości stworów zostawionych w obronie) okazało
   się **artefaktem**: stwór tapnięty atakiem odświeża się w naszym kroku
   odświeżania, czyli zdąży zablokować w turze wroga (CR 502.3; wyjątek
   „doesn't untap" obsługuje osobna gałąź). Znalazłem dwa „findingi" zbudowane na
   tym polu i oba były wymysłem metryki.
3. **Opcje bez projekcji** (`pass_priority`) nie mogą rozlewać się na całą
   decyzję — narzędzie odrzuca je i orzeka na reszcie (poprawka z tury 6
   utrzymana).

Po korektach: `cast_permanent` 8 remisów → **0 groźb**, `cast_spell` i
`activate_ability` (9) → **0 groźb**, `play_land` 37 → 0. Zostają sufity
per kind dla `attack` (4) i `block` (4) — to remisy *modelowo neutralnych*
wymian („+1 obrażenia za jednego stworą"), których zaostrzanie bez zmiany
funkcji wartości byłoby kręceniem wag dla licznika. Grzechotka jest więc
sufitem per kind, a nie pinem: trajektoria partii zmienia rozkład pozycji.


---

## Tura 8 (2026-09-02): uwagi właściciela z żywej gry — A picker, B hover, C equip, D nakładka

Właściciel przeszedł partię na stole i zgłosił cztery rzeczy. Wszystkie cztery są
w kodzie, każda w osobnym, zielonym comicie; kolejność = rosnący rozmiar zmiany.

| # | objaw | rozpoznane sedno | naprawa | brama |
|---|---|---|---|---|
| B | karty specjalne (Undercity, Day/Night, Poison) powiększają się tylko po kliku | `renderUndercity` UMIAŁ hover od M153/C, ale `renderTableView` podawał `hover` **tylko** Day/Night; panel trucizn nie miał nawet parametru; żadna z trzech kart nie miała reguły `:hover` w CSS | wspólny `attachSpecialCardHover` (jedyne miejsce podłączania) + przekazanie hoveru we wszystkich trzech wywołaniach + reguły `:hover img` i `cursor: pointer` | `test/uwagi-tura8-hover-kart-specjalnych.test.js` 5/5 — w tym **strażnik drutu** (scan wywołań) i **strażnik CSS** |
| D | „Koniec partii — wygrywa X" bez stanów życia i bez przyczyny | przyczyna od dawna jest w zdarzeniu `player_lost.reason` (`life_zero`/`poison_ten`/`empty_library`, CR 104.3b/704.10) i w `player_conceded`, ale tabela etykiet leżała *w środku* formatowania logu | `LOSS_REASON_LABELS` wyniesione do eksportu + czysty `gameOverNotice(view, state)`; nakładka buduje spany `ti-result`/`ti-life`/`ti-reason`, UI nie zgaduje (L48) | `test/uwagi-tura8-nakladka-konca-gry.test.js` 11/11 (w tym integracja na PRAWDZIWYM deck-oucie i separator w tekście, `f6900b5`) |
| A | modal Knockout Maneuver „zupełnie inny" niż modal blokowania/Fireball; zaznaczanie zamiast ptaszków | `.multi-target-*` i `.escape-exile-*` **nie mieli ani jednej reguły CSS** — ich wiersze były `<button>` z marką `[ ]`/`[x]` w tekście i osobnym przyciskiem „Podgląd"; wizard walki miał od M129/C natywne `<input>` w `<label>` (44 px, złote tło, klik w nazwę) | nowy `src/table/picker.js`: `renderPickerRow`/`renderPickerSection` — **wygląd i dotyk wspólne, logika per efekt**; pozycje celu = radio w grupie na pozycję; usunięto `addPeek`/„🔍 Podgląd karty"; `tools/table-tester` czyta `checked` i `.picker-name` | `test/uwagi-tura8-picker-wielocelowy.test.js` 10/10 (pozycja z silnika dla `knockout-maneuver`) + m195/m200/m257r5/m129 przerobione na nowy kontrakt (mini-DOM-y nauczyły się natywnej aktywacji kliku) |
| C | bot w jednej turze przełożył Thieves' Tools na dwa stwory | gałąź **przeniesienia** sprzętu liczyła tylko `delta = power(cel) − power(nosiciel)` i nie pytała o zawartość sprzętu; repro: `tools→marut` (7/7, warunkowa ewazja progu 3 = martwa) = **+11,00** i wybierane | jedna definicja `equipValuation(view, source, creature)` dla obu gałęzi (L28); przeniesienie „nic nie dodaje" = −12 (kara jak przy pierwszym założeniu); przeniesienie, które BUDZI efekt, jest dozwolone (naprawa, nie kaprys) | `test/uwagi-tura8-bot-equip-nie-przenosi-prozno.test.js` 7/7 (w tym anty-over-fix: `squires-lightblade` z pompą +1/+0 nadal wędruje za ciałem) + benchmark A/B 85,5% → 85,5% (§13.4) |

### 13.1 Wspólny helper prezentacji ≠ wspólna logika efektu

Klucz rozstrzygnięcia właściciela brzmiał: *logika musi być w każdym typie efektu
inna, ale wygląd może być taki sam*. Dlatego picker przyjmuje `kind`
(checkbox|radio), `group`, `rowClassName`/`toggleClassName`/`nameClassName` i
oddaje uchwyt `setChecked`/`setDisabled` — ale NIE wie, ile celów wolno zaznaczyć,
czy wybór jest legalny i co znaczy „zatwierdź". To nadal `legalCommands` silnika
(L48) i `multiTargetPlanOf` (pozycje z Oracle, iloczyn → wiersz na kandydata).
Dowód, że to jeden helper, a nie kopiuj-wklej: trzy kreatory (cele wielokrotne,
atakujący/blokujący, koszt escape) wywołują `renderPickerRow`, a `choice-request.js`
nie ma już ani jednej linii malującej ptaszek samodzielnie.

Klasy rodzinne (`multi-target-toggle`, `escape-exile-toggle`) zostały przeniesione
NA `<input>`, bo to na nie patrzy Żywy Tester (kontrakt M206: klik wiersza =
zmiana stanu). Tester zyskał przez to czytanie `checked` z fallbackiem na „[x]"
i branie nazwy z `.picker-name` — bez tej zmiany `nameOfRow` zwracałoby pusty
tekst i polityka „nie wybieraj ofiary będącej celem" (CR 601.2c) milczałaby.

### 13.2 Drut, nie żarówka (L120)

Dwie z czterech uwag (B i A) mają TEN SAM kształt przyczyny: komponent był
poprawny, a *miejsce użycia* nie przekazywało zależności albo nie miało stylu.
Test jednostkowy, który sam podaje komponentowi stub `hover`, nie może zobaczyć,
że aplikacja go nie podaje. Dlatego nowe testy mają asercje na wywołaniu
(`renderTableView` musi podawać `hover` trzem kartom; `main.js` musi używać
`gameOverNotice`; `equipValuation` musi być wywołany w obu gałęziach) i na
istnieniu reguł CSS dla każdej rodziny klas, którą kod DOM-owy produkuje.

### 13.3 Coś, czego świadomie NIE zrobiliśmy

- **C: nie wprowadziliśmy limitu „jedno equipnięcie na turę"**. Byłby to zakaz w
  modelu bota, którego nie ma w zasadach (CR 702.6a dozwala wiele aktywacji) i
  który blokowałby *naprawę* (sprzęt na martwym nosicielu → na żywym). Zamiast
  tego przeniesienie musi przejść te same badania, co pierwsze założenie: jeśli
  nie daje nic, jest gorsze od passu.
- **A: nie przerabialiśmy wizarda podziału obrażeń, many-wizarda ani `look`**
  (to nie „efekt wielocelowy" w sensie zgłoszenia i mają własne, już stylowe
  reguły CSS). One też mogą kiedyś przejść na picker — jest wpis w backlogu.
- **D: nie przenieśliśmy liczby tur ani seeda do nakładki** — seed jest w banerze
  końca gry, tura w logu; nakładka ma być czytelna na telefonie.


### 13.4 Bramy pomierzone po czterech naprawach

- `npm run test:all` (fast+slow, po C i po poprawce D): **4242 testy, 4242 pass,
  zero failów** (431 s pod obciążeniem batcha testera); `npm test` po A: 4224/4224;
  `npm run build`: 59 modułów / 3144,5 kB.
- Brama reżimu bota dla C: `test/bot-*.test.js test/audyt-bot-*.test.js
  test/equip-do-obecnego-nosiciela.test.js test/m244-equip-heurystyka.test.js
  test/m270-*.test.js test/m272-*.test.js` = **227/227** w 187 s; golden-master
  `test/fixtures/bot-decyzje-fixtures.json` bez regeneracji (fixture nie zawiera
  pozycji z przeniesieniem sprzętu), grzechotka `tieAkcyjne === 0` nietknięta.
- Benchmark A/B, ta sama próba `--seeds 24` = 2016 meczów, baseline z `git worktree
  add --detach /tmp/mtgbase8 ae8bc24`: heuristic **85,5% (1724) → 85,5% (1723)**,
  aggro 24,5% → 24,6%, random 4,5% → 4,5%. Werdykt: brak regresji (Δ = −1 mecz to
  szum tej próby, tura 7 nauczyła, że ±3 mecze nic nie znaczą); C wchodzi dla
  spójności modelu, nie dla win-rate.
- Żywy Tester, batch 12 partii (4 pary decków × profile greedy/explorer/random,
  seedy 781+, `--steps 300`): **12/12 rozegranych do końca, `DETEKTORY: brak
  zgłoszeń` w każdej partii**. Po przebudowie `dist/` nakładka D czyta się w
  transkrypcie jak należy, z przyczyną, o którą prosił właściciel (deck-out
  zdarzył się w 4 z 12 partii):

      == KONIEC PARTII == Koniec partii — wygrywa Gracz · Gracz: 22 ż. · Bot: 15 ż. · Bot wyczerpał bibliotekę

  Trzy pierwsze partie batcha mają jeszcze wersję sklejoną bez spacji
  („wygrywa BotGracz: 0 ż.") — wystartowały przed przebudową `dist/`; to z tego
  pomiaru urodził się `f6900b5` (separator w tekście, nie tylko w `gap` flexa, bo
  tester i czytnik ekranu czytają `textContent`).
- **Poboczny, nieplanowany efekt A — liczby są małe, więc podaję je wprost:** w
  12 partiach stół otworzył kreator wielocelowy 3 razy (2× `Severed Strands` z
  pozycjami „celów 1, ofiar 2", 1× `Diplomatic Relations` z pozycjami) i raz
  doszedł do zatwierdzenia:

      [sacrifice wizard] Severed Strands — wskaż cel czaru oraz stwora do poświęcenia (koszt): stwór przeciwnika:Vi — celów 1, ofiar 2
      [sacrifice wizard] rzucono: cel „Village Bell-Ringer (Nieprzyjaciel)", ofiara „Boros Challenger (Ty)"

  Przed turą 8 te same pozycje kończyły się na „opcji 0, potrzeba 1" i kreator był
  dla Testera nieprzeklikalny. Czyli: rodzina wielocelowa jest **już** w zasięgu
  audytu, ale jej realne pokrycie to wciąż 3 zdarzenia na 12 partii — to jest
  następna rzecz do pogrubienia (talia z kartami wielocelowymi w
  `tools/table-tester/decks/`), a nie powód, żeby twierdzić, że jest zbadana.

### 13.6 Pytanie kontrolne właściciela (tura 9): dwa równe ciała, oba profitują

Właściciel dopytał, czy naprawa C obejmuje też jego sedno: „gdyby były dwie
kreatury, którym obu ten equipment daje pompę, to czy zablokowane jest
bezsensowne wydawanie many na dwukrotne przerzucanie? Chodzi o to, żeby wybrał
najlepszy cel i tam już zostawił". Odpowiedź: **tak, i to jest strukturalne, nie
przypadkowe**. Gałąź przeniesienia (`wornByMine`) jest drabiną o czterech
szczeblach, a każdy z nich jest antysymetryczny, więc X->Y i Y->X nie mogą być
dodatnie jednocześnie:

| szczebel | warunek | efekt |
|---|---|---|
| 1 | celowi sprzęt nic nie dodaje | `score -= 12` (veto z tury 8) |
| 2 | celowi dodaje **więcej** niż nosicielowi (budzi martwy efekt) | `+= 4 + różnica` (naprawa) |
| 3 | ciało o ≥ 2 siły większe **i** ładunek nie gorszy | `+= 4 + delta` (M100/E13) |
| 4 | wszystko inne, w tym dwa równe ciała z tą samą pompą | `score -= 6` → pass |

Pomierzone na prawdziwych kartach (bez nadpisanych statystyk, `test/uwagi-tura9-bot-rowne-ciala-equip.test.js`):

- Wooden Stake (+1/+0, equip {1}) na Highland Game 2/1, kandydat Leafcrown Dryad
  2/2 → oferta **−4,00** przy passie 0,00, bot nie płaci. Brawler's Plate
  (+2/+2, trample, equip {4}) na tej samej parze → **−4,00** (droższy sprzęt nie
  zmienia wyroku, bo nie ma osobnej wagi kosztu w tym miejscu modelu).
- Schody 2/1 → 2/2 → 7/7: `->marut = +10,00`, `->leafcrown-dryad = −4,00` — bot
  idzie **jednym krokiem na najlepsze ciało**, nie płaci equipu dwa razy w turze.
- Sprzęt uwięziony na ciele, które nie może atakować, schodzi: Monastery Flock
  (0/5, defender) → Undead Servant (3/2) = **+8,00** (drabina nie blokuje
  naprawy; to anty-over-fix dla pierwszego punktu).
- Własność anty-ping-pongowa jest testem, nie rozumowaniem: wszystkie pary z
  pięciu ciał × dwóch sprzętów (40 konfiguracji) — jeśli X->Y > 0, to Y->X < 0,
  przy czym test wymaga, by zobaczył co najmniej 3 dozwolone awanse (inaczej
  nic by nie sprawdzał).

**Dwie znane granice modelu (wpis w `docs/backlog.md` §3, nie naprawiane w tej
turze, bo każda z nich to zmiana wagowa i wymaga benchmarku A/B):** (i) ładunek
sprzętu jest liczony od pompy i nowo grantowanych keywordów, a nie od tego, jak
dobrze nosiciel umie te statystyki spożytkować — pompowany 3/2 vanilla i 3/3 z
lataniem są dla drabiny *równoważni* (oba kierunki −4,00, czyli sprzęt zostaje
tam, gdzie już leży); (ii) ten sam powód, dla którego +1/+0 na defenderze o tej
samej sile co kandydat nie jest ciągnięte na atakującego. W obu przypadkach
konsekwencją jest co najwyżej gorszy wybór nosiciela, a nie spalona mana — czyli
zgodnie z zasadą z zgłoszenia: nie płacimy za ruch, którego model nie uważa za
poprawę. Zmiana wagowa bez A/B na 2016 meczach byłaby gustem (L118/L119).

Ta tura nie rusza `src/` — więc bez benchmarku i bez nowego milestonu; bramy:
subset reżimu bota (`test/bot-*.test.js test/audyt-bot-*.test.js
test/equip-do-obecnego-nosiciela.test.js test/m244-equip-heurystyka.test.js
test/m270-*.test.js test/m272-*.test.js` + plik tury 8 i tury 9) oraz `npm test`.


### 13.7 Tura 10: ta sama drabina od drugiej strony — pompa nie może więzić się u defendera

Pytanie właściciela z 13.6 brzmiało „czy zablokowane jest bezsensowne wydawanie many
na dwukrotne przerzucanie". Odpowiedź „tak" padła w 13.6 na liczbach, ale samo
mierzenie wydobyło brak: blokada ruchu bocznego działała także na ruch, który nie
jest boczny. Wooden Stake (+1/+0) na `Wishful Merfolk` (3/2, defender) i kandydat
`Undead Servant` (3/2, atakuje) mieli identyczną wycenę ładunku, więc przeniesienie
za {1} było karane −6 i sprzęt leżał na stworze, który nigdy nie zaatakuje.

Zmiana jest jedna i siedzi w definicji (L28): waga siły w `equipValuation` zależy od
tego, czy nosiciel umie ją spożytkować — ciało z `cantAttackStatic` albo takie,
którego obrażenia zapobiega ochrona blokera (`attackerNeutralizedByProtection`,
CR 702.16c), liczy połowę wagi pompy (siła na defenderze wciąż decyduje o bilansie
bloku). `value = (jałowy ? pumpPower : 2·pumpPower) + pumpToughness + ofensywne`.
Relacja „lepszy dom" nadal jest funkcją pary (sprzęt, nosiciel), a nie kierunku
ruchu, więc antysymetria (brak ping-pongu) przetrwała — i to jest testowane, nie
deklarowane.

| stół | przed M289 | po M289 | wyrok |
|---|---|---|---|
| Wooden Stake na defenderze 3/2 → atakujący 3/2 | −4,00 | **+7,00** | naprawa dozwolona (była zablokowana) |
| Wooden Stake na Flockerze 0/5 → atakujący 3/2 | +8,00 | +7,00 | bot płaci w obu wersjach |
| Wooden Stake na atakującym 2/1 → atakujący 2/2 (ruch boczny) | −4,00 | −4,00 | bez zmian: pas |
| Brawler's Plate (+2/+2, trample, {4}) na bliźniakach 2/1 i 2/2 | −4,00 | −4,00 | bez zmian: pas |
| pompa na lataczu 3/3 → vanilla 3/2 i odwrotnie | −4,00 | −4,00 | bez zmian (siła po pompie większa u nosiciela) |
| własność anty-ping-pongowa, 40 par ciał × sprzętów | trzyma | trzyma | T9/3 z ≥3 awansami |

Brami zmian wagowych w tym projekcie jest benchmark na tej samej próbie, więc i tu:
`git worktree` na `54c4371` (bez naprawy) vs kandydat, `node tools/benchmark.mjs
--seeds 24` = 2016 meczów. **Werdykt: agregat identyczny** — heuristic 85,5% (1723/2016) w obu gałęziach, aggro 24,6% (248/1008), random 4,5% (45/1008). To nie jest dowód „nic nie zmieniliśmy": ta pozycja (sprzęt na ciele, które nie atakuje, z równym co do siły kandydatem obok) nie zdarza się w talach benchmarku, więc win-rate nie ma czego mierzyć. Dowodem działania naprawy są stoły w `test/uwagi-tura9-bot-rowne-ciala-equip.test.js`, a benchmarkiem zamknęliśmy tylko tylną furtkę (że nic nie zepsuliśmy w reszcie gry bota).


### 13.8 Próba pogrubienia pokrycia pickera talia audytowa (tura 10, koniec: NIE DA SIĘ bez nowych kart)

Pokrycie kreatora celów z 13.4 (3 otwarcia na 12 partii) wygląda na dziurę w pętli
jakości, więc spróbowałem dobudować do niej materiał: talia `decks/wielocelowa.txt`
z 12 kartami otwierającymi picker i 12 ciałami, żeby tester dostawał te ekrany co
partię. Zabiły to dwa strażniki repo i oba miały rację:

- `test/m132-proporcje-landow.test.js` (M132/B): „każda talia ma co najmniej 1 ląd
  na 2 karty nielandowe" — mój projekt miał 24/8 (3.00, próg 2.00);
- `test/repo-decks.test.js` (M178, ADR 0023): „każda wspierana karta jest w
  DOKŁADNIE jednej talii" — 11 z 12 moich pickerów już leżało w innych talii
  (`bone-splinters: alara.txt, wielocelowa.txt`).

Drugi niezmiennik jest zasadą, nie wykroczeniem formatowym, więc talii nie da się
naprawić inaczej niż przenosząc karty między taliami. Świadomie tego NIE zrobiłem:
`decks/*.txt` karmią benchmark i audyt remisów, a przeniesienie karty zmieniłoby
skład par A/B — porównanie `--seeds 24` z `54c4371`/`ae8bc24` przestałoby być
jabłko-do-jabłka (lekcja tury 7 o porównywaniu tylko tego samego profilu).

Pomiar, który zostaje zamiast talii (gestość pickerów w istniejącym materiale):
spośród **443 kart wspieranych** tylko **7 deklaruje >1 celu**, 15 ma poświęcenie lub
odrzucenie w koszcie, 12 to equipmenty — a **kart wolnych (nieprzypisanych do żadnej
talii) jest zero**, więc nie ma z czego ułożyć nawet małego talonu audytowego.
Najgestojsza dostępna para (`ravnica` vs `worek-dziki`, po 3 karty pickerowe) w 4
partiach (explorer/greedy/random/defensive, seedy 911-914, `--steps 300`) dała 40
wpisów `kreator many` i 12 `[combat wizard]`, ale **zero otwartego kreatora
wielocelowego** — przy 3 otwarcia na 12 partii z 13.4 to nie jest poprawa, tylko
potwierdzenie, że surowiec się wyczerpał. Wszystkie partie: `DETEKTORY: brak
zgłoszeń`.

Wniosek dla następnej tury: pokrycie rodziny wielocelowej podnosi się **nowymi
kartami w katalogu**, nie tasowaniem talii. Przepis jest gotowy i legalny w momencie,
gdy karta będzie nowa (nowe karty są z definicji wolne, więc nie łamą ADR 0023):
Diplomatic Relations, Knockout Maneuver, Malamet Battle Glyph, Assert Perfection,
Time to Feed, Insatiable Appetite, Severed Strands, Bone Splinters, Lash of the
Balrog, Fake Your Own Death, Village Rites, Liliana's Triumph + 12 tanich ciał +
lądy w proporcji 1:2. Wpis w `docs/backlog.md` §1 i §4.

### 13.5 Errata do §13 (uczciwość cytatu)

W komunikacie commitu `d8fde3f` napisałem „strażnik long-line 15/15" i wprost lub
pośrednio powoływałem się na `test/dokumentacja-dlugie-linie.test.js`,
`test/dokumentacja-registra-i-decyzje.test.js`,
`test/budzet-lektury-straznika.test.js`. **Takich plików w repo nie ma** (pomiar:
`find test -name "*dokumentacja*"` zwraca tylko
`test/dokumentacja-budzet-lektury.test.js`; `git log -- test/dokumentacja-dlugie-linie.test.js`
pusty — nigdy nie istniały). Limity kolumn w tym projekcie są konwencją z
`AGENTS.md`, nie testem: w `test/` jest 318 plików z liniami > 120 znaków.
Faktyczni strażnicy doków, których nazwy wolno cytować:
`test/dokumentacja-budzet-lektury.test.js` (2/2: budżet lektury startowej +
dziennik historii poza lekturą obowiązkową) i `test/docs-decisions.test.js`
(22/22: spójność indeksu ADR). Mechanizm wpadki: `node --test <nieistniejąca-ścieżka>`
wypisuje „Could not find" na stderr, ale **kodu wyjścia nie psuje**, więc w summary
widać tylko zielone liczby z pozostałych plików. Reguła: przed zacytowaniem nazwy
testu — `ls test/<plik>` albo `git log -1 --format=%h -- <plik>`.

Objaw uboczny tej samej wpadki (udokumentowany, bo inaczej następna osoba szukałaby
przyczyny w silniku): w katalogu głównym pojawił się plik `marut` o rozmiarze 0 —
bash z backticków w komunikacie commitu wykonał `->marut`, czyli przekierowanie
wyprowadzania. Usunięty; pliku w `git status` już nie ma. Sam commit `d5bb962`
został z dziurawym message (nie da się poprawić bez force pushu), a dopisek
`0911fbb` — z komunikatem bez ogonków, bo pisałem go w heredoku ASCII.

---

## Tura 11 (2026-09-02): domknięcie (e) i start (b) — jakość ciała w wadze pompy oraz pierwsza nowa karta wielocelowa

Właściciel kazał ruszyć oba wątki, które tura 10 zostawiła „świadomie nietknięte":
 wagowe domknięcie equipu (e) i 4–6 kart wielocelowych pod pokrycie pickera (b).

### 14. (e) Trzeci stopień wagi siły — ciało, którego cios dojdzie

**Zgłoszenie, które to uruchomiło** (tura 10, odpowiedź na moje zastrzeżenie): para
„vanilla 3/3 vs latacz 3/3" miała oba kierunki przeniesienia ujemne (−4,00 / −4,00),
więc sprzęt zakotwiczał się na gorszym ciele.

**Zmiana:** w `equipValuation` (jeden punkt liczenia wartości, L121) waga punktu siły
ma teraz trzy stopnie: 1 (ciało nie atakuje / obrażenia zapobiegane — M289), 2 (atak
dojdzie, ścianę trzeba pokonać), 3 (ciało ma własną ewazję omijającą tę ścianę).

| Scenariusz (Wooden Stake +1/+0) | przed M290 | po M290 |
|---|---|---|
| D4 vanilla 3/2 (nosi) → latacz 3/3 | −4,00 | **+7,00** — bot płaci i przenosi |
| D3 latacz 3/3 (nosi) → vanilla 3/2 | −4,00 | −4,00 (bez zmian) |
| E1 gorehorn 3/3 (nosi) → angel 3/3 latacz | −4,00 | **+7,00** |
| E2 angel 3/3 (nosi) → gorehorn 3/3 | −4,00 | −4,00 |
| E3/E4 ta sama para, Brawler's Plate (+2/+2) | −4,00/−4,00 | +8,00/−4,00 |
| D5 dwa równe vanilla (2/1 i 2/2) | −4,00 | −4,00 |
| D1/D2/D6 pary z defenderem (M289) | +7,00/+7,00/+7,00 | bez zmian |
| FRESH pierwsze założenie: defender 3/2 vs atakujący 3/2 | 18,00 vs 6,00 | bez zmian |
| FRESH pierwsze założenie: latacz 3/3 vs vanilla 3/2 | 18,00 vs 18,00 | 18,00 vs 18,00 (remis — patrz niżej) |

**Wrażliwość na ścianę** (to odróżnia premię od „nagrody za kartę z lataniem"):
przy wrogim reachu (leafcrown-dryad) albo wrogim lataczu (token Spirit 1/1) premia
znika i D4 wraca do −4,00. Zapisane jako test T11/4.

**Czego świadomie NIE ruszyłem:** gałęzi FRESH (pierwsze założenie sprzętu). Tam waga
nadal idzie od mocy nosiciela i dla pary latacz 3/3 vs vanilla 3/2 daje remis
18,00/18,00 (T11/7 pinuje ten stan jako decyzję, nie przeoczenie). Jej piny siedzą
w kilkudziesięciu testach equipu, a każda zmiana wagowa wymaga własnego A/B — to
osobny commit do zatwierdzenia.

**Brama wagowa:** benchmark A/B `--seeds 24` (2016 meczów, profil identyczny,
baseline `f6a5459` z `git worktree`): heuristic 85,5% (1723/2016) → 85,5%
(1724/2016); aggro 24,6% (248/1008) → 24,5% (247/1008); random 4,5% (45/1008) →
4,5% (45/1008). Interpretacja: zero regresu i jeden przełączony mecz — ta zmiana
jest obroną zasady (ruch z realną wartością nie może być karany), a nie polowaniem
na win-rate; identyczny agregat nie jest dowodem poprawy i nie udaję, że jest.

### 15. (b) Karty wielocelowe: zrobione, zmierzone i WYCOFANE na polecenie właściciela

**Stan na 2026-09-03: w drzewie nie ma żadnych nowych kart.** Właściciel cofnął
zgodę na dokładkę do katalogu i nakazał usunięcie tego, co weszło. Commit `0434199`
(fan-out w `src/engine/spells.js`, `coordinated-assault` CLU 128, `dual-shot` SOI
153, wpisy w `MANA_COSTS`, talie, rodzina testów, golden-master) został
zrevertowany. Katalog, `decks/*.txt`, fixture bota, sufit grzechotki i seed
scenariusza wróciły do stanu z `f6a5459`. To, co poniżej, to wiedza z tej gałęzi:
nie kod, nie karty i nie obietnica, że wrócą.

**1. Luka silnika, którą pomiar (b) obnażył — w drzewie NIE naprawiona.** Fan-out
„each of up to N" żył od M157 wyłącznie w torze triggerów (`src/engine/triggers.js`).
Tor czaru aplikuje efekty RAZ, z pełną tablicą celów, a `pump`,
`grant_keywords_until_end_of_turn` i `damage` czytają `targets[effect.targetIndex ??
0]` (pliki `src/engine/effects.js`). Czyli każdy przyszły czar „up to two target
creatures" pompowałby pierwszy cel dwa razy, a drugi wcale. Patch istniał:
generyczny deskryptor `allTargets: true` w pętli efektów `src/engine/spells.js`
(aplikacja per cel, `continue` przed obsługą `pendingSpell`, zakaz łączenia z
efektami blokującymi decyzję) plus strażnik, że silnik nadal nie zna żadnej nazwy
karty (ADR 0002). Wycofany razem z kartami. Jeśli karta wielocelowa kiedykolwiek
wejdzie, to jest warunek zero, nie opcja — stąd L123.

**2. „Sieć nie działa" (tura 10) było moją błędną konkluzją, nie blokadą.**
`docs/cards/HOW_TO_ADD_CARD.md` ma opisany kanał awaryjny na wypadek martwego
egressu: agent ściąga te same URL-e przez `fetch_page` i zapisuje snapshot do
`docs/cards/scryfall-*.json`. Wszystkie teksty reguł, które wtedy przepisałem,
pochodziły 1:1 z API, nigdy z pamięci — tej zasady nie złamałem i nie złamię.

**3. Rykosze wejścia dwóch kart — realna cena, nie formalność.** Dokładka ruszyła
osiem bramek: `src/cards/mana-costs-data.js` (każda karta supported musi mieć
nadrukowany koszt), `test/card-data.test.js` (pokrycie snapshot ↔ rejestr),
`tools/generate-plan-decks.mjs` (przydział talii), `test/repo-decks.test.js` (M178
singleton + M228: suma nielandów 36 → 37, bo karta w planie >18 przestawia podział
Innistradu — Blazing Torch przeszedł brg → wu), `test/m138-audyt-stolu.test.js` (Z5,
etykieta slotów many), `test/bot-scoring-snapshot.test.js` (golden-master bit w bit),
`test/audyt-bot-walka-remisy.test.js` (sufit `block` 4 → 5). Plus rzecz, której nie
przewidziałem: `test/panel-rozgrywka-tura-przeciwnika.test.js` (M101/D) trzyma talję
człowieka sztywno na `decks/innistrad-brg.txt`, więc zmiana składu zbiła pin seedu
(seed 2 przestał rzucać dwoma opóźnionymi triggerami, przenieśliśmy na 18). L25
dotyczy więc także testów scenariuszowych: przy każdej zmianie talii trzeba
przejrzeć seedy wszędzie, gdzie ta talia jest podkładką. Po revertcie te pliki są
znowu w stanie sprzed eksperymentu, a rodzina bramkowa świeci: **124/124** (equip
tury 9 i 11, fixture, strażnicy talii i źródeł kart, Z5, m132, m203, panel M101/D,
grzechotki, M195).

**4. Atrybucja grzechotki zamiast podnoszenia progu — metoda zostaje.** Ten sam
audyt (`audytRemisow`, 7 par, 1 gra na parę) na trzech drzewach:

| Drzewo | block | attack | tieNoOp |
|---|---|---|---|
| `f6a5459` (sprzed tury 11) | 4 | 4 | 130 |
| `358ee35` (tylko M290, talie bez zmian) | 4 | 4 | 130 |
| drzewo z kartami (przed revertem) | 5 | 4 | 133 |

Wniosek: **sama waga M290 nie dołożyła ani jednego remisu** (wiersz identyczny co do
znaku), a dokładka wzięła się z innego rozdania po zmianie składu talii, nie z
mechaniki. Ten sam porządek dotyczy fixture’a: `node tools/bot-scoring-snapshot.mjs
--write` wyłącznie na GOTOWYM drzewie — pierwszy zapis zrobiliśmy przed wpisem do
`mana-costs-data.js` i golden-master świecił, choć kod był dobry. Stąd L124.

**5. Pokrycie kreatora celów — dane z eksperymentu (już nieaktualne jako stan).**
Na drzewie z kartami: 8 partii (`tools/table-tester/run-game.mjs`, po 300 kroków,
seedy 922–925 i 931–934) dało 2 otwarcia kreatora wielocelowego (baseline z tury 10
na 4 partiach: 0), trzy rzuty wielocelowe i zero zgłoszeń detektorów stołu; w jednym
transkrypcie widać było sam fan-out bota (dwa zdarzenia obrażeń, dwie ofiary —
zgodnie z rulingiem WotC, że tego samego stworzenia nie można wskazać dwa razy). Po
revertcie te otwarcia znikają razem z kartami: w katalogu zostaje 7 kart
deklarujących >1 celu na 443 wspierane, kart wolnych zero (ADR 0023). **Pokrycia tej
rodziny nie da się podnieść bez nowych kart, a nowych kart bez zgody właściciela
dodawał nie będę** — to jest otwarta decyzja, nie zadanie do odhaczenia.

**Baseline benchmarku po revertcie to baseline z §14** (heuristic 85,5%, aggro
24,5%, random 4,5%), bo talie są znowu te same co przy `358ee35`. Jednostronny run
z drzewa z kartami (heuristic 87,0%, aggro 21,4%, random 4,7%) został unieważniony
przez revert i nie jest punktem odniesienia dla niczyich A/B — wspominam o nim, że
nikt go nie wyciągnął z logów jako „nowszych liczb".

---

## 17. Tura 13 (2026-09-03): audyt równoległego rysowania wierszy — co zostało zrobione, a co nie

Pytanie właściciela z tury 8 („żeby nie było, że każdy efekt jest na innej, równoległej
funkcji wizualizującej wybory") rozszerzyło się w turze 13 na zasadę. Przed kodowaniem
zmierzyłem stan, zamiast go pamiętać; po refactorze zmierzyłem ponownie tymi samymi
sposobami, żeby liczby były porównywalne.

### 17.1 Kto rysował wiersz sam (przed → po)

| Miejsce rysowania wiersza | przed M292 | po M292 |
|---|---|---|
| `renderCombatWizard` (walka: ptaszki) | picker | picker |
| `renderMultiTargetWizard` (cele wielokrotne) | picker | picker |
| `renderEscapeExileWizard` (koszt escape) | picker | picker |
| `renderDamageDivisionWizard` (podział na cele) | **własny markup** | picker (`kind:'stepper'`) |
| `renderDamageWizard` (przydział blokującym) | **własny markup** | picker (`kind:'stepper'` + `actions`) |
| `mana-wizard.js` (źródła many, CR 106.7b) | **własny markup** | picker (`kind:'button'`) |
| `renderChoiceRequest` — ptaszek wyciszenia | **ręcznie lepiony `input`** | picker (`variant:'inline'`) |
| `render.js` panel akcji — ptaszek wyciszenia | **druga ręcznie lepiona kopia** | picker (`variant:'inline'`) |
| `renderLookWizard` (scry/surveil/look, chipy) | własny markup | własny markup (świadomie) |
| `renderFertileThicketWizard` (tap-to-mana) | własny markup | własny markup (świadomie) |

Liczby z pomiaru: wołań `renderPickerRow`/`renderPickerSection` 7 → 11, kreatorów w
`choice-request.js` idących przez helpera 3/8 → 6/8, a ręcznie lepionych
`choiceNode(…, '*-row')` i `createElement('input'|'label')` w `src/table/*.js` poza
`picker.js` — 4 → 0 (ostatnie dwa to były ptaszki ignorowania). `picker.js` urósł
121 → 299 linii; `index.html` stracił 4 reguły (duplikaty: `.combat-wizard-row`,
`.combat-wizard-row .combat-wizard-name`, `.damage-wizard-row`, `.damage-wizard-minus`,
po 261 znaków na blok, różnił je tylko selektor).

### 17.2 Dlaczego dwie pozostałe listy nie poszły za pickarem

`renderLookWizard` i `renderFertileThicketWizard` rysują chipy (`.look-wizard-card`,
`.thicket-card`) — to siatka kart z obrazkiem, nie wiersz „ptaszek + nazwa" ani stepper.
Wciskanie ich w `kind:'button'` dałoby komponentowi kształt, którego nikt nie używa, a
testom zmieniłoby selektory bez żadnej korzyści dotykowej (chipy mają własny cel dotyku
pinowany w `m138-*`). Zostają w `docs/backlog.md` §2 jako otwarta decyzja kształtu, z ceną:
360 linii własnego markupu.

### 17.3 Przypadek, który trzeba było przerobić, a nie obejsć

`test/m129-combat-wizard-dotyk.test.js` (M129/C) czytał tekst `index.html` i wymagał, żeby
KAŻDA rodzina kreatora miała własne `min-height: 44px`. To ta asercja utrzymywała przy
życiu duplikaty CSS z 17.1 — czyli strażnik jakości ręcznie pilnował braku jakości.
Z zaufania do pomiaru, nie do intencji autora testu, sprawdziłem mutacjami, co nowy test
łapie:

| Mutacja | stary `m129` | nowy `m129` |
|---|---|---|
| wycięty `min-height` z `.picker-row` (regresja w komponencie) | **zielony** (fałsz) | RED ×2 |
| dopisana kopia `.damage-wizard-row { min-height… }` | zielony | RED |
| `createElement('input'); i.type='checkbox'` w kreatorze | nie dotyczy | RED |
| ptaszek 24 px → 16 px | RED | RED |

Przy okazji wyszedł błąd parsera w samym teście: reguła stojąca zaraz po bloku komentarza
znikała z listy (komentarz wchodził w „selektor"), więc pierwszy pomiar mutacji był
fałszywie zielony. Poprawka: komentarze wycinane przed dzieleniem na reguły. Lekcja idzie
do `docs/LESSONS.md` jako L125.

### 17.4 Czego refaktor NIE zmienił

Semantyki reguł: sufity CR 510.1c (suma przydziału ≤ mocy) i 510.1d (każdy blokujący ≥
lethal, inaczej zerują się kolejni), kanalizacja, predykaty klikalności. Warunek
`canIncrease` wrócił do `canIncrement` (tam, gdzie był przed refactorami tury 7 i 8 —
lekcja L120), a handler dostał tylko guard na `disabled`. `disabled` na strzałkach ↑/↓
(„podnieś do lethal") w kreatorze obrażeń pozostało nieobecne, tak jak w źródle: stare
kody tylko no-opowały, więc dorzucanie blokady byłoby nieproszoną zmianą zachowania przy
przepisywaniu markupu. Jedyne świadome poszerzenie zachowania: klik w wyłączony stepper
jest ignorowany przez helper (stub DOM w testach UI odpala listenery niezależnie od
`disabled`, a na iOS-ie dotyk w `disabled` potrafi przebić się do rodzica).

### 17.5 Pomiar na prawdziwym DOM-ie i na żywym artefakcie

Jednostki liczą klasy na stubie DOM, więc dorzuciłem pomiar, którego stub nie udaje:
render obu kreatorów obrażeń, kreatora many i panelu opcji w **jsdom** (prawdziwy
`classList`, prawdziwy `innerHTML`, prawdziwa propagacja kliku). Wyniki (skrypt
jednorazowy w `/tmp`, nie artefakt repo — wnioski są w testach `m292-*`):

- wiersz przydziału i wiersz podziału mają w DOM-ie `class="picker-row damage-wizard-row picker-stepper"`,
  zero `<input>` w wierszu, `−` wyłączony na zerze, `+` aktywny, licznik `2` po dwóch klikach;
- klik w `.damage-wizard-name` wywołuje `onOpenCard('b1')` i NIE wysyła wyboru (`onComplete` 0 razy);
- klik w `.damage-wizard-up` zamienia kolejność przydziału (M150/B) — bez zmiany sumy;
- źródło many to `<button type="button" class="picker-row action choice-request-option mana-wizard-source picker-button">`
  z treścią „Tapnij: Island (U)" i ikoną `.ms-u`; klik w realnym DOM tapuje źródło;
- ptaszek wyciszenia: `label.className === "action-ignore"` (dokładnie), `input.type === "checkbox"`,
  klik zgłasza `onToggleIgnoredOption(klucz)` raz i nie odpala `onResponse`.

Na żywym artefakcie (`npm run build` → 3156,0 kB / 59 modułów, `tools/table-tester/run-game.mjs`,
cztery partie: seed 42 innistrad-brg vs ravnica tick-rate 0,5 oraz seed 3/9/15 srodziemie vs
ravnica profil random tick-rate 0,35): **17 otwarć i opłat kreatora many przez wiersz-przycisk
pickera** (ślady „pozostało N many"), **30 śladów kreatora walki** ptaszkowego (atak i bloki,
m.in. „atakuję 4 z 6 stworów"), przy `--tick-rate` ptaszkowane akcje, czyli ścieżka
`.action-ignore-input`; **detektory: 0 zgłoszeń we wszystkich czterech partiach**
(np. seed 42: akcje widziane 21 / kliknięte 19, modale 5, sondy noop 43).
Gałąź podziału obrażeń nie wyszła z tasowania w żadnej z czterech partii, więc jej życie na
prawdziwym DOM-ie opiera się na pomiarach jsdom powyżej i na `test/m172-*`/`test/m292-*` —
nie zgłaszam jej jako „krytej na żywo".
