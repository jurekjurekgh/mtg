# Audyt PR #108 — audyt PR #107 (F-A2/1, F-A2/2), fix etykiet tap (2026-09-09)

Audytor: sesja `arena/01a08691-mtg` (etap B2 planu
`docs/plans/PLAN_2026-09-09b-audyt-pr108-petla-jakosci.md`).
Zakres: `gh pr diff 108` = **4 pliki** (PR #108, 3 commity na gałęzi
`arena/01a08611-mtg`: `120627b` Plan; `7edaad9` A2 audyt #107;
`3ac644a` A3 fix F-A2/1). Commity A4/A5/A6 z planu sesji #108 NIE
powstały — sesja zakończyła się po A3 (osobny wątek: niepushowana
praca A4 wróciła plikiem `.patch` — patrz §4).
Metoda: odczyt diffu w całości, weryfikacja mutacyjna testów F9 (L13),
grep producentów/konsumentów, konfrontacja twierdzeń audytu z pomiarem,
sweep liczb vs `HANDOFF_2026-09-08k` i vs stan bieżący.

Werdykt: **fix F-A2/1 poprawny i dowiedzony RED→GREEN; 1 znalezisko
procesowe (domknięcie dokumentacyjne sesji #108 nie istnieje —
powtórka wady F-A2/2, którą sam audyt wytknął), 2 nieścisłości prozy
audytu bez wpływu na kod.** Silnik i bot nietknięte. Żadne znalezisko
nie wymaga fixa w kodzie.

## 1. Niezależnie POTWIERDZONE

- **Silnik/bot/decki/katalog nietknięte.** Diff #108: `AUDYT_PR107`,
  `PLAN_2026-09-09-audyt-pr107`, `src/table/render.js`, `test/audyt-b54-tekst`.
  Commit `3ac644a --stat`: 2 pliki (render.js + test, +31/−3). Zero zmian
  w `src/engine`, `src/controllers`, `src/cards`, `decks/`.
- **F-A2/1 (etykiety tap) POPRAWNY.** Trzy wpisy `describeEffect`:
  `tap_permanent` → „zatapnij”, `tap_all_lands_opponents_control` →
  „zatapnij wszystkie lądy przeciwnika”, `tap_permanents` →
  „zatapnij permanenty”. Forma rozkazująca zgodna z poświadczoną stroną
  untap (`untap_permanent` → „odkręć”, B54/F1); log zdarzeń
  (`session.js`: `tap_permanent: 'zatapianie celu'`) już zgodny.
  Ścieżka publiczna (tekst karty na stole) — katalog: 29 użyć
  `tap_permanent` + 2× `tap_permanents` (pomiar; proza audytu mówi
  „~16” — patrz F-B2/2).
- **F9 RED→GREEN dowiedzony mutacją (L13).** Przywrócenie trzech
  napisów „tap” → dokładnie 3 testy F9 RED (`not ok 11/12/13`),
  reszta pliku (10 pinów B54) GREEN; po odtworzeniu fixu 13/13 GREEN.
  Karty testowe niosą właściwe typy efektów realną ścieżką
  (registry → rulesText, L21 pkt 3): Twiddle → `tap_permanent`,
  Shiva → `tap_all_lands_opponents_control`, Aerith → `tap_permanents`.
- **Regex `RAW_TAP` poprawny.** Sonda 6 przypadków: „zatapnij”,
  „Zatapnięcie…”, „etap walki” → brak trafienia; surowe „tap”/„Tap”
  na granicy słowa → trafienie. Uzasadnienie w komentarzu testu
  (polskie `ń` poza `\w`) prawdziwe.
- **O1 (raw throw w execute) przeniesione wiernie.** `throw new Error(
  'pendingReplacementChoice bez frame…')` istnieje w `game-state.js`;
  jedyny producent (`destruction.js:31`) zawsze niesie frame — stan
  nieosiągalny, kod działa jako asercja. Bez zmian w tej sesji.
- **O3 (pozostałe surowe slugi) prawdziwe.** `surveil`, `clash`,
  `morbid` (w `search_basic_land_morbid`), `start_engines`, `station`,
  `unearth_return` nadal w `describeEffect` — kandydat do przyszłego
  sweepu językowego; poza zakresem (nowy zakres bez zlecenia).

## 2. Znaleziska → rejestr (bez fixów w kodzie)

### F-B2/1. Domknięcie sesji #108 nie istnieje (docs; średnie)
Sesja #108 wytknęła sesji #107 brak domknięcia (F-A2/2: dopisek A–E
w HISTORY + handoff + checkboxy + liczby finalne) — a SAMA nie domknęła
niczego:
- brak `docs/setup/HANDOFF_2026-09-09*.md` (najnowszy w repo: `...-08k.md`,
  opisujący sesję #107 do P5);
- zero wzmianek o #108 w `docs/PROJECT_HISTORY.md`;
- plan sesji #108 scalony z checkboxami A2–A6 niezaznaczonymi i pustym
  „Podsumowaniem wykonania”;
- §6 AUDYT_PR107 z placeholderami „_(A3 — hash po lądowaniu)_” /
  „_(A6 — hash po lądowaniu)_” (A3 = `3ac644a`; A6 nie istnieje);
- opis PR #108 niezaktualizowany (sekcja „Komity” pusta mimo 3 commitów).
Dodatkowo: etapy A4 (pętla jakości) i A5 (Żywy Tester) z planu nie mają
żadnych artefaktów w PR — praca A4 istniała tylko lokalnie (patrz §4).
Zamknięcie: rejestr w niniejszym audycie + domknięcie WŁASNEJ sesji
(B6: handoff, HISTORY, liczby, opis PR). Historii nie przepisujemy.

### F-B2/2. Dwie nieścisłości prozy audytu (docs; niskie, bez naprawy)
- „~16 użyć `tap_permanent` w katalogu” — pomiar: 29 linii
  z `tap_permanent` + 2 z `tap_permanents` (grep po `card-data.js`).
  Wniosek (ścieżka publiczna, fix słuszny) nie zmienia się.
- „forma bezokolicznikowa zgodna z 'odkręć'” — „odkręć” i „zatapnij”
  to tryb rozkazujący, nie bezokolicznik; zgodność FORMY (ta sama
  kategoria) prawdziwa, nazwa kategorii błędna.
Obie poza kodem; odnotowane, żeby nie kopiować liczb do pinów.

## 3. Obserwacje (bez naprawy; jawne)

- **O-B2/1.** README L36 („4965/4965, 3433,2 kB, quick 570/672”) to
  liczby P5 sesji #107 (sprzed commitów A–E i testów F9). Odświeżenie
  na końcu WŁASNEJ sesji (L92), nie w audycie.
- **O-B2/2.** Brak dowodu, że sesja #108 uruchomiła `test:all`, profil
  szybki benchmarku i Żywego Testera (planowane A5/A6 nie mają śladów).
  Bramki te przechodzę w B5/B6 niniejszej sesji.
- **O-B2/3.** Numeracja CR w komentarzach repo dryfuje względem CR
  2026-08-07 (madness 702.34→702.35, regenerate 701.12→701.19/614.8 —
  wykryte przy adopcji §4, zweryfikowane pobraniami z 2026-09-09).
  Treść reguł w kodzie zgodna; do rozważenia osobny sweep
  komentarzy (backlog, nie ta sesja).

## 4. Adopcja pracy zawieszonej sesji 01a08611 (patch z PR, etap A4)

Właściciel wrzucił do PR plik `01a08611-*.patch` (1069 linii) —
niepushowany stan roboczy etapu A4 poprzedniej sesji. Weryfikacja
niezależna (nie przepisanie na wiarę):

- **Skład:** połowa patcha to bajt-w-bajt wersje już scalonych plików
  (audyt, plan, render.js, test F9 — diff pusty); NOWE: 1 hunk
  w `game-state.js`, 3 hunki w `heuristic-bot.js`, 4 pliki testowe
  (16 testów: madness ×4, Vandalize ×5, PW ×3, Slabs/Inspiration ×4).
- **Przed fixami: 14/16 GREEN + 2 RED z DEKLAROWANYCH powodów**
  (A4-1/4: priorytet `p2` zamiast `p1`; A4-4/1: bot rzuca Slabsy
  w main1). Po fixach: 16/16 GREEN.
- **CR pobrane dziś** (ADR 0030, CR 2026-08-07, mtg.wiki): 117.3b
  (priorytet po rozstrzygnięciu — dosłownie), 702.35a + ruling DMR
  (madness: exile, rzut-albo-grób po dokończeniu efektu), 614.8/701.19a
  (regen: tap + heal + zdjęcie z walki), 702.89a (umbra: heal + zniszcz
  aurę). Treść reguł poprzednika ZGODNA; jego numery 702.34 i 701.12/
  701.15 STARE — poprawione w adoptowanych plikach.
- **Blast radius:** golden-master bota 4/4 bez regeneracji; pełny rdzeń
  5012/5012. Dwa piny `znaleziska-c-deckout` (Reunion@7/@20) wymagały
  aktualizacji: `isDrawOnly` słusznie spycha selekcję (Reunion to netto
  0 kart) pod rozwój stworami — stare piny utrwalały artefakt bazy 50,
  nie dobrą grę; guard deck-outu (6→3, 3 karty) GREEN bez zmian.
- **Werdykt adopcji:** 2 znaleziska PRAWDZIWE (priorytet madness,
  Slabs w main1) + 2 uzasadnione zamknięcia z pinami (Vandalize —
  ścieżki poprawne; PW w walce — brak wsparcia z premedytacją +
  strażnik katalogu). Wszystko wylądowało commitami `3dd5196`,
  `6a4d0e6`, `e23ff89` (każdy samodzielnie zielony, zweryfikowany
  w worktree).

## 5. Zdrowie PR #108 i baseline sesji

- `git status` czysty; `main` = `0b814ed` (squash #108).
- Baseline po adopcji A4: `npm test` fast **5012/5012** (~143 s),
  `npm run build` exit 0, **60 modułów / 3441,2 kB**.
- Brama sesji: `npm run test:all` + build + quick (B6); Żywy Tester (B5).

## 6. Rejestr znalezisko → commit (gałąź `arena/01a08691-mtg`, PR #109)

| Znalezisko | Commit | Strażnik |
|---|---|---|
| F-B2/1 (brak domknięcia #108) | niniejszy audyt + B6 (handoff/HISTORY/opis PR) | rejestr §2, bez zmian kodu |
| F-B2/2 (proza audytu) | — (odnotowane) | pomiar w §1 |
| §4/A4-1 (madness-priorytet) | `3dd5196` | `test/znaleziska-a4-madness-batch-x3.test.js` (A4-1/4 RED→GREEN) |
| §4/A4-2 (Vandalize piny) | `6a4d0e6` | `test/znaleziska-a4-vandalize-regen-aura.test.js` (5/5) |
| §4/A4-3 (PW piny) | `6a4d0e6` | `test/znaleziska-a4-drugi-atakujacy-pw.test.js` (3/3) |
| §4/A4-4 (Slabs/Inspiration) | `e23ff89` | `test/znaleziska-a4-triage-slabs-inspiration.test.js` (A4-4/1 RED→GREEN) + piny C |
