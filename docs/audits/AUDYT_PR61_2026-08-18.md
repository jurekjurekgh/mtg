# Audyt PR #61 (M142) — 2026-08-18

**PR:** [#61](https://github.com/jurekjurekgh/mtg/pull/61)  
**Tytuł:** M142: audyt M141 + Chittering Rats FoW + Fathom Fleet Cutthroat  
**Pliki (9):** `AGENTS.md`, ADR 0020 + README, plan batch 35, `card-images.js`, `render.js`, `session.js`, dwa pliki testów.

## Zakres zmian (co faktycznie weszło)

1. **ADR 0020** — tryb sesji (PR / audyt / inkrementalne commity). Dokumentacyjny, spójny z AGENTS (stan sprzed M143).
2. **Plan Batch 35** — lista 10 kart, kryteria nieodhaczone. To niedokończone zadanie na `main` (ADR 0021 pkt 3).
3. **Hover bez artId** — `nextHoverMode(..., availableModes)` ogranicza tory do `scryfall` gdy brak `artId`. Generyczne, ADR 0002 OK.
4. **`ptPair`** — `+1/+1` nie zwija się do `+1`; zwijanie tylko gdy obie wartości to ten sam string (`source_power`). Testy zachowaniowe — OK.
5. **Chittering Rats FoW** — `hand_top_choice_resolved` nie woła `nameOf`.
6. **Fathom Fleet Cutthroat / panel** — w diffie **brak zmiany silnika/panelu**. `permanent_destroyed` już było w `BOT_MOVE_CARD_EVENTS` i `BOT_RESOLUTION_EVENTS`. Test B to sonda end-to-end na istniejącym zachowaniu, nie RED→GREEN naprawy z tego PR.

## Engine / ADR 0002

Brak zmian w `src/engine/`. Core nietknięty. W UI został pre-istniejący fallback `'Chittering Rats'` przy `hand_top_choice_required` gdy brak `sourceCardId` — naruszenie ADR 0002 (nazwa karty w kodzie stołu). Nie wprowadzone w M142, ale leży na tej samej linii.

## Testy vs deklaracje (RED→GREEN)

| Test | Werdykt |
|---|---|
| `bug-ptpair-description` | Zachowanie `describeSpellEffects` — dobry. |
| Bug A (FoW) | **L5:** regex na źródło `session.js`. Nie odpala `describeGameEvent`. Nie rozróżnia widoku własnego i przeciwnika. |
| Bug B (panel) | Gra pełne partie; porównuje liczbę „zostaje zniszczony” w panelu vs log. Nie dowodzi, że M142 cokolwiek dodał. Przydatny jako strażnik regresji panelu. |

`npm test` na bazie sesji: zielony (2250).

## Znaleziska do naprawy

1. **Nadmiar FoW (CR 400.2 / wzorzec `card_drawn`).** Po M142 *własny* wybór karty na wierzch też jest bezimienny. Gracz zna swoją rękę — jak przy dobraniu (`playerId === HUMAN_ID` ⇒ nazwa). Przeciwnik nadal tylko „kartę”.
2. **Test A nie jest testem zachowania** — do przepisania na `describeGameEvent`.
3. **Fallback `'Chittering Rats'`** w `hand_top_choice_required` — usunąć (ADR 0002).

## Werdykt

M142 nie psuje silnika ani ADR 0002 w core. Hover i `ptPair` są poprawne. FoW Rats jest w dobrą stronę, ale za szerokie. Panel Cutthroat nie był nową naprawą w tym PR.

Naprawy #1–#3 w tej sesji, potem niedokończony plan Batch 35.
