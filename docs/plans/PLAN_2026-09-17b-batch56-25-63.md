# Batch 56 — karty właściciela 25, 27, 28, 30, 32, 34, 54, 58, 60, 63 (2026-09-17)

Zlecenie właściciela (2026-09-17): „Najwyższa pora na nowy batch kart.
Rozplanuj sobie dokładnie pracę i realizuj ją etapami, każdy commitując
i pushując. Sprawdzaj też HEAD czy się nie rozjechał, zgodnie z ENVIRONMENT."

Gałąź sesji: `arena/01a0ae26-mtg` (PR #125). Baza pomiarowa: `b915773`
(E4 poprzedniego zakresu) — **zweryfikowana po odzyskaniu HEAD** (workspace
odtworzył się ze świeżego klona z lokalnym HEAD na `main`; procedura
ENVIRONMENT §2 wariant groźniejszy: `git diff e4befba > /tmp/backup…`,
`git fetch --depth=200 origin arena/01a0ae26-mtg`, `git reset --mixed
FETCH_HEAD`).

Stan wyjścia **zmierzony**, nie przepisany z handoffu: `npm test`
**5646/5646** (0 fail), katalog **467 wspieranych kart realnych** (wszystkie
z `artId`), 40 tokenów, 5 basic landów, arkusz kolekcji **553** pozycje,
`withArt.length` w `test/art-ids-tool.test.js` **475**.

## Lista i rozpoznanie (Oracle + rulingi pobrane online 2026-09-17, ADR 0030)

| artId / plan | Karta | Druk (Scryfall) | Co wnosi | Status mechaniki |
|---|---|---|---|---|
| 25 ISD | Bonds of Faith | isd/5 | Aura {1}{W}: „Enchant creature / Enchanted creature gets +2/+2 as long as it's a Human. Otherwise, it can't attack or block." | **NOWA**: warunkowy pump po PODTYPIE gospodarza + warunkowy `cantAttack`/`cantBlock` („otherwise") |
| 27 KTK | Erase | ktk/9 | {W} Instant: „Exile target enchantment." | istnieje: cel `enchantment` + `exile_permanent` (Agate Assault) |
| 28 M11 | Kraken's Eye | m11/210 | Artefakt {2}: „Whenever a player casts a blue spell, you may gain 1 life." | istnieje **bliźniak**: Angel's Feather (M11, artId 223) — ten sam deskryptor, kolor `U` |
| 30 TMC | Containment Protocol | tmc/108 | Aura {2}{U}: ETB tapnij zaczarowanego stwora + „doesn't untap" | częściowo: `aura.doesntUntap` (Containment Membrane) + **NOWY** efekt `tap_enchanted_permanent` (lustro istniejącego `untap_enchanted_permanent`) |
| 32 AER | Shipwreck Moray | aer/45 | 0/5 {3}{U}: ETB dostajesz {E}{E}{E}{E}; „Pay {E}: +2/-2 until end of turn" | **NOWA mechanika: energia (CR 122.1)** — brak w silniku czegokolwiek o `{E}` |
| 34 ALA | Volcanic Submersion | ala/121 | {4}{R} Sorcery: „Destroy target artifact or land. Cycling {2}" | cycling istnieje; **NOWY** cel unijny `artifact_or_land` (dziś są tylko `artifact_or_enchantment`, `artifact_or_creature_or_enchantment`, `artifact_or_creature_or_land`) |
| 54 DSK | Cautious Survivor | dsk/172 | 4/4 {3}{G}: Survival — „At the beginning of your second main phase, if this creature is tapped, you gain 2 life." | **NOWA**: trigger na POCZĄTKU drugiej fazy głównej + warunek „if tapped" (intervening if, CR 603.4) |
| 58 AER | Mobile Garrison | aer/165 | 3/4 {3} Vehicle, Crew 2: „Whenever this Vehicle attacks, untap another target artifact or creature you control." | crew/Vehicle istnieją; **NOWY**: cel `artifact_or_creature` z ograniczeniem „you control" + „another" (wykluczenie źródła) na ścieżce czarów/triggerów |
| 60 M20 | Thornwood Falls | m20/258 | Land: wchodzi tapnięty, ETB +1 życie, {T}: {G} lub {U} | istnieje **bliźniak**: Dismal Backwater (M20, artId 197) — 1:1 ten sam wzorzec |
| 63 ORI | Dragon Fodder | ori/140 | {1}{R} Sorcery: „Create two 1/1 red Goblin creature tokens." | **NOWY token** `token_goblin` (1/1 czerwony Goblin); `create_token` z `amount: 2` istnieje |

Nowy **plan** w arkuszu: `Teenage Mutant Ninja Turtles` (karta 30) — brak
w `SINGLE_PLAN_DECKS`/`WOREK_DECKS`, potrzebny wpis (motyw miejsko-mroczny →
`worek-mroczny`, obok New Capenna i Duskmourn; worek jest też mniejszy od
legend).

## Nazwane brakujące reguły (generyczne, ADR 0002 — zero warunków na nazwę karty)

1. **Energia ({E}, CR 122.1)** — nowy ZASÓB GRACZA, nie permanentu: pole
   `player.energy`, jedyna droga zapisu w `src/engine/players.js` (wzorzec
   `addPoisonCounters`), zdarzenia `energy_counters_added` /
   `energy_counters_paid`, koszt zdolności `{ energy: n }` w `abilities.js`,
   efekt `get_energy`. Rulingi AER (2024-06-07): energia **nie jest maną**,
   nie znika na koniec kroków/faz/tur, nie da się zapłacić więcej niż masz,
   liczy się jak counter gracza. Dowiązania L84 obowiązkowe: `EVENT_TYPES` +
   opis zdarzenia, etykieta PL, wycena bota (lub `REVIEWED_UNVALUED`),
   `fingerprint` (pole gracza musi być w odcisku — inaczej replay/no-op
   ślepe, klasa L16), panel gracza w `render.js`.
2. **Cel „artifact or land"** — `spells.js`: walidacja + `targetCandidatesBySpec`
   (jedna reguła w dwóch miejscach: oferta i walidacja, pułapka M82).
3. **Cel „another target artifact or creature you control"** — rozszerzenie
   istniejącego typu triggerowego `artifact_or_creature` o ograniczenia
   `controlledBy: 'controller'` i `excludeSource: true` (wzorzec „another"
   już jest: `excludeAttachedHost`); ścieżka oferty i walidacji tak samo.
4. **`tap_enchanted_permanent`** — lustro `untap_enchanted_permanent`
   w `effects.js` + etykieta PL + wycena bota.
5. **Warunkowa aura po podtypie gospodarza** — `aura.conditionalPump`
   (bliźniak istniejącego `conditionalKeywords`) oraz warunki
   `{ hostHasSubtype }` / `{ hostLacksSubtype }` dla `cantAttack`/`cantBlock`
   (dziś `cantBlock` zna tylko `hostHasColor`). Ruling ISD: utrata bycia
   Human po deklaracji ataku/bloku **nie usuwa z walki**, tylko zdejmuje +2/+2
   — czyli pump i restrykcje liczone dynamicznie z podtypu.
6. **Trigger początku drugiej fazy głównej** — nowa gałąź `step_advanced`
   (`step === 'main2' && phase === 'postcombat_main'`) obok `main1`/`upkeep`/
   `end`; warunek `{ sourceTapped: true }` w `conditionHolds` (sprawdzany
   przy zgłoszeniu I przy rozstrzygnięciu — CR 603.4, mechanizm re-checku
   już jest w `game-state.js`). Rulingi DSK: trigger tylko w DRUGIEJ fazie
   głównej (nie w trzeciej i dalszych); brak triggera, gdy stwór nietapnięty
   na starcie fazy; tapnięcie go w fazie już nie pomoże; jeśli stwór zniknął
   z pola bitwy przed rozstrzygnięciem — liczy się stan OSTATNI na polu bitwy
   (LKI).
7. **Token `token_goblin`** — 1/1 czerwony Goblin (grafika z tokenu Scryfalla,
   ADR 0028/L26: bez zgadywania UUID).

## Etapy — każdy = osobny, samodzielnie zielony commit (push od razu)

Zasady wspólne (bez powtarzania w każdym punkcie):

- **Testy RED przed implementacją**, potem GREEN; dla każdej karty scenariusz
  **legalny i nielegalny** (ADR 0010 §koszty).
- **Dowiązania nowego deskryptora (L84)** przed pierwszym pełnym `npm test`:
  `EVENT_TYPES` + opis zdarzenia (`session.js`), etykieta PL (`render.js`,
  strażnik M122), wycena bota albo `REVIEWED_UNVALUED` (M157),
  `gameObjectDataOf` (`materialize.js`) — plus `fingerprint` dla pól stanu.
- **Karty wchodzą do talii wyłącznie generatorem** (ADR 0023/0024):
  `node tools/generate-plan-decks.mjs` po nadaniu `supported`.
- **Golden master**: każdy churn talii = świadoma regeneracja z **atrybucją**
  (która talia, dowód izolacji: ten sam kod na starej talii odtwarza starą
  wartość), bez zmian wag/progów (L124).
- Liczności pinowane w testach aktualizowane w etapie, w którym rosną.
- Po KAŻDYM etapie: `git log --oneline -1` + `git status` (ENVIRONMENT §2
  profilaktyka), `npm test`, `npm run build`, commit, push.

- [ ] **B0a — plan** (ten dokument): commit + push przed pierwszym kodem.
- [ ] **B0b — dane źródłowe**: 10 snapshotów `docs/cards/scryfall-*.json`
  (Oracle + `rulings` + `rulingsSource`/`rulingsPobrano`), 10 wierszy arkusza
  (25ISD…63ORI), 10 kosztów w `MANA_COSTS`, definicje 10 kart jako
  `in-development` (dane bez mechaniki — strażniki `zgloszenie-a-druk-karty-z-arkusza`
  i `obiekty-wsparcia-poza-rejestrem` wymagają, by snapshot ↔ rejestr ↔ arkusz
  się zgadzały), wpis `Teenage Mutant Ninja Turtles` w `WOREK_DECKS`,
  aktualizacja pinów (arkusz 553→563, `withArt` 475→485).
- [x] **B1 (M362) — energia** (`players.js`, `abilities.js`, `effects.js`,
  `fingerprint`, `render`, bot) + **32 Shipwreck Moray** `supported`.
  Testy: sanity danych; ETB daje dokładnie 4 liczniki; zapłata {E} daje
  +2/-2 do końca tury; brak energii = zdolność nielegalna (nie da się zapłacić
  więcej niż masz); energia przeżywa koniec tury (ruling) i jest widoczna
  w odcisku stanu (ten sam stan bez energii ≠ z energią); bot wycenia.
- [x] **B2 (M363) — pojedynki usuwające**: **27 Erase** (`exile target
  enchantment`, ruling: karta NIE idzie najpierw do grobu) + **34 Volcanic
  Submersion** (nowy cel `artifact_or_land` + cycling {2}). Testy: wygnanie
  aury i enchantmentu-stwora; cel nie-enchantment nielegalny; cycling z ręki
  (dobranie karty, karta w grobie) i jego nielegalność bez many;
  „artifact or land" odrzuca stwora, przyjmuje land; zniszczenie landu
  podstawowego nie wywraca stanu.
- [x] **B3 (M364) — aury**: **25 Bonds of Faith** (`conditionalPump` +
  warunkowy `cantAttack`/`cantBlock`) + **30 Containment Protocol**
  (`tap_enchanted_permanent` + `doesntUntap`). Testy: Human dostaje +2/+2
  i może atakować; nie-Human nie atakuje i nie blokuje, ale NIE dostaje
  pompa; zmiana podtypu po deklaracji ataku nie usuwa z walki (ruling);
  Containment Protocol tapnie przy wejściu i nie odkręca w untapie
  kontrolera.
- [ ] **B4 (M365) — pojazd w ataku**: **58 Mobile Garrison** (`artifact_or_creature`
  + „another" + „you control" + `untap_permanent`). Testy: trigger przy ataku
  odkręca inny własny permanent (nie sam pojazd, nie cudzy); brak legalnego
  celu = trigger bez efektu; crew 2 działa jak w istniejących pojazdach.
- [ ] **B5 (M366) — druga faza główna**: **54 Cautious Survivor**
  (`step_advanced` main2 + `sourceTapped` + LKI). Testy: tapnięty w main2 →
  +2 życia; nietapnięty → brak triggera (i tapnięcie w main2 nic nie daje);
  odkręcenie przed rozstrzygnięciem → brak efektu; śmierć stwora przed
  rozstrzygnięciem → użyty stan LKI; trigger NIE odpala w cudzej turze.
- [ ] **B6 (M367) — tokeny i ląd**: **63 Dragon Fodder** (`token_goblin`,
  dwa tokeny) + **60 Thornwood Falls** (bliźniak Dismal Backwater). Testy:
  dwa osobne tokeny 1/1 czerwone Gobliny (nie jeden 2/2), grafika/typ
  tokenu; ląd wchodzi tapnięty, daje 1 życie i produkuje {G} albo {U}.
- [ ] **B7 (M368) — integracja i pomiary**: generator talii, golden master
  (atrybucja churnu), README/PROJECT_HISTORY/ENGINE_MILESTONES/handoff,
  Żywy Tester na taliach z nowymi kartami, bramki finalne
  (`npm test`, `npm run test:all`, `npm run build`, quick benchmark —
  pełne B0 wyłącznie na wyraźną komendę właściciela, ADR 0018).

## Ryzyka rozpoznane z góry

- **Energia to nowy zasób w stanie gry** — trzy najczęstsze ciche awarie
  (L16/L21/L84): brak w odcisku (ślepy replay), brak w widoku sesji (gracz nie
  widzi, ile ma), brak wyceny u bota (zdolność „bez wyceny"). Wszystkie trzy
  mają strażników w B1.
- **Warunkowa aura** dotyka `permanents.js` (egzekwowanie restrykcji walki)
  i `attachments.js` — poprawka musi być widoczna dla OBIE ścieżki: oferta
  („czy mogę atakować?") i walidacja komendy.
- **Trigger drugiej fazy głównej** jest wrażliwy na wyścig z `jumpToStep`
  (bojowe komendy przeskakują kroki) — test musi przejść pełną rundę
  priorytetu, nie tylko skok.
- **Nowy plan (TMNT)** w arkuszu wymaga wpisu w mapie worków; strażnik
  „plany bez kart" (komentarz M202/A) pilnuje, żeby nie zostały martwe wpisy.
