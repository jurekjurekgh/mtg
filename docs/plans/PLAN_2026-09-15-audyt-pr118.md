# Plan 2026-09-15 — start sesji (ADR 0020 A/B) + audyt PR #118

**Zlecenie:** „Kontynuujemy projekt. Przeczytaj obowiązkową lekturę i
zatrzymaj się. Czekaj na zadanie.” Lektura obowiązkowa (AGENTS.md §0)
ukończona: AGENTS.md, ADR 0001–0030 + rejestr, LESSONS.md (L1–L143),
ENVIRONMENT.md, HANDOFF_2026-09-14e.md, hierarchia testów (ADR 0019).

**Pętla:** prompt nie nazywa tematu → ADR 0021 → obowiązkowy start ADR 0020:
PR sesji na GitHubie PRZED jakimikolwiek zmianami (A) → audyt ostatniego
scalonego PR (#118, sesja `arena/01a09fda`, scalony 2026-09-14 12:27 UTC) (B)
→ STOP, raport gotowości, czekam na zadanie. Żadnych prac rozwojowych przed
zadaniem.

## 0. Stan wejściowy (zmierzony w tej sesji, nie przepisany)

- `main` = `e9860cb` (squash-merge PR #118); `origin/main` = ten sam SHA.
- Gałąź sesji `arena/01a0a3aa-mtg` — świeża od `main`, czyste drzewo.
- `npm test`: **5496/5496**, 0 fail, 178,0 s (zgodne z README „szybki rdzeń
  5496/5496”).
- `npm run build`: **61 modułów / 3660,4 kB** (zgodne z README).
- Quick benchmark (672 mecze, 126,2 s): **heuristic 82,9% (557/672)**, aggro
  30,7% (103/336), random 3,6% (12/336) — zgodne z README (82,9%; baza PR:
  82,6%); aggro bez zmian; random obniżył się 4,2%→3,6% w konsekwencji
  polityki mulligana (hałas próbki, patrz raport).
- `npm run test:all` (brama PR): **5506/5506**, 0 fail, 298,5 s (zgodne z
  README „pełny zestaw 5506/5506”).

## 1. Zakres audytu PR #118 (ADR 0020 B)

PR #118 = 27 plików (diff 1871 linii, `gh pr diff 118`), dwa etapy sesji
`arena/01a09fda`:

- **14e** — audyt PR #116 (raport `AUDYT_PR116_2026-09-14.md`, APPROVE) +
  naprawy znalezisk P1–P6 (wyłącznie cytaty CR/daty rulingów w komentarzach
  i komunikatach testów) + strażnik E3 (2 nowe pary `cr-numery-mechanik-
  straznik`) + W1 (`warp_card`/`turn_manifest_face_up` do
  `OPTION_IGNORABLE_TYPES` + regresja `choice-ignore`). Zero zmian logiki.
- **14f** — zgłoszenie właściciela „talie podejrzanie często startują bez
  lądów”: pomiar `tools/deck-land-ratio.mjs` (nowe narzędzie, M132) wykazał
  talie zgodne z regułą 1:2 i uczciwe tasowanie → root cause w polityce
  bota. JEDYNE zmiany funkcjonalne PR-u: silnik nosi jawny licznik
  `mulligans` w wariantach `resolve_mulligan_choice`; heuristic-bot i
  aggro-bot mulliganują przy ręce 0–1 lądów (cap 2) i przy odłożeniu na
  spód oddają najdroższe czary, trzymają lądy. Nowy plik testowy
  `test/bot-mulligan.test.js` (11 testów) + regeneracja golden-mastera.

Metoda: pełne czytanie diffu plik po pliku (L78 — ekstrakcja per plik do
`/tmp`); niezależne pomiary (§0); weryfikacja ONLINE każdego cytatu CR
poprawionego w PR (CR 2026-08-07, mtg.wiki) i każdej daty rulingu (API
Scryfall) — cytat dosłowny w raporcie (ADR 0030); przegląd ścieżki
mulliganu: oferta ≡ walidacja (L48), cap enumeracji (L19), FoW/public info
(ADR 0003/0017), ADR 0002 (brak wyjątków po nazwie), determinizm (ADR 0005),
L13 (mutacyjność testów na nowe pole).

## 2. Etapy i kryteria ukończenia

- [x] **E0** — ten plan (commit + push PRZED zmianami; PR sesji na GitHubie,
      ADR 0020 A). Uwaga o proweniencji w §4.
- [ ] **E1** — raport `docs/audits/AUDYT_PR118_2026-09-15.md`: werdykt,
      skład PR, per-funkcja weryfikacja (silnik/boty/testy/narzędzie/
      dokumenty), tabela pomiarów, weryfikacja online (dodatek ze źródłami i
      datami dostępu), znaleziska (blokujące/komentarzowe/obserwacje).
      Commit + push.
- [ ] **E2** — naprawy (jeśli wystąpią) znalezisk KOMENTARZOWYCH audytu w
      tej gałęzi — każde osobnym commitem, `npm test` zielony przed każdym
      commit (L13). Zmiana logiki/policyki = poza zakresem (zadanie
      właściciela).
- [ ] **E3** — bramki finalnego headu (test:all), aktualizacja README
      „Bieżący stan” (jeśli liczby się zmieniły), handoff
      `docs/setup/HANDOFF_2026-09-15.md`, opis PR z werdyktem; STOP i raport
      gotowości — czekam na zadanie.

## 3. Granice

- Bez zmian w mechanikach, kartach, taliach, generatorze ani polityce botów
  (zmiana polityki to zadanie właściciela; audyt tylko mierzy i raportuje).
- Lektura audytowa idzie przez diff PR, nie przez ponowne czytanie całości
  źródeł; budżet lektury startowej (100k tokenów) nienaruszony (plany/
  audyty/handoffy nie wchodzą do mierzonych plików — test
  `dokumentacja-budzet-lektury` liczy AGENTS.md, ADR-y, LESSONS.md,
  ENVIRONMENT.md).
- Bez pełnego B0 (tylko na komendę właściciela, ADR 0018).

## 4. Proweniencji sesji (fakt, nie ozdobnik — L142)

Wcześniejszy odcinek tej sesji (przed resetem sandboxa) wykonał lekturę i
E0 (commit `0f91d46` + PR #119). Reset sandboxa (ENVIRONMENT.md §2) utracił
ten stan: na originie nie istnieje gałąź `arena/01a0a3aa-mtg` ani żaden PR
tym numerem — odtwarzam E0 od zera na gałęzi od `e9860cb`. Do `main` nic z
utraconego stanu nie dotarło. Pomiary §0 są pomiarami bieżącymi (po
resecie), nie przepisem z notatek.
