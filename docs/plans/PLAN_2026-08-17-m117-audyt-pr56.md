# PLAN M117 — audyt PR #56 (M104–M116) + naprawy · 2026-08-17

Sesja: `arena/01a00fa6-mtg`. Polecenie właściciela: **audyt poprzedniego PR
i naprawa tego, co się znajdzie** — bez dużych nowych funkcji, bez pełnego B0
(profil szybki wystarcza, ADR 0018).

Punkt wyjścia (zmierzony na starcie): `npm run test:all` **2051/2051**,
HEAD = `1cbcf0c` (squash PR #56 w `main`).

## Zakres audytu (ADR 0016)

1. **Dane kart batchy 33–34** wobec Oracle ze Scryfalla (20 kart).
2. **Zmiany w engine** z M110–M116 (root cause’y: `object_tapped` przy
   tapnięciu za manę, `xValue`, `targetIndex` w `damage`, obniżki kosztu,
   tryby modalne) — czy nie regresują i czy są generyczne (ADR 0002).
3. **Mechaniki nowych kart** — brak wyjątków po nazwie/ID karty.
4. **Strażniki danych** — czy klasa błędu, którą znajdę, jest wykrywalna
   maszynowo (lekcja L23: dane w dwóch reprezentacjach = strażnik).

## Etapy

### E0 — rozpoznanie (ZROBIONE)
- baseline `npm run test:all` 2051/2051; przegląd handoffu, TODO, LESSONS.

### E1 — ilustracje i dane źródłowe kart (ZNALEZIONE BŁĘDY)
Ustalenia:
- **B1: `krumar-initiate` ma ZMYŚLONY `imageUri`** —
  `…/front/9/1/91b1f0f3-krumar-initiate.jpg` nie jest adresem Scryfalla
  (brak UUID druku) i zwraca **404**. Prawdziwy druk (TDM 84):
  `bc66680f-24ab-433a-8197-feac3a174075`. Dokładnie pułapka nr 5 z handoffu
  („nie zmyślać adresów obrazków”) — zrealizowana w poprzedniej sesji.
- **B2: 20 kart batchy 33–34 nie ma plików `docs/cards/scryfall-<id>.json`**,
  wymaganych przez ADR 0010 §2a. To nie jest kosmetyka: strażnik
  `test/card-data.test.js` („imageUri zgadza się z plikiem Scryfall”) ma
  klauzulę `if (!expected) continue` — **brak pliku = brak weryfikacji**.
  Właśnie tą dziurą przeszło B1.
Kryterium ukończenia: poprawiony adres, 20 plików `docs/cards/`, strażnik
odrzucający (a) adres `cards.scryfall.io` bez UUID, (b) kartę `supported`
z drukiem bez pliku źródłowego. `npm test` zielone.

### E2 — zgodność Oracle 20 kart batchy 33–34
Każda karta odpytana ze Scryfalla po UUID druku (`/cards/<uuid>?format=text`)
i porównana z `oracleText`, kosztem, typami, P/T. Kryterium: raport w tym
planie + ewentualne poprawki danych.

### E3 — audyt zmian silnika z M110–M116
Przegląd root cause’ów pod kątem regresji i generyczności; testy
anty-over-fix tam, gdzie ich brakuje.

### E4 — mechaniki bez wyjątków po nazwie (ADR 0002)
Skan `src/engine`, `src/controllers` pod kątem warunków na `cardId`/nazwę
wprowadzonych w M109–M116.

### E5 — domknięcie
`npm run test:all`, build, benchmark szybki, aktualizacja
`docs/PROJECT_STATE.md`, `docs/LESSONS.md`, `docs/TODO.md`, handoff, PR.

## Ryzyka / pułapki

- Sandbox potrafi zresetować historię gita → **commit + push po każdym
  zielonym kroku** (pułapka 1 z handoffu).
- „Zielony” = CAŁY `npm test`, nie testy dopisanego pliku.
- Node bez sieci — Scryfall wyłącznie narzędziem `fetch_page`.
- Zmiana danych kart rusza seedy testów scenariuszowych (L25) — po każdej
  zmianie w `decks/*.txt` lub katalogu przejrzeć testy grające pełne partie.
  (W tej sesji talie NIE są ruszane — audyt nie dodaje kart.)

## Wykonanie

_(uzupełniane w trakcie)_
