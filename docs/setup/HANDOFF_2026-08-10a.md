# Handoff 2026-08-10a — po M68 daybound/nightbound (globalny znacznik)

## Stan po sesji 2026-08-10 (PR #39 `arena/019fe7ec-mtg` — OPEN do main)

- **Kod:** `npm test` **1223/1223**, build **50 modułów / 1343.2 kB**, 198 realnych kart
  + 1 token (199 supported), 9 talii singleton, benchmark 1080 meczów 0 crashy
  (77.5%/60.7% — progi 0.78/0.57 utrzymane; procesTriggers zmienione, boty bez zmian).
- **Zakres M68 (zgłoszenie właściciela — spójność globalnych mechanizmów):**
  1. **Inicjatywa + Lochy** — już były (M24): globalna karta The Undercity na stole
     (img Scryfall tclb/20), znacznik „Inicjatywa", pokoje per gracz (renderUndercity).
     Bez zmian.
  2. **Daybound/Nightbound (CR 708.9)** — `state.dayNight: null|'day'|'night'`
     (globalny znacznik jak inicjatywa) + `lastTurnSpellsCastByPlayer`; `setDayNight`
     transformuje in-place daybound→nightbound / nightbound→daybound (tylko karty
     z tymi keywordami — zwykły transform DFC, np. Civilized Scholar, nietknięty);
     wyzwalacze: wejście daybound przy null → day (708.9c), rzut czaru przy
     dayNight !== 'night' i daybound na polu bitwy → night (708.9d), upkeep aktywnego
     przy night bez czaru w JEGO poprzedniej turze → day (708.9f); wejście nightbound
     w nocy (resolvePermanentSpell — transform przed zdarzeniem wejścia).
  3. **Karta Day//Night na stole** — `renderDayNight` (img ze Scryfall TVOW 21,
     front/back wg designation), status „Dzień"/„Noc", ukryta gdy null; DAY_NIGHT_TOKEN
     w card-data.js; #daynight w index.html (+CSS); els.daynight w main.js.
  4. **Publiczne:** PlayerView.dayNight (jak initiativePlayerId); fingerprint.dayNight.
- **Commity:** plan (7b7f139) → engine (a00cb61) → UI+testy (75b4c8c) → docs (następny).
  Uwaga: sandbox znów cofnął HEAD do main — M68 cherry-pickowane na 6f93717 (push po
  każdym commicie chroni).

## Kolejka (po M68)

- **Batch 28** — czeka na listę właściciela (ADR 0010: Scryfall ZAWSZE z `set=`;
  api zablokowane → `fetch_page`). Pierwsza karta daybound/nightbound z MID/VOW wejdzie
  na gotową mechanikę M68.
- **B2-w2** — infra OFF (~4× wolniej); z włączonym lookahead bot zyskuje +5/+34 p.p.;
  decyzja o koszcie i ewaluacja na pełnym B0.
- **Talie singleton** — rosną z batchami (strażnik `repo-decks`).

## Pułapki (środowisko / projekt) — aktualne

- `edit_file` psuje PL → **python3 Path.read_text/write_text**, commit msg via /tmp.
- **Sandbox potrafi COFNĄĆ lokalny HEAD do main** (3× w tej sesji!) — po każdym commicie
  `git push origin arena/...`; przy rozjeździe: `git fetch && git reset --hard FETCH_HEAD`
  (jeśli working tree == remote — sprawdź `git diff FETCH_HEAD`) i `git cherry-pick`
  commitów z reflogu na właściwą bazę.
- **Nowe w M68:** `setDayNight` transformuje wyłącznie permanenty z keywordami
  `daybound`/`nightbound` (stary transform DFC — Civilized Scholar — nietknięty);
  rzut czaru wyzwala noc tylko gdy `dayNight !== 'night'` (naturalny „first time");
  upkeep sprawdza czary AKTYWNEGO gracza per `lastTurnSpellsCastByPlayer` (nie globalny
  `lastTurnSpellsCast`); wejście nightbound robi transform PRZED zdarzeniem wejścia
  (ETB na właściwej stronie); dayNight w fingerprint.
- Scryfall: curl i node fetch NIE działają (SSL/sieć) — wyłącznie `fetch_page`.
- Hunter seeds w testach z talią green/red/black (table-session, bot-pausa, audit C2):
  2/3/4/47 — przelosowywać po zmianie talii.
- Pełne B0 (13500) ~16 min; quick `--seeds 4` (1080) ~90 s.
- Lektura nowej sesji: AGENTS.md → PROJECT_STATE (M68) → HANDOFF_2026-08-09a…f →
  HANDOFF_2026-08-10a → ENGINE_MILESTONES (M64-M68) → WORKFLOW → plany M65/M66/B27/M68 →
  ADR 0002/0003/0005/0010/0011/0013/0014.

## Do weryfikacji w następnej sesji

- `npm test` 1223/1223, `npm run build` 50/1343.2 kB.
- Benchmark 1080 0 crashy (progi 0.78/0.57).
- Testy iPada: panel Day//Night na stole pojawia się dopiero przy dayboundzie;
  inicjatywa/lochy bez zmian.
- Scryfall Batch 28: ZAWSZE `set=` + `fetch_page` (api blokada).
