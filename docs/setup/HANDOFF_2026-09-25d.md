# Handoff — sesja 2026-09-25d („Kontynuujemy projekt": audyt PR #137 + pętla jakości)

**Gałąź:** `arena/01a0d870-mtg` · **PR:** #138 · **Baza:** `7ccc440` (squash #137)
**Status:** praca skończona i zmierzona; **scalenie = decyzja właściciela**.
Agent nie scala.

**Zakres:** audyt #137 (ADR 0020 B) + naprawa jednoczesnego odkręcenia + odmiana
etykiety. Bez nowego batcha kart (ADR 0029). Bez pełnego B0 (ADR 0018).
Bez nowej lekcji — `LESSONS.md` jest powyżej progu 100k, klasa jest w
komentarzu `untapControlled` i w raporcie.

## Co zrobiła sesja

| commit | treść |
| --- | --- |
| `256c179` | plan `docs/plans/PLAN_2026-09-25d-audyt-pr137-i-petla-jakosci.md` |
| `a9b8297` | CR 502.3: migawka źródeł przed odkręceniem; oferta „zostaw wszystkie" ponad capem |
| `a029901` | odmiana logu i etykieta bez zaszytej „liry"; jeden klucz `pendingUntapChoice` |
| `a4d9619` | pin: zostawienie źródła nie zjada stunu i trzyma własny cel |

Raport: `docs/audits/AUDYT_PR137_2026-09-25d.md`.

## Co następna zmiana odkręcania musi wiedzieć

Blokada kroku czyta **migawkę z chwili ustalenia**, nie żywy `tapped` /
`untapVersion`. Samo porównanie z żywym źródłem wraca do błędu zależnego od
kolejności `Map`. Testy A1b/A5 (cel u przeciwnika) tego nie złapią — pin jest
w `test/audyt-pr137-jednoczesne-odkrecenie.test.js`.

Cytat zmiany kontrolera to CR 110.2, nie 400.3.

## Świadomie nie ruszane

- Phasing (502.1): zero trafień w `src/` i w katalogu. Nie budować bez karty.
- `untapChoice` nie jedzie w kopiach. Dziś nie zmienia partii (tylko Lyra;
  token-kopia artefaktu nie dożywa untapu). Przy trwałej kopii takiego
  artefaktu dopisać deskryptor do ścieżek kopiowania (L47) i przypiąć testem,
  który dożyje kroku odkręcania.
- C5 nie modeluje retargetu `setPointerCapture`. Ścieżka ze zgłoszenia (press
  zaczyna się na ptaszku) jest zamknięta. Nie naprawiać retargetu bez pomiaru
  w przeglądarce.

## Pętla jakości

Żywy Tester, artefakt przebudowany przed pomiarem. Transkrypty w `/tmp`, nie
w repo. Obie partie: 0 zgłoszeń detektorów, odczytane ręcznie.

- `warhammer-ubr` vs `wiedzmin-wur`, seed 91, 149 kroków.
- `theros` vs `kaladesh`, seed 15, 96 kroków. Lira nie weszła do gry.

## Bramy

| brama | wynik |
| --- | --- |
| `npm test` | **6596/6596**, 0 fail, 199 s |
| `npm run test:all` | **6606/6606**, 0 fail, 336 s (regresja benchmarku trzyma próg) |
| `npm run build` | **61 modułów / 4294,9 kB** |

Pierwszy przebieg `npm test` padł na strażniku numerów (`CR 110.2` nie było w tabeli). Numer dopisany po odczycie lustra. Pełnego B0 nie było.

## Następna sesja

Nie powtarzać seedów 91 i 15 jako dowodu czystości. Nie ruszać progu B0.
Jeśli właściciel zagra lirą na własnego stwora i cel wstanie w tym samym
kroku — to regresja pinu B1, nie nowa reguła.
