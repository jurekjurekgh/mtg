# Plan kontynuacji projektu — sesja 2026-09-23c (uwagi z gry: 15 pozycji A–M)

## 0. Czytanie obowiązkowe (jak zawsze, AGENTS.md §0)

1. `AGENTS.md` (zasady stałe) — punkt 4 zawiera listy „Czego NIE czytasz na start”;
2. `docs/decisions/` — **29 ADR-ów** (0008 w archiwum od 2026-09-01);
3. `docs/LESSONS.md` (rejestr reguł) + `docs/LESSONS_PRZYPADKI.md` (narracje) + `docs/LESSONS_ARCHIWUM.md`;
4. `docs/setup/ENVIRONMENT.md` (sandbox, git, GitHub CLI).

## 1. Stan projektu

- **Bramy po sesji:** `npm test` (szybki rdzeń) **6290/6290**,
  pełna brama `node tools/run-tests.mjs all` **6300/6300** (0 fail, ~388 s),
  build **59 modułów / 4119,4 kB**.
- **PR #134** (gałąź `arena/01a0ceb4-mtg`) — OPEN, base `main`. Commity sesji
  (od `08fc24b`): `08fc24b` plan E1–E5, `fdfd73f` E/L/F2/F3, `516d72e` B2,
  `e337884` pin M102/U4, `38e1249` G, `b9d0245` F1, `7eb1633` C+H,
  `1f3376f` J, `5a55504` B1, `9ac5c34` F1-domknięcie, `4a02699` M,
  `364c423` I, `654c562` D, `b926929` A, `c20bf92` K + domknięcie E5 (ten
  dokument, M421, wpis w planie).
- **Zakres sesji:** wszystkie 15 pozycji zgłoszeń z gry (A–M) zamknięte;
  nowe pokrętła strategii: `morbidMain1Penalty` (90), `morbidMain2Bonus` (8),
  `graveReturnManaWeight` (4), `libraryTapSafeMargin` (30).
- **Pliki strażników sesji:** `test/uwaga-z-gry-2026-09-23-{a-morbid-main2,
  m-acidic-slime-singleton-land,y-chronic-flooding-mielacy-pip,
  z-cemetery-recruitment-najdrozszy,k-klik-opcji-bez-reflow}.test.js`.
- Katalog/decki bez zmian: **550 wpisów = 499 `supported` + 43 `token` + 8 `back`**,
  `artId` 502, talie bez regeneracji.

## 2. Otwarte punkty dla właściciela

- PR #134 czeka na przegląd/merge (agent nigdy nie merge'uje — ADR 0020).
- Pętla jakości B1 (Żywy Tester) i B2 (łowy CR) — do uruchomienia na komendę.
- Bench `--full` wyłącznie na komendzie właściciela (ADR 0018).

## 3. Ryzyka / watchlisty

- **K (aktywacja opcji)** — `installPressActivation` (gestures.js) obsługuje
  panel akcji i menu kontekstowe; modale i kreatory zostają na natywnym
  `click` (krótkie przyciski + stabilny `scrollbar-gutter`). Jeśli właściciel
  zgłosi gubienie kliku w modalu — rozszerzyć tę samą rodzinę (L41), nie
  dopisywać drugiego mechanizmu.
- **I (mielące źródła)** — próg 30 dotyczy TAPNIĘĆ/płatności; dobory i mille
  z czarów zostają na `librarySafeMargin` 20. Nie obniżać progu, by „naprawić”
  stary test — kontrolę przenosi się na zdrową bibliotekę (wzorzec B/3).
- **A (Morbid)** — wyjątek `flash` przypięty kartą lokalną (`syn-flash-morbid`),
  bo katalog nie ma karty z flash + Morbidem; przy pierwszej takiej karcie
  dodać pin na prawdziwych danych.
- **M (cel-ląd)** — premia za unikat liczona po `cardId` wśród lądów wroga;
  ląd bez produkcji kolorów (`{C}`) nie dostaje sygnału (inaczej zapala się pin
  C53/C w `batch53-bot`).

## 4. Artefakty sesji

- Plan: `docs/plans/PLAN_2026-09-23c-uwagi-z-gry-14-pozycji.md` (wiersze A–M
  + bloki „Zmierzone 2026-09-23c/d/e/f”);
- Milestone: **M421** w `docs/ENGINE_MILESTONES.md` i `docs/PROJECT_HISTORY.md`;
- PR: https://github.com/jurekjurekgh/mtg/pull/134 (base `main`, head
  `arena/01a0ceb4-mtg`).
