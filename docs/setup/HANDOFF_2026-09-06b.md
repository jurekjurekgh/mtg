# Handoff 2026-09-06b — arena/01a07682-mtg: usunięta karta-sonda, jest strażnik pochodzenia

## Stan
- Gałąź `arena/01a07682-mtg`, PR **#101** (otwarty). Faza 1 (audyt PR #100 + pętla
  jakości) zamknięta w `docs/setup/HANDOFF_2026-09-06a.md`; ta notka opisuje fazę 2.
- Faza 2 (ta sesja): ALARM właściciela — w `decks/wiedzmin.txt` leżała karta
  `Stifle`, której nie ma w jego kolekcji. Decyzja właściciela: usunąć kartę,
  zdolność zostawić, prześwietlić katalog, zbudować strażnika. Wykonane w całości.
- Bramki po fazie 2: `npm test` **4506/4506**, `npm run build` 59 modułów /
  3318,6 kB, benchmark szybki heuristic **84,8% (570/672)** (bez zmian wobec
  baseline'u fazy 1), Żywy Tester `wiedzmin` vs `tarkir-bg` seed 4008 — 0 zgłoszeń
  detektorów, 0 wystąpień `Stifle`.

## Co teraz obowiązuje (następna sesja musi to znać)
1. **ADR 0029**: katalog kart (`src/cards/card-data.js`) rośnie WYŁĄCZNIE z
   batchy właściciela — słownikiem jest `tools/collection-art-ids.csv`
   (`Ilustracja = <artId><set>`, 598 wierszy). Agent nie dopisuje kart, nawet
   prawdziwych, nawet jako nośnik mechaniki. Wolno tylko: tokeny (`token_*`) i
   lądy podstawowe.
2. **Strażnik**: `test/proweniencja-katalogu.test.js` (5 asercji; filtr
   kwalifikacji lustrzany z `tools/generate-plan-decks.mjs`). Czerwienieje na
   karcie bez artId, na rozjeździe nazwy/planu ze słownikiem, na karcie
   „specjalnej" niebędącej tokenem, na wierszu w `decks/*.txt` spoza słownika i
   na osieroconym kluczu w `MANA_COSTS`.
3. **Brak nośnika mechaniki = karta synteretyczna w pliku testu** (wzorzec:
   `SONDA` w `test/audyt-kontrzenie-zdolnosci.test.js` — definicja podana do
   `gameObjectDataOf` wystarczy, bo engine nie zna rejestru, ADR 0002).
   Propozycję realnej karty zgłasza się właścicielowi (backlog §2).
4. **Usuwanie karty jest pełne**, gdy znika: definicja, klucz `MANA_COSTS`,
   `docs/cards/scryfall-<id>.json`, przydział `plan` (potem
   `node tools/generate-plan-decks.mjs`) i testy pinujące nazwę karty.

## Obserwacje dla właściciela (bez decyzji nie ruszam)
- Kolumna „Kolory" w liście talii `README.md` to skrót (nie jest pilnowana);
  `wiedzmin` ma `UBRG`, a w talii leży karta biała (`Maritime Guard`) — jeśli
  kolumna ma znaczyć coś konkretnego, trzeba to zapisać, inaczej będzie
  kolejne kłamstwo w opisie.
- Grzechotka remisów (`test/audyt-bot-walka-remisy.test.js`) podniesiona
  `block` 4 → 6: przyrost w 100% z pary `wiedzmin|tarkir-bg` po utracie karty
  (zmierzone per para, nie zgadnięte); klasy przejrzane (polityka M169/J+L i
  płaski model addytywny). Zaostrzenie wyceny walki nadal czeka na JAWNĄ
  komendę na pełne B0 (ADR 0018).
- Budżet lektury startowej ~99,8k/100k — każda nowa lekcja wymaga kondensacji
  `docs/LESSONS.md` (narracja → `docs/LESSONS_PRZYPADKI.md`, numery 1:1).

## WISI: wypchnięcie
`GH_TOKEN` wygasł w sesji — commity fazy 1 (`d0787ad`, `0166f28`, `9ebb0e4`,
`2a2b295`) i fazy 2 czekają na `git push origin arena/01a07682-mtg` (fast-forward,
siłowy push zakazany i niepotrzebny). Opis PR #101 do wklejenia:
`/home/user/pr101-body.md` (poza repo — nie jest w snapshotcie; jeśli przepadł,
opis trzeba odtworzyć z `docs/PROJECT_HISTORY.md`).
