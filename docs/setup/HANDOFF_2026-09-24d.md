# Plan kontynuacji projektu — sesja 2026-09-24d (Żywy Tester na kartach batcha 59 + kwota alt-kosztu)

## 0. Czytanie obowiązkowe (jak zawsze, AGENTS.md §0)

1. `AGENTS.md` (zasady stałe);
2. `docs/decisions/` — ADR-y (stan: 0001–0030, 0008 w `archive/`);
3. `docs/LESSONS.md` (rejestr reguł; najnowsze **L165–L168**) + `docs/LESSONS_PRZYPADKI.md` + `docs/LESSONS_ARCHIWUM.md`;
4. `docs/setup/ENVIRONMENT.md` (sandbox, git, GitHub CLI; §2 odzyskiwanie po resecie).

## 1. Stan projektu

- **Bramy po sesji:** `npm test` **6519/6519**, 0 fail; build **60 modułów /
  4241,6 kB** (baza wejściowa sesji: 6511/6511, 60/4239,9 kB → +8 testów M428).
- **PR #136** (gałąź `arena/01a0d408-mtg`) — OPEN, base `main` (`f9bc44b`);
  opis uzupełniany kumulatywnie (sesje 24b + 24c + 24d).
- **Batch 59 zamknięty** (M427). Ta sesja domknęła zlecenie właściciela:
  raport talii + audyt Żywym Testerem celujący w karty batcha.
- **Naprawione w tej sesji (M428):** kwota kosztu alternatywnego =
  suma symboli. `join-the-dance` flashback `cost` 4 → **5** (druk `{3}{G}{W}`),
  `boulder-salvo` surge 3 → **2** (druk `{1}{R}`, karta z batcha 58); etykieta
  flashbacku w `render.js` przechodzi przez `costSymbols` (dwa miejsca: tytuł
  grupy i `commandLabel`). Strażnik:
  `test/audyt-m428-kwota-alt-kosztu.test.js` (+ piny w batch 58/59).

## 2. Otwarte punkty dla właściciela

- PR #136 czeka na przegląd/merge (agent nigdy nie merge'uje — ADR 0020).
- **Kolejny batch:** lista kart wyłącznie od właściciela (artId/set), standard
  = 10 kart; karta dwustronna liczy się jako JEDNA.
- **Narzędzie `tools/cr-numery.mjs`:** nowy cytat `CR <numer>` w `src/`/`test/`/
  `tools/` wymaga weryfikacji wobec pliku CR i `--zapisz`; bez pliku CR nie
  dopisuj numeru do tabeli.
- **O-4** (CR 616.1 — kolejność efektów zastępczych) i **O-5** (`colorsFrom`)
  — nadal „KARTY SPOZA KATALOGU": implementować dopiero, gdy wejdzie karta,
  która je rozróżnia. Kumano's Blessing jest pierwszym klientem O-4 (jedna
  aura na raz — silnik bierze pierwszą pasującą).
- Pełny bench `--full`/macierz B0 wyłącznie na komendę właściciela (ADR 0018).

## 3. Ryzyka / watchlisty

- **Reset sandboxa zabiera katalogi poza repozytorium i cofa `HEAD`.** Procedura:
  `git fetch` → `git diff FETCH_HEAD` → `git reset --mixed FETCH_HEAD`
  (NIGDY `checkout`/`clean`; L136). W tej sesji zdarzył się trzeci taki reset —
  odzysk potwierdzony (`HEAD` == remote po `git fetch`/`reset`).
- **Żywy Tester mierzy `dist/`** — po każdej zmianie w `src/` najpierw
  `npm run build`; tymczasowa talia `decks/audyt-*.txt` łamie 4 strażniki
  talii (M203/7, M178, M338/3, round-trip) — usuwać przed commitem.
- **Karty 1-of w taliach singletonowych** rzadko wchodzą na stół (8 partii na
  taliach standardowych pokazało 4 z 10 kart). Audyt konkretnych kart robić na
  dedykowanej talii `decks/audyt-*.txt` (2× każda karta, ~50 % landów).
- **Skan Oracle↔definicja musi czytać CAŁY napis kosztu**, nie same pipy —
  kwota i pipy rozjeżdżają się osobno (L168). Wyjątki nazywać wprost:
  `cleave` trzyma kwotę w `manaCost`, `adventure` nie ma kosztu przy
  słowie-kluczu, `kicker` z kilkoma kosztami jest nieparowalny.
- **Bramka `dayNightDriven`:** każdy nowy efekt obracający DFC musi jawnie
  zdecydować, czy jest „parą daybound/nightbound".
- **Pola stanu i znaczniki:** nowe pole obiektu → `identity.js`; nowy klucz
  stanu → odcisk albo `STATE_FINGERPRINT_EXCLUSIONS` z powodem; nowe pole EOT
  → znacznik + cleanup + reset 400.7.
- **Mutacje testowe na kopiach** (`cp`/`/tmp`), nie `git checkout`; po każdym
  zielonym kroku commit + push.

## 4. Artefakty sesji

- Plan: `docs/plans/PLAN_2026-09-24d-zywy-tester-batch59.md` (A0–A4, zamknięty).
- Test: `test/audyt-m428-kwota-alt-kosztu.test.js` (8 testów: skan symboli
  rodziny alt-kosztów, lista pominiętych, dowód RED, piny etykiet).
- Tymczasowa talia audytowa: `decks/audyt-batch59.txt` — **usunięta** po
  partiach (transkrypty `/tmp/zb-{P1..P6,Q1,Q2}.txt` poza repozytorium).
- Wpisy: **M428** w `docs/PROJECT_HISTORY.md` i `docs/ENGINE_MILESTONES.md`,
  lekcja **L168** w `docs/LESSONS.md` + narracja w `docs/LESSONS_PRZYPADKI.md`.
