# Handoff 2026-09-05e — arena/01a072eb-mtg (ostateczny)

## Stan
- Gałąź: `arena/01a072eb-mtg`, PR #100 (https://github.com/jurekjurekgh/mtg/pull/100) — OTWARTY,
  wszystkie 4 commity wypchnięte, PR body zaktualizowany.
- Commity na gałęzi:
  - `cfe069f` — audyt PR #99 + plan sesji.
  - `85dd79c` — M130: zamknięcie ślepoty wyceny (exploit/opponent_target/color_choice)
    + tieProjection + etykiety dla 13 typów resolve_*.
  - `f634c6e` — L131: lekcja w rejestrze.
  - `a037ff0` — M130-cd: kolejne 3 typy z wyceną (fabricate/manifest_dread/
    reveal_exile_hand), tieProjection+summarize dla 8 kolejnych typów,
    naprawa widoku exile (manaCost/power/toughness w playerView).
- Token GH_TOKEN działał poprawnie po odświeżeniu właściciela.
- Katalog kart: 598 kart (bez nowych kart — ADR 0021 4c).

## Bramy zielone
- `npm test`: 4486/4486.
- `npm run build`: 59 modułów / 3301.9 kB.
- Benchmark szybki (672 meczów): heuristic **84.7% (569/672)** vs aggro 26.8%/random 3.9%
  (poprawa z 84.1%/565 na starcie sesji).
- Golden-master `test/fixtures/bot-scoring-snapshot.json`: hash `fb8cece4b670…`
  — 1/12 partii zmieniła się świadomie (tarkir-bg|warhammer-ubr; Exploit — bot
  poświęca token 1/1 zamiast najsilniejszego stwora).

## Wyniki pętli jakości

### Bot-tie-audit (przed vs po)
- Przed audytem: 50+ remisów „bez danych" dla resolve_*, w tym 3 typy decyzji
  z WSZYSTKIMI opcjami na 0 pkt (prawdziwa ślepota).
- Po: **bez-danych = 1** (cast_adventure_creature vs pass — uczciwy przypadek,
  w którym jedna z opcji to pass bez projekcji, klasyfikator wymaga ≥2 projekcji
  żeby wyrokować). Pozostałe 7 „bez-danych" z poprzedniego pomiaru zamknięte.
- GROZY (remisy przy różnych danych): 20 (block=5, attack=7, activate_ability=1,
  resolve_springbloom=2, resolve_exploit_choice=1, resolve_opponent_target=1,
  resolve_color_choice=1, resolve_escape_exile=1, resolve_manifest_dread=1)
  — wszystkie to uczciwe remisy przy równych celach (np. dwie ofiary Exploita
  to oba 1/1, dwa cele obrażeń mają identyczną wartość, dwa landy do
  poświęcenia w springbloom są równie kosztowne).

### Dodane wyceny w scoreCommand (`src/controllers/heuristic-bot.js`)
- resolve_exploit_choice: 40 - value(ofiara), skip=20.
- resolve_opponent_target: 100+ za zabicie wrogiego stwora 1 obrażeniem,
  kara za własne cele.
- resolve_color_choice: 5 + needScore×6 + enemyInColor.
- resolve_fabricate: tokens=15, counters=10.
- resolve_manifest_dread: 10 - keepValue(karty).
- resolve_reveal_exile_hand: 20 + manaCost/power/toughness wrogiej karty,
  20 - manaCost własnej (tylko fallback).

### Naprawa playerView (src/engine/game-state.js)
Strefa exile (jawna, CR 406.3) nie niosła manaCost/power/toughness — to
sama luka co M274 w grobie. Dodane, co odblokowało poprawną wycenę
cast_adventure_creature z wygnania.

### Lekcja L131 (docs/LESSONS.md)
Reguła: dodając nowy typ resolve_*, trzeba JEDNOCZEŚNIE dodać case w
scoreCommand + nazwę w summarize() + tieProjection.

## Żywy Tester (tools/table-tester/)
- 6/6 partii de facto się kończy (w logach `/tmp/tt/` jest `== KONIEC PARTII ==`).
- Pozorne timeouty (final-fantasy×zendikar s41, worek-legend×theros s61, 90 s)
  to fałszywy detektor końca w skrypcie (po `KONIEC PARTII` skrypt czeka na
  napis „Nowa partia", który w niektórych ścieżkach UI nie przychodzi).
  **To nie jest błąd core, tylko skryptu Żywego Testera.** Nie diagnozowałem
  dalej — poza zakresem pętli jakości.

## Dokumenty
- `docs/audits/AUDYT_PR99_2026-09-05.md`
- `docs/plans/PLAN_2026-09-05-audyt-pr99-petla-jakosci.md`
- `docs/setup/HANDOFF_2026-09-05e.md` (ten plik)
- Lekcja L131 w `docs/LESSONS.md`

## Do zrobienia na następnej sesji (opcjonalne, nie blokuje scalenia PR)
1. Kontynuować pętlę jakości: sprawdzić czy GROZY w block (5) i attack (7)
   są faktycznie uczciwymi remisami czy ślepotą modelu (block modeling
   gang-bloków, attack — presja/trade-off).
2. Naprawić fałszywy detektor końca partii w Żywym Testerze (czekanie
   na "Nowa partia" po KONIEC PARTII).
3. Żadnych kart do dodania bez listy od właściciela (ADR 0021 4c).
4. Pełny benchmark B0 (23 400 meczów) tylko na jawną komendę (ADR 0018).
5. Scalenie decyzja właściciela (Squash and merge) — NIE mergować samemu.

## Nie ruszane
- Żadnych nowych kart.
- Nic nie force-pushowane, nic nie mergowane.
- ADR 0002 (core bez special-case po nazwie/ID karty) zachowany.
