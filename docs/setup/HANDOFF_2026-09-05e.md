# Handoff 2026-09-05e — arena/01a072eb-mtg

## Stan
- Gałąź: `arena/01a072eb-mtg`, PR #100 (https://github.com/jurekjurekgh/mtg/pull/100) otwarty.
- Lokalne commity (NIE WYPCHNIĘTE — GH_TOKEN wygasł w połowie sesji,
  push z `f634c6e` zwrócił „could not read Username for 'https://github.com'"):
  - `cfe069f` Sesja arena/01a072eb: audyt PR #99 + plan sesji — wypchnięty.
  - `85dd79c` Sesja arena/01a072eb: M130 — zamknięcie ślepoty wyceny
    rozstrzygnięć wyboru (resolve_*) — **do wypchnięcia**.
  - `f634c6e` Sesja arena/01a072eb: L131 — lekcja o decyzjach resolve_* bez
    wyceny — **do wypchnięcia**.
- Bramy zielone: `npm test` 4486/4486, `npm run build` 59 mod./3295.4 kB,
  benchmark szybki 84.7% (569/672 — poprawa z 84.1%/565).
- Katalog kart: 598 kart po Batch53 (bez zmian tej sesji).
- Golden-master: `test/fixtures/bot-scoring-snapshot.json` hash
  `76a49999835e7b15…` — 1/12 partii się zmieniło świadomie (tarkir-bg vs
  warhammer-ubr; exploit w talii — bot poświęca token 1/1 zamiast najsilniejszego
  stwora, tak jak przewidywała nowa heurystyka).

## Co zrobiono tej sesji

### E1 — Audyt PR #99 (poprzedni scalony)
- PR #99 był dobry: wszystkie commity green, benchmark 84.1%, lekcja L129
  (brakująca wartość celu rejestrowana jako 0 zamiast odrzucenia) w rejestrze.
- Raport: `docs/audits/AUDYT_PR99_2026-09-05.md`.

### E2 — Pętla jakości (ADR 0021 4b)
1. **Audyt rodzin (`node tools/family-audit.mjs`)** — czysty.
2. **Audyt remisów bota (`node tools/bot-tie-audit.mjs`)** — odsłonił klasę
   L117 w praktyce: w decyzjach `resolve_*` tieProjection zwracał null, więc
   nie dało się odróżnić uczciwych remisów od ślepoty wyceny.
3. **Dodano tieProjection + etykiety summarize** dla 13 typów decyzji
   resolve_* (discard, search, trigger_target, exploit, opponent_target,
   color_choice, modal, look_top, satyr_look, graveyard_top, delirium, mentor,
   room).
4. **Ponowny audyt wykazał PRAWDZIWĄ ślepotę wyceny**: trzy typy decyzji nie
   miały w ogóle case w `scoreCommand` i padały do `default: finish(0)`:
   - `resolve_exploit_choice` (M69 Exploit) — bot poświęcał pierwszego stwora
     z listy (zwykle najsilniejszego, bo w tej kolejności są enumerowane).
   - `resolve_opponent_target` (Cuombajj Witches — przeciwnik wybiera cel
     1 obrażenia) — bot obracał 1 obrażenie w siebie, w swojego najlepszego
     stwora.
   - `resolve_color_choice` (Benevolent Blessing protection / Manor Gate) —
     bot wybierał zawsze W (pierwszy kolor na liście), niezależnie od planszy.
5. **Dodano wycenę** w `scoreCommand` (src/controllers/heuristic-bot.js):
   - Exploit: poświęć NAJSŁABSZEGO kandydata (40 - value); skip = 20 pkt.
   - Opponent_target: zabij wrogiego stwora z 1 wytrzymałością (100+); zrań
     wrogiego (30); kara za zranienie własnego; największa kara za zabicie
     własnego.
   - Color_choice: preferuj kolor potrzebny do rzucenia kart w ręce ×6 +
     liczba wrogich stworów w tym kolorze (ochrona).
6. **L131** dodana do `docs/LESSONS.md`: reguła, że nowy typ `resolve_*`
   musi dostać JEDNOCZEŚNIE case w scoreCommand, nazwę w summarize i
   tieProjection; strażnik `bot-tie-audit.mjs`.

## Wyniki po naprawie
- Bot-tie-audit: z 50+ remisów „bez-danych" → 7 (wszystkie w niszowych
  typach: resolve_escape_exile, resolve_fabricate, resolve_manifest_dread,
  resolve_rebound_cast, resolve_grave_free_cast, resolve_adventure_creature,
  cast_adventure_creature). „GROZY" (remisy przy różnych danych) dla głównych
  3 typów spadły z 10 (4+4+2) do 3 uczciwych remisów przy równych celach
  (np. dwie ofiary Exploita to oba 1/1 tokeny, wszystkie 5 kolorów z tą samą
  dostępnością na ręce).
- Benchmark lekko w górę (84.1% → 84.7%).

## Żywy Tester (tools/table-tester/)
- 6 partii × 3 profile (explorer, greedy, aggro) — uruchomiono 6 w profilach
  explorer/greedy. W logach z `/tmp/tt/` **WSZYSTKIE 6 partii de facto się
  kończą** (widać `== KONIEC PARTII ==` oraz wynik: gracz/bot wygrywa).
- Pozorne timeouty (final-fantasy×zendikar s41, worek-legend×theros s61, 90 s)
  to nie zawieszenie silnika, a prawdopodobnie błąd detekcji końca w samym
  skrypcie table-tester (po `KONIEC PARTII` pojawia się jeszcze ekran
  „kolejna partia" z AKCJE: (brak) i skrypt czeka na „Nowa partia" zamiast
  zamknąć transkrypt). Nie diagnozowałem dalej — nie dotyczy core.
- Dependencies w `tools/table-tester/` zainstalowane (`npm i`, jsdom).

## Do zrobienia na następnej sesji
1. **Najpierw:** `git push origin arena/01a072eb-mtg` (wymaga odświeżenia
   GH_TOKEN lub konfiguracji credential helper). Commity lokalne 85dd79c
   i f634c6e czekają.
2. Zaktualizować PR body (#100) o nowe zmiany w eksploit/opponent/color.
3. Opcjonalnie: kontynuować pętlę jakości — dodać wycenę i tieProjection
   dla pozostałych 7 niszowych typów z audytu (escape_exile, fabricate,
   manifest_dread, rebound_cast, grave_free_cast, adventure_creature,
   resolve_scry/surveil/springbloom — niektóre mają już wycenę, inne
   nie).
4. Opcjonalnie: naprawić fałszywe timeouty w Żywym Testerze (detekcja końca
   partii po `KONIEC PARTII`).

## Nie ruszane
- Żadnych nowych kart dodanych (ADR 0021 4c: bez listy od właściciela).
- Pełny benchmark B0 nie uruchamiany (ADR 0018: tylko na komendę).
- Nic nie force-pushowane, nic nie mergowane.
