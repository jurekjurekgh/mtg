# Handoff 2026-08-10c — po M70 (uwagi właściciela A/B/C/D z testów na iPhonie)

## Stan po sesji 2026-08-10 (PR #40 `arena/019febbd-mtg` — OPEN do main)

- **Kod:** `npm test` **1255/1255**, build **50 modułów / 1385.2 kB**, 207 realnych kart
  + 1 token (208 supported), 9 talii singleton. **Pełne B0 (13 500 meczów):
  0 crashy (heuristic 78.6% ogółem; 63.4% vs aggro / 93.8% vs random)** — progi 0.78/0.57 utrzymane.
- **Zakres M70 (zgłoszenia właściciela z Pages, screenshoty IMG_7204-7206):**
  1. **C — Idyllic Grange (engine):** dodane brakujące `entersTapped: true` obok
     warunku (warunek tylko uchyla flagę — `playLand` czyta `entersTapped`);
     trigger countera +1/+1 ożywiony (`enters`→`enter_battlefield`,
     `requiresTarget` WEWNĄTRZ obiektu triggera — `createAbility` dropuje
     top-level param). **Sonda Batchu 25:** ten sam martwy event `'enters'`
     wykryty w `fertile-thicket` i `springbloom-druid` (oba ETB naprawione);
     dodatkowo `fertile_thicket_reveal` ogląda teraz wierzch WŁASNEJ biblioteki
     (CR 401.4 — filtr kontrolera na wspólnej liście, jak mill_from_bottom M58),
     `resolve_fertile_thicket` waliduje `bottomOrder` jako permutację, a
     Springbloom „up to two" to dwie decyzje gracza (`queueSearchChoice` z
     `chain`) zamiast deterministycznych pierwszych 2 landów ze wspólnej listy
     (mógł ukraść landy przeciwnika). Boty aggro/heuristic nauczone nowych
     komend `resolve_fertile_thicket`/`resolve_springbloom`.
  2. **A — etykiety grup wyborów:** opis CO wybieramy („Wybierz: Mulligan
     (2 opcje)", „Wybierz: Deklaracja atakujących (2 opcje)"); grupy celów z
     nazwą źródła BEZ prefiksu („Aura: Benevolent Blessing (3 opcje)", „Cel
     czaru: …"); odmiana liczebnika (1 opcja / 2–4 opcje / 5+ opcji / 12–14
     opcji); nagłówek modala = ta sama nazwa (`choiceGroupTitle` → `introLabel`);
     `commandLabel` default → REASONING_ACTION_LABELS (nie surowy typ).
  3. **A2 — surowy HTML w modalu aury:** opcje `renderChoiceRequest` przez
     `innerHTML` (ikony many), nazwy kart nadal escape'owane w `commandLabel`.
  4. **B — Surveil:** `.look-wizard-card` jasny chip (#f4f4f5/#e4e4e7/var(--text))
     — koniec czarnego tekstu na ciemnym tle w jasnym modalu.
  5. **D — „3 kolumny":** treść etykiety akcji w jednym inline
     `span.action-label` (min-width:0) — flex-itemy to tylko diament `::before`
     i span; zastosowane w panelu akcji, opcjach modala i menu kontekstowym
     (tam też wspólna `choiceGroupLabel` zamiast „Wybierz wariant (N): …").
- **Commity:** plan (add07da) → engine C (3fc9b2d) → A2 modal (df477f8) →
  A etykiety (9985617) → D kolumny (f046a9a) → B chipy (df8c39b) → docs (następny).

## Kolejka (po M70)

- **Batch 29** — czeka na listę właściciela (ADR 0010: Scryfall ZAWSZE z `set=`;
  api zablokowane → `fetch_page`).
- **B2-w2** — infra OFF (~4× wolniej); z włączonym lookahead +5 p.p. vs random /
  +34 p.p. vs aggro; decyzja o koszcie.
- **Talie singleton** — rosną z batchami (strażnik `repo-decks`).

## Pułapki (środowisko / projekt) — aktualne

- `edit_file` psuje PL → **python3 Path.read_text/write_text**, commit msg przez /tmp;
  długie skrypty pythona zapisuj do /tmp i uruchamiaj `python3 /tmp/x.py`
  (UWAGA: samo zapisanie skryptu NIE wykonuje zmian — uruchomić!).
- **Sandbox potrafi COFNĄĆ lokalny HEAD do main** — po każdym commicie
  `git push origin arena/...`; przy rozjeździe: `git fetch && git reset --mixed FETCH_HEAD`
  i cherry-pick z reflogu.
- **GH_TOKEN potrafi wygasnąć w trakcie sesji** (2026-08-10: 2. push dnia padał na
  „could not read Username" mimo działającego 1.) — commity są bezpieczne lokalnie,
  push retry; w ostateczności reconnect GitHub w Arena.
- **Nowe w M70:** eventy triggerów to ZAMKNIĘTA lista w engine (26 zdarzeń,
  strażnik `HANDLED_TRIGGER_EVENTS` w batch25-etb-enters-fix) — nowy trigger
  bez wpisu w engine jest martwy (nauczka: testy behawioralne, nie definicyjne);
  `entersTappedCondition` bez `entersTapped: true` nie działa (strażnik registry);
  `queueSearchChoice` jest top-level (eksport; chain dla „up to N");
  `zones.library` to WSPÓLNA przeplatana lista — czytaj zawsze przez filtr
  kontrolera i składaj z powrotem z zachowaniem przeplotu;
  ChoiceMiniEl/MiniEl symulują przeglądarkę: innerHTML strip-tagga textContent,
  getter innerHTML serializuje rekurencyjnie.
- Hunter seeds: bez zmian talii w M70 → bez przelosowania (audit C2 3, bot-pausa 7,
  endure 4, delirium 25, Forever Young 12, session-abilities-integration 4).
- Pełne B0 (13500) ~16 min; quick `--seeds 4` (1080) ~90 s; regression ~1 min.
- Lektura nowej sesji: AGENTS.md → PROJECT_STATE (M70) → HANDOFF_2026-08-09a…f →
  HANDOFF_2026-08-10a/b/c → ENGINE_MILESTONES (M64-M70) → WORKFLOW → plany M65/M66/
  B27/M68/B28/M70 → ADR 0002/0003/0005/0010/0011/0013/0014.

## Do weryfikacji w następnej sesji

- Na iPhonie (po merge): (a) etykieta mulligana „Wybierz: Mulligan (2 opcje)";
  (b) aura z kosztem — ikony many zamiast surowego HTML i brak kolumn;
  (c) Surveil — nazwy kart czytelne; (d) Idyllic Grange przy <3 innych Plains
  wchodzi TAPPED, przy 3+ — UNTAPPED + licznik na wskazanym stworze;
  Fertile Thicket/Springbloom Druid ETB działają.
