# Handoff — M272 (odznaka DIAMENTOWA), 2026-09-01

## Stan

- Gałąź `arena/01a058db-mtg`, **PR #91 OTWARTY** (base `main`, nie scalać —
  decyzja właściciela, `Squash and merge`).
- `npm test` **4037/4037**, `node --test test/bot-benchmark.test.js` **10/10**,
  build **57 modułów / 3021,2 kB**.
- Drzewo robocze czyste, wszystko wypchnięte.

## Co zrobiono w tej części sesji

Domknięta **odznaka DIAMENTOWA** (M272) — piąta z rzędu seria pięciu
unikalnych błędów reguł. Szczegóły w `docs/ENGINE_MILESTONES.md` (M272).

| # | Reguła | Istota |
|---|--------|--------|
| 17 | CR 704.5s + 122.1e | Saga poświęcana przez SBA omijała `deathZoneFor` |
| 18 | CR 122.1d + 614.6 | 5 ścieżek odkręcania ignorowało licznik stun i blokadę |
| 19 | CR 701.7a + 702.12 | destroy Equipment bez indestructible/shield/regeneracji |
| 20 | CR 122.1b | 5 emiterów poświęcenia gubiło `toZone` → „dies" mimo wygnania |
| 21 | CR 704.5m + 104.4b | cyklowanie z pustej biblioteki nie kończyło partii |

Nowe helpery (choke pointy): `untapByEffect` i `destroyPermanentByEffect`.

**Wycofany fałszywy alarm:** CR 506.4 (zmiana kontroli usuwa z walki) jest
zaimplementowane w `state-based.js:95–124` od M201. Moja sonda wołała
`applyEffect` bez przebiegu SBA i pokazywała stan pośredni. Naprawa cofnięta
w całości, wniosek → **L111**. Nie zgłaszać tego ponownie jako błędu.

## Co dalej (propozycja, nie zobowiązanie)

1. **Żyła L107 nadal czynna.** Najskuteczniejsze narzędzie tej serii to
   strażnik SKANUJĄCY ŹRÓDŁA — dwa razy znalazł ścieżkę przeoczoną przez
   audyt ręczny (#20 Springbloom Druid, #21). Warto objąć takim skanem
   kolejne kontrakty zdarzeń.
2. Niezaudytowane rodziny zdarzeń: `card_discarded` (4 emitery),
   `object_tapped` (7 — tapowanie nie ma efektu zastępującego, więc niski
   priorytet), zdarzenia stref w `table/session.js`.
3. Kolejna odznaka wymagałaby błędów #22–#26 tą samą metodą (L11/L12/L13).

## Pułapki potwierdzone w tej części (poza LESSONS.md)

- **Test `dokumentacja-budzet-lektury` ma twardy limit 100k tokenów na całą
  lekturę startową.** Dopisanie lekcji potrafi go przekroczyć (L111 dał
  ~100,2k) — wpisy trzeba pisać zwięźle, a nie podnosić limit.
- Strażnik skanujący źródła musi mieć **okno kontekstu dopasowane do kodu**
  (znacznik bywa stawiany PO pętli) i **zawężenie do właściwej operacji**
  (podgląd wierzchu biblioteki to nie dobranie). Dwa fałszywe trafienia po
  drodze poprawiły test, nie kod — to prawidłowa reakcja.
