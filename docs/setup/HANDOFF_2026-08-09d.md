# Handoff 2026-08-09d — po M65 audyt Batchu 26 (4 błędy MtG + crash B0)

## Stan po sesji 2026-08-09 (PR #39 `arena/019fe7ec-mtg` — OPEN do main)

- **Kod:** `npm test` **1182/1182**, build **50 modułów / 1289.5 kB**, 188 realnych + 1 token
  (189 supported), 9 talii singleton. **Pełne B0 (9 talii / 50 seedów / 13 500 meczów)
  0 crashy**: heuristic **92.0% vs random, 65.5% vs aggro**, aggro 94.2% vs random
  (progi 0.78/0.57 utrzymane; por. 90.4%/61.8% po M64 — wzrost dzięki naprawionym
  zdolnościom kolorowym i crew).
- **Zakres M65 (audyt Batchu 26, zlecenie właściciela):**
  1. **Crew = instant (CR 701.36)** — Bomat Bazaar Barge (B26) + Irontread Crusher (B21):
     usunięty `timing: 'sorcery'` (Oracle bez „Activate only as a sorcery").
  2. **Kolorowe koszty zdolności (CR 118.2)** — spłaszczone `colors` w 4 definicjach
     (kabira-vindicator, bladed-sentinel, trestle-troll, reassembling-skeleton);
     zagnieżdżenie `[['W']]` czyniło zdolności MARTWYMI (nigdy nie oferowane).
  3. **Trestle Troll regenerate** — `effect: {type:'regenerate'}` nie istnieje w
     applyEffect → aktywacja odrzucana; fix `effect: []` (keyword zakłada tarczę).
  4. **Index jako wybór gracza** — `pendingIndex` w PlayerView (FoW jak scry), wizard
     kolejności w `choice-request.js` (kind 'index'), request dla pojedynczej komendy
     w `buildChoiceRequestEntries`, etykiety + polskie logi.
  5. **Face-down bez keywordów (CR 708.2)** — `effectiveKeywords` → [] dla faceDown
     (zakryty flyer nie odblokowuje smoka / nie blokuje flyerów).
  6. **Crash pełnego B0 (pre-existing, seed 1025)** — transform na LKI stub = no-op
     (źródło poza bitwiskiem, CR 608.2b); pełne B0 nie było liczone po M64.
- **Commity:** plan (4b7c8e1) → face-down (aedd1aa) → cards crew+colors (219fa4c) →
  Index UI (e8d741b) → transform LKI (a962d2b).
- **Testy:** `test/audit-batch26-fixes.test.js` (13 behawioralnych) +
  `test/choice-request-ui.test.js` (+2 wizard Index).

## Kolejka (po M65)

- **Batch 27** — czeka na listę właściciela (ADR 0010: Scryfall ZAWSZE z `set=`;
  api zablokowane → `fetch_page`).
- **B2-w2** — infra OFF (~4× wolniej); z włączonym lookahead bot zyskuje
  +5/+34 p.p.; decyzja o koszcie i ewaluacja na pełnym B0.
- **Talie singleton** — rosną z batchami (strażnik `repo-decks` + „każda nielandowa
  w talii").

## Pułapki (środowisko / projekt) — aktualne

- `edit_file` psuje PL → **python3 Path.read_text/write_text**, commit msg via /tmp.
- **Nowe w M65:** `cost.colors` zdolności aktywowanych MUSI być płaskie (`['W']` /
  `['B','G']`), NIGDY zagnieżdżone (`[['W']]` — `colorRequirementsOf` robi map→[kolor]);
  crew bez `timing` (instant, CR 701.36); `effectiveKeywords` faceDown → [];
  transform na źródle poza bitwiskiem = no-op (nie throw); `pendingIndex` w PlayerView
  wymaga FoW (cards tylko dla decydenta); pojedynczy `resolve_index_choice` w UI pakować
  w request 'index' (wizard), nie grać wprost (no-op).
- Biblioteka = wspólna lista obu graczy `[0]`=top; spód własny = ostatnia własna karta.
- Obrażenia: damage_dealt ZAWSZE po prewencji; ochrona w combat (sourceId) i non-combat.
- Pełne B0 (13500) — ~15 min; quick `--seeds 4` (1080) — ~80 s. Zmiana przestrzeni
  komend bota (definicje zdolności/kart) = pełne B0.
- Hunter seeds w `table-session.test.js` — przelosowywać po zmianie talii (nie zmianie
  definicji).
- Lektura nowej sesji: AGENTS.md → PROJECT_STATE (M65) → HANDOFF_2026-08-09a/b/c/d →
  ENGINE_MILESTONES (M64+M65) → WORKFLOW → plan audytu B26 → ADR 0002/0003/0005/0010/
  0011/0013/0014.

## Do weryfikacji w następnej sesji

- `npm test` 1182/1182, `npm run build` 50/1289.5 kB.
- Pełne B0 13500 / 0 crashy (heuristic 92.0%/65.5%, aggro 94.2%).
- Scryfall Batch 27: ZAWSZE `set=` + `fetch_page` (api blokada).
