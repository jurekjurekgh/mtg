# Handoff 2026-08-12 — M80: Jill, Shiva's Dominant — cel ETB także własne permanenty

## START TUTAJ

1. Przeczytaj `AGENTS.md`, `docs/PROJECT_STATE.md` (M80), ten plik.
2. `npm test` — oczekuj **1413** zielonych.
3. `npm run build` — 50 modułów / ~1530.9 kB.
4. Sesja = gałąź `arena/019ff818-mtg` = PR (otwarty z tej sesji).

## Stan po sesji

- **Kod:** poprawny cel triggera ETB Jill, Shiva's Dominant.
- **PR:** otwarty z gałęzi `arena/019ff818-mtg` (plan → fix → test → docs).
- **Bot bez zmian** — pełne B0 niewymagane. Progi 0.78/0.57 (pomiar #44).

## Co naprawiono

- Oracle Jill: „up to one **other target nonland permanent**” — brak
  ograniczenia do przeciwnika, więc kontroler może celować także we własne
  permanenty niebędące lądem.
- Root cause: `other_nonland_permanent` w `triggers.js` odfiltrowywał
  `controllerId === sourceObject.controllerId`. Usunięto filtr — kandydatami
  wszystkie nie-landy poza źródłem (obu graczy), bez hexproof, najsilniejszy
  pierwszy (spójne z `nonland_permanent` / Thistledown Players).
- Landy nadal nie są celami. „up to one” = można odmówić (allowNone).

## Testy

- Zaktualizowano błędny test „Jill: nie zwraca własnych permanentów ani
  landów” — własne nie-landy są teraz CELAMI; tylko landy wykluczone.
- Nowe: kandydaci obejmują własny stwór (najsilniejszy pierwszy); cel własny
  wraca na rękę; cel przeciwnika nadal działa; same landy → brak celu.
- `npm test`: **1413 pass / 0 fail**. `npm run build`: 50 modułów / 1530.9 kB.

## Kolejka (po M80)

- Weryfikacja na telefonie (Pages, po merge): Jill celuje we własne
  nie-landy, cel przeciwnika nadal działa, landy poza opcjami.
- Batch 31 — czeka na listę właściciela (ADR 0010: Scryfall z `set=`,
  `fetch_page`).
- Phone verify / platinum hunt / B2-w2 lookahead OFF / tester roadmap.

## Pułapki

- `edit_file` psuje polskie znaki → **python3** Path.read_text/write_text.
- Sandbox potrafi cofnąć HEAD do main — `git fetch && git rebase FETCH_HEAD`.
- Nie commituj bez `npm test`. Nie odpalaj testera bez `npm run build`.
- `PlayerView.pendingClash.cards` = **cardId**. Escape cost z **registry**.
