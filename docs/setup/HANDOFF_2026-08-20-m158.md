# HANDOFF 2026-08-20 — M158 (Batch 39: 10/10 kart)

## Stan

- **PR #66** (`arena/01a01eb3-mtg`) — sesje M156+M157+Wypchnięte; **commity
  M158 D/E (662eab5, 8328f4b) + docs czekają na push — GH_TOKEN wygasł**
  (objaw: push = "could not read Username"; reconnect GitHub w Arenie).
- `npm test` **2474/2474**, `test:slow` 9/9 (test:all 2483), build
  **51 modułów / 2121.4 kB**. Batch 39 komplet — szczegóły:
  `docs/PROJECT_STATE.md` (M158).

## Nowe mechaniki Batch 39 (dla następnych sesji)

- Madness (CR 702.34): pole `madness` karty → odrzucanie w
  resolve_discard_choice kieruje do exile + `pendingMadnessCast`
  (resolve_madness_cast; castPermanent z madnessCast).
- pendingRevealChoice (Invasion of the Giants II) — wzorzec pełnej
  checklisti nowego pendingu (6 bramek! patrz M72/L — nowy pending MUSI
  przejść przez: firstPendingDecision, execute-gate, pass-gate, cast-gate,
  playerView-oferta, oba boty, fingerprint, COMMAND/EVENT_TYPES, etykiety).
- next_spell_discount — rabat konsumowany w castSpell/castPermanent
  (9+1 punktów `spellsCastThisTurn += 1` — konsumpcja wstawiona PRZED nimi).
- Wrapper apply_to_each_target dla czarów "each of up to N targets".

## Pułapki nowe

- **Testy scenariuszowe z zamrożonymi seedami** — każda zmiana talii
  (dokładanie karty/lądu) przelosowuje rozgrywkę: 4–6 testów do
  przelosowania na batch (L25). Hunter: /tmp/hunt{,A,4}.sh (znika po
  resecie sandboxa — skrypt do odtworzenia jest w historii commitów M158).
- **`node -e import && python3`** — łańcuch z importem modułu NAJPIERW:
  jeśli import padnie (syntax), reszta łańcucha (wstawki danych!) NIE
  wykona się po cichu — zawsze weryfikować efekty wstawki (M158: karta
  Revolutionist wypadła raz z tego powodu).
- **Dodawanie landu do talii z istniejącą linią "Nx Mountain"** — bumpować
  licznik w linii, NIE doklejać "1x Mountain" (round-trip test to łapie).
- reset sandboxa zdarzył się 2× w tej sesji (ENVIRONMENT §2 — procedura
  działa: stash → reset --hard FETCH_HEAD → stash pop → cherry-pick).

## Kolejka

- Po push: przegląd właściciela PR #66; potem audyt PR i pętla jakości
  wg ADR 0021 (Żywy Tester na nowych kartach Batch 39 obowiązkowo —
  L50/L51: wyceny istnieją, ale żywy stół zweryfikuje etykiety/modale).
- Backlog (nie kolejka): wyceny REVIEWED z L28 (create_copy_token itd.);
  Gianty w katalogu (Invasion III czeka na pierwszego).

## Aktualizacja (zgłoszenie A po teście właściciela)

- Morph/Megamorph w logu: pole `keyword` w zdarzeniu ability_activated (obie
  ścieżki) + etykieta „aktywuje Morph: <nazwa>" — commit 4e0bd9c.
