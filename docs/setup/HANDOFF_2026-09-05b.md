# Handoff — audyt PR #97 + pętla jakości, 2026-09-05 (sesja b)

Gałąź sesji: `arena/01a0712d-mtg` · PR: **https://github.com/jurekjurekgh/mtg/pull/98**
(open, NIE scalony — scalenie to decyzja właściciela, preferowane „Squash and merge").
Baza sesji: `5fb7994` (= squash PR #97, `main`).

## Stan na koniec sesji

- **Audyt PR #97 zamknięty:** 21 plików diffu (`d67b684..5fb7994`), raport
  `docs/audits/AUDYT_PR97_2026-09-05.md`. Werdykt: **PR dobry** — bramy, Oracle,
  rulingi i mutacje (F1/A1/C-FIX-2 niezależnie potwierdzone RED→GREEN) zgodne.
  **0 nowych znalezisk F**, obserwacje O1–O5.
- **Pętla jakości (ADR 0021):**
  - **O3 naprawione:** rodzina F3 — fakty historyczne rzutu (`wasKicked`,
    `wasCast`, `manaFromTreasureSpent`) na stubie LKI; Kor Sanctifiers /
    Geological Appraiser / Marut działają mimo kill-in-response
    (`test/audyt-pr97-flagi-rzutu-lki.test.js`).
  - **L47 skorygowana:** `copyableDescriptorKeys` nie istnieje; 4 ścieżki
    kopiowania per-ścieżka.
  - **Żywy Tester (2 partie + 2 kontrolne):** (1) kafel mówi o warunku
    triggera (kicker/offspring/ifCast/dies/attacks/end_step) i typie celu —
    wspólny `triggerConditionClause` + `targetTypeLabel`
    (`test/audyt-pr98-warunki-triggerow-na-kaflich.test.js`, mutacja 6/8 RED);
    (2) etykiety pseudo-triggerów `storm`/`ward` + strażnik M122 rozszerzony
    na cały `src/engine` i obie składnie (mutacja RED z adresem pliku).
- **Bramy na HEAD:** `npm test` **4459/4459**, `npm run test:all` **4469/4469**,
  `npm run build` **59 modułów / 3268,7 kB**, benchmark próbka 10/10 (w audycie).
  Pełne B0 NIE uruchamiany (ADR 0018).
- Zero nowych kart, zero zmian w wycenach bota (golden-master nietknięty).
- Plan: `docs/plans/PLAN_2026-09-05-audyt-pr97-petla-jakosci.md` (odhaczony).

## Commity (każdy osobno zielony)

| commit | treść |
|---|---|
| `ce76ecc` | plan sesji (PR #98 otwarty PRZED kodem, ADR 0020 A) |
| `4085358` | raport audytu PR #97 (per plik + werdykt + mutacje + O1–O5) |
| `948f310` | O3 — flagi rzutu na LKI (rodzina F3), 4 testy (RED 3/4) |
| `73faa5d` | korekta L47 do stanu faktycznego |
| `5c07350` | Żywy Tester: warunki triggerów + typ celu na kaflach (8 testów) |
| `6ff467f` | Żywy Tester: etykiety storm/ward + strażnik M122 na cały src/engine |
| *(domknięcie)* | README 4469/4459/3268,7; PROJECT_HISTORY; ten handoff |

## Gdzie szukać dalej

1. **Standardowa kolejność (ADR 0020/0021):** PR na start → audyt PR #98 →
   pętla jakości. Raport audytu PR #97 wskazuje O5 (`ability-cost-pips`
   dopasowuje pipy per KARTA — podnieść, gdy pojawi się karta z dwiema
   kolorowymi aktywacjami).
2. **`eachUpkeep` bez etykiety w gałęzi upkeep** (transformowane wilkołaki):
   upkeep renderuje tylko `noSpellsLastTurn`; klauzula „w każdym upkeep"
   nie istnieje — kandydat na kolejny przebieg `triggerConditionClause`
   (gałąź upkeep nie korzysta jeszcze z helpera).
3. **Pułapka własnej sesji (2×):** replace-first-occurrence na powtarzalnym
   stringu w `card-data.js` (koszt `{1},{T},discard`) trafia w Goblin Picker
   zamiast w Óina — zawsze kotwicz kontekst (komentarz nad linią), weryfikuj
   `git diff` przed testem. Więcej: raport audytu, „Incydent procesowy".
4. **`docs/setup/HANDOFF_2026-09-05.md` pkt 6** (dryf liczb README) —
   tę sesję domknięto na świeżym `test:all`; utrzymać dyscyplinę ENVIRONMENT §7.
5. Backlog C-R1–C-R7 z audytu Batch53 pozostaje bez akcji (rekomendacje).
