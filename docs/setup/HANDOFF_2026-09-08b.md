# Handoff 2026-09-08b — E9: wyzwanie wyłapywacza błędów II (5 kolejnych uproszczeń vs CR), PR #105

## Stan bieżący

Sesja `arena/01a07c4e-mtg`, [PR #105](https://github.com/jurekjurekgh/mtg/pull/105)
do `main` — kontynuacja po HANDOFF_2026-09-08 (E8; ta sama gałąź, ten sam PR).
Wyzwanie odznakowe **E9 (srebro) zakończone 5/5**: 5 KOLEJNYCH unikalnych
uproszczeń vs zasady MtG, każdy fix RED-first w osobnym commicie, plan
(`dce786d`) pushnięty przed kodem, checkboxy odhaczone (`c295da1`).
Sprawdź bieżący HEAD/CI na GitHub — plik nie pinuje własnego SHA.

## Bramki i dane do porównania

- `npm test`: **4771/4771** (po E8: 4766; testy E9 minus sfalszowany F3,
  plus 3 testy-guardie refutacji).
- `npm run test:all`: **4781/4781** (~226 s).
- `npm run build`: **59 modułów / 3396,6 kB**.
- `node tools/benchmark.mjs` (szybki): heuristic **85,0%** (571/672) —
  bez zmian vs E8.
- **Pełnego B0 NIE uruchamiano** (ADR 0018/0025). Żadnych nowych kart/
  talii/limitacji Oracle (ADR 0029); syntetyki wyłącznie w testach.

## Znaleziska E9 (commit: fix)

- **F1 `bfd9cbc` — powrót z grobu bez choroby (CR 302.6):**
  `put_graveyard_card_onto_battlefield` (Disa) nie ustawiał
  `summoningSickness` — ożywiony stwór atakował w tej samej turze. Fix:
  freeze z flagą PRZED `applyEnterCounters`. Test:
  `test/e9-f1-powrot-bez-sickness.test.js`.
- **F2 `fc3d492` — masowa zmiana kontroli a walka (CR 506.4):**
  `control_to_owners_all_creatures` (Trostani) nie wołał `removeFromCombat`
  — przejęty atakujący „atakował" nowego kontrolera. Uwaga testowa: bloker
  kontrolowany przez nie-obrońcę jest NIELEGALNY przy declare_blockers
  (legalny układ: owner p1 / controller p2 = obrońca). Test:
  `test/e9-f2-trostani-usuwa-z-walki.test.js`.
- **F3 — SFALSZOWANE (refutacja 2026-09-08):** pierwotne „znalezisko"
  (explore rzekomo bez wyboru) było błędem agenta: CR 701.44a (dawniej
  701.54a) daje wybór — „…may put the revealed card into their graveyard";
  reminder text: „then put the card back on top or into your graveyard".
  Oryginalna maszyna `pendingExplore` była POPRAWNA; „fix" `a7ae267`
  COFNIĘTY całościowo (`613a379`), przywrócony typ `resolve_explore_choice`
  w protokole/case'ach/botach. Test-guardia:
  `test/e9-f3-refutacja-wyboru.test.js` (decyzja + oba warianty + land→ręka).
- **F4 `d207034` — search→battlefield bez choroby (CR 302.6):**
  `resolve_search_choice` przy destination battlefield ustawiał tylko
  `tapped`. Fix: `summoningSickness` dla creature (landy nietknięte);
  siostrzana klasa F1, inny moduł. Test:
  `test/e9-f4-search-battlefield-sickness.test.js`.
- **F5 `03b71f0` — Throne: podwójny trigger ETB (CR 603.6c):** Throne emituje
  DWA zdarzenia jednego wejścia (object_moved→BF **i**
  permanent_entered_battlefield); pompa triggerów odpalała ETB podwójnie
  (Omenspeaker: scry 2 dwa razy). Fix: dedupe per wchodzący obiekt
  (`etbEnterFired`) w jednym przebiegu `processTriggersScan` — pomija CAŁY
  blok matchera (także backup/devour/dayNight). Test:
  `test/e9-f5-throne-etb-raz.test.js`.

Metodologiczna puenta F5: triggery ETB testowalne jednostkowo, ale obiekt
musi nieść pełny kształt z `gameObjectDataOf` (definiCard/REGISTRY) — ręczne
`abilities: [{type:'triggered',...}]` dawały fałszywe 0 odpaleń w sondach.
Puenta refutacji F3 (wpisane do warsztatu): PRZED zgłoszeniem znaleziska
zweryfikować dosłowny tekst reguły w CR/reminder text — „it's up to you /
you may" w reminder text to PEŁNOPRAWNY wybór, nie uproszczenie silnika
(„darmowy strict-upgrade" z F3 nie istniał; bugiem była własna poprawka).

## Wnioski / obserwacje

- Klasy błędów się powtarzają: „śmieciowa ścieżka wejścia na BF bez
  invariantów" (F1+F4+cast-sickness z audytów), „zdarzenie wejścia vs CR"
  (F5), „efekt masowy pomijający cleanup walki" (F2, jak E8/B5).
- Strażniki testowe (A3: etykiety/grupowanie; proweniencja katalogu) realnie
  pracują — wymusiły spójną amortyzację martwego typu komendy.
- Plan przed kodem + RED-first + osobne commity — rytuał bez wyjątku;
  każdy push fetch + porównanie za:0/przed:1 (brak konfliktów, PR rósł liniowo).

## Co dalej (propozycje, decyzja właściciela)

- Nierozstrzygnięte proby z fazy rozpoznania E9: kształt komendy `crew`;
  duplikat wpisu blokera (znaleźć właściwy resolver `pendingDamageAssignment`).
- Nieczytane tropy (niski priorytet): morph face-down cast; ninjutsu ×3;
  morbid caravan-vigil; fear-of-abduction; Aura vs
  staticCombatDamagePrevention (combat.js:55–88).
- 0 kart w katalogu (tylko dla przyszłych batchów ownera): skulk, shadow,
  horsemanship, bushido, battle cry, soulshift, undying, lure, monarch.
- E9 domyka sesję: PR #105 czeka na squash-merge właściciela (agent nie scala
  — ADR 0021).

---

## DOPISEK E10 (2026-09-08, po refutacji F3): audyt poprawek + revert B4 + ADR 0030

Właściciel zarządził dogłębny audyt zmian PR #105 wg dosłownych tekstów CR
(`docs/audits/AUDYT_PR105_POPRAWKI_2026-09-08.md`). Wynik: 8/9 potwierdzonych,
F3 sfalszowany (cofnięty), **E8/B4 (`2e15ba6`) drugim fałszywym znaleziskiem**:
CR 702.19b wyłącza prewencję z wyliczenia lethal PRZYDZIAŁU („…but not any
abilities or effects that might change the amount of damage that's actually
dealt") — stan sprzed B4 był zgodny z regułami; fix pozwalał na przydział 0
w chronionego (sonda: walidacja przepuszczała, boty przydzielały 0).

Naprawa na polecenie właściciela: **ADR 0030** (`367e92c`; zmiany regułowe
wymagają pobrania CR+rulingów online, pamięć treningowa nie jest źródłem),
revert B4 + guardia (`227c142`), kondensacja LESSONS po przekroczeniu budżetu
lektury (99.6k/100k, `be09633`). Bramki: fast 4772/4772, test:all 4782/4782,
build 3396,2 kB, bench 85,0% (bez zmian).
