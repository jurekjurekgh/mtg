# Handoff — faza 2 PR #98: C-R1–C-R7 (wyceny bota), 2026-09-05 (sesja c)

Kontynuacja sesji `arena/01a0712d` (1 sesja = 1 PR #98). Faza 1 (warunki
triggerów na kaflach, etykiety storm/ward) domknięta w HANDOFF_2026-09-05b.
Faza 2 = realizacja rekomendacji systemowych C-R1–C-R7 z
`docs/audits/AUDYT_BATCH53_2026-09-05.md` jako ŚWIADOME zmiany wycen bota
(nie refaktor), wg planu `docs/plans/PLAN_2026-09-05b-cr1-cr7-wyceny-bota.md`.

## Stan na koniec sesji

- Wszystkie etapy E1–E8 wykonane; C-R6 zamknięte PROBE'm bez zmiany kodu
  (plot wyceniony poprawnie — patrz audyt, §Rekomendacje).
- `test:all` **4486/4486**, szybki rdzeń **4476/4476**, build **59 modułów /
  3283,1 kB**. Benchmark szybki: heuristic **84,1%** (565/672); progi
  `test/bot-benchmark.test.js` zielone.
- Snapshot bota (`test/fixtures/bot-scoring-snapshot.json`, hash `d9e266e5…`)
  regenerowany na E2 (drzewo C-R1+C-R7 zielone); finalny `--write` na drzewie
  ukończonym NIE zmienił hasha (E3–E7 nie ruszają ścieżek fixture) — L124
  spełniony dosłownie: pierwszy zapis = freeze gotowego drzewa.
- CI zielone; scalenie PR #98 = decyzja właściciela (Squash and merge kończy
  sesję).

## Commity (każdy osobno zielony, pełny `npm test`)

- `3bcfcef` — plan E1–E8.
- `0abc269` — C-R1+C-R7: tabela `ETB_EFFECT_BONUS` po typach efektów w
  `cast_permanent` (cele wymagane tylko przy pasującym celu wroga; wykluczenia
  przeciw podwójnemu liczeniu), koszty dopłat kicker/offspring + wartość
  warunkowego ETB per wariant; epsilon 0,001×waluta rozstrzyga idealne remisy
  rzutów (grzechotka remisów, tura 6); C-FIX-1 przepisany.
- `331252a` — C-R2: `openZoneCard` (grób∪wygnanie) w `resolve_trigger_target`
  (jedno- i wielocelowa); klasyfikacja friendly dla zwrotu własnej karty z
  grobu (effect-intent + strażnik).
- `4587a9c` — C-R3: wartość ofiary (nie-stwory po koszcie×2) + flaga
  `reflexReady` w ofertach `resolve_sacrifice_choice` (engine, precedens
  M150); jałowy refleks → skip.
- `4a7b7fc` — C-R4: `becomesBlockedPump` w sim walki (staty efektywne tylko
  w gałęziach bloku).
- `5bc06cc` — C-R5: trucizna jako drugi zegar (racing od 6+, dopłata 20,
  lethal +1000 za przenikający infect ≥ 10−poison; wycisza kary B3/M297).
- `57b4fdf` — C-R6: PROBE bez zmian (dokumentacja w audycie + plan).
- Domknięcie: README/PROJECT_HISTORY/ten handoff.

## Gdzie szukać dalej

- Wyceny ETB są KONSERWATYWNE (celowo): tabela `ETB_EFFECT_BONUS` w
  `src/controllers/heuristic-bot.js`; typy poza tabelą → 0. Żywy Tester może
  domknąć pojedyncze typy (job_select, amass, discover itd.) po realnych
  błędach — nie na siłę.
- C-R3(a) dotyczy TYLKO decyzji refleksowych (`reflexiveAbility`); inne
  opcjonalne ofiary (devour/endure) mają własne gałęzie — nie łączyć.
- Nieujętne pola: `cmd.reflexReady === undefined` = decyzja bez refleksu
  (Grave Exchange) — bot traktuje ją starą polityką; nie zmieniać bez testu
  anti-over-fix (jest w `test/audyt-batch53-cr3.test.js`).
- Kolejne rekomendacje z audytów Batch53 (sekcje poza C-R1–C-R7) — patrz
  AUDYT_BATCH53 §Rekomendacje; pełne B0 tylko na komendę (ADR 0018).
- Reguły: komunikat commita przez plik (heredoc z polskimi znakami potrafi
  się zgubić — tym razem raz się zdarzyło, amend naprawił); epsilon remisowy
  czyta koszt z REJESTRU (obiektowy manaCost to sztuczka testów pinów).
