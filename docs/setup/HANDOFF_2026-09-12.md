# Handoff 2026-09-12 — zgłoszenia właściciela A (druk karty) i B (cienka biblioteka) + przegląd 509 kart + W3 (podział obrażeń blokera) i W4 (przydziały przed zadaniem obrażeń) + W5 (trample: lethal z całego kroku) + E6 (pętla jakości) + E7 (domknięcie) + B1–B4 (obserwacje audytu PR #113 i polityka bota) (PR #114, arena/01a0925f)

Sesja „kontynuujemy projekt" (ADR 0020/0021): PR #114 otwarty przed kodowaniem,
audyt PR #113 plik po pliku, plan w `docs/plans/PLAN_2026-09-11b-audyt-pr113-wyzwanie-3-5.md`.
W trakcie właściciel **zatrzymał poszukiwanie nowych błędów** i zlecił dwa
znaleziska z partii testowej (A — ilustracja karty, B — bot z cienką
biblioteką), a przy A rozszerzył zlecenie na przegląd wszystkich kart.
Stan gałęzi: `ea63934` (E6 — nazwy tokenów mechanik silnika) po `078e6ed` (E6/W5 — etykieta wizarda), `abd19d6` (odhaczenie E2), `9ee170e` (dokumenty W5), `fa52619` (W5), `be3780a` (pomiar W5), `cbf0938` (dokumenty W4), `41dc498` (W4), `c5adc97` (W3) — wszystko wypchnięte, PR #114 otwarty, baza `main@a2a5f0d`.

## Co zrobiono (w kolejności commitów)

- `17024f3` — plan sesji + triaż tez starego planu (wyzwanie 3–5/5). W3
  zlokalizowany i zmierzony: `combat.js` `processCombatPass` — obrońca zadaje
  PEŁNĄ moc każdemu atakującemu, którego blokuje, a CR 510.1a/d wymaga podziału
  sumy („divided as its controller chooses"); tylko trample zachowuje ograniczenie
  śmiertelności z 702.19b. Właściciel przekierował wtedy sesję na A i B;
  wstępny kierunek (eager per-pass blocker decision + interleaved dealing
  z deduplikacją po pierwszym indeksie) został w implementacji `c5adc97`
  **zastąpiony dwiema fazami przebiegu** — dokładnie w kolejności, którą
  narzuca CR 510.1 („First, the active player announces … then the defending
  player announces …"), bez deduplikacji i bez ryzyka podwójnego zadania
  obrażeń atakujących przy wznowieniu decyzji blokera. Leniwa pauza blokera
  odrzucona jak wcześniej.
- `7889ad4` — zgłoszenie A + przegląd wszystkich kart:
  - **Przyczyna A zmierzona, nie zgadywana**: `set: 'ISD'` w katalogu był
    poprawny od początku, JMP nigdy nie było setem karty w silniku. Błędny był
    wyłącznie `imageUri`: snapshot pobrano `cards/named?exact=Curiosity` **BEZ
    `set=`**, a Scryfall zwraca wtedy swój DOMYŚLNY druk nazwy (JMP #147).
    Warstwy FOT/KON biorą `imageUri` jako fallback przy braku pliku lokalnego
    (`docs/setup/ILUSTRACJE_KART.md`) — stąd JMP w wysokiej rozdzielczości.
  - Fix: pobranie set-aware, snapshot przepisany, `imageUri` → ISD #49
    (`b212c36a`), procedura w `docs/cards/HOW_TO_ADD_CARD.md`, nowy test
    `test/zgloszenie-a-druk-karty-z-arkusza.test.js` (A/1–A/6) z zapadnią
    `test/fixtures/druki-kart-zapadnia.json`.
  - **Przegląd 509 kart** (zlecenie właściciela, raport
    `docs/cards/WERYFIKACJA_DRUKOW_2026-09-12.md`): krok 1 offline (arkusz CSV →
    katalog → snapshot → UUID obrazu → `source`; 455 potwierdzonych), krok 2
    sieć (41 wpisów: 33 rekordy po UUID obrazu + 1 wyszukiwanie wsadowe; zgoda
    właściciela na `fetch_page` do api.scryfall.com z 2026-09-12). Poprawiono 9
    kart: curiosity JMP→ISD, expunge VMA→USG, welder-automaton GNT→AER,
    enter-the-enigma set MKM→DSK, angelic-benediction DVD→ALA #3, fireball
    CLB→JVC #56, spread-the-sickness MM2→MBS #56, cogwork-assembler 2XM→AER
    (set + obraz), shivs-embrace M14→M11 #156. Wspólny mechanizm: snapshot
    składany ręcznie bez `source` (przy fireball także zmyślony `set_name`
    „Jumpstart 2025" zamiast „Duel Decks Anthology: Jace vs. Chandra").
  - 26 snapshotów składanych ręcznie dostało `source` + `zweryfikowano`;
    zapadnia zmalała: `bezSetu` 46→43, `bezZrodla` 42→13, `uwagaSet` 4→1 (stan po `cdafc3c`).
    Przegląd odtwarzalny offline: `node tools/check-card-printings.mjs`.
- `76c40d9` — zgłoszenie B (bot z cienką biblioteką mieli się sam):
  - **Silnik** (`resources.js`): auto-tap przy płatności (CR 601.2h) nie narzuca,
    KTÓRE źródła płacą, więc dogrywka istniejącego pierwszeństwa kolorów odkłada
    na koniec źródła, których tapnięcie miele bibliotekę kontrolera
    (`millsLibraryOnTap` — trigger `self_becomes_tapped` na źródle albo
    `enchanted_permanent_tapped` na załączniku z `applyTo: 'enchanted_controller'`).
  - **Bot** (`heuristic-bot.js`): podatek biblioteczny liczony RAZ w
    `scoreCommand` i odejmowany w `finish` (jak wardTax — ścieżki wyceny rzutu
    mają wiele wyjść: aura, bestow, epsilon gęstości). Trzy składowe z DANYCH
    karty (ADR 0002): `tap_for_mana` na mielącym permanencie; płatność, której
    bez mielącego źródła nie da się złożyć (koszt − pula − czyste lądy); rzut
    karty tworzącej POWTARZALNY trigger doboru/millu (kwota × horyzont
    `repeatLibraryDrainTurns`). Triggery jednorazowe (`enter_battlefield`,
    `dies`) wyłączone — L41, bez podwójnego liczenia z `etbEnterBonusValue`
    i `drawDeckingPenalty`. `applyTo: 'enchanted_controller'` przy rzucie
    pomijane: odbiorca zależy od celu (Chronic Flooding na cudzym lądzie miele
    przeciwnika i zostaje zyskiem).
  - **Parametry** (`heuristic-params.js`): `libraryDeckOutPenalty` 120,
    `libraryThinPenalty` 60, `libraryThinPerCardPenalty` 6, `librarySafeMargin`
    20 (próg właściciela „~<20"), `repeatLibraryDrainTurns` 3. Kara rośnie
    stopniowo, więc przy zdrowej bibliotece przewaga kartowa zostaje wartością.
  - Testy: `test/zgloszenie-b-bot-cienka-biblioteka.test.js` (B/1–B/5, 11 testów),
    w tym strażnik rodziny rzutów wyprowadzonej z `COMMAND_TYPES` (lekcja M324/F1).

- `cdafc3c` — **Jwari Shapeshifter rozstrzygnięty**: właściciel potwierdził
  (2026-09-12), że `227ROE` w arkuszu to jego pomyłka i karta jest z WWK. Pomiar
  wskazywał to już przy przeglądzie: Scryfall nie zna druku ROE
  (`named?exact=Jwari+Shapeshifter&set=roe` → 404 „No cards found"), a katalog
  i obraz od początku niosły WWK nr 32 (id `587f91f6-b46e-4dd5-a86d-1048054dd3c0`,
  Kev Walker) — aplikacja pokazywała właściwy druk, rozjeżdżał się tylko arkusz
  (`ROE` to kolejny set bloku Zendikar i siedziba `Jwari Ruins`). Zmiany:
  `tools/collection-art-ids.csv` `227ROE` → `227WWK`, snapshot pobrany set-aware
  (`&set=wwk`) z `zweryfikowano` i bez `uwaga`, zapadnia `bezSetu` 44→43 oraz
  `uwagaSet` 2→1 (zostaje wyłącznie ethersworn-shieldmage), raport
  `WERYFIKACJA_DRUKOW_2026-09-12.md` zaktualizowany.

- **W3** (`c5adc97`): bloker blokujący dwóch lub więcej atakujących zadawał
  KAŻDEMU z nich pełną moc (7/6 vs dwóch 2/4 → 7 i 7 = 14 > moc 7); CR 510.1a/d
  wymaga podziału sumy, a CR 510.1 kolejności „najpierw atakujący, potem
  blokujący". Przebieg obrażeń ma teraz dwie fazy; decyzja przydziału należy do
  kontrolera BLOKERA (`pendingDamageAssignment` z `role: 'blocker'`, `phase`
  w resume, mapy przydziałów rozdzielone wg fazy — dwie decyzje w jednym
  przebiegu przechodzą po kolei); walidacja `validateBlockerDamageAssignment`
  (pełna suma = moc, sufit, żywe cele, BEZ lethal-first — to trample
  atakującego, CR 702.19b); wariant domyślny lethal-first dla botów (nadal
  JEDNA oferta, `finish(0)` bez zmian); widok i wizard znormalizowane
  (`sourceId`/`targets`/`targetKey`). Podwójny blok osiągalny przez statykę
  Cenn's Tactician (M166/E, `blockSlotsFor`). Testy W3/1–W3/7
  (`test/wyzwanie-3-bloker-dwóch-atakujacych-510-1d.test.js`) + test wizarda po
  stronie blokera; mutacje (L13) złapane: pełna moc na każdego, brak wymogu
  pełnej sumy, zły decydent, resume bez fazy, zły walidator, brak decyzji,
  widok bez roli.

- **W4** (`41dc498`): przydziały obrażeń ogłaszane PRZED zadaniem czegokolwiek,
  obrażenia zadawane równocześnie (CR 510.1/510.2), SBA dopiero po zadaniu
  (CR 510.3 + 704.3/704.5g), drugi przebieg to osobny krok (CR 510.4). Przebieg
  ma trzy fazy: `assign-attackers`, `assign-blockers`, zadanie — dzięki temu moc
  i lethal pochodzą ze stanu z początku przebiegu (infect z tego samego kroku nie
  zmniejsza przydzielonej mocy), a żaden stwór nie ginie w trakcie ogłaszania
  (przed W4 bloker z sondy nie zadawał NIC). Widok decyzji niesie wszystkie
  stwory czekające na przydział w przebiegu (jedna komenda zamyka fazę, CR
  510.1e), zebrane przydziały są niesione między decyzjami (`assignmentsSoFar`
  w `pendingDamageAssignment`, scalane w `execute`) i resetowane między
  przebiegami. Efekt uboczny, też regułowy: drugi i kolejny atakujący przebiegu
  dostaje prawdziwą decyzję zamiast domyślnego przydziału w ciszy. Testy
  W4/1–W4/8 (`test/wyzwanie-4-przydzial-przed-obrazeniami-510-2.test.js`);
  mutacje (L13) złapane: moc blokera liczona przy zadaniu zamiast w planie, brak
  scalania `assignmentsSoFar`, bloker z oznaczonymi obrażeniami nie zadaje,
  pominięcie faz przydziałów, brak resetu między przebiegami.
  **Golden master bota zregenerowany świadomie** (`bot-scoring-snapshot.mjs
  --write`): zmieniła się DOKŁADNIE jedna partia (tarkir-bg|warhammer-ubr@1000) —
  decyzje 221→222, nowy wpis to `resolve_damage_assignment` z score 0 (bot ma
  jeden wariant), `scoreSum` bez zmian (2989.0243), reszta partii bit w bit.
  Powód: silnik pyta teraz o przydział, który wcześniej brał w ciszy ten sam
  default. Sonde pomiarową zostawiono w `tools/probe-w4-infect-przydzial.mjs`.

- **W5** (`fa52619`): trample liczy lethal blokerów także z obrażeń
  PRZYDZIELANYCH w tym samym kroku przez inne stwory (CR 702.19b: „take into
  account damage already marked on the creature **and damage from other creatures
  that's being assigned during the same combat damage step**"; CR 702.2b: każde
  niezerowe obrażenia od źródła z deathtouch są lethal; CR 510.1e: sprawdza się
  SUMĘ przydziałów kroku). Przed zmianą przydział 0 na blokera + całość na gracza
  był odrzucany jako `trample_blocker_below_lethal`, choć drugi atakujący
  przydzielał blokerowi lethal (pomiar: `tools/probe-w5-trample-lethal-w-kroku.mjs`).
  Walidacja dostała kontekst całej mapy i przebiegu; widok niesie
  `assignedByOthers`/`lethalByOthers`, a bramka trample w wizardzie je uwzględnia.
  Polityka domyślna (lethal-first) i oferta botów BEZ zmian → golden master bez churn.
  Dodatkowo domknięta dziura klasy ZAWIESZENIE: komenda `resolve_damage_assignment`
  bez wpisu dla źródła bieżącej decyzji pytała w kółko o tę samą decyzję — teraz
  brak wpisu = akceptacja wariantu domyślnego z oferty (L48: oferta == walidacja).
  Testy W5/1–W5/8 (`test/wyzwanie-5-trample-lethal-z-tego-samego-kroku-702-19b.test.js`)
  + przypadek UI; 6 mutacji (L13) złapanych.

- **E6 — pętla jakości ZROBIONA** (`docs/audits/AUDYT_E6_ZYWY_TESTER_2026-09-12.md`):
  10 partii żywym testerem na świeżym `dist/` (greedy ×3, explorer, defensive ×2,
  impatient, hoarder ×2, random; g7–g10 uzupełniająco PO naprawach), lektura
  ręczna wzdłuż trzech osi. Dwa znaleziska, oba
  naprawione z weryfikacją dwustronną: F-E6-1 — „token_servo ginie" w logu
  (tokeny mechanik silnika bez nazwy; naprawa generyczna przez regułę sluga
  w `src/engine/tokens.js`, `ea63934`), F-E6-2 — wizard trample nie wyjaśniał,
  dlaczego po W5 można przydzielić mniej niż lethal (dopisek „od innych w tym
  kroku: N", `078e6ed`). Żadna partia nie utknęła, `NIEWYCENIONE` puste,
  w g2 oba nowe wizardy W3/W4/W5 przeszły przez prawdziwy artefakt; partie
  uzupełniające g7–g10 czyste (0 detektorów, 0 utknięć, `NIEWYCENIONE` puste).

## Bramki (zmierzone na końcu sesji)

- `npm test`: **5183/5183** (~198 s) — baza 5150 + 6 testów A + 11 testów B
  + 7 testów W3 + 1 test wizarda po stronie blokera + 8 testów W4.
- `npm run test:all`: **5205/5205** (pomiar E7, po W5 i E6).
- `npm run build`: OK, 61 modułów / 3515,4 kB.
- `node tools/check-card-printings.mjs`: kod wyjścia **0**; rozjazd katalog ↔
  arkusz dokładnie **1** i jest udokumentowany `uwaga` (ethersworn-shieldmage,
  `536CON` = skrót płaszczyzny Alara, druk ARB — decyzja właściciela 2026-08-05).
- Quick benchmark (`node tools/benchmark.mjs`, 672 mecze, 145,6 s): heuristic
  **84,2%** (566/672), aggro 26,5% (89/336), random 5,1% (17/336) — wynik
  IDENTYCZNY przed i po W3 oraz po W4 (nowe decyzje nie wieszają botów: L48,
  oferta == walidacja, boty biorą jedyny wariant). Przed B: 84,7% (569/672) — 3 mecze
  różnicy za cenę tego, że bot nie deck-outuje się sam. Bez pełnego B0 (~23k
  meczów) — ADR 0018, decyzja właściciela.
- Golden master `bot-scoring-snapshot`: **bez churn** po B i po W3; po W4
  zregenerowany ŚWIADOMIE (jedna partia, +1 decyzja `resolve_damage_assignment`
  z score 0, `scoreSum` bez zmian — uzasadnienie wyżej i w opisie commitu).

### Aktualizacja 2026-09-12c (B1–B6, po E7) — liczby z pomiaru, nie przepisane

- `npm test`: **5232/5232** (5201 po E7; +8 testów B2 `b2-odcisk-straznik-pokrycia`,
  +9 testów B4 `b4-hascreaturetype-rodzina`, +14 testów B5 `b5-bramka-logu-gracza`).
- `npm run build`: 61 modułów / **3533,1 kB**.
- Quick benchmark: heuristic **84,2% (566/672)**, aggro 26,5% (89/336), random
  5,1% (17/336), 672 mecze w 135,0 s — IDENTYCZNIE jak przed B1/B2/B4 (odcisk nie
  jest w gorącej pętli AI: używają go tylko sonda no-op i replay).
- Golden master `bot-scoring-snapshot`: **bez zmian** po B1 i po B4 — ani sytuacja
  „lethal pokryty przez innego atakującego", ani rozjazd podtypów z `typeGrant`
  nie występują w korpusie 672 meczów.
- Żywy Tester na świeżym `dist/` (L76): **10 partii** (greedy, impatient, explorer,
  defensive; po B2, po B4 i po B5 — osobne zestawy talii i seedów), **666 sond
  no-op**, **0 zgłoszeń detektorów**, 0 niewycenionych ruchów bota; transkrypty
  w `tmp-audyt-b2-2026-09-12/`, `tmp-audyt-b4-2026-09-12/` i
  `tmp-audyt-b5-2026-09-12/` (poza repo, M239).
- **B5 (`f910765`) — F5 naprawione**: wrostkowa bramka głównego logu gracza
  (14 członów w `noteBotMove`, lokalne domknięcia i lokalne zbiory w
  `createSession`) wyekstrahowana do czystych funkcji modułowych `isMainLogEvent`
  / `phaseHeaderText` / `isHumanControllerEvent`, zbiory `MAIN_LOG_NOISE`,
  `TRANSFORM_DIGEST_EVENTS`, `HUMAN_DIGEST_EVENTS`, `BOT_RESOLUTION_EVENTS` na
  poziomie modułu. Równoważność udowodniona sondą wobec transkrypcji 1:1 starego
  warunku: **68 208 kombinacji, 0 rozjazdów**; mutacje 9/9 złapane. To zamyka
  klasę „zmiana bramek UI weryfikowana tylko ręcznym czytaniem transkryptów"
  (D/E, M99, M100/E5, M100/E8, M106/Z3, M151, M167/E, E2, E6/A2, E7/E).

### Aktualizacja 2026-09-12d (zapadnia A, klasa E, drugie strony, wizard blokera)

- `npm test`: **5255/5255** (5232 po B6; +15 D `druki-druga-strona-i-uuid-obrazu`,
  +1 A/7 w ratchecie, +7 B `w3-wizard-podzialu-blokera-ui`). Ratchet druków: **7/7**;
  fixture `bezSetu` 0 / `bezZrodla` 0 / `uwagaSet` 1.
- `npm run build`: 61 modułów / **3533,4 kB** (było 3533,1 kB — przyrost to gałąź
  etykiety w `renderDamageWizard`).
- `node tools/check-card-printings.mjs`: exit **0**; klasy A 349 / B 110 / B2 7 /
  brak-snapshotu 43, problemowe C/D/E/F i B-source-uuid-ROZJAZD = **0**,
  DO POBRANIA = **0**, UUID obrazu potwierdzony offline 458.
- Żywy Tester: 3 partie na świeżym `dist/` — **129 sond no-op, 0 zgłoszeń
  detektorów**, 0 niewycenionych ruchów bota (`tmp-audyt-w3ui2-2026-09-12/`).
- Quick benchmark uruchomiony PONOWNIE po zmianie UI (dla pewności, nie z pamięci):
  672 mecze w 144,2 s, heuristic **84,2% (566/672)**, aggro 26,5% (89/336),
  random 5,1% (17/336) — IDENTYCZNY jak przed W3/W4/W5/E6/B1/B2/B4 i jak przed
  obu etapami druków (żadna z tych zmian nie dotyka gorącej pętli AI: odcisku
  używają tylko sonda no-op i replay). Golden master `bot-scoring-snapshot` bez
  churn — potwierdzony przejściem całego `npm test` (5255/5255), a nie odczytem
  wizualnym.

## Kolejka / otwarte (nie zapomniane)

- **HISTORIA (zrealizowane, nie kolejka):** W3 (`c5adc97`), W4 (`41dc498`),
  W5 (`fa52619`), E6 (`078e6ed`+`ea63934`), E7 (`b87bb3f`) — plan
  `PLAN_2026-09-11b-audyt-pr113-wyzwanie-3-5.md` odhaczony w całości; szczegóły
  i sonda W4 (`tools/probe-w4-infect-przydzial.mjs`, Chained Throatseeker +
  Gurmag Drowner vs Segmented Krotiq) w `docs/PROJECT_HISTORY.md` i w opisie
  PR #114 §5–§9. Wcześniejszy wpis tej sekcji („następne w kolejce: W5 —
  kandydat nie wybrany … potem W5, E6, E7") był NIEAKTUALNY i sam sobie
  przeczył (klasa F8: nieaktualna treść w dokumencie, który jest punktem
  zaczepienia następnej sesji) — skasowany zamiast przepisywany.
- **Panele walki w UI — stan zmierzony 2026-09-12 (korekta własnego wpisu).**
  Wcześniejszy zapis w tej sekcji („panel podziału obrażeń nie wyrenderował się
  ani razu") był MYLĄCY i został zmierzony ponownie po uwadze właściciela
  („przecież jest panel, jak blokuję"): panel DEKLARACJI BLOKÓW jest i był
  ćwiczony — „Zatwierdź bloki" w 10 transkryptach, „Deklaracja blokujących"
  w 12, `[modal choice] Wybierz blokujących (przełączniki przy każdym
  atakującym): Zatwierdź bloki / Bez bloków / Zamknij (dokończysz później)`.
  Wizard PODZIAŁU OBRAŻEŃ też się renderuje — od strony ATAKUJĄCEGO
  (`tmp-audyt-b5-2026-09-12/kaladesh-51-greedy.txt`, linie 426–444): „Makeshift
  Mauler i Farbog Explorer blokują Merchant's Dockhand" → `[modal choice]
  Rozdziel obrażenia bojowe — przydziel moc atakujących blokującym: Merchant's
  Dockhand (moc 6) | Makeshift Mauler (wytrz. 5, śmiertelne 5) −15+1↑↓ |
  Farbog Explorer (wytrz. 3, śmiertelne 3) −11+1↑↓ | Zatwierdź przydział |
  Użyj domyślnego przydziału (zabójcze obrażenia po kolei blokerów) | Zamknij`;
  profil kliknął „Użyj domyślnego przydziału", a log pokazał skutek 5 + 1
  (lethal-first w kolejności deklaracji, CR 510.1c) — czyli etykiety rozróżniają
  strony (`choiceGroupLabel`), wiersze niosą wytrzymałość i kwotę śmiertelną,
  a domyślny przydział z B1 jest widoczny dla gracza.
- **Wizard po stronie BLOKERA (W3) — ZAMKNIĘTY 2026-09-12** (`68c3801`, zlecenie
  właściciela „zrób ten wizard podziału po stronie blokera skoro jest potrzebny").
  Pomiar wykazał, że sam wizard ISTNIAŁ i był już częściowo pokryty: ścieżka
  w silniku (`combat.js` — `pending.role === 'blocker'`, budowa widoku, emisja
  `damage_assignment_required`, `validateBlockerDamageAssignment`), normalizacja ról
  w `renderDamageWizard`, test UI z syntetycznym `role: 'blocker'`
  (`test/choice-request-ui.test.js` — „renderDamageWizard (W3): podział blokera →
  assignments z kluczami attackerId": intro, nagłówek „Highland Game (moc 7)",
  lethal-first 4+3, bramka niedoboru, brak „do gracza") oraz testy regułowe W3/1–W3/7.
  **Korekta przesady**: w opisie commitu `68c3801` napisałem, że „nie było ani
  jednego dowodu, że ten ekran działa" — to nieprawda, dowody powyżej istniały.
  Historii nie przepisuję (force push zakazany), więc korekta jest tutaj.
  **Co faktycznie brakowało i co dodaje ten commit**: (a) istniejący test UI wołał
  wizard z `defaultCommand: null`, więc ścieżka przycisku „Użyj domyślnego
  przydziału" przy roli blokera nie była pokryta — i dokładnie tam przetrwał
  DEFECT: etykieta zaszyta po stronie atakującego („zabójcze obrażenia po kolei
  blokerów") mimo odwróconych ról; teraz jest role-aware („po kolei atakujących");
  (b) nie było testu spinającego PRAWDZIWY stan gry → `playerView(p1)
  .pendingDamageAssignment` → wizard → komendę → `execute` → obrażenia (syntetyczny
  `pending` mógł rozjechać się z tym, co naprawdę buduje silnik); (c) nie było
  równoważności bramki UI z walidatorem silnika. Nowy
  `test/w3-wizard-podzialu-blokera-ui.test.js` (B/1–B/7) domyka wszystkie trzy:
  deterministyczny scenariusz stołu (segmented-krotiq 6/5 + licznik +1/+1 = 7/6
  blokuje DWA gurmag-drowner 2/4 dzięki statyce Cenn's Tactician), zgodność
  bramka-UI ≡ walidator-silnika na 36/36 osiągalnych podziałach, 3 podziały ponad
  moc odrzucone (`damage_exceeds_power`), podział 0/7 inny niż lethal-first
  zastosowany dosłownie (CR 510.1c), brak etykiety trample po stronie blokera
  (CR 702.19b). Żywy Tester na świeżym `dist/`: 3 partie (worek-mroczny/kaladesh
  greedy seed 51, innistrad-wu/tarkir-bg impatient seed 2026, kaladesh/alara
  defensive seed 44) — 129 sond no-op, 0 zgłoszeń detektorów, 0 niewycenionych
  ruchów; panele deklaracji bloków obecne („Zatwierdź bloki" ×4, „Deklaracja
  blokujących" ×5), wizard podziału w tych partiach NIE wystąpił (wymaga podwójnego
  bloku — dodatkowy blok daje w katalogu jedna karta,
  `grantsExtraBlockWithCounter`, `card-data.js:7642`, tylko w
  `decks/worek-mroczny.txt`, i potrzeba jeszcze licznika +1/+1 w momencie
  deklaracji). Transkrypty: `tmp-audyt-w3ui2-2026-09-12/` (poza git, M239).
- **Obserwacje audytu PR #113 — ZAMKNIĘTE pomiarem 2026-09-12** (werdykty, dowody
  i metoda: `docs/audits/AUDYT_PR113_2026-09-11.md`; sam plik audytu w sesji
  2026-09-11 nie powstał — wpis E2 w `PLAN_2026-09-11b` skorygowany):
  **F1** naprawione (`ebae99d` — odcisk: 51 pól obiektu i 23 klucze stanu wróciły
  do projekcji, wykluczenia etykiet jawne z powodem, strażnik pokrycia;
  `madnessQueue` dopisane do `PENDING_DECISION_FIELDS`); **F7** naprawione
  (`1e23847` — test rodzinny `hasCreatureType`: 30 miejsc w 8 modułach, zero
  pokrycia, a przy okazji znaleziony i usunięty rozjazd `effectiveSubtypesOnBattlefield`
  vs `effectiveSubtypes` wbrew CR 305.7/613.1d); **O4** naprawione (`a00707d` —
  jedna implementacja przynależności do przebiegu first strike, były trzy kopie);
  **F8** naprawione (liczby z pomiaru: „11 pól" → 51+23, „23 miejsca" → 30);
  **F2** i **F3** odrzucone z dowodem (F2: `armedOnTurn` jest diagnostyczne,
  rejestr czyści cleanup `permanents.js:1136`; F3: wszystkie trzy emisje
  `permanent_cast` niosą `object`); **F5** potwierdzone częściowo — bez pinów są
  lokalne domknięcia bramki logu (`phaseHeaderFor` 2162, `TRANSFORM_DIGEST_EVENTS`
  2441, `recordTurnEvent`, wrostkowa bramka 14 warunków ~2623), a eksportowane
  `isBotMoveNoise` i `describeGameEvent` (69 plików testów) piny mają — naprawione
  jako B5 (`f910765`): ekstrakcja bramki do `isMainLogEvent` + test rodzinny
  B5/1–B5/14 + dowód równoważności na 68 208 kombinacjach; **O1** nie do odtworzenia (`grep sentinel` w src/
  znajduje tylko nazwy kart — potrzebny opis właściciela); **O3** bez akcji;
  **F4** nie wraca.
- **Kolejka po B1–B6 — stan 2026-09-12 po decyzjach właściciela**: (1) O1
  („martwy sentinel") — **właściciel polecił pominąć**, nie wraca do kolejki
  (pomiar i tak nie znalazł kandydata w logice: `grep -rn sentinel src/` zwraca
  tylko nazwy czterech kart); (2) zapadnia druków — **ZAMKNIĘTA** (`c0a1908`,
  szczegóły niżej); (3) do decyzji właściciela: squash-merge PR #114, pełny B0
  (ADR 0018 — bez polecenia nie uruchamiamy).
- Polityka bota przy przydziałach obrażeń (zlecenie właściciela) — **B1 zrobione**
  (`2d30130`): domyślny przydział trample korzysta z lethal pokrywanego przez inne
  stwory w tym samym kroku (CR 702.19b/702.2b), przydziały liczone sekwencyjnie
  w kolejności deklaracji, pokrycie czytane tylko z ogłoszonych (`onlyAssigned` —
  bez tego dopłatę płaciłby ostatni atakujący zamiast pierwszego). Reguły bez zmian
  (W5 już to zalegalizowało), golden master i benchmark bez zmian — wartość to
  spójność z walidatorem W5 i lepszy punkt startowy wizarda dla człowieka.
- Zapadnia druków (`test/fixtures/druki-kart-zapadnia.json`) — **ZAMKNIĘTA
  2026-09-12** (`c0a1908`): `bezSetu` **43 → 0**, `bezZrodla` **13 → 0**,
  `uwagaSet` 1 bez zmian (`ethersworn-shieldmage`, druk ARB potwierdzony przez
  właściciela). Sieć okazała się NIEpotrzebna do samej naprawy: pomiar wykazał,
  że treść wszystkich 56 snapshotów już była zgodna z arkuszem (set snapshotu ==
  set z `tools/collection-art-ids.csv` bez względu na wielkość liter, UUID z
  `image_uris` == `imageUri` katalogu, obecne `name`/`set`/`collector_number`) —
  brakowało wyłącznie jawnego `source`. 13 plików to surowe odpowiedzi Scryfall
  z własnym `uri = /cards/<id>` (i `id` == UUID obrazu), więc `source` jest kopią
  tego pola; 43 pliki dostały `source = https://api.scryfall.com/cards/<UUID z
  image_uris>` plus klucz `proweniencja` z datą, metodą i POPRZEDNIM adresem.
  Pomiar klas narzędzia `tools/check-card-printings.mjs`: C 13 → 0, F 43 → 0,
  B-source-uuid-spójny 32 → 88, A 349 i E 21 bez zmian. Zakres online jest wąski
  i tak udokumentowany: schemat `/cards/<id>` potwierdzono JEDNYM pobraniem
  (Apprentice Wizard, `2xm` nr 40), resztę przerwała awaria proxy środowiska.
  Raport z metodą i tabelą przed/po: `docs/audits/WERYFIKACJA_DRUKOW_KOLEKCJI_2026-09-12.md`.
- Temat druków — **ZAMKNIĘTY 2026-09-12** (`3821187`, zlecenie właściciela „zrób
  teraz to co proponujesz — 21 klasy E"): klasa **E 21 → 0** (wszystkie zacieśnione
  do `/cards/<id>` z kluczem `proweniencja` i zachowanym poprzednim adresem; dla
  `scorned-villager` UUID z `card_faces[0].image_uris`, bo transform nie ma
  `image_uris` na wierzchu). Siedem kart „do pobrania z sieci" to — jak wskazał
  właściciel — **drugie strony kart dwustronnych**; pomiar to potwierdził (layout
  `transform`, ten sam UUID obrazu co snapshot przedniej twarzy, nazwa obecna w jej
  `card_faces`, w arkuszu ten sam `artId`+set, np. 486ISD), więc decyzja: **osobne
  snapshoty NIE są potrzebne** (Scryfall opisuje transform jako jeden obiekt karty —
  duplikat byłby drugim źródłem prawdy dla jednego druku). Narzędzie dostało klasę
  `B2-druga-strona-pokryta-snapshotem`, która wymaga, by nazwa naprawdę była twarzą
  snapshotu „brata" — samo współdzielenie UUID nie wystarcza (przypadek ujemny
  z danych: `token_rat` dzieli UUID z `lab-rats`). Pomiar po: A 349, **B 88 → 110**,
  **B2 7**, `brak-snapshotu` 51 → **43** (wszystkie poza arkuszem), klasy **C/D/E/F
  i B-source-uuid-ROZJAZD = 0**, **DO POBRANIA = 0**, UUID obrazu potwierdzony
  offline **455 → 458**, rozjazd 1 (udokumentowany `uwaga`), exit 0. Przy okazji
  trzy root cause'y: (i) UUID obrazu czytany także z `card_faces[0]` (inaczej
  transformy nie potwierdzały druku i dawały FAŁSZYWY rozjazd „source wskazuje inną
  kartę"), (ii) porównanie setu tylko gdy obie strony mają set (44 wpisy syntetyczne
  mają w katalogu `set: null` — `token_tarmogoyf: tm3c vs null` był fałszywym
  rozjazdem), (iii) jedna implementacja helperów — ratchet importuje `uuidOf`/
  `sourceUuid`/`sourceSet`/`uuidObrazuSnapshotu` z narzędzia zamiast własnych kopii
  (L41/F7: to dryf dwóch kopii sprawił, że test i narzędzie różnie widziały
  transformy); narzędzie ma eksporty + main-guard, przegląd jest czystą funkcją
  `przegladDrukow`. Plus nazwa snapshotu tokena niezgodna z konwencją
  (`scryfall-token-tarmogoyf.json` przy id `token_tarmogoyf`) była dla przeglądu
  NIEWIDOCZNA — `git mv` + ścieżka w `test/real-cards-batch21.test.js`. Nowy test
  `test/druki-druga-strona-i-uuid-obrazu.test.js` (D/1–D/15, w tym mutacja pokrycia
  i przypadek ujemny `token_rat`), ratchet rozszerzony o A/7. Zostaje tylko sierota
  `docs/cards/scryfall-undercity-dungeon.json` (dane dungeonu — silnik tworzy go
  dynamicznie jako `undercity`, karty nie ma w rejestrze; `source` kanoniczne,
  pilnuje jej D/14).

## Pułapki środowiska (potwierdzone w tej sesji)

- **Przywrócenie workspace potrafi cofnąć lokalny ref gałęzi i usunąć pliki
  nieśledzone** (zmierzone 2026-09-12): w środku sesji `HEAD` wrócił do punktu
  wyjścia brancha (`a2a5f0d`), 153 pliki wyglądały na niecommitowane, a 21 plików
  obecnych na remote (raport, handoff, testy B1/B2/B4/B5, sonda W5) ZNIKNĘŁO z
  drzewa; transkrypty `tmp-audyt-*/` i `tools/table-tester/node_modules` również.
  Procedura, która zadziałała bez straty: (1) zabezpieczyć jedyną niecommitowaną
  pracę (kopia do `/tmp`), (2) `git fetch origin <branch>`, (3) `git diff
  --name-status FETCH_HEAD` — co jest M (moja praca), a co D (utracone),
  (4) `git reset --hard FETCH_HEAD` (fast-forward, bez przepisywania historii),
  (5) odtworzyć kopię, (6) `cd tools/table-tester && npm ci` (jsdom z committed
  lockfile). Zawsze porównywać `git log` z `git ls-remote` — o commitach decyduje
  remote. Uwaga na `&&` po `grep`: pusty wynik przerywa łańcuch i dalsze kroki
  (w tym zapisy) nie wykonują się — raz kosztowało to „poprawkę", która nigdy nie
  zaszła.
- `curl`/`fetch` z piaskownicy do Scryfall jest zablokowany — sieć mają tylko
  `fetch_page` i `web_search`. Nie ponawiać prób shellowych.
- `fetch_page` potrafi oddać URL z uszkodzonym proxy (SignatureDoesNotMatch).
  Gdy zacznie się psuć, nie ponawiać w kółko — dowód offline zwykle wystarcza
  (tu: 404 z `named?exact=…&set=roe` był rozstrzygający bez kolejnych pobrań).
- Przepisywanie snapshotu MUSI zachować zestaw pól: `test/real-cards-batch29.test.js`
  i `batch30` asertują `object: 'card'`, `oracle_text`, `mana_cost`. W tej sesji
  przepisanie curiosity/expunge/welder-automaton zgubiło `object`/`layout`
  i Batch 29 zaświecił się na czerwono (przywrócone).
- Git w piaskownicy ma refspec tylko dla `main`: `origin/arena/01a0925f-mtg`
  nie istnieje lokalnie, więc stan zdalny sprawdzaj przez
  `git ls-remote origin refs/heads/arena/01a0925f-mtg` (przed każdym pushem).
- Inwarianty stanu: załącznik wymaga `kind: 'aura'` (albo `equipment`) oraz
  istniejącego gospodarza — sam `attachedTo` rzuca `assertStateInvariants`.
- Trigger z tapnięcia odpala się tylko przez `execute` (zdarzenie
  `object_tapped`); goły `spendMana` tapuje bez przetwarzania triggerów.
- **Reset piaskownicy w środku sesji** (potwierdzone 2026-09-12): drzewo robocze
  przetrwało, ale historia gita została sklonowana od nowa — HEAD skoczył na bazę
  `main@a2a5f0d`, a commit, który nie zdążył się wypchnąć, zniknął (pliki zostały
  w drzewie jako modyfikacje). Ratunek: `git fetch origin refs/heads/<branch>` →
  `git reset --mixed FETCH_HEAD` (**nie `--hard`** — w drzewie są niezcommitowane
  pliki) → `git status` pokazuje tylko realną różnicę. Pliki poza repo
  (`/home/user/pr-body.md`, `/home/user/msg.txt`, `/tmp/*`, `.arena/`) **nie
  przetrwały** — opis PR i komunikaty commitów trzeba było odtworzyć.
- Polskie znaki: edycja przez `python3`/`pathlib`, nie `edit_file`; komunikaty
  commitów z pliku poza repo (`/home/user/msg.txt`); `gh pr edit` na tym repo
  nie działa — `gh api -X PATCH repos/jurekjurekgh/mtg/pulls/114 -F body=@plik`.

## Plany / dokumenty

- `docs/plans/PLAN_2026-09-11b-audyt-pr113-wyzwanie-3-5.md` — plan sesji.
- `docs/cards/WERYFIKACJA_DRUKOW_2026-09-12.md` — raport przeglądu druków
  (metoda, pokrycie, tabela 41 wpisów zweryfikowanych online, odstępstwa).
- `docs/cards/HOW_TO_ADD_CARD.md` — procedura: pobranie JEDNEGO rekordu
  set-aware, arkusz kolekcji autorytetem druku.
- `tools/check-card-printings.mjs` — przegląd offline (kod wyjścia 0/1).
- Testy: `test/zgloszenie-a-druk-karty-z-arkusza.test.js` (+ zapadnia
  `test/fixtures/druki-kart-zapadnia.json`), `test/zgloszenie-b-bot-cienka-biblioteka.test.js`.

## Domknięcie sesji (E7, 2026-09-12)

Plan `PLAN_2026-09-11b-audyt-pr113-wyzwanie-3-5.md` jest odhaczony w całości
(E1, E2, W3, W4, W5, E6, E7). Bramki końcowe: `npm test` **5195/5195**,
`npm run test:all` **5205/5205**, `npm run build` 61 modułów / **3523,6 kB**,
quick benchmark **84,2% (566/672)** (aggro 26,5%, random 5,1%, 141,4 s) — wynik
IDENTYCZNY jak przed W4/W5/E6, bez zawieszeń. Golden master bota: po W4
zregenerowany świadomie (jedna partia, +1 decyzja z score 0), po W5 i E6 bez churn.

Stary plan `PLAN_2026-09-11-wyzwanie-5-bledow-zasad.md` skorygowany: tezy 3 i 5
nieaktualne (pomiar + źródła online), tabela stanu pięciu wyzwań, kroki E4/E6
skreślone, E5 = W3. Wyzwanie właściciela (5 unikalnych błędów vs CR) domknięte:
1–2 w PR #113, 3–5 w PR #114 (W3/W4/W5).

Do decyzji właściciela: squash-merge PR #114 (kończy sesję) oraz ewentualny pełny
benchmark B0 (`--full`, ~23k meczów) — bez polecenia nie uruchamiamy (ADR 0018).
Materiał na kolejną sesję: obserwacje audytu PR #113 (F1–F8, O1/O3/O4) oraz
polityka bota przy przydziałach obrażeń (domyślny lethal-first nie wykorzystuje
pokrycia lethal przez inne stwory — po W5 jest to legalne, ale bot z tego nie
korzysta; zmiana dotyczyłaby wyceny, nie reguł).

## Stan końcowy sesji (2026-09-12d) — NADPISUJE „Domknięcie sesji (E7)" powyżej

Sekcja E7 wyżej jest zapisem historycznym z chwili `b87bb3f` (bramki 5195/5195,
3523,6 kB) i jej lista „materiał na kolejną sesję" jest już nieaktualna: obserwacje
audytu PR #113 (F1–F8, O1/O3/O4) zostały rozliczone pomiarem w turze B (werdykty
w sekcji „Kolejka / otwarte"), zapadnia druków zamknięta w dwóch etapach, a polityka
bota przy przydziałach obrażeń domknięta jako B1.

Ostatnie trzy commity sesji:

- `3821187` — drugi etap druków: klasa E 21 → 0 (zacieśnienie `source` do
  `/cards/<UUID>` z pomiarem przed zapisem), nowa klasa B2 dla siedmiu tylnych
  twarzy transformów (decyzja: BEZ osobnych snapshotów, z uzasadnieniem i
  mierzalnym warunkiem pokrycia), trzy przyczyny źródłowe naprawione w narzędziu
  (UUID z `card_faces`, porównanie setu tylko gdy obie strony mają set, koniec
  dryfu dwóch implementacji helperów), nazwa snapshotu tokena Tarmogoyf zgodna
  z konwencją, testy D/1–D/15 + A/7.
- `68c3801` — wizard podziału obrażeń po stronie BLOKERA: etykieta domyślnego
  przycisku zależna od roli („po kolei atakujących") + test end-to-end B/1–B/7
  (realny stan → `playerView` → DOM → komenda → `execute`) z równoważnością bramki
  UI i `validateBlockerDamageAssignment` na wszystkich 36 osiągalnych podziałach.
  W opisie tego commitu padła przesada („nie było ani jednego dowodu") — istniał
  syntetyczny test roli `blocker` w `test/choice-request-ui.test.js`, który
  przechodził `defaultCommand: null` i dlatego wady etykiety nie łapał. Korekta
  jest tu i w `docs/PROJECT_HISTORY.md`; przepchnięta historia NIE jest przepisywana.
- `3286467` — dokumenty: ten plik, raport
  `docs/audits/WERYFIKACJA_DRUKOW_KOLEKCJI_2026-09-12.md` (sekcja 6 = stan końcowy
  z tabelą przed/po, sekcja 7 = metoda i pomiary drugiego etapu, sekcja 8 =
  odtworzenie z komendami), dwa wpisy w `docs/PROJECT_HISTORY.md`. Opis PR #114
  zaktualizowany przez REST PATCH (`gh api -X PATCH .../pulls/114 -F body=@plik`):
  nowa sekcja 12, bramki i lista „Otwarte" odświeżone, tabela z sekcji 11
  oznaczona jako pomiar po ETAPIE 1.

Bramki końcowe sesji: `npm test` **5255/5255**, `npm run build` 61 modułów /
**3533,4 kB**, quick benchmark **84,2% (566/672)**, narzędzie druków exit 0
(DO POBRANIA 0, rozjazd 1 udokumentowany), Żywy Tester łącznie **13 partii /
795 sond no-op / 0 zgłoszeń detektorów / 0 niewycenionych ruchów bota**,
CI PR #114 zielone (`3286467`, 4m28s).

Prawdziwy materiał na kolejną sesję (nie z listy E7):

- wycena bota przy przydziałach obrażeń: domyślny lethal-first nie wykorzystuje
  pokrycia lethal przez inne stwory — po W5 i B1 jest to legalne, ale bot z tego
  nie korzysta (zmiana dotyczyłaby wyceny, nie reguł);
- `docs/cards/scryfall-undercity-dungeon.json` — sierota: snapshot karty, której
  nie ma w rejestrze (silnik tworzy dungeon dynamicznie jako `undercity`). Test
  D/14 pilnuje, żeby nie przybywało takich plików; decyzja, czy dungeon ma dostać
  wpis w rejestrze, należy do właściciela (ADR 0029: katalog rośnie tylko
  z kolekcji właściciela);
- po odtworzeniu środowiska pracy: `cd tools/table-tester && npm ci --no-audit
  --no-fund` (tester stołu ma WŁASNY manifest i lockfile z `jsdom` ^24.0.0 — nie
  jest zależnością głównego `package.json`), a `node tools/table-tester/run-game.mjs`
  zapisuje transkrypty względem SWOJEGO katalogu.

Do decyzji właściciela pozostaje wyłącznie squash-merge PR #114 (kończy sesję)
oraz ewentualny pełny benchmark B0 (`--full`, ~23k meczów) — bez polecenia nie
uruchamiamy (ADR 0018).

## Aktualizacja 2026-09-12e — oba punkty „materiału na kolejną sesję" domknięte

Właściciel zlecił dokładnie te dwie pozycje, które sekcja „Stan końcowy sesji
(2026-09-12d)" zostawiła jako materiał na kolejną sesję, i obie są zamknięte
(plan tury: `docs/plans/PLAN_2026-09-12e-wycena-przydzialow-i-obiekty-wsparcia.md`,
odhaczony w całości z wynikiem).

**1. Wycena bota przy przydziałach (`08f6638`).** Pomiar pokazał, że bot NIE ma
czego wyceniać: `legalCommands` oferuje dokładnie jeden wariant
`resolve_damage_assignment` (M66/R), a `heuristic-bot.js` odpowiada
`return finish(0)` — jedyną dźwignią jest jakość tego deterministycznego planu.
Pokrycie lethal istniało tylko w gałęzi trample (B1), a strona blokera nie miała
kontekstu wcale (`defaultBlockerDamageAssignment` bez parametru,
`buildDefaultDamageAssignments` bez mapy sekwencyjnej dla `role === 'blocker'`).
Zmierzone straty sprzed zmiany: atakująca 4/4 bez trample blokowana przez 3/3
i 3/3, lethal pierwszego pokryty przez trzeciego stwora → plan `[3,1]` i jeden
zabity (drugi bloker przeżywał z 1 obrażeniem); po zmianie `[0,4]` i dwa zabite.
Symetrycznie po stronie blokera: moc 4 na dwóch atakujących (2/2 i 3/3), lethal
pierwszego pokryty przez drugiego blokera → `[2,2]` i jeden zabity; po zmianie
`[0,4]` i dwa zabite. To WYCENA, nie reguły: suma przydziału bez zmian
(CR 510.1a), walidatory nietknięte (P/7 sprawdza każdy plan w
`validateDamageAssignment`/`validateBlockerDamageAssignment`), liczba ofert bez
zmian (M66/R), człowiek dostaje ten sam plan jako punkt startowy wizarda.
Świadoma korekta testu B1/5: jego druga asercja utrwalała dokładnie to
zachowanie, które właściciel kazał zmienić (`[{b1:3},{b2:3}]` → `[{b1:0},{b2:6}]`);
pierwsza część (bez pokrycia → `[2,4]`) zostaje, tak samo B1/1–B1/4 i B1/6.
Bramki: `npm test` **5273/5273**, build 61 modułów / **3538.7 kB**, golden master
**bez churn**, quick benchmark **84.2% (566/672)** identycznie (poprawka
symetryczna — obie strony biorą ten sam plan, więc macierz win-rate jej nie
pokazuje), Żywy Tester **7 partii / 317 sond no-op / 0 zgłoszeń detektorów /
0 niewycenionych ruchów bota** (`tools/table-tester/tmp-audyt-p-2026-09-12/`,
M239). W partii m3 był żywy wizard przydziału wielu blokerów (Skilled Animator
moc 1 vs Tiller of Flesh lethal 4 + Incubator lethal 2 — bez pokrycia, plan
lethal-first 1/0, `execute` OK); podwójnego bloku po obu stronach nie trafiła
żadna z 7 partii, więc dowodem są testy deterministyczne P/1–P/10.

**2. Sierota `scryfall-undercity-dungeon.json` (`f1062cd`).** Właściciel: karta
jest w grze, panel specjalny po „Take the initiative" działa i NIE wolno go
zepsuć; czy JSON jest używany — nie wie, decyzja agenta. Pomiar: plik cytowany
wyłącznie przez test D/14 i dokumenty; `docs/cards/*.json` nie są czytane w
runtime ani w buildzie (to materiał źródłowy do ręcznego kodowania
`card-data.js`); panel bierze obraz z `UNDERCITY_DUNGEON.imageUri`
(`https://api.scryfall.com/cards/tclb/20?format=image`), a snapshot ma
`legacy_image_uri` IDENTYCZNE z tym adresem oraz pełny Oracle obu twarzy —
jedyny zapis komnat, z którego zakodowano `UNDERCITY_ROOMS` w
`src/engine/effects.js`. W arkuszu kolekcji nie ma ani `undercity`, ani `tclb`.
**Decyzja: NIE kasować** (proweniencja adresu panelu i Oracle obu twarzy) i
**NIE dopisywać do rejestru** (ADR 0029 — katalog rośnie wyłącznie z kolekcji
właściciela; zmieniłoby to liczbę kart 509 i klasyfikację narzędzia). Przyczyna
źródłowa „sieroctwa": model przeglądu znał tylko karty z rejestru, a obiekty
wsparcia są eksportami tego samego modułu poza rejestrem — i nazwa pliku nie była
równa id obiektu (`undercity-dungeon` vs `undercity`). Naprawa: `git mv` na
`scryfall-undercity.json`, w narzędziu kategoria wyprowadzona z `card-data.js`
(`obiektyWsparcia()`, `adresDruku()`, `przegladObiektowWsparcia()` — zero nazw na
sztywno, F7/L41), klasy `W-potwierdzony-offline` / `W-bez-snapshotu` /
`W-snapshot-bez-zgodnosci`, testy OW/1–OW/8 (w tym zgodność Oracle z komnatami
w silniku i sprzężenie z panelem), D/14 przeformułowany. Pomiar: `undercity`
= W-potwierdzony-offline, `day-night` = W-bez-snapshotu (adres z UUID, pobranie
wymaga sieci); klasy rejestru i `DO POBRANIA` bez zmian, narzędzie exit 0.

Bramki po turze: `npm test` **5273/5273**, `npm run build` 61 modułów /
**3538.7 kB**, quick benchmark **84.2% (566/672)**, narzędzie druków exit 0
(A 349 / B 110 / B2 7 / brak 43, DO POBRANIA 0, rozjazd 1 udokumentowany),
Żywy Tester sesji **20 partii / 1112 sond no-op / 0 zgłoszeń detektorów**.

Materiał na kolejną sesję (po tej turze):

* `day-night` nie ma snapshotu (klasa `W-bez-snapshotu`) — pobranie wymaga sieci,
  w tym środowisku niedostępnej (proxy `SignatureDoesNotMatch`).
* **Hipoteza do zmierzenia (nie finding):** wycena DEKLARACJI bloków
  (`declare_blockers`) nie wie, że podwójny blok z pokryciem lethal może być
  korzystną wymianą — po tej turze plan przydziału już to wykorzystuje, ale
  decyzja, KTÓRE stwory blokują, jest podejmowana wcześniej i tego nie modeluje.
  Przed jakąkolwiek zmianą: pomiar, ile partii w korpusie w ogóle dochodzi do
  podwójnego bloku (w 7 partiach tej tury zero), bo ryzyko regresji win-rate jest
  realne, a zysk niepewny.
* Etykieta „Użyj domyślnego przydziału (zabójcze obrażenia po kolei blokerów)"
  nie wspomina o pokryciu z tego samego kroku — treść nie jest błędna (lethal-first
  pozostaje bazą), więc zostawiona bez zmian (minimalny patch); do rozważenia,
  jeśli właściciel uzna ją za mylącą.

Do decyzji właściciela pozostaje squash-merge PR #114 oraz ewentualny pełny
benchmark B0 (`--full`, ~23k meczów, ADR 0018).
