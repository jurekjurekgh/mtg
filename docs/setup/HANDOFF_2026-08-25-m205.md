# HANDOFF M205 — 2026-08-25

## Co zrobiono

Tryb domyślny wg ADR 0020/0021 (prompt „kontynuujemy” = pętla domyślna,
bez pytania o kolejkę). Gałąź `arena/01a038fe-mtg`, **PR #78**.

1. **Audyt PR #77 (M204)** — raport `docs/audits/AUDYT_PR77_2026-08-25.md`.
   Poprawka w `spells.js` czysto kosmetyczna i poprawna. Jedno realne
   znalezisko:
   - **M205/1** (`37e51cb`) — testy regresyjne dołożone w PR #77 były ŚLEPE:
     przechodziły identycznie po cofnięciu fiksu, który deklarowały.
     Przepisane na realny kształt transkryptu, przypięte mutacją.
2. **M205/2** (`8204777`) — **„znany problem" z HANDOFF M204 ZAMKNIĘTY**:
   auto-pass człowieka przy niepustym stosie zostawia dowód w logu,
   tester zbiera go pod `--quiet`, detektor uznaje za dowód.
   Pomiar: seed 42 **2 → 0 zgłoszeń**, 4 kolejne partie 0 zgłoszeń.
3. **M205/3** (`273d1ce`) — raport audytu + `.gitignore` na transkrypty
   w podkatalogach.
4. Lekcje **L61** (test bez weryfikacji mutacyjnej bywa ślepy) i **L62**
   (kolejność renderu jest częścią kontraktu; log rysowany od najnowszego).

## Stan testów

- `npm test`: **3205/3205** (baza sesji: 3200).
- `npm run build`: 54 moduły / 2638,1 kB.
- Benchmark: nie uruchamiano pełnego B0 (ADR 0018 — tylko na komendę
  właściciela). Profil szybki: `node tools/benchmark.mjs` (~2–4 min).

## Reguła warta zapamiętania (weszła do LESSONS jako L61)

„Przypięte testem" znaczy: **pokazano, że test czerwienieje po cofnięciu
naprawy**. Bez tego pomiaru test bywa zielony z niewłaściwego powodu, a
następna sesja uzna temat za zabezpieczony. Procedura kosztuje ~30 s:
mutacja → plik testu MUSI paść → cofnięcie mutacji → zielone → oba wyniki
do commita/raportu.

## Kolejka (propozycja, nie zobowiązanie)

- Pętla jakości Żywym Testerem **dłuższymi partiami i z ręcznym czytaniem
  transkryptów** — detektory na tych parach talii są już ciche, więc kolejne
  znaleziska raczej nie przyjdą z nich samych (tak było w M203/M204).
  Profile `explorer`/`random` są najmniej przeorane.
- Przy okazji audytu następnego PR: **sprawdzić mutacją** testy regresyjne
  dołożone w tej sesji (L61 działa w obie strony).

## Pułapki środowiska (potwierdzone w tej sesji)

- `tools/table-tester/node_modules` nie istnieje po starcie sesji —
  `npm i` w katalogu narzędzia (~2 s, rejestr npm jest dostępny).
- Transkrypty Żywego Testera są gitignorowane (także w podkatalogach).
- Polskie znaki: edycje plików przez `python3` + `pathlib` (UTF-8).
