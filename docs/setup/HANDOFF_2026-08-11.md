# Handoff 2026-08-11 — M71 srebrna odznaka (4 błędy vs CR) + zgłoszenia właściciela A–D

## Stan po sesji 2026-08-11 (PR `arena/019fed61-mtg`)

- **Kod:** `npm test` **1292/1292**, build **50 modułów / 1402.0 kB**, 207 realnych
  kart + tokeny, 9 talii singleton. Quick B0 (1080 meczów): **0 crashy**
  (heuristic 74.3% ogółem; 53.6% vs aggro / 95.0% vs random). Pełne B0 (13500) —
  wynik w opisie PR.

## Zakres M71

### Znalezione błędy vs CR (srebrna odznaka, RED→GREEN)
1. **CR 510.4/510.5 — first-strike resume (combat.js).** `resolveCombatDamage`
   używał `startPass = resume.pass` (boolean) jako indeksu `passes=[true,false]`.
   `passes[true]`=passes[1]=false → przy wznowieniu decyzji pomijał przebieg
   first strike; `passes[false]`=passes[0]=true → wznawiał przebieg zwykły od
   indexsu 0 i **ponownie rozdawał obrażenia niezablokowanych atakujących**
   (objaw D właściciela: „walka rozstrzygnęła się dwukrotnie"). Fix: startIndex
   true→0 / false→1. Realne karty: porcelain-legionnaire, ainok-tracker
   (first_strike), True Conviction (double_strike).
2. **CR 702.16d+702.15 — lifelink za zapobiegnięte obrażenia (combat.js).**
   `dealt` liczone przed prewencją protection; kontroler źródła z lifelink
   zyskiwał życie za obrażenia, których nie zadał (osiągalne: aura z flash
   Benevolent Blessing po deklaracji bloków). Fix: kwota po prewencji protection
   w assignDamageToBlockers i ścieżce bloker→atakujący.
3. **CR 702.16b — martwe celowanie protection (spells.js/abilities.js).**
   `validateTargets` brał kolory GRACZA (zawsze puste) zamiast źródła; czarny
   czar/zdolność mógł celować w stwora z protection od black. Fix: `sourceColors`
   (kolory źródła) przez wszystkie call-site validateTargets/collectLegalTargets
   (castSpell, castCleave, castModal, castEscape, adventure, activateAbility, equip).
4. **CR 702/704 — log „? ginie" (state-based.js + session.js).**
   `creature_destroyed` bez cardId; nameOfObject(fromId) nie znajdował obiektu
   (nowe id w grobie). Fix: cardId w evencie + render przez nameOf.

### Zgłoszenia właściciela A–D z testów (naprawione)
- **A (UI):** karta Undercity nie dała się otworzyć na pełnym ekranie.
  Fix: renderUndercity `onClick` + `openUndercityFullscreen()` w main.js
  (renderCardFullscreen printu lochu).
- **B (bot):** boty „skipowały szukanie" Secret Entrance (Undercity, pokój 1).
  `resolve_search_choice` w kategorii 'ability' z punktacją 0, a rezygnacja
  (`found:null`) jest pierwszą ofertą. Fix: heuristic `case
  'resolve_search_choice'` (znajdź > fail-to-find, land premiowany) + aggro
  (`found != null`).
- **C (log):** patrz bug 4.
- **D (engine):** patrz bug 1 (ten sam root cause).

## Testy
- `test/bug-hunt-2026-08-11.test.js` — 1a–1c (first/double-strike resume),
  2a–2b (protection-lifelink), 3 (protection-target), 4 (podwójna walka),
  5 (creature_destroyed cardId), 6–7 (boty szukają basic landu).
- `test/table-ui.test.js` — renderUndercity klik → onUndercityClick.
- Hunter seed w table-session (delirium) przelosowany **25 → 48** po zmianie
  zachowania bota (resolve_search_choice).

## Kolejka (po M71)
- **Pełne B0** na stanie po M71 (w toku w sesji; wynik do wpisania w opis PR).
- **Batch 29** — czeka na listę właściciela (ADR 0010: Scryfall ZAWSZE z `set=`;
  api zablokowane → fetch_page).
- **B2-w2** — infra OFF (~4× wolniej); z włączonym lookahead +5/+34 p.p.
- **Talie singleton** — rosną z batchami (strażnik repo-decks).

## Pułapki (środowisko / projekt)
- `edit_file` psuje PL → **python3 Path.read_text/write_text**; skrypty z /tmp
  jawnie uruchamiać.
- **Sandbox potrafi COFNĄĆ lokalny HEAD do main** — po każdym commicie
  `git push origin arena/...`; przy rozjeździe `git fetch && git reset --mixed
  FETCH_HEAD` i cherry-pick z reflogu.
- **GH_TOKEN potrafi wygasnąć** w trakcie sesji — commity są lokalnie bezpieczne,
  push retry; w ostateczności reconnect GitHub w Arena.
- Zmiana zachowania bota (punktacja `resolve_*`) przelania hunter seedów —
  sprawdź pełne npm test przed commitem.
- Eventy triggerów = ZAMKNIĘTA lista (26 zdarzeń, strażnik HANDLED_TRIGGER_EVENTS);
  nowy trigger bez wpisu w engine jest martwy (testy behawioralne, nie definicyjne).
- Pełne B0 (13500) ~16 min; quick `--seeds 4` (1080) ~90 s.
- Lektura nowej sesji: AGENTS.md → PROJECT_STATE (M71) → HANDOFF_2026-08-10a…c →
  HANDOFF_2026-08-11 → ENGINE_MILESTONES (M70-M71) → plan M71 → ADR 0002/0013/0014.

## Do weryfikacji w następnej sesji
- Na telefonie (Pages, po merge): (a) klik na kartę Undercity otwiera pełny ekran;
  (b) bot szuka basic landu przy Secret Entrance zamiast skipować;
  (c) log walki nie pokazuje „? ginie" przy wzajemnym zabiciu;
  (d) first/double strike z trample/wieloma blokerami zadaje w pełni, walka
  nie rozstrzyga się dwukrotnie.
