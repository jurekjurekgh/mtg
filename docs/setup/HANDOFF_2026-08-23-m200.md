# HANDOFF 2026-08-23 — M200 (audyt PR #70 + uwagi właściciela A–R)

## Stan

- **PR #72** (`arena/01a02f35-mtg`), **OTWARTY** (decyzja o scaleniu —
  właściciel; sesja nie scala).
- `npm test` **3023/3023** · build **53 moduły / 2561.2 kB** · katalog
  **459 kart** · próbka bota (`test/bot-benchmark.test.js`) **9/9**.
- Raport audytu: `docs/audits/AUDYT_PR70_2026-08-23.md`.
- Poprzednia sesja M200 (PR #71, `arena/01a02ebc-mtg`) zamknięta przez
  właściciela bez scalenia; jej fixy N1–N3 przejęte i zweryfikowane w PR #72.

## Co wykonano

1. **Przejęcie PR #71 z weryfikacją:** N1 (MANA_SOURCE_MAP vs deskryptory
   `add_mana`), N2 (phyrexian pips w czarach, CR 118.9), N3 (martwy trigger
   `combat_damage_to_you`), O-N3 (redundantny pre-check). Każdy fix: stan
   zielony + weryfikacja mutacyjna na kodzie PR #70 (RED→GREEN).
2. **Audyt PR #70 (E2a–E2e):** raport w `docs/audits/`. Znaleziska:
   - **N5** — pre-check w gałęzi Wooden Stake (CR 603.4) — fix;
   - **L** — Ruthless Invasion: „this turn” jako zamrożony zbiór zamiast
     efektu ciągłego — fix (`turn.cantBlockRestrictions`, read-time,
     wygaśnięcie w `nextTurnStep`);
   - **Formidable** — zgłoszone (brak keywordu w danych + reguła),
     **WYCOFANE na decyzję właściciela** (mechanika = warunkowe trample
     ataku; silnik implementuje prawidłowo). Zmiany odwrócone.
   - **M/M2** (Frightful Delusion) — werdykt: poprawne na bieżącym kodzie;
     testy pinują (oferta kontera = wyłącznie czary; cel-czar nazwany).
3. **Uwagi właściciela (każda osobnym commitem):**
   - A — wycofane (właściciel: legalny cel → zdolność musi się rozstrzygnąć);
   - A2 — tytuły wyborów lochu (`Ścieżka w Undercity`);
   - **B** — root cause: sesja nie eksponowała `cardIdByName` → linki nazw
     kart w logu (M167/E2) martwe od urodzenia (klasa L5) — fix + test E2E;
   - C/C2 — mulligan: wizard zaznaczania (komendy z `legalCommands`) +
     etykieta z żywej liczby kart;
   - **D+E2** — root cause: `any_creature_dies` odpalał się na poświęceniu
     NIE-stwora (CR 700.4c: „die” = stwory) — fix;
   - **E** — root cause: bramka `pay_or_sacrifice` (i rodziny) odrzucała
     `tap_for_mana`/aktywację many — kreator many nie działał (L28:
     wspólny predykat 3 bramek);
   - F — werdykt: poprawne (CR 502.4/601.2f) — test pinuje;
   - G — opis dnia/nocy zgodny z implementacją M68;
   - H — wycena aury z `losesKeywords` (Grounded nie na nie-latającego);
   - **R** — Gray Slaad: self-mill wyceniany +18 mimo ukrytej biblioteki
     (FoW) → +6 + stopniowane ryzyko deck-outu (marża stwor-vs-przygoda
     3 pkt → ~20 pkt, zmierzone).
4. **M193** (plan niedomknięty z PR #70) — K5–K7 domknięte.

## Kolejka / obserwacje dla następnej sesji

- **Pętla jakości Żywym Testerem NIEWYKONANA** w tej sesji (decyzja
  właściciela: koniec po fixach) — najbliższy priorytet: audyt Żywym
  Testerem na zbudowanym artefakcie (talie batchy 46–48, profile
  greedy/explorer/defensive/random/impatient) + ewentualnie nowe detektory.
- **U2 (audyt PR #68, nadal otwarte):** `epicCastOffers` na ścieżce EPIC nie
  filtruje `additionalCost` — przy pierwszym epic-czarze z kosztem
  dodatkowym w talii spellslinger.
- **O1 (z N3):** nadwyżka trample BLOKERA nie idzie w gracza, którego bloker
  broni (CR 702.19) — engine daje całą moc blokera atakującemu.
- **Formidable:** temat zamknięty decyzją właściciela — nie podjmować bez
  nowego zlecenia; twierdzenia o regułach MtG weryfikować u źródła przed
  wdrożeniem (L56/L57).
- Reszta stałych pułapek: `docs/setup/ENVIRONMENT.md` (reset workspace,
  polskie znaki w `edit_file`, `gh pr edit` → `gh api -X PATCH`, token GH
  wygasał w trakcie sesji — 2× w tej).

## Nienegocjowalne (przypomnienie)

ADR 0020 (PR → audyt → inkrementalne commity), ADR 0021 (pętla domyślna),
ADR 0022 (pełny Oracle albo unsupported — z wyjątkiem rozstrzygnięć
właściciela o mechanikach własnych kart), ADR 0018 (pełne B0 tylko na
komendę), ADR 0002 (zero przypadków po nazwie karty w core), L56/L57
(fakty i reguły weryfikowane u źródła przed wdrożeniem).
