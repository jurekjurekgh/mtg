# Handoff 2026-08-09e — po M66 UX walki i many (A/B/C/D/R)

## Stan po sesji 2026-08-09 (PR #39 `arena/019fe7ec-mtg` — OPEN do main)

- **Kod:** `npm test` **1197/1197**, build **50 modułów / 1317.2 kB**, 188 realnych + 1 token
  (189 supported), 9 talii singleton. **Pełne B0 (9 talii / 50 seedów / 13 500 meczów)
  0 crashy**: heuristic **91.7% vs random, 65.6% vs aggro**, aggro 93.7% vs random
  (progi 0.78/0.57 utrzymane).
- **Zakres M66 (uwagi właściciela + 2 błędy z rozpoznania):**
  1. **A** — spacja przed `)` w kosztach akcji: `.action` flex `gap:0` + margin na `::before`.
  2. **A2** — MANA_COSTS kończyło się na Batchu 24 (39 kart!): walidacja kolorów pominięta
     (Might {G} za {U} akceptowany) — uzupełnione ze Scryfall (181 wpisów) + strażnik
     w `card-data.test.js` (każda supported nie-ląd ma wpis).
  3. **B** — atakujący/blokujący bez list kombinacji: `groupCombatDecisions` + `renderCombatWizard`
     (przełączniki; goad/must-attack zablokowane, menace 0/≥2, cantBlockAlone z partnerem).
  4. **C** — log walki bez `?`: damage_dealt/attackers_declared/blockers_declared niosą cardId
     (LKI); poprawione mapowanie (blokerzy blokują atakującego, nie odwrotnie).
  5. **D** — pojedynczy bloker bez trample dostaje PEŁNĄ moc (3/3 vs 1/1 = 3).
  6. **R** — rozdzielanie obrażeń przy wielu blokerach/trample = decyzja gracza:
     `pendingDamageAssignment` + `resolve_damage_assignment` (1 wariant w legalCommands =
     lethal-first; kombinacji NIE enumerujemy), `renderDamageWizard` (steppery +/−).
  7. **Fixy B0** — kolejność pending (pendingTriggerTargets PRZED pendingDamageAssignment w
     `firstPendingDecisionPlayerId` — bramka execute dla triggerów jest wcześniejsza);
     `remove_counter` jako EFEKT = no-op przy braku licznika (Kappa ×2 z jednego combatu).
- **Commity:** plan (d5686ad) → MANA_COSTS (1d93b61) → log walki+D (2726c12) → R engine
  (9223e55) → UI wizardy (ac9d7b7) → CSS A (e2b8cbb) → fixy B0 (14e00b2).

## Kolejka (po M66)

- **Batch 27** — czeka na listę właściciela (ADR 0010: Scryfall ZAWSZE z `set=`;
  api zablokowane → `fetch_page`).
- **B2-w2** — infra OFF (~4× wolniej); z włączonym lookahead bot zyskuje +5/+34 p.p.;
  decyzja o koszcie i ewaluacja na pełnym B0.
- **Talie singleton** — rosną z batchami (strażnik `repo-decks`).

## Pułapki (środowisko / projekt) — aktualne

- `edit_file` psuje PL → **python3 Path.read_text/write_text**, commit msg via /tmp.
- **Sandbox potrafi COFNĄĆ lokalny HEAD do main** (zdarzyło się w tej sesji — M65 zostało
  tylko na remote). Po każdym commicie: `git push origin arena/...` — remote jest kopią
  zapasową; przy rozjeździe `git fetch && git reset --mixed FETCH_HEAD` odtwarza historię.
- **Nowe w M66:** `pendingDamageAssignment` w `firstPendingDecisionPlayerId` MUSI być PO
  bloku `pendingTriggerTargets` (bramki execute w tej kolejności — inaczej bot dostaje
  ofertę odrzucaną trigger_target_unresolved); `remove_counter` jako efekt = no-op przy
  braku licznika (koszt nadal rzuca); walka NIE wysyła kombinacji do UI — atakujący/
  blokujący przez `renderCombatWizard`, obrażenia przez `renderDamageWizard`;
  MANA_COSTS = pełne (181) — strażnik w card-data.test.js pilnuje.
- Walka: `resolve_combat` może skończyć się pending (multi-bloker/trample) — po nim
  wymagany `resolve_damage_assignment` (1 wariant = default lethal-first; gracz przez
  wizard). Pojedynczy bloker bez trample = pełna moc (bez decyzji).
- Pełne B0 (13500) ~13-15 min; quick `--seeds 4` (1080) ~90 s. Zmiana przestrzeni komend
  bota = pełne B0.
- Hunter seeds w `table-session.test.js` — przelosowywać po zmianie talii.
- Lektura nowej sesji: AGENTS.md → PROJECT_STATE (M66) → HANDOFF_2026-08-09a/b/c/d/e →
  ENGINE_MILESTONES (M64-M66) → WORKFLOW → plany M65/M66 → ADR 0002/0003/0005/0010/0011/
  0013/0014.

## Do weryfikacji w następnej sesji

- `npm test` 1197/1197, `npm run build` 50/1317.2 kB.
- Pełne B0 13500 / 0 crashy (91.7%/65.6%, aggro 93.7%).
- Testy iPada: A (brak spacji przed `)`), B (wizard ataku/bloków), C (nazwy w logu),
  D (3/3 vs 1/1 = 3), R (rozdzielanie obrażeń stepperami).
- Scryfall Batch 27: ZAWSZE `set=` + `fetch_page` (api blokada).
