# Handoff 2026-09-08 — E8: wyzwanie wyłapywacza błędów (5 bugów vs CR), PR #105

## Stan bieżący

Sesja `arena/01a07c4e-mtg`, [PR #105](https://github.com/jurekjurekgh/mtg/pull/105)
do `main` — kontynuacja po HANDOFF_2026-09-07g (ta sama gałęzia, ten sam PR;
właściciel jeszcze nie scala, agent nie scala — ADR 0021). Wyzwanie
odznakowe **E8 zakończone 5/5**: znalezione, opisane w planie (pushniętym
PRZED kodem — `63e31f7`) i naprawione 5 unikalnych uproszczeń vs zasady
MtG. Każdy fix RED-first, osobny commit. Plan
`docs/plans/PLAN_2026-09-07-wyceny-bota-i-modale.md` (sekcja E8) odhaczony,
podsumowanie per bug w pliku. Sprawdź bieżący HEAD/CI na GitHub — plik nie
pinuje własnego SHA.

## Bramki i dane do porównania

- `npm test`: **4762/4762** (po E7: 4750; +12 od nowych testów E8,
  netto +2 po aktualizacji pinów starego uproszczenia B3).
- `npm run test:all`: **4772/4772**, pomiar ~222 s.
- `npm run build`: **59 modułów / 3394,6 kB** (`dist/mtg-table.html`).
- `node tools/benchmark.mjs` (szybki): heuristic **85,0%** (571/672) —
  bez zmian vs E7 (pasmo bazowe; bot korzysta z default-assignmentu, więc
  B3 minimalnie zmienia jego przydziały, ale winrate stabilny).
- **Pełnego B0 NIE uruchamiano** (ADR 0018/0025). Żadnych nowych kart/
  talii/limitacji Oracle (ADR 0029); syntetyki wyłącznie w testach.

## Co zrobiono (E8, 2026-09-08)

- **Plan E8 pushnięty przed kodem** (`63e31f7`): 5 bugów z kotwicami w kodzie
  i checkboxami do odhaczania.
- **B1 `888a234`** — regeneracja konsumowała WSZYSTKIE tarcze naraz
  (`filter(id !== object.id)`); CR 701.15b: jedna tarcza = jedno zniszczenie.
  Fix: konsumpcja dokładnie jednej instancji. Harness
  `test/helpers/e8-destroy-harness.js` (realna ścieżka
  `destroyPermanentByEffect`).
- **B2 `4bcf5ab`** — flaga „nie odtapuje się": zużywana tylko przy `tapped`
  (odkręcony cel nosił ją wiecznie i pomijał PÓŹNIEJSZY untap step) +
  porównanie controllerId-z-efektu (skip wypadał w cudzym untap stepie po
  zmianie kontrolera). Fix: zużycie na untap stepie obecnego kontrolera
  niezależnie od stanu tapped.
- **B3 `2b944c0`** — bez trample suma obrażeń mogła być < moc (stare
  uproszczenie „niedobór legalny" z trample-lethal); default lethal-first
  gubił resztę przy wielu blokerach; wizard startował od 0 i bramkował tylko
  trample. Fix: `damage_must_be_fully_assigned`, dolewka do ostatniego
  blokera (konwencja M66), wizard prefill jak silnik + bramka pełnej sumy.
  Zaktualizowane piny: trample-lethal-przed-graczem, choice-request-ui
  (steppery, M101/B6, M150/B), m136-sonda, Sherlock 1c.
- **B4 `2e15ba6`** — `lethalOf` ignorował protection: trample zatrzymywał się
  na blokerze w pełni chronionym (CR 702.19b + 702.16d). Fix: lethal 0 przy
  pełnej prewencji; walidacja/default/wizard korzystają automatycznie.
- **B5 `b0c02d9`** — `gain_control_until_end_of_turn` nie ruszał
  `state.combat` (CR 506.4): przejęty bloker blokował swojego nowego
  kontrolera, przejęty atakujący „atakował" w jego stronę. Fix:
  `removeFromCombat` po przejęciu (helper z objects.js, jak regeneracja).
- **Zamknięcie `3e34e44`**: plan odhaczony 5/5 + wyniki per bug; PR #105
  z części 5 (E8) — body przez REST API (`gh pr edit` pada na deprecację
  Projects classic).

## Wnioski / obserwacje

- 3 z 5 bugów to uproszczenia „jednorazowo poprawne, ale nie-permanencyjne"
  (B1 tarcze, B2 flaga, B3 niedobór) — klasa: stan jednorazowego efektu bez
  niezmiennika zużycia. Warto przy następnych efektach czasowych od razu
  pytać: „co się dzieje przy DRUGIM zdarzeniu?".
- `gh pr edit` na tym repo wywala się na GraphQL `projectCards` (deprecacja
  Projects classic) — używać `gh api repos/{owner}/{repo}/pulls/N -X PATCH`.
- Node `--test` w tym repo: asercje `state.objects.get('id')` po zmianie
  strefy łapią STARE id (zmiana strefy = nowe id, CR 400.7) — szukać po
  cardId albo złapać nowe id (pułapka przy testach zniszczeń).

## Co dalej (propozycje, decyzja właściciela)

- Review E8 w PR #105 (część 5) — squash-merge po akceptacji.
- Ewentualne kolejne wyzwania odznakowe w tym stylu (plan-przed-kodem,
  RED-first) sprawdziły się — sekcja E8 planu jest gotowym wzorcem.
- Pełny B0 nadal czeka na jawne polecenie (ADR 0018/0025).
