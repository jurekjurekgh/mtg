# Handoff 2026-08-13 — M82: Batch 31 (10 kart) + 3 nowe talie

## START TUTAJ
1. Przeczytaj `AGENTS.md`, `docs/PROJECT_STATE.md` (M82), ten plik,
   `docs/plans/PLAN_2026-08-13-batch31-kart.md`.
2. `npm test` — oczekuj **1442** zielonych.
3. `npm run build` — 50 modułów / ~1570.3 kB.
4. Sesja = gałąź `arena/019ff818-mtg` = PR #46 (otwarty z tej sesji).

## Stan po sesji
- **Kod:** Batch 31 — 10 realnych kart (Furious Forebear, Jwari Shapeshifter,
  Floodhound, Inspire Awe, Cogwork Assembler, Dread Warlock, Steel Sabotage,
  Warrior's Sword, Awaken the Sleeper, Impact Tremors) + 3 nowe talie.
- **Bot bez zmian** — pełne B0 niewymagane (progi 0.78/0.57, pomiar #44).

## Co weszło
- Nowe generyczne mechaniki (ADR 0002): trigger z grobu + opcjonalna płatność
  (Furious), enter-as-copy (Jwari), investigate/Clue (Floodhound), prewencja
  combat „except enchanted" (Inspire Awe), token-kopia artefaktu (Cogwork),
  cant-be-blocked-except-color (Dread), counter-artifact-spell (Steel),
  job select + Warrior subtype (Sword), czasowa kontrola do EOT (Sleeper),
  creature-you-control-enters (Impact Tremors).
- Root cause: `legalActivatedAbilities` oferował TYLKO stwory jako cele
  zdolności — Cogwork Assembler (cel `artifact`) dawał botowi nielegalny cel.
  Naprawa przez wspólną `legalTargetCandidates`.

## Talie
- Nowe: `decks/ostrza.txt`, `decks/mechanicy.txt`, `decks/sojusznicy.txt`.
- Dopiski: azorius (+Furious/Jwari/Floodhound/Steel), green (+Inspire Awe),
  black (+Dread Warlock), red (+Sword/Sleeper/Tremors/Cogwork).

## Testy
- `test/real-cards-batch31.test.js` — 12 behawioralnych.
- Zaktualizowane: `test/art-ids-tool.test.js` (228→238), `test/attachment.test.js`,
  `test/real-cards-batch4.test.js` (equipment subtypes), `test/table-session.test.js`
  (seedy endure/delirium), `test/session-autopass.test.js` (humanDecision),
  `test/batch25-etb-enters-fix.test.js` (trigger whitelist), `test/card-data.test.js`,
  `test/repo-decks.test.js` (red 57→61), `test/bot-benchmark.test.js` (bez zmian
  progów — B0 przechodzi).
- `npm test`: **1442/0**. `npm run build`: 50 modułów / ~1570.3 kB.

## Kolejka (po M82)
- Phone verify (Pages, po merge): nowe karty na stole, talie ostrza/mechanicy/sojusznicy.
- Batch 32 — czeka na listę właściciela.
- Phone verify / platinum hunt / B2-w2 lookahead OFF / tester roadmap.

## Pułapki
- `edit_file` psuje polskie znaki → **python3** Path.read_text/write_text.
- Sandbox potrafi cofnąć HEAD do main — `git fetch && git reset --hard FETCH_HEAD`
  (praca bezpieczna na remote).
- Nie commituj bez `npm test`. Nie odpalaj testera bez `npm run build`.
- `legalActivatedAbilities` musi używać `legalTargetCandidates` (nie tylko
  stwory) — inaczej bot wybiera nielegalny cel.
- `enterAsCopy` rozstrzygane przy wejściu PRZED SBA (0/0 ginęłoby wcześniej).
