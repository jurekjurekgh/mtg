# HANDOFF 2026-08-24 — sesja M203 (audyt PR #74 + naprawa narzędzia audytu)

Sesja: `arena/01a03566-mtg` · PR: **#75** · baza: `main` = `7bdef17` (PR #74).

> To jest skrót JEDNEJ sesji. Reguły trwałe są w `AGENTS.md`, ADR-ach,
> `docs/LESSONS.md` i `docs/setup/ENVIRONMENT.md` — nie tutaj.

## Stan po sesji (zmierzone)

| Pomiar | Wartość |
|---|---|
| `npm test` | **3188/3188 pass** (było 3181; +7 testów M203) |
| `npm run build` | **53 moduły / 2626.0 kB** |
| egress HTTPS | zablokowany (rejestr npm działa) |
| Żywy Tester | `cd tools/table-tester && npm i` działa; `--list-decks` wypisuje talie |

Commity sesji: plan (`091b6f6`), naprawa testera + dokumentacja + strażnik
(`03ebe2b`), sprostowanie egress + usunięcie `commit-msg.txt` (`cb6c0d1`),
raport audytu + L60 + PROJECT_STATE (ostatni).

## Co zrobione

1. **Audyt PR #74** (ADR 0020 B) — raport `docs/audits/AUDYT_PR74_2026-08-24.md`.
   Zweryfikowane jako poprawne: N1 (mana ograniczona), brąz 1 (CR 704.5m),
   brąz 3 (CR 616.1), N2 (`prefer: 'opponent'`), N4 (`exileAdditionalCostCandidates`).
2. **Naprawione:** Żywy Tester grał inną talią, niż nagłówkował transkrypt
   (domyślne `green`/`red` nie istnieją od M178) — walidacja nazw, `--list-decks`,
   domyślne z `BENCH_DECKS`, dokumentacja talii przepisana, strażnik rozjazdu
   nazw dokumentacja↔`decks/`. Nowa lekcja **L60**.
3. **Skorygowane:** wpis M202 o egress w `PROJECT_STATE.md` (był odwrotny niż
   pomiar); usunięty `commit-msg.txt` z korzenia repo.

## Otwarte — czeka na decyzję właściciela

- **N-NEW-1: Halo Forager** (`halo-forager`, `decks/worek-basni.txt`) — darmowy
  rzut z grobu **kosztuje manę** (silnik pobiera MV) i nie wymaga X = MV, choć
  Oracle mówi „without paying its mana cost" (CR 118.5/118.9a). Karta ma
  `supported` + puste `limitations` = deklaruje zgodność, której nie ma
  (ADR 0022). Do wyboru: **pełny fix** (decyzja „wybierz X" → protokół, oba
  boty, kreator w stole, testy) albo **`unsupported`** do czasu fixa.
  Półśrodek odradzam (L44).
- **D z M202** — konwencja `unshift` (oferty) vs `push` (triggery)
  w `playerView`: pierwsza oferta aury wskazuje stwora przeciwnika. Jedna
  zmiana konwencji + pomiar benchmarku zamiast trzech łatek.

## Kolejka (ADR 0021 — pętla domyślna, nie pytanie o kolejkę)

Następna sesja bez tematu od właściciela: PR na start → audyt **PR #75** →
pętla jakości (Żywy Tester + polowanie na CR innymi ścieżkami). Obszary CR
już zweryfikowane jako poprawne są wypisane w `PROJECT_STATE.md` (§M202
„nie badać drugi raz") i w `docs/audits/AUDYT_PR74_2026-08-24.md` §1 — nie
badać ich ponownie.

## Pułapki środowiska (skróty — pełne w `ENVIRONMENT.md`)

- Reset workspace w trakcie sesji → push po każdym zielonym kroku; force push
  **zakazany** (ADR 0020 D).
- Polskie znaki: edycje przez `python3` + `pathlib` (`edit_file` je uszkadza).
- Egress zablokowany: dane kart przez `fetch_page`, nie z sandboxa.
- Benchmark: pełne B0 tylko na komendę właściciela (ADR 0018).
