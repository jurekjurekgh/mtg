# Handoff 2026-09-16 — audyt PR #123 (APPROVE z zastrzeżeniami) + naprawy A1/A2 + domknięcie O5 (PR #124, `arena/01a0aac8-mtg`)

Zlecenie: „Kontynuujemy projekt." — bez nazwanego tematu → pętla domyślna
ADR 0021. Wykonano: PR sesji → pełny audyt ostatniego scalonego PR (**#123**,
ADR 0020 B: 27 commitów — E1–E4 + 15e/15f/15g + odznaki M359 Brąz, M360
Srebro, M361 Złoto) → naprawy znalezisk behawioralnych (RED→GREEN + mutacje)
→ domknięcie O5 z audytu #121 (weryfikacja kontekstu cytatów 702.16) →
domknięcie dokumentacji.
Raport: [`docs/audits/AUDYT_PR123_2026-09-16.md`](../audits/AUDYT_PR123_2026-09-16.md).
Plan: [`docs/plans/PLAN_2026-09-16d-audyt-pr123-i-petla-jakosci.md`](../plans/PLAN_2026-09-16d-audyt-pr123-i-petla-jakosci.md).
PR: [#124](https://github.com/jurekjurekgh/mtg/pull/124) (scala właściciel, ADR 0007/0020).

## Werdykt audytu PR #123: APPROVE z zastrzeżeniami

Logika 27 commitów poprawna; deklaracja „E2 wyłącznie komentarze" potwierdzona
mechanicznie (V1: 0 niekomentarzowych linii); cytaty CR za każdą odznaką
zweryfikowane literalnie (mtg.wiki, CR effective 2026-08-07). Dwa błędy
naprawione w tej sesji, jeden dług dokumentacyjny domknięty:

- **A1 (M359, CR 514.3a) — naprawiona:** model cleanupu liczył tylko aktywność
  STOSU, a reguła dosłownie otwiera priorytet także po SAMYM wykonaniu SBA
  („…any state-based actions would be performed and/or any triggered
  abilities…"). Scenariusz potwierdzony sondu behawioralną: stwór utrzymywany
  EOT-buffem ginął w chwili wejścia w cleanup (514.2 zdejmuje buff → SBA
  704.5f), a okna nie było. Naprawa: `cleanupEventIsSbaEvidence` — dowody SBA
  w strumieniu zdarzeń (`creature_destroyed`, `object_moved` z polem `sba`,
  `permanent_sacrificed` Saga 704.5s, `token_ceased_to_exist`,
  `counter_removed`); w zamkniętym cleanupie te typy przychodzą wyłącznie
  z SBA (efekty żyją na stosu), więc brak drogi do fałszywego otwarcia.
  Świadomie poza zestawem: goły `object_moved` (discard 514.1 go emituje),
  `player_lost`, `legend_rule_choice_started`. Zero nowych pól stanu →
  fingerprint bez zmian. Testy T1–T3 (przed: 3× RED), mutacje: wyłączenie
  dowodu `creature_destroyed` → T1+T2 RED.
- **A2 (M360/B3, klasa L16) — naprawiona:** `pendingCombatSecondPass` (flaga
  drugiego kroku obrażeń przy first/double strike) blokuje rundę passów i
  zmienia przyszłe możliwości, ale nie trafiła do fingerprintu — dwa stany
  różniące się tylko tą flagą miały identyczny odcisk (replay i sonda no-op
  ślepe). Naprawa: pole w `PENDING_DECISION_FIELDS`. Przyczyna lukі:
  strażnik `fingerprint-pending-decisions.test.js` bierze ground truth z ciał
  `firstPendingDecision*`, a ta blokada siedzi w bramce pass-round — klasa
  blokad spoza firstPendingDecision nie ma automatycznego strażnika (patrz
  O2 w raporcie). Test T4 (przed: RED — identyczne JSON), mutacja: usunięcie
  wpisu → RED.
- **A3 (dokumentacja, L92) — domknięta:** README „Bieżący stan" i handoff 15c
  opisywały stan sprzed odznak (5555/5565 i 5514/5524 vs finalne liczby);
  README zaktualizowany pomiarami z finalnego HEAD, ten handoff zastępuje 15c.

## E3 = O5 (domknięcie z audytu #121): 24 sprostowania kontekstu 702.16

Pełny przegląd wszystkich wystąpień 702.16 w `src/` (60+) przeciw literalnemu
tekstowi (mtg.wiki/page/Protection, CR 2026-08-07: b=targetowanie, c=Aury,
d=Sprzęt/Fortyfikacje, e=prewencja, f=blokowanie atakujących) — systemowy
dryf podreguł w 8 plikach (G3 z #121 naprawił tylko 4 miejsca w bocie):
blokowanie cytowane jako a/e/d → f (combat ×4), prewencja jako a/d → e
(combat ×2, effects, permanents ×2), jakość jako d/e → a (effects,
permanents, attachments ×2, card-data, registry), equip jako c → d
(attachments), zakresy b–e → b–f (attachments ×2, render, bot — jakość
konsultują wszystkie bramy DEBT łącznie z blokowaniem), zła reguła cały:
`card-data.js` „This token can't block" cytował 702.16e → CR 509.1b (can't
block to NIE ochrona). Bonus: „Offspring, CR 702.16x?" ze znakiem zapytania
w kodzie → literalnie CR 702.175 (mtg.wiki/page/Offspring). Wszystkie
poprawki komentarzowe (diff -U0 bez linii nie-komentarzowych = pusty);
dotknięte pliki testowe 10/10 zielone.

## Bramki (finalny head, zmierzone)

- `npm test` **5614/5614** (0 fail, ~220 s; baza `1521ac8`: 5610);
- `npm run test:all` **5624/5624** (0 fail, ~361 s; mierzone na stanie E2 —
  E3/E4 są wyłącznie komentarzowe/dokumentacyjne);
- `npm run build` **63 moduły / 3745,3 kB** (baza: 63/3742,6 kB);
- `node --test test/bot-benchmark.test.js` **10/10** (~171–179 s, macierz
  w budżecie, każda talia z katalogu).

## Otwarte (następna sesja)

- **Zadanie od właściciela — do odebrania** (pętla ADR 0021 czeka na wskazanie
  tematu; kandydat: kolejna odznaka regułowa albo żądanie właściciela).
- Pełne B0 wyłącznie na komendę właściciela (ADR 0018).

## Znaleziska właściciela z testów (dopisek 2026-09-16, PR #124)

Cztery znaleziska z testów właściciela (Manor Gate ×3, Undercity ×1), każde
osobnym commitem z testem RED→GREEN; bramki finalne: fast **5631/5631**,
test:all **5641/5641**, build **63 moduły / 3754,0 kB**, bot-benchmark
**10/10**:

- **A1 (`53baa568`):** tytuł wyboru koloru przy wejściu (panel + modal)
  „Kolor (np. ochrona)" — dopisek zgadywał CEL wyboru po pierwszym użyciu
  deskryptora (aura). pendingColorChoice niesie teraz `purpose`
  ('mana' playLand / 'protection' aura) + `sourceCardId`; tytuł:
  „Manor Gate — wybór koloru (produkcja many)".
- **A2 (`1d4240ee`):** po wyborze koloru brak śladu na karcie — widok
  wystawia `chosenColor` permanentu (jawny, ADR 0017), kafel pokazuje badge
  „Wybrany kolor: Czarny" (helper `chosenColorBadge`, wzorzec
  protectionBadges). chosenColor celowo NIE wchodzi przez kontrakt addObject
  (stan nadają EFEKTY, nie fabryka).
- **A3 (`8c612528`):** aktywacja {T} po wyborze czarnego obiecywała
  (i produkowała) wyłącznie zieloną. effects.js `add_mana` złącza deskryptor
  many z `chosenColor` obiektu — ta sama reguła, którą kreator many
  (getSourceForObject, M193/A) robił od początku; etykieta
  (manaEffectLabel + threading przez describeEffect/describeAbility w kaflu
  i wierszu „Aktywuj:") pokazuje „dodaj 1 manę zieloną lub czarną".
- **B (`dfa3e039`, regresja):** „undercity — wybierz kartę do ręki" —
  wirtualne karty poza rejestrem (The Undercity, Day // Night) nie były
  w mapie nazw sesji, więc nameOf spadał do surowego cardId. Eksportowana
  lista `virtualCardNames()` zasiewa nameById (wzorzec M188/B dla tokenów).

## Domknięcie pozostałości (dopisek z tej samej sesji, zlecenie właściciela)

Wszystkie obserwacje z audytów #123 i #121 naprawione w PR #124 (każda z
testem RED→GREEN i mutacją L13, osobne commity):

- **O1 (audyt #123, `46ad2dc`):** `landSplit` bez kolorowych pipów — jawny
  błąd zamiast NaN-crashu (`remainders[i % 0]` → TypeError); regeneracja
  24 talii bit w bit.
- **O2 (audyt #123, `279fccd`):** strażnik L16 rozszerzony na CAŁY silnik —
  `pendingReadsAcrossEngine` (skan stripComments po src/engine/*.js; świadomie
  NIE maskNonCode — literały regex z apostrofami połykały kod: 6 z ~70 pól);
  próg ≥60 + piny A2; 70/70 pól pokrytych odciskiem na wejściu.
- **O1 (audyt #121, `c9895cea`):** kreator many fail-CLOSED przy nieznanym
  cardId/rejestrze (oferta ≤ walidacja, L48); dwa testy vm na stany awaryjne.
- **O2 (audyt #121, `4466367d`):** `pendingOptionalTrigger.effects` — pełna
  tablica efektów w widoku + wycena F1 po wszystkich pozycjach. Zakres luki
  doprecyzowany: mill na [1+] NIE był luką (oferta anotuje `selfMill`
  z dowolnej pozycji, M167/B); luka = draw_cards/draw_then_discard/
  mill_from_bottom poza [0]. Golden-master bot-scoring nietknięty.
- **O3 (audyt #121, `2b4638dd`):** proza o tokenach — dokładne zbiory
  (8 w deskryptorach, wszystkie ze zdublowanymi wpisami + 3 ręczne:
  clue/food/incubator) zamiast mylącego „8 z 11"; liczby zmierzone skanem.
- **O4 (audyt #121, `16735581`):** badge `graveyardTypesBadge` — etykieta
  MECHANIKOWA „X = n (typy kart w grobach)" bez nazwy karty (deskryptor
  generyczny, ADR 0002); helper wyodrębniony (wzorzec protectionBadges).

## Pułapki / lekcje tej sesji

- **Odznaka regułowa nowego MODELU domyka pytanie: „jakie JESZCZE zdarzenia
  tej samej reguły CR otwierają ten sam efekt?"** — A1 to dokładnie pominięta
  gałąź „…and/or any state-based actions" z reguły, którą PR cytował
  poprawnie (cytat ✓, model niekompletny).
- **Nowa struktura blokująca priorytet = wpis do `PENDING_DECISION_FIELDS`
  NATYCHMIAST** — grep po `firstPendingDecision` nie wystarcza, gdy blokada
  siedzi w bramce passów (A2). Checklista L95 ma punkt „strażniki
  priorytetu"; rozszerzyć mentalnie o „blokady spoza firstPendingDecision".
- **`gh pr view <nr>` bez `--json` pada na GraphQL Projects (classic)** —
  zawsze `--json` (ENVIRONMENT §3). `git show <sha>` z PR-u wymaga fetcha
  gałęzi sesji (main ma tylko squash).
- **Sondy `.arena/`:** `applyEffect` przyjmuje targets jako **ID** (stringi),
  nie obiekty — bug pierwszych trzech sond; czwarta działa.
- **Deskryptorowa etykieta zgadująca CEL decyzji to zapach projektowy (A1):**
  „(np. ochrona)" sugerowało, że autor kodu wiedział lepiej — cel wyboru musi
  nieść pending z engine (purpose), a UI tylko go nazywa.
- **Dwie kopie tej samej reguły rozjeżdżają się cicho (A3, L41):** union
  „{G} lub wybrany kolor" istniał w getSourceForObject (kreator), a brakło
  go w effects.js add_mana (produkcja) — UI obiecywał jedno, silnik robił
  drugie. Każda reguła opisu, która opisuje ZACHOWANIE silnika, musi mieć
  jedno źródło albo iść w parze z testem integracyjnym obu ścieżek.
- **Wirtualne karty gry (B):** każdy cardId poza rejestrem i deskryptorami
  tokenów (loch, Day // Night) MUSI trafić do nameById — etykiety
  źródłowe „${nameOf(sourceCardId)} — …" inaczej wyciekają surowy slug.
