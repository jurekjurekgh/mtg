# HANDOFF 2026-08-23 — M201 (audyt PR #72 + zgłoszenia właściciela F/M/M2 + U2/O1)

## Stan

- **PR #73** (`arena/01a0300c-mtg`), **OTWARTY** — scalenie to decyzja właściciela.
- `npm test` **3043/3043** · build **53 moduły / 2567.8 kB** · próbka bota
  (`node --test test/bot-benchmark.test.js`) **9/9** · katalog 459 kart.
- Raport audytu: `docs/audits/AUDYT_PR72_2026-08-23.md`;
  plan sesji: `docs/plans/PLAN_2026-08-23-m201-audyt-pr72-petla-jakosci.md`.

## Co wykonano (każde osobnym, zielonym commitem)

1. **N1 — KRYTYCZNE (audyt PR #72).** Debug `process.env.BOT_DEBUG_SCORES &&
   cmd.objectId === 'slaad'` w `scoreCommand` trafił do artefaktu; `process`
   nie istnieje w przeglądarce, więc stół wywracałby się na pierwszym ruchu
   bota. Fix + **generyczny strażnik grafu modułów artefaktu**. Lekcja **L58**.
2. **N2.** `combat_damage_to_you` odpalało trigger raz na atakującego; ruling
   WotC: raz na zadanie obrażeń. Grupowanie per komenda (wzorzec Disa).
3. **M (zgłoszenie właściciela) — PRAWDZIWY BŁĄD.** „cel: ?” na stosie:
   rejestr LKI (CR 603.10) zapisywany w `moveObjectDirectly`
   (`state.lastKnownObjects`), czytany centralnie w `nameOfObject`.
4. **M2, F — werdykty regułowe** (silnik zgodny z Oracle/CR) + testy pinujące.
5. **U2 — BŁĄD REGUŁ.** Darmowy rzut (epic/suspend/rebound) pomijał koszty
   dodatkowe (CR 601.2h). Warianty zapłaty w ofercie + `payFreeCastAdditionalCost`
   w trzech ścieżkach; granica: koszt „discard N” → jawny reject.
6. **O1 — teza odrzucona u źródła** (CR 702.19a: trample nie działa przy
   blokowaniu) + testy pinujące.

## Kolejka / obserwacje dla następnej sesji

- **Pętla jakości Żywym Testerem NIEWYKONANA.** `tools/table-tester` wymaga
  `npm i` (jsdom), a egress HTTPS sandboxa bywa zablokowany (ENVIRONMENT §4).
  Zamiennik użyty w tej sesji i wart powtórzenia: **fuzz headless na
  `createSession`** — własne przebiegi z prostą polityką gracza + inwariant
  (np. „każdy cel czaru na stosie istnieje”). Tą drogą znalazłem repro M.
- **O2 (obserwacja z audytu):** `turn.cantBlockRestrictions` wygasa przy
  tworzeniu nowej tury zamiast w cleanup (CR 514.2) — bez skutku w grze.
- **Granica U2:** darmowy rzut karty z kosztem „discard N” (Cathartic Reunion
  z Epic Experiment) nie jest obsługiwany — jawny reject
  `additional_cost_unsupported`; test pilnuje granicy.
- Warto rozważyć strażnika klasy L58 również dla `tools/` (tam globalne Node
  są legalne — strażnik celowo skanuje wyłącznie graf artefaktu).

## Nienegocjowalne (przypomnienie)

ADR 0020 (PR → audyt → inkrementalne commity), ADR 0021 (pętla domyślna),
ADR 0022 (pełny Oracle albo unsupported), ADR 0018 (pełne B0 tylko na
komendę właściciela), ADR 0002 (zero przypadków po nazwie karty w core),
L56/L57 (reguły i dane weryfikowane u źródła PRZED wdrożeniem), L58 (kod
stołu jedzie do przeglądarki — zero globali Node).
