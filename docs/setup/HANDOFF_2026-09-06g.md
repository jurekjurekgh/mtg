# Handoff — 2026-09-06g (Część 5: NA1/NA2/NA3 + uncover bota)

## Stan zamknięcia sesji
- Branch `arena/01a07711-mtg` = PR #102, HEAD `da6f7e4`, push = head, drzewo czyste.
- Commity sesji (Część 5): `c9ed6bb` (M318 Guildgate gvardian), `a3e59af`
  (M319 etykiety Cloak + numeracja), `1bccf3e` (M320 bot vs ward), `da6f7e4`
  (M321 uncover bota + fix P/T uncover). Wcześniej: `963af06` (M315),
  `6d8dfa9` (M317), `82771e7` (dokumentacja Części 4).
- Bramki: `npm test` **4564/4564**, `test:all` **4574/4574**, build 59 /
  **3344,7 kB**, bench --quick **84,8%** (570/672), golden-master bez zmian
  (hash `9d4a1a9c54f82f49…` — regen robiony dla M315, M317/M318/M319/M320/M321
  nie zmieniają śladu).
- Żywo: worek-legend|ravnica s41, 700 kroków — **0 zgłoszeń detektorów**
  (transkrypt `/tmp/audyt-m321-wl-s41.txt`, plik ulotny).

## Co zrobiono (kolejność właściciela: A→B→C→wycena uncover)
1. **NA3 (M318)** — Guildgate: silnik zgodny; skan 10240 kombinacji + pełna
   sesja; test-gvardian `test/m318-guildgate-czarna-mana.test.js` (4).
   Odpowiedź właścicielowi: {1}{B} = 2 many (sama bramka = 1) albo bramka
   tapnięta (entersTapped).
2. **NA1 (M319)** — etykiety „Nazwa (Cloak N)" zamiast „(Morph)"; stałe numery
   kopii jak tokeny (M172/D); badge kafla „zakryty (Cloak N)"; FoW nietknięte.
3. **NA2 (M320)** — bot nie celuje w ward {2} bez many na dopłatę (podatek
   ward w scoreCommand; fizzle = poniżej passu); RED potwierdzony stashem.
4. **Uncover (M321)** — bot wycenia `turn_cloak_face_up` (tylko main, many,
   zysk > koszt+ward); PRZY OKAZJI BUG z M315: uncover nie przywracał P/T —
   odkryty cloak zostawał 2/2; naprawione przez faceDownOriginal (P/T) +
   odtworzenie w turnFaceUp.

## Dopowiedzenie NA3 (po szczegółach właściciela, 2026-09-06 wieczór)
- Właściciel: „odtapowany Guildgate + kilka innych lądów (bez Swampów), w ręku
  Severed Strands; panel miał tylko „Użyj Dimir Guildgate", brak rzutu; był w
  Głównej 1". Repro pełną sesją (testy M318/E-G): przy nietapniętych landach
  oferta SS **jest** bez tapnienia (E: TAK); brak oferty wyłącznie gdy brak
  celu (stwora przeciwnika, CR 601.2b-c) albo brak własnej ofiary (CR 601.2h)
  albo <2 many (F/G). Hipoteza „najpierw tapnę bramkę" niemożliwa: bramka=1
  mana, SS={1}{B}=2 (G). Odpowiedź wysłana; czekamy na potwierdzenie stanu
  (czy stwór przeciwnika był na stole). Bez zmian w silniku.

## Nierozwiązane / otwarte
- **Odpowiedź właścicielowi** (do wysłania w rozmowie): potwierdzenie badge'y
  M315 („Latanie · Ward {2} · zakryty (Cloak)" — diag `/tmp/diag-m315b.mjs`,
  plik ulotny; wniosek w testach m315) + wyjaśnienie NA3 (koszt {1}{B} = 2
  many / tapnięta bramka) + podsumowanie M319/M320/M321.
- Station: pompowanie ponad próg = ujemny scoring bota (heurystyka, PÓŹNIEJ —
  decyzja właściciela: do heurystyki, nie legalności).
- Złagodzenie semantyki pinezki wyłącznie decyzją właściciela.

## Pułapki na kolejną sesję
- `addObject` gubi pola spoza kontraktu (tapped/faceDown/ward) — ustawiać po
  dodaniu przez `state.objects.set(...)`.
- W komentarzach blokowych `*/` w tekście (np. „cast_*/activate") zamyka
  komentarz — SyntaxError w teście.
- `git stash push <path>` cicho nie działał raz; drugi raz OK — zawsze
  weryfikuj `grep` po stash/pop.
- `gh pr edit` zepsute — body PR wyłącznie REST: `gh api -X PATCH
  repos/jurekjurekgh/mtg/pulls/102 -F body=@…`; plik body odtwarzać z GitHub
  przed PATCH-em (katalog /home/user nie przetrwa).
- Pliki w `/tmp` i `/home/user` (poza repo) są ulotne.
- turn_cloak_face_up: koszt z rejestru (manaCost karty) — playerView nie niesie
  cloakTurnUpCost.
