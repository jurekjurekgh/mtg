# Handoff 2026-09-14e — audyt PR #116 (batch 55 + koszty zdolności) + pętla jakości (PR #118, `arena/01a09fda`)

Trzecia sesja 2026-09-14. Zlecenie „Kontynuujemy projekt" — prompt nie nazwał
tematu, więc obowiązała pętla domyślna (ADR 0021): PR na starcie → audyt
ostatniego scalonego PR (#116) → pętla jakości. Sesja `arena/01a09fcc` (PR
#117) zaczęła ten sam audyt i została zamknięta przez właściciela bez scalenia
— ten PR robi audyt od zera i dostarcza raport na `main`.

Plan: [`docs/plans/PLAN_2026-09-14e-audyt-pr116-petla-jakosci.md`](../plans/PLAN_2026-09-14e-audyt-pr116-petla-jakosci.md).
PR: [#118](https://github.com/jurekjurekgh/mtg/pull/118) (scala właściciel, ADR 0007/0020).

## Co zrobiono (w kolejności commitów)

- **`9c9da75` — E0: plan** — push PRZED kodowaniem (ADR 0020 A).
- **`c5c138d` — E1: raport audytu** — `docs/audits/AUDYT_PR116_2026-09-14.md`:
  **APPROVE (zielone)**. Pełne czytanie diffu 116 plików (wszystkie `src/`,
  testy, narzędzia, snapshoty), niezależne pomiary (5482/5482, 5492/5492,
  build 61/3657,5), weryfikacja ONLINE każdego cytatu CR (edycja 2026-08-07)
  i każdego rulingu (API Scryfall) — 6 znalezisk komentarzowych P1–P6,
  2 obserwacje pokrycia O1–O2. Zero blokujących.
- **`52b1ad2`…`eeb7496` — naprawy P1–P6** — każda osobnym commitem, bez
  zmiany logiki (patrz §13 raportu): P1 cytaty Gift (702.174b/702.174j),
  P2/P3 daty rulingów (BLB 2024-07-26, AKH 2017-07-14), P4 timing-madness
  (702.35a + ruling DMU; 10 miejsc z testami), P5 atrybucja L142,
  P6 604.3→611.3a (11 miejsc; cytat 702.73a nienaruszony).
- **`4a67459` — E3: strażnik** — 2 nowe pary w
  `cr-numery-mechanik-straznik.test.js` (madness+702.35b; „przeliczane"+604.3)
  z cytatami ADR 0030; weryfikacja mutacyjna (cofnięcie naprawy = RED).
- **`fca3f4b` — W1 (E2, Żywy Tester)** — oś 3 detektora zgłosiła „Rzuć za
  warp:" bez ptaszka wyciszenia: `warp_card` i `turn_manifest_face_up`
  brakowały w `OPTION_IGNORABLE_TYPES` (kontrakt `actions.mjs` mówi, że
  mają ptaszek jak plot/suspend/cloak). Naprawa + regresja w
  `test/choice-ignore.test.js`; dowód: ta sama partia po naprawie = 0 detektorów.

## Bramki (finalny head, zmierzone)

- `npm test` **5485/5485**, 0 fail, 147,9 s (baza: 5482/5482).
- `npm run test:all` **5495/5495**, 0 fail, 304,6 s (baza: 5492/5492).
- `npm run build` **61 modułów / 3658,7 kB** (baza: 3657,5 kB — komentarze).
- Quick benchmark: **heuristic 82,6% (555/672) / aggro 30,7% / random 4,2%**
  — identyczny z bazą; golden master bez churnu (polityka bota nietknięta).

## Pętla jakości (E2) — co przećwiczył Żywy Tester na świeżym dist

12 partii (6 zwykłych + 6 celowanych chwilowymi taliami wg M331; talie
usunięte przed bramką): Embalm na żywo (wygnanie z grobu → token-kopia 2/1 →
walka), Gift na żywo (etykieta „dar dla przeciwnika: Food", log „Obiecujesz
dar (Food) — … przed efektami czaru", Food tworzony przez odbiorcę i
aktywowany), Duskmantle Seer na żywo (upkeep: obie strony odsłaniają, utrata
życia = MV, karta do ręki bez „dobrania", FoW ukrywa nazwę cudzej karty),
Lifecrafter's Gift, Brightwood Tracker (decyzja satyr-look nie wystąpiła na
żywo — pokryta 7 testami jednostkowymi). 5/6 partii zwykłych bez zgłoszeń
detektorów; 1 zgłoszenie = W1 (naprawione).

## Pułapki i lekcje sesji

- **L143 (nowa):** sweep numerów CR zmienia NUMER, nie znaczenie — 702.34e
  (dawny timing-madness) nie ma odpowiednika w 702.35a–c; zamiast 1:1
  przepisania numeru trzeba sprawdzić, co podreguła znaczy w aktualnym CR
  (naprawa: 702.35a + ruling DMU 2023-01-06).
- **Dopisek do L137 (nowy):** kontrakt osi 3 ptaszka jest DWUKIERUNKOWY —
  PR #116 naprawił dryf po stronie testera (actions.mjs), a UI wciąż miało
  własną lukę (`warp_card`, `turn_manifest_face_up`); złapał ją detektor
  w pierwszej partii, w której etykieta się pojawiła (W1).
  Narracje L142/L143: `docs/LESSONS_PRZYPADKI.md` (L66: budżet lektury —
  szczegóły poza LESSONS).
- Proweniencja dat rulingów w komentarzach kart: „2017-04-18?" przy BLB to
  sygnatura kopiuj-wklej z sąsiedniej karty — po naprawie każda data ma
  dosłowny cytat.
- `gh pr diff` czytany per plik z ekstrakcją do `/tmp/*.diff` — `cat` kilku
  plików naraz ucina środek (L78).
- Chwilowe talie audytowe (`decks/audyt-*.txt`) MUSZĄ zniknąć przed bramką
  (M178/ADR 0023 — strażnik „dokładnie jedna talia").

## Otwarte (następna sesja)

- Pętla jakości zaakceptowana — kolejna sesja wraca do ADR 0021 od początku
  (PR → audyt ostatniego scalonego PR).
- D1/O1/O2 z audytu PR #115 — dalej nieblokujące.
- S-1 (literalne `\n` w `oracleText`, 20 kart) — bez zlecenia właściciela nie
  ruszane; audyt K ujawnił, że kosztuje to 1 zdolność poza zasięgiem
  strażnika (`strandwalker`).
- `[Trigger:]` prefix w linii stosu — kandydat UX z handoffu; w 12 partiach
  E2 nie był widoczny jako problem (nie ruszany).
- Pełne B0 wyłącznie na komendę właściciela (ADR 0018).
