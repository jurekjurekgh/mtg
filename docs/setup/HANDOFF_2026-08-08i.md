# Handoff 2026-08-08i — po M57 (złota odznaka: 5 błędów vs zasady MtG)

## Stan po sesji 2026-08-08 (PR `arena/019fe265-mtg` — M54 + 3 odznaki + M55)

- **Branch:** `arena/019fe265-mtg` — PR #36 OPEN do `main` (Squash and merge).
  HEAD: **d9601e0** (fix złotej odznaki). Commity 12 → 13.
- **Stan:** `npm test` **1131/1131**, build **49 modułów / 1225.8 kB**,
  withArt **168**, benchmark 1080 meczów 0 crashy. B0: progi 0.78/0.57.

## M57 — 5 błędów vs zasady MtG (złota odznaka, d9601e0)

1. **CR 514.1** — cleanup odrzucał nadmiar dla OBU graczy; nieaktywny tracił
   karty. Fix: tylko aktywny gracz.
2. **CR 119.3** — combat damage_dealt niósł kwotę PRZED prewencją; triggery
   „deals combat damage" odpalały przy 0 zadanych (Kappa). Fix: event z kwotą
   zadaną + guard `ev.amount > 0` w skanach triggerów + tracker bloodthirst.
3. **CR 611.2c** — buffy „do końca tury" (Hysterical Blindness, Turn the
   Tide, Angel of the Dawn, Your Temple) były jednorazowe. Fix:
   `state.untilEndOfTurnBuffs` — efekty ciągłe czytane przy każdym odczycie
   statystyk (effectivePower/Toughness/Keywords), czyszczone w cleanup,
   w fingerprint. NOWA KONWENCJA: nowe karty z „...until end of turn" na
   wielu celach → rejestrować w untilEndOfTurnBuffs, nie modifyStats.
4. **Opcjonalne płatności triggerów** (Panic Spellbomb {R}, Zoraline {W}{B})
   — canPayTrigger liczył tylko pulę; gracz z nietapniętym landem nie widział
   oferty. Fix: producibleMana.
5. **CR 104.3c** — dobranie z pustej biblioteki przez EFEKT karty nie kończyło
   gry. Fix: drawPlayerCards kończy grę (draw_cards, draw_cards_both_players).

## Kolejka (po scaleniu PR #36)

- **Batch 25** — 10 kart (Scryfall ZAWSZE z set=; api zablokowane → fetch_page).
- **B2-w2 bota** — lookahead (infra gotowa, wymaga przeprojektowania ewaluacji).
- Talie singleton — rosną z batchami (strażnik repo-decks).

## Pułapki (środowisko / projekt)

- `edit_file` psuje polskie znaki → python3 (Path.read_text/write_text).
- NOWE efekty „until end of turn" na wielu celach → untilEndOfTurnBuffs
  (nie modifyStats per-obiekt).
- Obrażenia: combat damage event z kwotą ZADANĄ; triggery „deals damage"
  z guardem ev.amount > 0.
- Dobranie z pustej biblioteki = przegrana (także z efektów) — testy nowych
  kart muszą dawać bibliotekę.
- canPayTrigger: produkowalna mana (pula + nietapnięte źródła).
- Testy z seedami sesji (logi) przelosowywane przy każdej zmianie talii.
- Lektura: AGENTS.md → PROJECT_STATE (M54–M57) → HANDOFF_2026-08-08f/g/h/i →
  ENGINE_MILESTONES (M53–M57) → WORKFLOW → plany → ADR 0002/0003/0005/0010/
  0011/0013/0014.
