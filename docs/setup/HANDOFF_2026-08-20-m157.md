# HANDOFF 2026-08-20 — M157 (uwagi właściciela z review PR #66)

## Stan

- **PR #66** (`arena/01a01eb3-mtg`) — sesja M156+M157. Commity M156
  wypchnięte; **commity M157 (4) czekają na push — GH_TOKEN wygasł
  w trakcie sesji** (1ac0a8d, 7afa3f4, 00b4046, 2ab3124). Po reconnect
  GitHub w Arenie: `git push origin arena/01a01eb3-mtg` + zaktualizować
  opis PR (treść poniżej w „Opis PR do wklejenia").
- `npm test` **2458/2458**, `test:slow` 9/9 (test:all 2467), build
  **51 modułów / 2088.6 kB**.

## Decyzje właściciela (review PR #66, 2026-08-20)

1. **ADR 0022:** KAŻDA karta `supported` = 100% zgodna z Oracle albo
   nieobsługiwana. `limitations` tylko fakty strukturalne (token/tył
   DFC/format). `notes` nigdy nie opisują luk — strażnik pilnuje także
   słowa „uproszczenie".
2. **F4:** tylko implementacja (a) — wykonane (Wftblade: wielocelowy
   trigger ETB, deskryptor `requiresTarget.count/upTo`).
3. **L28:** inwentaryzacja wycen — wykonana jako stały strażnik
   `test/bot-targeted-effect-valuation-guard.test.js` (+4 naprawy bota).
4. Uszkodzony plik audytu — usunięty; treść pod nową nazwą
   `docs/audits/AUDYT_2026-08-20-batch38-zywy-tester.md`.

## Co zrobiono w M157 (commity)

1. `1ac0a8d` — ADR 0022 + plan + ratunek pliku audytu (blob → nowa nazwa).
2. `7afa3f4` — fixy **A–F**: hover bez niby-karty; 8 tokenów ze obrazami
   Scryfall; **Skilled Animator** (animacja „as long as" + Station sync);
   **stun** (pauza+render przy zejściu liczników i pierwszym untapie);
   log całej rozgrywki; **panel trucizny** (Poison Counter tecc/13).
3. `00b4046` — **F4(a)**: wielocelowy trigger ETB w 100% Oracle.
4. `2ab3124` — **L28**: strażnik wycen czarów/zdolności + naprawy
   (Mournful Zombie, Spreading Insurrection, 2× grób).

## Pułapki nowe (M157)

- **Ścieżka `docs/audits/AUDIT_BATCH38_ZYWTESTER_2026-08-20.md` jest
  „przeklęta" na FS sandboxa** — ls widzi wpis, każdy open ENOENT, nawet
  świeżo utworzony plik pod tą nazwą. Nie odtwarzać; używać nowej nazwy.
- **GH_TOKEN wygasł po ~6 h sesji** — objaw: `git push` = „could not read
  Username". Reconnect w Arenie; commity lokalnie bezpieczne.
- Patche pythona: NIE zgadywać treści komentarzy (polskie cudzysłowy
  „”/… się rozjeżdżają) — kotwice brać z `grep -n` dokładnych linii.
- Nowe elementy UI wymagają wpisu w mini-DOM testów (`test/table-ui.test.js`
  `installMiniDom` — lista id) — inaczej `el('poison')` rzuca.
- Weftblade w testach ETB: rzut → **2 pasy** → dopiero decyzja celu
  (permanent wchodzi przy rozstrzygnięciu stosu, nie przy rzucie).

## Opis PR do wklejenia (po reconnect)

Sekcja „M157" do dopisania w #66: ADR 0022 (pełny Oracle albo brak
wsparcia), fixy A–F z testów właściciela (hover, tokeny 8×, Skilled
Animator linked-animacja + Station, stun-render, pełny log, panel
trucizny), F4(a) wielocelowy trigger ETB (Weftblade 100% Oracle),
L28: strażnik wycen celowanych czarów/zdolności + 4 naprawy bota.
npm test 2458/2458, test:slow 9/9, build 51/2088.6 kB.

## Kolejka (po decyzji o merge PR #66)

- Pętla jakości wg ADR 0021; ewentualne uwagi z testów M157 na telefonie
  (zwłaszcza: panel trucizny, pauzy stun, pełny log — wydajność przy
  bardzo długich partiach).
- Weryfikacja na żywym stole: Weftblade 2 cele w UI (modal grupuje
  warianty — patrz L32), Mournful Zombie, Spreading Insurrection.
- Backlog (nie kolejka): wycena create_copy_token/animate_permanent
  (świadome wpisy REVIEWED w strażniku).
