# Handoff 2026-09-11 — zgłoszenia właściciela A–G z gry przy stole (PR #113, arena/01a08d0e)

Stan: **wszystkie zgłoszenia A–G zamknięte**, bramka PR zielona. Ta sama
gałąź/PR co audyt PR #112 i pętla jakości (ADR 0013/0020) — PR #113 czeka na
squash właściciela (nigdy nie scalaj sam, nie pushuj do `main`).

## Co zrobiono (w kolejności commitów)

| Commit | Treść |
|---|---|
| `a63a0dc` | **E1** — Furious Forebear nie odpala przy własnej śmierci (i przy współpoległych z tej samej partii SBA) |
| `299c1b5` | **A** — rodzaj źródła w kluczu wariantów płatności → kreator many się otwiera (Forest vs Scorned Villager) |
| `fe37168` | **B1+B2** — rozdział III Sagi jako opóźniona zdolność w `state.turnAbilityGrants` (LKI źródła, CR 603.10) + poświęcenie Sagi jako SBA `sacrificeFinishedSagas` (CR 714.4) i rozdział z każdego licznika lore (CR 714.2b) |
| `e6d3a5b` | **E2+E3** — predykat `isBotDecisionPrompt` przy obu pisarzach logu (prompty bota w sekcji „Ruch bota") + `appendTextWithManaIcons` (ikony many w logu i w modalu) |
| `baaba03` | **F** — `playerView` niesie `enchantPlayer`/`enchantedPlayerId`, `cardInfo.cursedPlayerId`, badge „Klątwa: …" w `buildFace` i `buildStateOverlay` |
| `90dfc48` | **G** — klątwa na własnego gracza `-curseSelfTargetPenalty` (1000), na przeciwnika `+curseEnemyBase`; `HOSTILE_ENCHANTED_PLAYER_EFFECTS` + `auraIsHostile` |
| `da01e00` | **C** — Exploit: koszt millu z danych karty, bramki zapasu biblioteki, cena ofiary z keywordami/zdolnościami i premią za token, `sourceId` w `resolve_exploit_choice` |
| `19be457` | **G/4** — cele aury „Enchant player": przeciwnik pierwszy, rzucający ostatni |
| `a75cc5d` | **D** — gałąź gangu blokerów znaczy atak jako jałowy (premii wyścigu nie da się przebić kary) + świadoma regeneracja golden-mastera |
| `fbbb34a`, `8172cf3`, `9403798`, `39473e8`, `ca14d4d`, `e24d6a0`, `0a945fc` | plan: triaż z pomiarami + wpisy wykonania po każdym segmencie |

Szczegóły (RED, mutacje, bramki, luki pokrycia): `docs/plans/PLAN_2026-09-10c-zgloszenia-wlasciciela-a-f.md`
i opis PR #113 (sekcja „Zgłoszenia właściciela z gry przy stole (A–G)").

## Bramki (zmierzone na końcu sesji)

- `npm run test:all` (bramka PR): **5151/5151**, exit 0
- `npm test` (szybki rdzeń): **5141/5141**
- `npm run build`: 61 modułów / **3486,9 kB**
- Benchmark quick: heuristic **84,7% (569/672)** — pomiar A/B przed i po D
  (bez zmian); pełna macierz B0 wyłącznie na komendę właściciela (ADR 0018)
- Żywy Tester na `dist/`: detektory **0** we wszystkich partiach pomiarowych
  (klątwa bota na przeciwniku, partia 5001 po zmianie kolejności celów,
  Exploit poświęcający vanilla 1/1)

## Kolejka / otwarte (nie zapomniane)

- **Kolejność opcji celu dla innych czarów**: poprawka G/4 dotyczy wyłącznie
  aury `enchantPlayer`. Gdyby właściciel chciał „przeciwnik pierwszy" także
  dla innych celów-graczy (np. mill/discard), to osobna decyzja — dziś
  kolejność bierze się z `state.players`.
- **Luka E2**: bramka `isBotDecisionPrompt` w pętli `apply` nie ma testu
  karcianego (żadna karta katalogu nie produkuje promptu bota w strumieniu
  komendy człowieka; ward nie ma karty) — mutacja tej bramki → 0 RED.
- **Luka C/D**: bramka biblioteczna Exploitu i scenariusz D są przypięte
  testami, nie transkryptem (tester nie drukuje liczby kart w bibliotece,
  a scenariusz gangu nie wystąpił w partiach pomiarowych).
- Z poprzednich sesji: `session.js` `ctrl === null` → `significant = true`
  (konserwatywna pauza bez strażnika); skan nightbound czyta surowe
  `o.keywords` zamiast `effectiveKeywords` (dziś żadna karta nie nadaje
  daybound efektem).
- Katalog rośnie wyłącznie z kolekcji właściciela (ADR 0029) — agent nie dodaje
  kart; w katalogu jest 509 kart, w tym JEDNA aura „enchant player"
  (Curse of the Pierced Heart) i DWA stwory z exploitem (Gurmag Drowner,
  Silumgar Butcher).

## Pułapki środowiska (potwierdzone w tej sesji)

- **Workspace jest re-klonowany na `f7e4d11` przy starcie tury** — lokalna
  gałąź nie ma commitów sesji, a zdalny head jest poprawny. Rytuał przed
  commitem: `git fetch origin arena/01a08d0e-mtg` → `git reset <zdalny head>`
  (mixed, bez dotykania drzewa) → `git status` musi pokazać WYŁĄCZNIE pliki
  bieżącej zmiany. Commit bez pusha **przepada** między turami — jeśli push
  jest zablokowany, zapisz `git format-patch` poza repo (`~/patches/`).
- `tools/table-tester` potrzebuje `npm i` (jsdom) — `node_modules` nie
  przetrwa tury; talie są wbudowane w `dist/`, więc po dodaniu talii
  tymczasowej trzeba `npm run build`, a po pomiarze usunąć plik i przebudować
  (3 testy `repo-decks` słusznie protestują przeciw talii widmu).
- `gh pr edit --body-file` **pada** na tym repo (GraphQL „Projects (classic) is
  being deprecated"); działa REST: `gh api -X PATCH repos/<owner>/<repo>/pulls/<n> -F body=@plik`.
- Zmiana wyceny bota czerwieni golden-master (`test/bot-scoring-snapshot.test.js`)
  — przy ŚWIADOMEJ zmianie regeneracja `node tools/bot-scoring-snapshot.mjs --write`
  i wpis w commicie, która partia i jak się zmieniła.
- `concede` ma w trace bota score `-Infinity` (w JSON-ie `null`) — asercje
  „żaden wariant nie jest gorszy niż X" muszą go wyłączać.

## Plany / dokumenty

- Plan segmentu: `docs/plans/PLAN_2026-09-10c-zgloszenia-wlasciciela-a-f.md`
- Audyt PR #112: `docs/audits/AUDYT_PR112_2026-09-10.md`
- Poprzedni handoff: `docs/setup/HANDOFF_2026-09-10b.md`
