# Handoff 2026-09-07e — PR #104, audyt PR #103 i M339–M348

## Stan bieżący

Sesja `arena/01a07b98-mtg`, [PR #104](https://github.com/jurekjurekgh/mtg/pull/104)
do `main`. Agent nie scala. Kod zamknięty i wypchnięty w **`396431e` (M348)**;
po nim wyłącznie dokumenty zamknięcia. Sprawdź bieżący HEAD/stany PR i CI na
GitHub — plik nie pinuje własnego przyszłego SHA. Obie blokady 401 z tej
sesji usunięte przez odświeżenie połączenia GitHub w Arena.

Plan `docs/plans/PLAN_2026-09-07-audyt-pr103-petla-jakosci.md` wykonany E0–E4.
Audyt: `docs/audits/AUDYT_PR103_2026-09-07.md` (59/59 plików poprzedniego PR,
19 prób mutacyjnych jego testów, F1–F10 z naprawami i dowodami). Historia
całości w `docs/PROJECT_HISTORY.md`, reguły w LESSONS/ADR.

## Bramki i dane do porównania

- `npm test`: **4695/4695** (start 4636; +59 testów).
- `npm run test:all`: **4705/4705**, w tym 10 testów benchmarku-regresji;
  pomiar na końcowym kodzie 219,3 s. Końcowy commit dokumentów ma osobną
  bramkę fast + build; wynik CI sprawdzany po ostatnim pushu.
- `npm run build`: **59 modułów / 3369,8 kB**, plik `dist/mtg-table.html`.
- `node tools/family-audit.mjs`: brak naruszeń.
- Quick po M343: **570/672 (84,8%) heuristic**, aggro 89/336 (26,5%),
  random 13/336 (3,9%) — identycznie z bazą. Późniejsze zmiany tylko w
  UI/narzędziach. Fixture `bot-scoring-snapshot.json` i progi nietknięte.
- **Pełnego B0 NIE uruchamiano**. Raport #103 z 5700 meczów tylko
  zweryfikowano (570 komórek, sumy, zgodność TXT z formatterem JSON).
- Żadnych nowych kart/talii/limitacji Oracle. Nośniki syntetyczne wyłącznie
  w testach. `decks/` i snapshoty kart kolekcji bez zmian.

## Co naprawiono

| M | Istota |
|---|---|
| M339 | numery cloaków z jawnej kolejności, nie tajnego cardId; FoW sprawdzona na całym widoku |
| M340 | manifest daje istniejące procedury morph/megamorph |
| M341 | wygrana proliferacji po ocenie całego wyboru, bez zależności od kolejności ID |
| M342 | brak fikcyjnej premii ETB odsłonięcia; poprawiony błędny test m334/C |
| M343 | SBA po zakończeniu pendingSpell (nawet pustego ogona), także zanik tokenów; korekta m336/E2 i testu Rage of Purphoros |
| M344 | niepróżny strażnik bramki manifestu, z powrotem oferty i wykonaniem |
| M345 | błędne odpowiedzi/429 nie kasują rulingów; pierwsza pusta lista ma proweniencję |
| M346 | najnowszy ogon logu, właściwe nowe odrzucenia, strukturalny detektor starego ogona |
| M347 | oznaczenie dodatkowego kosztu wygnania ≠ pusta cena; etykiety panelu z rekordów okien |
| M348 | logSystem zamiast wywoływania tablicy; nowa akcja nie przepada po anulowaniu kreatora; runtime errors obserwowane od beforeParse |

## Żywy stół — dowody i ograniczenia

Końcowy artefakt, wszystkie partie z 0 wyjątków JS i 0 flag:
- innistrad-wu|warhammer-ubr, seed 10401, explorer/policy13/tick0.2:
  19 okien, koniec −2/20;
- mirrodin-brg|zendikar, seed 10402, defensive/policy17/tick0.3:
  15 okien, koniec −9/23;
- warhammer-wg|wiedzmin, seed 10403, impatient/policy19/tick0.15:
  126 okien, koniec 14/−5; sprawdzone quiet i snapshot-every 1.

M346: 14 flag starego ogona → 0 w obu trybach. M347: fałszywy pusty koszt
1→0 w verbose. M348: **6 rzeczywistych wyjątków → 0 w obu trybach** — przed
poprawką gra też kończyła się i zwykłe detektory mówiły 0. Teraz `runtimeErrors`
trafiają do `runDetectors` i wyniku narzędzia. Nie wycisza ich profil impatient.

Transkrypty w `tools/table-tester/audyt-pr104/` są gitignorowane; nie trzeba
ich odzyskiwać, by powtórzyć testy. Dowody w audycie. jsdom nie mierzy layoutu,
obrazów ani gestów na telefonie. Heurystyka nie gwarantuje optymalnego ruchu;
polityka odsłonięcia nadal konserwatywnie ograniczona do main (nie jest to
ograniczenie legalności silnika). Nie stroić wag bez pomiaru.

## Następna sesja / wznowienie

1. Lektura zgodnie z AGENTS §0: AGENTS, aktualne ADR, LESSONS, ENVIRONMENT,
   rzeczywisty ostatni PR i najnowszy handoff. W TEJ sesji lektura już wykonana;
   nie powtarzać od początku po samym odświeżeniu GitHub.
2. Sprawdź, czy #104 scalono/zamknięto. Jeśli tak, stara sesja nie koduje i
   nie modyfikuje GitHuba; nowa pracuje na swojej przydzielonej gałęzi.
   Audyt najnowszego scalonego PR poprzedza nową implementację (ADR 0020).
3. Nie ma niedokończonego batcha. Bez nowej listy właściciela kontynuacją jest
   audyt i pętla jakości (ADR 0021), nie wybieranie backlogu ani dodawanie kart
   do testów na stałe. Pełny B0 tylko na nowe jawne polecenie właściciela.
4. Ponów bramki z powyższymi wartościami. Przed testerem build i w razie
   potrzeby `npm ci --prefix tools/table-tester`. Zawsze przeglądaj także stderr.

## Pułapki

- Startup budget po dokumentacji: **99 904/100 000**. L141 nowa; dopiski
  L12/L13/L138, narracje L133/L135 wyniesione 1:1 do PRZYPADKI. Każdy nowy
  wpis wymaga pomiaru/kondensacji, nie podnoszenia limitu ani kasowania kotwic.
- Snapshot/odrzucenia czytają DOM od początku, a raportują chronologicznie.
  Przypadki `(koszt)` rozstrzygaj po znaczeniu, nie samych spacjach; nie
  pomijaj całego panelu, bo obok może być prawdziwy błąd ceny.
- `session.log` to tablica, `session.logSystem(text)` zapisuje komunikat UI.
  Normalne odrzucenie komendy ≠ wyjątek JavaScript.
- Mutacje cofaj kopią w finally, nie `git restore`/resetem brudnego drzewa.
  Każdy zielony przyrost osobno commit + natychmiast push na gałąź sesji.
- 401: poproś o ponowne połączenie GitHub w Arena; nie żądaj haseł/tokenów.
  Przed wznowieniem publikacji sprawdź OPEN, stan drzewa i rozbieżność refs.
- `dist/`, node_modules i `.arena/` nie persistują po odtworzeniu sandboxa.
  `.arena/` wykluczono lokalnie przez `.git/info/exclude`; nie dodawać sond,
  logów, kopii archiwum ani artefaktów do indeksu.
