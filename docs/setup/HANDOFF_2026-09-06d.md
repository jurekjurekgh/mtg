# Handoff 2026-09-06d — arena/01a07711-mtg: kontynuacja pętli jakości, M311–M313 (PR #102)

## Stan
- Gałąź `arena/01a07711-mtg`, PR **#102** (kumulatywny opis z
  `/home/user/pr-body.md`, aktualizacja wyłącznie REST:
  `gh api -X PATCH repos/jurekjurekgh/mtg/pulls/102 -F body=@/home/user/pr-body.md`
  — `gh pr edit` nadal zepsuty / GraphQL). Scalenie = decyzja właściciela
  (squash). 1 sesja = 1 gałąź = 1 PR — wszystko wpada do #102.
- **Uwaga porządkowa (ważne dla następnej sesji):** LOKALNA gałąź była
  przestawiona na squash-merge PR #101 (`6ce4cab`), a praca fazy 3 żyła
  w drzewie roboczym; ORIGIN zachował jednak pełną linię fazy 3
  (`5466cfb` plan+audyt, `e19c836` F1, `850134c` F2, `215ed1d` F3,
  `1df6b22` docs+HANDOFF c, `34607b4` M310, `e80d395` README L92).
  Pracę sesyjną zrebase'owano na `e80d395` (tymczasowe commity
  odtworzeniowe wypadły jako patch-identyczne z upstreamem; drzewo
  bit-w-bit identyczne przed/po — pusty diff). Hashe commitów sesyjnych
  PO rebase: `5b34472` (M311), `3794667` (M312), `8ef11f3` (M313),
  `a2d0034`→amend (docs). F1/F2 potwierdzone w kodzie (komentarz F2
  w picker.js; pin m305/4 zielony). Wniosek na przyszłość: przy rozjeździe
  log↔drzewo NAJPIERW `git fetch` + sprawdź origin, dopiero potem
  rekonstruuj.
- Commity sesji kontynuacyjnej:
  - `5e086d1` — **M311 (tor A)**: Apprentice Wizard — kreator many netował
    koszt aktywacji z produkcją (opis „+2" kłamał, plan płatności źle
    liczył). Kontrakt: pełna produkcja + `activationCost {generic, colors}`
    osobno; skip gdy `produkcja − kosztGeneryk ≤ 0`; solver
    `Σamount − ΣkosztGeneryk ≥ need`, pipy kosztu = dodatkowe wymagania;
    silnik płaci koszt PRZED produkcją. Stary pin
    `table-mana-wizard.test.js` przełożony (amount 3, deepEqual
    activationCost, fake `info` z `costColors`). Testy m311 (4).
  - `b107c56` — **M312 (tor B)**: Village Rites — surowe encje HTML ikon
    many w wierszach kreatora. Routing TREŚCIOWY w `addRow`
    (choice-request.js): `labelOverride.includes('<')` → kanał `html`
    (innerHTML, kontrakt M104/A2), czysty tekst → `label` (textContent);
    bramka nazwy picker.js (radio/checkbox): `html || (label !== null &&
    label !== '')`. Ślepe labelOverride→html ZŁAMAŁO 4 piny mini-DOM —
    nie wracać (m298/A4, m299/7, m300/5, m300/6). Testy m312 (3).
  - `71da22f` — **M313 (tor C)**: panel prędkości (Start your engines!).
    Silnik miał speed w całości (DFT Batch 24); brakowało projekcji
    `speed` w playerView i panelu UI. Panel `#speed` wg wzorca Poison:
    marker tdft/14 (front „Start Your Engines!" / przy speed ≥ 4 TYŁ
    „Max Speed"), wiersze `.speed-count` tylko dla graczy z speed > 0,
    licznik „X z 4" z „(maks.)", klik = `openSpecialCardFullscreen`,
    hover; CSS `.speed-*` (index.html). Testy m313 (5) + pin
    player-view.test.js (speed w projekcji).

## Bramki zielone (świeży pomiar na zamknięcie sesji, L92)
- `npm run test:all`: **4537/4537**; szybki rdzeń `npm test`: **4527/4527**
  (start kontynuacji: 4522/4522; +5 m313).
- `npm run build`: 59 modułów / **3327,6 kB**.
- Benchmark szybki (zmierzony): heuristic **84,8% (570/672)**, aggro 26,5%
  (89/336), random 3,9% (13/336) — bez dryfu. Pełny B0 nie odpalany
  (ADR 0018).
- Żywy Tester (regresja po torach): kaladesh|alara s23 greedy policy-seed 1,
  partia do końca, prędkość obu graczy do 4 (ścieżka „maks." panelu
  ćwiczona), **0 zgłoszeń detektorów**; pokrycie UI 27/25, sondy noop 91.

## Co teraz obowiązuje (następna sesja musi to znać)
1. **Rekonstrukcja po resecie gałęzi**: jeśli drzewo robocze ma treść bez
   commitów (rozjazd log vs `git status`), NAJPIERW zrekonstruuj commity
   z diffów (jak wyżej), dopiero potem kodź dalej. Nie zakładać, że pamięć
   sesyjna o commitach jest prawdziwa — weryfikować `git log`.
2. **M311**: kreator many: źródło-zdolność = pełna produkcja + koszt
   aktywacji osobno; etykieta „+3 — koszt aktywacji {1}{U}"; solver
   liczy netto per źródło. Pin: `test/m311-kreator-many-produkcja.test.js`
   + przełożony pin `test/table-mana-wizard.test.js` (L180).
3. **M312**: kanał etykiety wiersza wybiera TREŚĆ (`includes('<')` → html,
   inaczej textContent), nie wywołanie; picker gate musi liczyć się z html.
   Pin: `test/m312-etykiety-html-wiersze.test.js` (mini-DOM zjada tagi w
   textContent — asercje po children/innerHTML).
4. **M313**: speed = licznik publiczny gracza (jak poison) w playerView;
   panel `#speed` renderuje TYLKO zapalonych (speed > 0), speed ≥ 4 →
   obraz markera = TYŁ („Max Speed") + „(maks.)" w tekście. Pin:
   `test/m313-speed-panel.test.js` (5; helper `panelZPrzechwytem(run)`
   wywołuje render WEWNĄTRZ kontekstu createElement — synchronicznie, L135
   styl: nie async w mini-DOM helperach).
5. **Station (M310)**: pompowanie ponad próg legalne (Oracle), scoring
   bota ujemny; pin `test/m310-station-ponad-prog-scoring.test.js` (3).
6. L135 i L92 bez zmian; `gh pr edit` dalej zepsuty — tylko REST PATCH.

## Następne kroki (propozycja, decyzja właściciela)
- Kolejne tematy z pętli jakości (ADR 0021) / Batch 52 (ADR 0029 — agent
  nie dodaje kart).
- PR #102 rośnie; właściciel squasuje, gdy uzna.
