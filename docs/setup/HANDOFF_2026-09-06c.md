# Handoff 2026-09-06c — arena/01a07711-mtg: audyt PR #101, F1–F3, domknięte wątki E3

## Stan
- Gałąź `arena/01a07711-mtg`, PR **#102** (otwarty, kumulatywny opis z
  `/home/user/pr-body.md`). Scalenie = decyzja właściciela (squash).
- Commity nad `6ce4cab` (= scalony PR #101):
  - `5466cfb` — plan sesji (`docs/plans/PLAN_2026-09-06b-audyt-pr101-petla-jakosci.md`).
  - `2d196a0` — audyt PR #101 (`docs/audits/AUDYT_PR101_2026-09-06.md`).
  - `e19c836` — **F1**: martwa wewnętrzna bramka `pendingSatyrLook.cards`
    (klasa L48/7) — usunięta, zachowanie bit w bit.
  - `850134c` — **F2**: martwy parametr `openLabel` w `renderPickerName`
    (A7 z audytu PR #100, zmierzony) — usunięty, zachowanie bit w bit.
  - `215ed1d` — **F3**: pozycja celu OPCJONALNA w kreatorze wielocelowym
    (`slotOptional` z komend silnika; kreator mapuje pustą pozycję opcjonalną
    na wariant `null`; Tester ma gałąź trybu pozycyjnego) +
    `test/m309-slot-opcjonalny-kreator.test.js` (6).
  - `<docs>` — PROJECT_HISTORY (faza 3), L135 + kondensacja L126/L131,
    plan (rozliczenie), HANDOFF (ten plik), README L92, korekta cytuatu CR
    w audycie (§4.1). **Commit lokalny — patrz UWAGA o wypchnięciu.**

## Bramki zielone (świeży pomiar na zamknięcie sesji, L92)
- `npm run test:all`: **4522/4522**; szybki rdzeń `npm test`: **4512/4512**
  (start sesji: 4506/4506; +6 m309).
- `npm run build`: 59 modułów / **3320,0 kB** (po F1/F2: 3318,8/3318,9).
- Benchmark szybki PO F1/F2/F3 (zmierzony, nie zgadnięty): heuristic
  **84,8% (570/672)**, aggro 26,5% (89/336), random 3,9% (13/336) — identycznie
  wobec baseline'u PR #101. Pełny B0 nie odpalany (ADR 0018).
- `bot-tie-audit` 24 partie: 11322 decyzji / 631 remisów (29,1%) / 213
  realnych (12,2%) — bit-w-bit identycznie na HEAD i na bazie `6ce4cab`
  (F1/F2 zero dryfu; rozjazd wobec 625/197 w opisie PR #101 = zmiana talii
  w fazie #101, potwierdzone rekonstrukcją Stifle w bazie: 619/200, przybliżona).

## Co teraz obowiązuje (następna sesja musi to znać)
1. **L135**: nowy KSZTAŁT komendy (np. `null` na pozycji celu) musi mieć
   obsługę u WSZYSTKICH konsumentów — kreator (`commandFor*`), sterownik
   testera (rozpoznawanie intro), wycena bota. Deskryptor kształtu czytany
   z KOMEND silnika (`plan.slotOptional`), nie z rejestru kart (ADR 0002).
2. **Pusta pozycja OPCJONALNA** („up to one target") = wariant `null` — wybór
   kompletny; OBOWIĄZKOWA bez zaznaczenia = niekompletna (status „Brakuje"
   wymienia tylko obowiązkowe). Pin: `test/m309-slot-opcjonalny-kreator.test.js`.
3. **Budżet lektury**: po L135 i kondensacji L126+L131 (narracja →
   `LESSONS_PRZYPADKI.md`) zmierzono **99,96k/100k** — kolejna lekcja znów
   wymaga kondensacji (procedura przetestowana, adresacja pilnowana przez
   `test/docs-decisions.test.js`).
4. **Station**: legalność aktywacji ponad progiem potwierdzona (CR/silnik
   bez górnej granicy), a SCORING BOTA karze pompowanie ponad poziom —
   `charge >= threshold → −15` (+ koszt tapu −3, baza +2 → netto ≈ −16,
   poniżej passu); strażnik `test/m310-station-ponad-prog-scoring.test.js`
   (para graniczna charge 5/6 na Gunshipie, próg z deskryptora — Rammer 9 na
   charge 6 dalej buduje; mutacje: kara→+4 ⇒ RED, stała „6" ⇒ RED).
   7× pompowanie w partii worek-legend s7 robił profil `greedy` TESTERA
   (sonda pokrycia UI klika każdą „Aktywuj:" z definicji), nie heurystyka
   bota — tester będzie pompował dalej (to jego rola), bot nie.
5. **Audyt PR #101 §4.1**: cytowania CR zweryfikowane (608.2b ✓, 701.19a–c ✓);
   błędny cytat „701.3/701.34a «look at»" poprawiony na 701.16d + 701.19a–c
   (w CR obow. 2026-08-07: 701.3 = attach, 701.34a = proliferate).

## Pętla jakości — liczby
- Żywy Tester: 6 zakończonych partii, **0 zgłoszeń detektorów**; transkrypty
  w `/tmp/audyt-*.txt` (efemeryczne — kluczowe fakty zapisane w
  PROJECT_HISTORY i audycie): worek-legend|warhammer-ubr s7 (Bot 13:−3),
  srodziemie|mirrodin-wu s23 (Gracz 11:−1), kaladesh|zendikar s41 (Bot 12:0),
  dominaria-brg|worek-dziki s29 `impatient` (Bot 17:−7),
  worek-mroczny|theros s13 `hoarder` (Gracz 20:0, po F3 — ta partia przed F3
  stawała na kreatorze pozycyjnym „Assert Perfection"),
  worek-mroczny|theros s41 `hoarder` (Gracz wygrywa wyczerpaniem biblioteki).
- E3b: pary wymóg×zakaz po stronie bloków niemożliwe (brak „must block"
  w silniku, brak lure/provoke w danych); strona ataku (M270) z PR #101.

## Obserwacje dla właściciela (bez decyzji nie ruszam)
1. **GROZY `block`/`attack` = model addytywny** — nadal czekają na JAWNĄ
   komendę na pełne B0 (ADR 0018); nic nie zmieniono.
2. **A6** (`resolve_exploit_choice` próg 40−value) — nadal backlog.
3. Transkrypty żywych partii żyją w `/tmp` (poza snapshotem) — jeśli mają być
   dowodem trwałym, trzeba je przenieść do repo/jako artefakty; dziś trwałym
   zapisem są liczby i detektory w dokumentach.

## WISI / następna sesja
- Kolejny krok wg handoffu 2026-09-06a pozostaje: wycena walki trade-off
  (wymaga pełnego B0 na komendę właściciela).
- `main` nietknięty, push tylko fast-forward, `--force` nieużyty.

## UWAGA: stan wypchnięcia (przerwana autoryzacja GitHub)
Token `GH_TOKEN` wygasł pod koniec sesji (`gh auth status` → „token is no
longer valid"; push → „could not read Username"). Commit `<docs>` czeka na
`git push origin arena/01a07711-mtg` po reconnectacji właściciela
(fast-forward, siłowy push zbędny). Opis PR #102 jest gotowy do wklejenia:
`/home/user/pr-body.md` (poza repo; ten sam plik służy do
`gh pr edit 102 --body-file` albo PATCH `pulls/102`).
