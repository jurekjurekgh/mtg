# Handoff 2026-08-13 — M88: naprawa transkryptu Żywego Testera

## START TUTAJ
1. Przeczytaj `AGENTS.md`, `docs/PROJECT_STATE.md` (M88), ten plik,
   `docs/plans/PLAN_2026-08-13-m88-tester-output.md`.
2. `npm test` — oczekuj **1524** zielonych.
3. `npm run build` — 50 modułów / 1618.8 kB.
4. Sesja = gałąź `arena/019ffd38-mtg` (HEAD = 3f3bd77, PR #51).

## Stan po sesji
- **Kod:** 3 commity (plan → fix + testy → snapshoty).
- **Tester:** wydzielony moduł `tools/table-tester/extract.mjs` z trzema
  czystymi ekstraktorami (extractBotMoves, extractModalChoice, extractTileText).
  run-game.mjs używa ich w closeBotMove, resolveModal i tiles (snapshot).
- **Bot nietknięty** → B0 bez zmian (progi 0.78/0.57 utrzymane).

## Co naprawiono (M88)
- **A. closeBotMove zlepiał wpisy modala** — `body.textContent` +
  `replace(/\s+/g, ' ')` + `slice(0, 400)` dawał „Faza: Główna 1G Garruk's
  Companion wchodzi na pole bitwy" w jednej linii. Fix: extractBotMoves czyta
  KAŻDY `<div.bot-move-line>` osobno i loguje jako osobną linię z '  • '.
- **B. resolveModal obcinał kontekst** — `intro.slice(0, 120)` +
  `text(chosen).slice(0, 80)` gubił opcje i wybór. Fix: extractModalChoice
  zwraca intro + każdą opcję osobno z markerem ▶ dla wybranej + confirmText.
- **C. tiles zlepiał kafle** — `textContent` całego kafla dawał
  „Vow of Wildness3VEnchantment" (nazwa+koszt bez separatora). Fix:
  extractTileText czyta pola (.fname, .fcost, .ftype, .fbox, .fpt, .fbadges)
  osobno i łączy separatorem ' · '.

## Testy
- `test/table-tester-output.test.js` — 6 RED→GREEN:
  - extractBotMoves: osobne linie + pusty modal;
  - extractModalChoice: pełne intro + ▶ + combat wizard;
  - extractTileText: separatory · + pusty kafel.
- `npm test`: **1524/0**. `npm run build`: 50 modułów / 1618.8 kB.
- Bot nietknięty → pełne B0 niewymagane (progi 0.78/0.57 utrzymane).

## Snapshoty
- `tools/table-tester/audyt-m88-blk-tok-66.txt` — black vs tokens.
- `tools/table-tester/audyt-m88-soj-inn-44.txt` — sojusznicy vs innistrad.
- Wcześniejsze przebiegi: `/tmp/audyt-m88-after-{19,waz-101}.txt`.

## Kolejka (po M88)
- Phone verify (Pages): sprawdzić, że realna przeglądarka pokazuje
  wpisy modala wizualnie rozdzielone (CSS .bot-move-list flex column gap
  — powinno działać bez zmian).
- Batch 33 — czeka na listę właściciela.
- Dalsze audyty żywym testerem (wzorzec M80–M88) na innych taliach.

## Pułapki
- `edit_file` psuje polskie znaki → **python3** Path.
- Nie commituj bez `npm test` i `npm run build`.
- `edit_file` na index.html nadpisuje całe bloki — `read_file` PRZED zmianą.
