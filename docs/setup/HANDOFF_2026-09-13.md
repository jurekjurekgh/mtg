# Handoff 2026-09-13 — audyt PR #115 + sweep numerów CR + katalog czasowników Żywego Testera (E0–E5) (PR #116, arena/01a09c9e)

Sesja domyślna z [ADR 0021](../../docs/decisions/0021-default-session-work-no-queue-question.md):
PR na starcie (0020 A) → audyt poprzedniego scalonego PR (0020 B) → pętla
jakości. Poprzedni scalony PR to **#115** (`8300db6` — znaleziska Crew A1–A4
+ Shaman B). Plan: [`docs/plans/PLAN_2026-09-13-audyt-pr115-petla-jakosci.md`](../plans/PLAN_2026-09-13-audyt-pr115-petla-jakosci.md).

## Co zrobiono (w kolejności commitów)

- **`818fb81` — E0: plan sesji + PR #116** otwarty PRZED kodowaniem
  (ADR 0020 A). Plan trzyma kryteria E0–E5 i kolejność commitów.
- **`aa68716` — E1: audyt PR #115** → [`docs/audits/AUDYT_PR115_2026-09-13.md`](../audits/AUDYT_PR115_2026-09-13.md).
  Werdykt **APPROVE**, zero znalezisk blokujących. Zmierzone wejście: 59 plików,
  +5159/−359 (diff 505 050 B); `npm run test:all` na `8300db6` = **5406/5406**
  (475 393,77 ms); `npm run build` = 61 modułów / 3618,0 kB;
  `check-card-printings` exit 0 (ROZJAZDY 1: `ethersworn-shieldmage`).
  Znaleziska nieblokujące: **D1** martwy parametr `baseMana` w
  `manaForActivation` (`abilities.js:266–282`); **D2** Oracle-vs-CR „other"
  (Irontread Crusher wg Scryfall bez „other", Gila Courser z „other" —
  filtr `id !== id` poprawny wobec CR 702.122a/702.171a); **O1**
  `planGrantManaColors` (`resources.js:1173`) z zaszytym celem
  `{castingSpell:true, artifactSpell:false}` — nieosiągalny (oba tokeny
  Powerstone są `{tap}`-only); **O2** `fundableCostedPlan` zwraca najdłuższy
  PREFIKS, nie agregat.
- **`ef13afc` — E3: sweep numerów CR** (41 plików, +106/−106 w `src/`,
  `test/` i bieżących dokumentach). Poprawione pary: deathtouch 702.4 → 702.2b,
  first strike 702.4 → 702.7, landwalk 702.33 → 702.14, echo 702.29 → 702.30,
  madness 702.34x → 702.35a/b, flashback 702.34b → 702.34a, fabricate 702.122
  → 702.123, reinforce 702.29a → 702.77a, delirium 702.34 → 207.2c (ability
  word bez wpisu), crew 701.36 → 702.122 w `ENGINE_MILESTONES.md`/`ROADMAP.md`.
  Dokumenty historyczne zachowują numerację z epoki. Strażnik:
  `test/cr-numery-mechanik-straznik.test.js` (11 testów, para mechanika↔numer
  na `src/` + `test/`, cytat reguły w komentarzu — ADR 0030).
  Źródło kanonu: TXT CR `MagicCompRules%2020260819.txt` (media.wizards.com,
  efektywny 2026-08-07); kanon: Deathtouch 702.2, Double strike 702.4,
  First strike 702.7, Landwalk 702.14, Cycling 702.29, Echo 702.30,
  Kicker 702.33, Flashback 702.34/702.34a, Madness 702.35a–c,
  Reinforce 702.77/702.77a–b, Crew 702.122/702.122a, Fabricate 702.123,
  Plot 702.170, Saddle 702.171/702.171a.
- **`b48d5d5` — E2: katalog czasowników akcji Żywego Testera** (znalezisko
  pętli jakości): PR #115 (A1) przemianował etykietę crew/saddle
  „Aktywuj:" → „Obsadź:"/„Osiodłaj:", a wzorce testera i osi 3 detektorów
  zostały na `^Aktywuj:` — przez 6 partii na taliach z pojazdami etykieta
  stała w panelu 54× i **nie powstało ani jedno kliknięcie**, żaden detektor
  nie zapalił (klasa M256/L46: etykieta istnieje, więc cisza). Naprawa
  u źródła: `tools/table-tester/actions.mjs` (jedna tabela: pula /
  bezpieczne / priorytety greedy) używana przez `run-game.mjs`
  i `detectors.mjs`; uzupełnione czasowniki znane silnikowi od dawna
  (Ucieczka, Przygoda, Zagraj z przygody, Ninjutsu, Channel, Plotuj,
  Przygotuj manę, Zawieś, Rzuć z Cleave/odbiciem/wygnaną/zawieszone,
  Zagraj aurę/za bestow/za manę ze Skarbów, Obróć twarzą do góry);
  usunięte martwe angielskie wpisy (`Escape:`, `Plot:`).
  Strażnik: `test/tester-wzorce-akcji.test.js` (6 testów — etykiety
  crew/saddle z prawdziwego `commandLabel`, rot literałów w `render.js`,
  spójność klasyfikacji, pass/poddanie poza pulą).
- **E5: dokumenty** — ten handoff, wpis w `docs/PROJECT_HISTORY.md`,
  README (liczby z pomiaru).

## Bramki (zmierzone na końcu sesji)

| bramka | wynik |
|---|---|
| `npm test` (szybki rdzeń) | **5413/5413**, 0 fail, ~170 s |
| `npm run test:all` (brama PR) | **5423/5423**, 0 fail, 290,1 s |
| `npm run build` | **61 modułów / 3618,0 kB** |
| quick benchmark (`node tools/benchmark.mjs`) | **672 gry / 111,6 s** — heuristic **84,2%**, aggro **28,0%**, random **3,6%** |
| Żywy Tester (6 partii pętli + 2 celowane) | po naprawach **0 detektorów**, 0 niewycenionych |

Quick benchmark — pary heuristic < 100%: `dominaria-brg|innistrad-brg` 87,5%,
`dominaria-brg|dominaria-wu` 93,8%, `dominaria-brg|final-fantasy` 93,8%,
`final-fantasy|innistrad-brg` 93,8%. **Pełne B0 niewykonane** — wyłącznie na
komendę właściciela (ADR 0018).

Dowód end-to-end naprawy E2 (transkrypty w `/tmp/crew-2026-09-13/`, nie trwałe):

- crew: `kaladesh vs warhammer-wg s101` (profil `explorer`) — klik „Obsadź:"
  → kreator załogi („opcji 3, potrzeba 1, celuję w 1") → załoga
  Merchant's Dockhand + Welder Automaton → kafel „Artifact Creature —
  Vehicle … obsadzony · 5/5"; druga aktywacja: Merchant's Dockhand + Glitch
  Ghost Surveyor; 0 detektorów, partia wygrana 20–0.
- saddle: `worek-dziki vs zendikar s7` — „Osiodłaj: Trained Arynx" i
  „Osiodłaj: Gila Courser" → „Trained Arynx zyskuje: osiodłanie";
  29/30 widzianych akcji klikniętych, 0 detektorów.

## Kolejka / otwarte (nie zapomniane)

1. **Znaleziska nieblokujące audytu** (do decyzji przy okazji zmian w danym
   pliku, nie „na siłę"): D1 martwy parametr `baseMana`; O1
   `planGrantManaColors` z zaszytym celem (nieosiągalny dziś — wróci, gdy
   w katalogu pojawi się kosztowe źródło „tylko do artefaktów/czarów");
   O2 prefiks zamiast agregatu w `fundableCostedPlan`.
2. **Oś 3 detektorów** celowo bez lądów i „Wybierz:" (mulligan, deklaracje
   walki) — rozszerzenie zakresu to osobna decyzja UX, nie skutek uboczny.
3. **Nowe czasowniki w puli** (Ucieczka/Przygoda/Ninjutsu/Channel/Plotuj/
   Przygotuj manę/Zawieś/…) są klikalne od tej sesji, ale w 8 partiach nie
   pojawiły się — pokrycie potwierdzi pierwszy rzut taką kartą; brak
   pokrycia odnotowany, nie ukryty.
4. **Crew/saddle** pokryte na Bomat Bazaar Barge, Trained Arynx i Gila
   Courserze; Irontread Crusher nie trafił do partii w tej sesji.
5. **Domknięcie lektury testów PR #115**: ~26 plików testowych przeczytanych
   wyrywkowo (pełny audyt objął `src/`, testy celowane i mutacyjne piny) —
   lista w raporcie audytu; wrócić, jeśli obszar znów będzie zmieniany.
6. Katalog kart rośnie **wyłącznie z kolekcji właściciela** (ADR 0029).

## Pułapki środowiska (potwierdzone w tej sesji)

- `gh pr edit` nie działa na tym repo → ciało PR podmieniaj przez
  `gh api -X PATCH repos/jurekjurekgh/mtg/pulls/116 -F body=@plik`.
- Długie bramki przekraczają limit bash (30 s): `npm test` ~170 s,
  `npm run test:all` ~290–480 s (zależnie od obciążenia maszyny) → `start_process` + `wait_for: exit`
  (background przez `&` w bashu też jest ubijany).
- Brak lokalnego CR na dysku; adres z datą `MagicCompRules%2020260807.txt`
  → 404; działa `MagicCompRules%2020260819.txt` (efektywny 2026-08-07).
  Nie pobierać ponownie — kanon zapisany wyżej.
- Żywy Tester wymaga `npm ci` w `tools/table-tester` **i świeżego**
  `npm run build` (testy CLI testera w CI nie instalują jsdom — import
  `jsdom` jest leniwy, M203).
- Profil `explorer` maksymalizuje pokrycie UI — to on odkrył blind spot
  crew/saddle; `greedy` odtwarza regresję wyników (M80–M96).
- Pliki w `/tmp` (transkrypty) i `/home/user` (robocze diffy, treści
  commitów) **nie przeżywają resetu sandboxa** — odtwarzaj komendami
  zapisanymi w tym handoffie.

## Plany / dokumenty

- plan sesji: [`docs/plans/PLAN_2026-09-13-audyt-pr115-petla-jakosci.md`](../plans/PLAN_2026-09-13-audyt-pr115-petla-jakosci.md)
- raport audytu: [`docs/audits/AUDYT_PR115_2026-09-13.md`](../audits/AUDYT_PR115_2026-09-13.md)
- strażnik numerów CR: `test/cr-numery-mechanik-straznik.test.js`
- strażnik wzorców akcji: `test/tester-wzorce-akcji.test.js`
- katalog czasowników: `tools/table-tester/actions.mjs`

## Stan końcowy sesji (2026-09-13)

PR **#116** (`arena/01a09c9e-mtg` → `main`) otwarty, commity: `818fb81`
(plan), `aa68716` (audyt), `ef13afc` (sweep CR), `b48d5d5` (E2) + commit
dokumentów E5. Bramka `npm run test:all` zielona. **Scala właściciel** —
agent nie scala (ADR 0007/0020).
