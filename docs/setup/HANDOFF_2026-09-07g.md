# Handoff 2026-09-07g — wyceny bota i modale, PR #105 (cz. 2)

## Stan bieżący

Sesja `arena/01a07c4e-mtg`, [PR #105](https://github.com/jurekjurekgh/mtg/pull/105)
do `main` — kontynuacja po HANDOFF_2026-09-07f (ta sama gałęzia, ten sam PR;
właściciel jeszcze nie scala, agent nie scala — ADR 0021). Kod i dokumenty
zamknięcia wypchnięte; sprawdź bieżący HEAD/CI na GitHub — plik nie pinuje
własnego SHA. Plan
`docs/plans/PLAN_2026-09-07-wyceny-bota-i-modale.md` wykonany E1–E5,
checkboxy odznaczone, podsumowanie w pliku. Historia:
`docs/PROJECT_HISTORY.md` (sekcja „cz. 2 — wyceny bota i modale").

## Bramki i dane do porównania

- `npm test`: **4740/4740** (baza sesji 4698; +42 od E1–E3 i E6).
- `npm run test:all`: **4750/4750** (baza 4708), pomiar ~280 s.
- `npm run build`: **59 modułów / 3388,1 kB** (`dist/mtg-table.html`).
- `node tools/benchmark.mjs` (szybki): heuristic **84,8%** (570/672),
  vs random 96,1%, aggro bije heuristic w 26,5% — pasmo bazowe.
- **Pełnego B0 NIE uruchamiano** (ADR 0018/0025). Żadnych nowych kart/
  talii/limitacji Oracle (ADR 0029); syntetyki wyłącznie w testach.
- Budżet lektury: 99 904/100 000 — bez zmian, bez nowego wpisu LESSONS.

## Co zrobiono

- **E1 telemetria „akcja bez wyceny"**: `scoreCommand` liczy trafienia
  `default` (`lastUnvaluedType`), `trace.unvalued`, `unvaluedDecisions()`,
  mostek `__mtgDebug.botUnvalued()`, run-game „== NIEWYCENIONE ==",
  detektor `detectUnvaluedBotChoices` (pass_priority informacyjnie).
  `hashTrace` stripuje `unvalued`. Test detektora RED→GREEN.
- **E2 wyceny 20/20 typów** (4 pakiety, każdy osobnym commit+push):
  A `d7427b2` (optional_draw, damage_target, hand_creature, kary celu
  trybów modalnych), B `36e2ea9` (redirect+copy_targets jednym case,
  enter_as_copy, amass, epic), C `8b8ec70` (look_top, hand_top,
  reveal_exile_grave, destroy_equipment, land_type, moonlit,
  cast_adventure_creature + ekspozycja widoku `pendingDestroyEquipment`,
  `pendingMoonlitChoice` decydent-only), D `ce01f2c` (5 jawnych
  `finish(0)` dla jednowariantowych; `default` = wyłącznie przyszłe typy
  silnika). Mutacje kontrolne pakietu C: **7/7 RED**. Fixture snapshotu
  zregenerowana świadomie (`tools/bot-scoring-snapshot.mjs --write`;
  drift scoreSum +14 w 1/6 partii, decyzje 188=188, kinds równe).
- **E3 modale**: enumeracja klasowa 16 modalnych czarów + triggerów
  (etherwrought-page, inspiring-bard) w
  `test/modale-generowanie-ofert.test.js` — **zero błędów silnika**,
  klasa piniowana (test-only `5e8b425`): tryb bez celu zawsze dostępny,
  oferta per legalny kandydat (źródło też), pusta pula trybu celowanego
  nie zabija trybu bezcelowego, same tryby celowane + pusto → dokładnie
  skip; bot przy zawężonej puli wybiera żywy tryb.
- **E4 żywy stół**: 4 partie po buildzie — exit 0, stderr 0 B,
  „DETEKTORY: brak zgłoszeń" i **„NIEWYCENIONE: brak"** we wszystkich:
  alara|wiedzmin greedy s10501 (9:−5), forgotten-realms|ravnica explorer
  s10502 (22:20, wyczerpanie bibliotek), final-fantasy|worek-basni
  impatient `--snapshot-every 3` s10503 (20:−2), tarkir-wur|mirrodin-wu
  defensive s10504 (24:−1). Taktyka bota: Chronic Flooding → Basilisk
  Gate, Twiddle (tap) → Plague Reaver, Agate Assault (wygnanie artefaktu)
  → Warrior's Sword, stack aur na własnym nosicielu, living weapon,
  blokada dwoma stworami, domyślny zabójczy przydział obrażeń bojowych;
  modale UI (Scry, Aerith — Winda) bez zakłóceń.

## Dodatek E6 (redirect po zgłoszeniach właściciela z testów, ten sam PR)

- **A1** (`97e6ddb`): tap czystej zdolności many liczy kandydatów po
  TIMINGU rzucania (`manaUnlockCandidates`: instant zawsze CR 307.5,
  reszta tylko własna główna main1/main2) — dotąd sorcery/stwór w ręce
  „odblokowywał" tap w cudzym upkeepie, a mana wyparowała (CR 500.4),
  źródło zostawało zatapiane. Telemetria: „NIEWYCENIONE: brak" utrzymane.
- **A2** (`2cee62b`): transformacja permanentu ZAWSZE treścią panelu
  „Rozgrywka" (+ miniatura NOWEJ twarzy) — wcześniej znikała, gdy stos
  schodził po passie człowieka.
- **B** (`3520d82`): hover kart specjalnych działa od PR #104; strażnik
  pinuje teraz też `renderSpeedPanel` i CSS `.speed-card`. **Właściciela
  proszę o test na świeżym buildzie/pages** — zgłoszenie odpowiada stanowi
  sprzed PR #104.
- Weryfikacja żywa: s20611, s20612, s20603-pofix — czyste; panel pokazuje
  oba transformy wilkołaka; benchmark heuristic 85,0% (pasmo bazowe).

## Pułapki natrafione (dla kolejnych sesji)

- `addObject` po cichu GUBI pola spoza kontraktu (`attachedTo`) —
  załączanie wyłącznie przez `attachEquipmentToCreature`/`attachAura`
  (attachments.js, kontrakt przy L21); zwraca FROZEN obiekt.
- Wycena `destroy_equipment` patrzy na kontrolera SPRZĘTU
  (`attachedTo` w projekcji battlefield widoku), nie gospodarza.
- Node `assert/strict` nie eksportuje nazwą `assert` — wyłącznie
  default import. `git commit -F plik` (heredoc), nigdy `-m` z
  polskimi cudzysłowami/backtickami.

## Następna sesja / wznowienie

1. Lektura wg AGENTS §0; sprawdź, czy #105 scalono. Jeśli tak — ta
   gałęzia nie koduje; nowa sesja zaczyna od audytu tego PR (ADR 0020 B);
   materiał audytu: plan cz. 2, pakiety A–D, testy
   bot-wyceny-pakiet-c/d, modale-generowanie-ofert, fixture snapshotu,
   dokumenty zamknięcia.
2. Telemetria E1 zostaje na stałe: nowe typy komend silnika bez case
   wyceny będą widoczne jako „NIEWYCENIONE" w run-game i
   `bot.unvaluedDecisions()` — nie usuwać `default` jako licznika.
3. Kandydaci dalszych osi (niezlecone): wyceny LOOKAHEAD_TYPES dla
   nowych case'ów C (look_top/hand_top w głębszym horyzoncie), tolerancja
   benchmarku aggro (26,5%) — tylko na polecenie właściciela.
4. Pełny B0 wyłącznie na jawne polecenie właściciela. Przed testerem
   build; `npm ci --prefix tools/table-tester` w razie odtworzenia
   sandboxa. Zawsze czytać stderr.
