# Handoff 2026-08-04 — po Batch 15 + M31 (kreator/bot) + M32 (singleton)

## Stan po sesji

- **Branch:** `arena/019fccce-mtg` (PR #24 — czeka na scalenie przez właściciela)
- **Komendy:** `npm test` → **639/639 zielone**, `npm run build` → **44 moduły / 638.0 kB**
- **Karty:** 74 wspieranych (Batche 1–15), 77 wpisów z artId; każda ma `plan` (setting)
- **Talie:** **paradygmat singleton** (M32) — max 1 karta (lądy podstawowe dowolnie),
  min 15 nielandowych. 6 nowych talii (green/black/red + innistrad/azorius/wiedzmin)
  zastąpiło wszystkie dotychczasowe.
- **Bot:** wagi M19 bez zmian (singleton faworyzuje heurystykę). Pełna macierz B0
  (6 talii, 50 seedów, 6300 meczów, 0 niedokończonych): heuristic **95.0% vs random,
  74.1% vs aggro**, aggro 91.9% vs random. Progi regresji: **0.78 / 0.53**.

## Co weszło w tę sesję (PR #24, 5 commitów)

1. **Batch 15 (M30)** — 10 kart (Howl, Goblin Picker, Dragon Arch, Trigon, Aerith,
   Esper Stormblade, Forge Devil, Shatter, Sweet Oblivion, Village Rites) + ~12 nowych
   mechanik (escape, modal, hybrid, koszty discard/remove-counter/sacrifice, destroy…).
2. **M31** — używalny kreator talii (dodaj-z-filtrów, wyczyść, statystyki, landy na
   górze, biblioteka IndexedDB) + plany kart z arkusza (`fetch-plans.mjs`).
3. **M31 (bot)** — pełny B0, diagnoza/fix long-game (maxCommands 5000), strojenie B4
   (wagi M19 optymalne).
4. **M32 — paradygmat singleton.** Skasowano wszystkie talie; `validateDeck`
   (maxCopies=1, minNonland=15); 6 nowych talii hybrydowych; pełny benchmark na nich.
5. **Usunięto karty syntetyczne (syn-*)** z katalogu — kreator talii pokazuje tylko
   realne karty. `createHeuristicBot` dostał opcjonalny parametr `registry` (testy B3
   wstrzykują lokalne karty damage/pump).

## Kolejka

- **Bot:** marnotrawstwo tapowania lądów (boty tapują wszystkie landy co turę bez
  celu) — osobny temat, właściciel powiedział „nie warto"; re-strojenie wag odkładam
  (heurystyka silna na singleton).
- **Batch 16** czeka na listę właściciela (ADR 0010 §2a + runbook). Nowe talie łatwo
  rozbudować przez kreator (singleton + min 15).

## Pułapki środowiska

- Sandbox blokuje bezpośredni HTTPS (curl/Node fetch) — dane Scryfall i arkusz kolekcji
  przez narzędzie `fetch_page` agenta (kompaktowy eksport `&range=A:D`).
- Sandbox resetuje HEAD do bazy między zdarzeniami (commity giną lokalnie, ale są na
  zdalnym po push); pliki worktree przetrwają. Po resecie: `git fetch` + `reset --mixed`
  do remote (zachowuje worktree), potem commit na nowo.
- Pełny benchmark B0 na 6 taliach ~2 min (singleton); próbka regresji w `npm test`.
