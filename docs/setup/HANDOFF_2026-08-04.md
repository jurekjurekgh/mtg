# Handoff 2026-08-04 — po Batch 15 (M30) + M31 (kreator/bot)

## Stan po sesji

- **Branch:** `arena/019fccce-mtg` (PR #24 — czeka na scalenie przez właściciela)
- **Komendy:** `npm test` → **672/672 zielone**, `npm run build` → **44 moduły / 643.0 kB**
- **Karty:** 74 wspieranych (Batche 1–15), 77 wpisów z artId (w tym DFC tyły)
- **Bot:** bez zmian logiki — wagi M19 (mana=1.1, permanent=0.9) pozostają optymalne
  przy 74 kartach (strojenie B4: brak poprawy). Pełna macierz B0 (19 talii, 50 seedów,
  63 000 meczów): heuristic **83.2% vs random, 60.8% vs aggro** (Batch 14: 84.1/63.0 —
  lekki spadek). Progi `0.66 / 0.53` bez zmian.

## Co weszło w tę sesję

### Batch 15 (M30) — 10 kart (PR #24, commit 1)
Howl of the Night Pack, Goblin Picker, Dragon Arch, Trigon of Corruption, Aerith
Rescue Mission, Esper Stormblade, Forge Devil, Shatter, Sweet Oblivion, Village Rites.
Nowe mechaniki: tokeny za liczbę landów podtypu, koszty discard/remove-counter,
destroy artifact, obrażenia w kontrolera, mill celu-gracza, warunek „inny
wielokolorowy", dodatkowy koszt rzutu sacrifice, modal „Choose one", Escape z
cmentarza, cheat-into-play wielokolorowego stwora, hybrid mana.

### M31 — kreator talii + bot (PR #24, commit 2)
- **Kreator talii używalny:** dodaj-z-filtrów, wyczyść, statystyki (typy/kolory/krzywa
  many), landy podstawowe na górze, **biblioteka IndexedDB** (load/save/save-as/delete
  + wczytywanie z `decks/`). IndexedDB = cache pod ITP; trwałość = eksport do `decks/`.
- **Filtr Plan:** `tools/fetch-plans.mjs` wyciąga kolumnę „Plan / Setting" (setting/plane)
  z arkusza kolekcji → `tools/collection-art-ids.csv` (kolumna Plan) + `plan` w kartach.
  **Wymaga jednorazowego uruchomienia z dostępem do sieci** (sandbox blokuje arkusz jak
  Scryfall): `node tools/fetch-plans.mjs`.
- **Bot B0 + strojenie:** pełna macierz (patrz wyżej). Diagnoza 2 niedokończonych gier
  (long-game: generatory tokenów → board-stall + marnotrawstwo tapowania landów;
  kończą się taliczeniem ~tura 60) → `maxCommands` 3000→5000 → 0 niedokończonych.
  Strojenie B4 (15 ewaluacji): brak poprawy nad wagi M19.

## Kolejka

- **Filtr Plan:** wyciągnięty z arkusza (kompaktowy eksport `&range=A:D`) i wpisany
  do kart + `tools/collection-art-ids.csv` (kolumna Plan). `tools/fetch-plans.mjs`
  służy do odświeżania przy nowych kartach.
- **Bot:** marnotrawstwo tapowania landów (boty tapują wszystkie landy co turę nawet
  bez celu) — osobny temat, nie strojenie wag; wydłuża long-game.
- **Batch 16** czeka na listę właściciela (ADR 0010 §2a + runbook).

## Pułapki środowiska

- Sandbox blokuje bezpośredni HTTPS (curl/Node fetch → błąd): dane Scryfall przez
  narzędzie `fetch_page` agenta, arkusz kolekcji przez `tools/fetch-plans.mjs`
  uruchamiane przez właściciela z siecią.
- Pełny benchmark B0 ~22 min (macierz 19 talii × 50 seedów); próbka regresji w `npm test`.
- Commity w sandboxie tracą się po resecie — nie rozdzielaj powiązanych zmian w pliku.
