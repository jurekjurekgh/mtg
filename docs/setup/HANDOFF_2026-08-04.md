# Handoff 2026-08-04 — po scaleniu Batch 14

## Stan po sesji

- **Branch:** `arena/019fcb90-mtg`
- **Komendy:** `npm test` → **632/632 zielone**, `npm run build` → **43 moduły / 581.8 kB**
- **Karty:** 64 wspieranych (Batche 1–14), 67 wpisów z artId (w tym DFC tył)
- **Bot:** heuristic 84.1% vs random / 63.0% vs aggro, progi `0.66 / 0.53` (bez zmian — to dodanie kart)

## Co weszło w tę sesję (Batch 14 — 10 kart)

1. **Ainok Tracker (KTK)** — first strike + morph {4}{R}
2. **Spectral Prison (AVR)** — aura lock_untap + sacrifice on spell targeting
3. **Raucous Carnival (DSK)** — conditional entersTapped (life ≤13)
4. **Cloudbound Moogle (FIN)** — flying, ETB +1/+1 counter, plainscycling
5. **Insatiable Appetite (ELD)** — Food sacrifice choice (+5/+5 or +3/+3)
6. **Stirring Bard (CLB)** — defender, initiative, grant menace + haste
7. **Hunter's Blowgun (LCI)** — equipment with conditional deathtouch/reach
8. **Geological Appraiser (LCI)** — ETB if cast → discover 3
9. **Lodestone Needle // Guidestone Compass (LCI)** — flash, stun counters, craft, explore
10. **Panic Spellbomb (SOM)** — sac for can't block, dies → may pay {R} draw

## Nowe mechaniki generyczne w engine

- **Defender** — keyword blokujący atak
- **Flash** — rzucanie permanentów z priorytetem (jak instant)
- **Stun counters** — zamiast odkręcenia, zdejmij licznik
- **Deathtouch w walce** — ≥1 damage = lethal
- **Conditional keywords wg tury** — equipment z `conditionalKeywords`
- **Warunkowe entersTapped** — `entersTappedCondition`
- **Food tokens** — `resolve_food_choice` blocking choice
- **Discover** — `resolve_discover_choice` blocking choice
- **Explore** — `resolve_explore_choice` blocking choice
- **Craft transform** — exile self → return transformed
- **"Can't block"** — `cantBlock` flag czyszczony w cleanup
- **"Aura host targeted by spell"** trigger
- **"If you cast it"** — `wasCast` flag
- **Grant keywords until end of turn** effect

## Kolejka

**Batch 15 (5 kart) czeka na listę właściciela** — procedura ADR 0010 §2a + runbook kart.

## Pułapki środowiska

- Sandbox resetuje się między zdarzeniami (HEAD wraca do bazy mimo plików worktree).
- Remote bywa odcięty (GH_TOKEN) — po podłączeniu push działa.
- Pełny benchmark ~12 min; próbka regresji w `npm test`.
