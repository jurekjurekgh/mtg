# Handoff 2026-09-07b — ciąg dalszy PR #103: M333–M335 (rodzina zakrywania do wyczerpania)

Sesja ta sama co `HANDOFF_2026-09-07a.md` (ten sam branch `arena/01a078a2-mtg`,
ten sam PR **#103**, nadal otwarty), tylko przedłużona o drugi blok pracy. Ten
plik jest punktem startu następnej sesji; tamten opisuje zamknięcie PR-owe.

## 1. Co weszło po 07a (4 commity, wszystkie wypchnięte)

| Commit | Znalezisko | Strażnik |
|---|---|---|
| `12130ce` **M333** (F6c) | przyczyna zakrycia dla WSZYSTKICH mechanik (manifest, morph, disguise — nie tylko cloak), jedno źródło brzmienia w `src/table/session.js`, `faceDownCause` zdejmowane przy obrocie manifestu, drukowany ward zerowany pod zakryciem (CR 701.40a) | `test/m333-przyczyna-manifest.test.js` (6) |
| `601615e` **M334** (F11+F10) | `turn_manifest_face_up` u bota wpadało w `default: finish(0)` → bot płacił zawsze (remis z pasem + stabilne sortowanie ofert); naprawione JEDNYM `case` dla rodziny i podatkiem wardu liczonym ZE STANU. Plus: migawka `faceDownOriginal` manifestu nie niósł P/T, więc obrót zwracał 2/2 zamiast ciała karty | `test/m334-bot-manifest-odsloniecie.test.js` (6, trzy mutacje) |
| `a9112a2` **M335** (F12) | `manifest_dread` z JEDNĄ kartą w bibliotece: gałąź bez decyzji zwracała prawdę → `pendingSpell` na stosie na zawsze → partia wisi z „Błąd wewnętrzny stołu". Naprawa `return;`, bez maskowania u konsumenta | `test/m335-manifest-bez-wybory.test.js` (4) |
| `15d1a27` **D2** + ten plik | §7 audytu PR #102 (z dowodem A/B z żywego stołu), kontynuacja w handoffie 07a i w `PROJECT_HISTORY.md`, sekcja o talii celowanej w `TESTER_STOLU.md`, **L138**+**L139**, kondensacja rejestru | strażnicy dokumentów |

Bramka końcowa: `npm test` **4618/4618**, `npm run build` 59 modułów /
**3365,9 kB**, `node --test test/bot-benchmark.test.js` **10/10**,
`node tools/benchmark.mjs --quick`: heuristic **84,8% (570/672)**, aggro 26,5%
(89/336), random 3,9% (13/336) — zero dryfu;
`test/fixtures/bot-scoring-snapshot.json` bez regeneracji; budżet lektury
startowej **99 984 / 100 000** tokenów.

## 2. Tematy zamknięte (nie zaczynać od nowa)

- wycena rodziny obrotów twarzą do góry u bota (w 07a §4 zapisane jako
  „nie zaczęte") — M334, z parą porównawczą manifest/cloak na TEJ SAMEJ karcie
  (`wormfang-newt`, wartość 3: pod manifestem opłaca się obrócić, pod cloakiem
  nie, bo podatek wardu wychodzi ze stanu);
- przyczyna zakrycia jako etykieta rodzinna (F6/F6b/F6c) — M333, domknięte także na żywym stole („(Manifest)" w logu, na kafelku i w numeracji kopii);
- stan pod zakryciem (ward, P/T) — M333 + F10 w M334;
- `gameObjectDataOf` BEZ pola `ward` — ŚWIADOMY wybór, nie do poprawki:
  jedynym konsumentem wardu na obiekcie jest `wardAmountOf` (czyta obiekt przed
  kartą), a M333 zeruje ward u źródła w trzech funkcjach zakrywania; dodanie
  pola do białej listy pozwoliłoby drukowanemu wardowi wejść pod zakrycie
  ścieżką RZUTU (CR 708) — czyli naprawa w złą stronę. W katalogu nie ma dziś
  żadnej karty z drukowanym wardem (zmierzone na `REAL_CARDS`).

## 3. Co zostaje otwarte (w tej kolejności)

1. **Pełny benchmark B0 (`--full`)** — wymaga ROZKAZU właściciela
   (ADR 0018/0025), nie odpalać samodzielnie.
2. **Morze/skry u bota** (plan `PLAN_2026-09-06c`, §E) — tylko jeśli wniesie
   wartość mierzalną na `--quick`; kolejność obowiązkowa: sonda
   `tools/bot-scoring-snapshot.mjs` → wycena → próg (L124: przypisać trzema
   drzewami, zanim podniesie się sufit).
3. **Decyzja stylistyczna dla właściciela**: `morph`/`disguise` w etykiecie
   przyczyny zostają po angielsku (tak brzmią na karcie). Zmiana = jeden wpis w
   tabeli `src/table/session.js` + 6 asercji w `m333`.
4. Transkpty Żywego Testera z tej sesji (`tools/table-tester/audyt-pr103/
   m334-manifest-g1.txt`, `m335-fix-g1.txt`) zostają lokalnie, nie w repo —
   fragmenty będące dowodem są przepisane w §7 audytu, więc przy nowym sandboxie
   nic nie przepada.

## 4. Pułapki tej rodziny (żeby nie odkrywać drugi raz)

- `decks/worek-mroczny.txt:12` MA `1x Manifest Dread`, ale `benchmarkDecks()`
  odrzuca talie `worek*` (`tools/benchmark.mjs:96-100`), więc benchmark tej
  ścieżki NIE widzi: zero dryfu nie jest dowodem, że nic się nie zmieniło —
  dowodem jest partia testera i sonda w teście;
- każda gałąź efektu zwracająca prawdę MUSI postawić `state.pending*` (L138), i
  odwrotnie: przy naprawie wiszenia NIE sprząta się `pendingSpell` u
  konsumenta;
- `applyEffect` ma ~4500 linii, a `return true` znaczy „tak" też w sąsiednich
  helperach — skan „`return true` bez decyzji" rób na wyciągniętym ciele
  JEDNEJ gałęzi (równowaga nawiasów), nie po całym pliku (mój pierwszy skan
  dał 30 fałszywych trafień, drugi 0 fałszywych, bo okno linii objęło gałąź
  sąsiednią — oba bezużyteczne);
- harness testów bota: `addMana(state, 'p2', N)` jest WYMAGANE, żeby botowi w
  ogóle zaofertowano obrót (same odkręcone lądy nie przechodzą
  `producibleMana`/`canPayColoredCost`), a face-down permanent pojawia się na
  polu bitwy dopiero po DWÓCH `pass_priority`; `playerView(state,p).zones
  .battlefield`, nie `.battlefield`;
- `createCardRegistry()` zwraca obiekt NIEiterowalny — do skanu katalogu bierz
  eksport `REAL_CARDS`;
- chwilowa talia w `decks/` odpala czterech strażników (M178/M203) i wchodzi do
  `dist/` — usuń plik i PRZEBUDUJ przed bramką;
- cięcie plików skryptem: krótki, jednoznaczny klucz + `node --check` przed
  `git add` (L139). Ta sesja zostawiła na chwilę commit `dd67ea7` z plikiem o
  złej składni, bo `s.index("return;")` trafił w `return;` w komentarzu dodanym
  minutę wcześniej; ratował `--amend`, dopóki commit był lokalny.
