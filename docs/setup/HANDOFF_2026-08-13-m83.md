# Handoff 2026-08-13 — M83: audyt rozgrywki żywym testerem

## START TUTAJ
1. Przeczytaj `AGENTS.md`, `docs/PROJECT_STATE.md` (M83), ten plik,
   `docs/plans/PLAN_2026-08-13-audyt-zywy-tester-m83.md`.
2. `npm test` — oczekuj **1452** zielonych.
3. `npm run build` — 50 modułów / ~1574 kB.
4. Sesja = gałąź `arena/019ff818-mtg` = PR #46 (otwarty z tej sesji).

## Stan po sesji
- **Kod:** 10 błędów z audytu żywym testerem (UI/log + 1 engine craft + 1 bot).
- **Bot zmieniony** (re-equip kara) → pełny B0 bez niedokończonych; progi
  win-rate utrzymane (test benchmarku 7/7).

## Co naprawiono
1. Log walki: „A i B i C blokuje" → „A, B i C blokują" (session.js).
2. „Faza: Faza główna" → „Faza: Główna 1" (stepLabelOf).
3. „Brak bloków" w modalu pomijany (jak „Brak ataku").
4. PlayerView battlefield niesie `morph` → etykieta obrotu ma koszt.
5. „→ cel: ?" dla celu-gracza na stosie (stack-view).
6. Czytelne opisy 13 triggerów (render.js).
7. Etykieta czaru X pokazuje „X=N".
8. Bot nie re-equipuje ciągle tego samego stworu (heuristic-bot.js).
9. Insatiable Appetite: „+5/+5 albo +3/+3", nie „zyskaj 3 życia".
10. Craft bez artefaktu do wygnania = no-op (nie crash).

## Testy
- `test/audit-m83-tester.test.js` — 10 regresji (RED→GREEN).
- `npm test`: **1452/0**. `npm run build`: 50 modułów / ~1574 kB.
- Benchmark: progi win-rate utrzymane; craft crash naprawiony.

## Kolejka (po M83)
- Phone verify (Pages): nowe etykiety, log walki „blokują", morph koszt, X=N.
- Batch 32 — czeka na listę właściciela.
- Phone verify / platinum hunt / B2-w2 lookahead OFF / tester roadmap.

## Pułapki
- `edit_file` psuje polskie znaki → **python3** Path.read_text/write_text.
- Sandbox potrafi cofnąć HEAD do main — `git fetch && git reset --hard FETCH_HEAD`.
- Nie commituj bez `npm test`. Nie odpalaj testera bez `npm run build`.
- Zmiana bota wymaga pełnego B0 (craft crash był regresją ujawnioną przez B0).
