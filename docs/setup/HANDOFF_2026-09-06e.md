# Handoff 2026-09-06e — arena/01a07711-mtg: M314 Veteran + Ghost Warden (PR #102)

## Stan
- Gałąź `arena/01a07711-mtg`, PR **#102** (opis kumulatywny, REST PATCH;
  `gh pr edit` nadal zepsuty). Head: `3f75358` (wypchnięty, FF na `ed933f2`).
- Poprzednie: HANDOFF_2026-09-06d (M311–M313 + rekonstrukcja fazy 3).

## M314 — co się stało i co ustalono
- **Zgłoszenie:** brak okna na pump #2 (Ghost Warden) po tym, jak trigger
  Tenth District Veteran odtapił Wardena w walce. Hipoteza: pinezka
  „nie przerywaj auto-passu".
- **Weryfikacja:** silnik OK — okna istnieją i są poprawnie sekwencjonowane:
  1. po rozstrzygnięciu triggera priorytet ATAKUJĄCEGO (CR 116.3c) z pełną
     ofertą — jeszcze PRZED deklaracją bloków;
  2. po blokach okno w `combat_damage` (M172/C).
  Piny: `test/m314-veteran-okna-walki.test.js` M314/3 + M314/4 (zielone
  z natury = dowód kontraktu), sesyjnie M314/5 (bez pinezki sesja stoi
  z pumpem #2).
- **Pinezka — zaprojektowane zachowanie (bez zmian kodu):** checkbox GRUPY
  (render.js, Feature 2026-08-11 + M91/Uwaga B) wycisza WSZYSTKIE warianty
  aktywacji naraz; wyciszona opcja NIE otwiera żadnego okna — auto-pass
  przelatuje okna walki i sam rozstrzyga (M314/6). Hamulec ręczny gracza:
  krzyżyk (pauza auto-passu do jawnej decyzji) albo odznaczenie ptaszka.
- **Naprawa (jedyne zmiany kodu):** `tenth-district-veteran` descriptor +
  `notSelf: true` (Oracle „untap ANOTHER target creature you control";
  gramatyka istniała — M158 Breaching Hippocamp). Skutki: 1 kandydat →
  auto-cel bez pytania (M242 — „pytanie o jeden legalny cel to szum"),
  ≥2 kandydatów → decyzja bez atakującego (M314/1b), Veteran solo → trigger
  nie odpala (`no_targets`, CR 603.3d, M314/2). Pin `real-cards-batch28`
  („atak odkręca inny stwór") przełożony na model auto-celu.

## Bramki (świeży pomiar po M314)
- `npm test`: **4534/4534** (+7 m314); `test:all`: **4544/4544** (świeży
  pełny pomiar po M314 — +7 m314, +3 różnica sondaży test:all).
- Build: 59 modułów / **3327,9 kB**. Benchmark szybki: 84,8% (570/672) —
  bez dryfu.
- Żywo: ravnica|srodziemie s31 (greedy, policy-seed 1) — partia do końca,
  **0 zgłoszeń detektorów**, obie karty w grze.

## Dla następnej sesji
1. L92 świeży (4544/4544 po M314) — odświeżać przy następnym domknięciu.
2. M314/6 dokumentuje SEMANTYKĘ pinezki — jeśli właściciel zechce ją złagodzić
   (np. okno walki zawsze przerywa), to DECYZJA WŁAŚCICIELA (Feature 2026-08-11);
   nie robić bez komendy.
3. L135/L92/ADR bez zmian; `gh pr edit` dalej zepsuty — tylko REST PATCH.
