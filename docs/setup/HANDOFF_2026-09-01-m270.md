# Handoff M270 — odznaka SREBRNA (2026-09-01)

## Stan
- Gałąź `arena/01a058db-mtg`, **PR #91 OPEN** (base `main`, do scalenia przez
  właściciela: `Squash and merge`).
- `npm test` **3990/3990**, `npm run test:all` **4003/4003**,
  build **56 modułów / 3010.8 kB**. Drzewo czyste, wszystko wypchnięte.

## Co zrobiono
Zdobyta **odznaka srebrna**: 5 kolejnych unikalnych błędów CR (#6–#10) po
brązie z M269. Wyłącznie techniką **L107** (rodziny ścieżek duplikujących
wspólny helper) — patrz `docs/ENGINE_MILESTONES.md`, sekcja M270.

| # | CR | Rzecz |
|---|-----|------|
| 6 | 400.7 | `enteredOnTurn` przy transform-return i craft |
| 7 | 122.1e | `destroy_equipment_attached` omijało `deathZoneFor` |
| 8 | 122.1b | tarcza zdejmowana ręcznie (2 ścieżki) |
| 9 | 508.1c | **DEADLOCK**: goad + „can't attack alone" |
| 10 | 122.1b + 704.5g | tarcza w SBA — trzecia kopia, klasa domknięta |

Commity: `827c0cc`, `acc2c1a`, `046e761`, `bfe262e`, `937ba3c`, `d28ff90`.

## Nowa wiedza trwała
- **L108** (`docs/LESSONS.md`) — deadlock reguł: szukać par „musisz X" /
  „nie możesz X" i zawsze testować „gracz ZAWSZE ma legalną opcję".
- Potwierdzone choke pointy: `deathZoneFor` (strefa śmierci), `removeCounter`
  (liczniki), `changeLife` (życie), `markDamage` (obrażenia),
  `moveObjectDirectly` (strefy). Wszystkie mają teraz strażników klasowych.

## Sugerowany następny krok
Odznaka **złota** (10 błędów) tą samą metodą, albo zmiana kierunku na
Wirtualny Stół. Tropy L107 nie są wyczerpane: warto przejrzeć pozostałe
emitery pod kątem ścieżek omijających `markDamage` i `moveObjectDirectly`.
Odrzucone tropy S1–S8 spisane w notatkach sesji — nie wracać.
