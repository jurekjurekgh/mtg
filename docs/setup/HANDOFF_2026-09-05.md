# Handoff — audyt PR #96 + naprawy F1–F4, 2026-09-05

Gałąź sesji: `arena/01a07073-mtg` · PR: **https://github.com/jurekjurekgh/mtg/pull/97**
(open, NIE scalony — scalenie to decyzja właściciela, preferowane „Squash and merge”).
Baza sesji: `d67b684` (= squash PR #96, `main`).

## Stan na koniec sesji

- **Audyt PR #96 zamknięty:** 114 plików diffu (`bf615b1..d67b684`), raport
  `docs/audits/AUDYT_PR96_2026-09-05.md`. Werdykt: **PR dobry** — mechaniki
  generyczne (ADR 0002), testy real-cast z obiema gałęziami, migracja
  Warhammera kompletna. **4 znaleziska** (F1 reguła, F2 docs, F3
  reguła-corner, F4 log-minor) — wszystkie naprawione w tym PR z mutacjami.
- **Bramy zmierzone na HEAD:** `npm test` **4434/4434**,
  `npm run test:all` **4444/4444**, `npm run build` **59 modułów / 3260,7 kB**.
  Benchmark (szybki profil): `node --test test/bot-benchmark.test.js` → **10/10**.
  Pełny B0 NIE uruchamiany (ADR 0018 — tylko na wyraźną komendę właściciela).
- **Pętla jakości (ADR 0021) zamknięta:** 2 partie żywego testera na
  przebudowanym artefakcie (Warhammer-wg/ubr w grze — batch53 ćwiczony
  realnie), **„DETEKTORY: brak zgłoszeń”** w obu.
- **Zero nowych kart** (ADR 0021 §4c) i zero zmian w wycenach bota.
- Plan: `docs/plans/PLAN_2026-09-05-audyt-pr96-petla-jakosci.md` (odhaczony).

## Co zrobiono (commity, każdy osobno zielony)

| commit | treść |
|---|---|
| `b79fc9c` | plan sesji (PR #97 otwarty PRZED kodem, ADR 0020 A) |
| `896ff7e` | raport audytu PR #96 (per plik + werdykt + F1–F4) |
| `ad45507` | F1 — Óin: koszt generyczny {1} + strażnik klasowy pipów |
| `732892b` | F3 — token Offspring z LKI (printLki + wasOffspring na stubie) |
| `2b113b9` | F4 — take ofiary refleksyjnej emituje `reflexive_sacrifice_resolved` |
| *(dokumentacja, domknięcie)* | F2 — README: 4434/4444/3260,7 kB; PROJECT_HISTORY; ten handoff |

## Znaleziska w skrócie (szczegóły w raporcie audytu)

- **F1 (reguła):** Óin the Brave — `cost.colors: ['R']` przy Oracle `{1}`
  (bezbarwna nie otwierała oferty). Fix: zdjąć `colors` + strażnik klasowy
  `test/ability-cost-pips.test.js` (53 zdolności z pipami w katalogu, 0 miss).
- **F2 (docs, L92):** README na liczbach etapu audytu (4412/4402/3226,2).
- **F3 (reguła-corner):** Offspring — brak tokenu po zejściu źródła (ruling BLB
  wymaga tokenu z LKI). Fix: `printLki` na wpisie stosu + `wasOffspring` na
  stubie (CR 603.10/608.2b); bez dotykania fingerprintu (ostra lista pól).
- **F4 (log):** `take` nie emitował `reflexive_sacrifice_resolved` (martwa
  gałąź `sacrificed:true` w session.js). Fix: emisja przed `reflexive_sacrifice`.

## Gdzie szukać dalej

1. **Zalecenie na następną sesję (ADR 0020):** otwórz PR na `main` przed
   pierwszą zmianą w kodzie, przeaudytuj ostatni scalony PR (#97), potem
   pętla jakości na przebudowanym `dist/`.
2. **Lekcja narzędziowa (2× w tej sesji):** równoległe `edit_file` do tego
   samego pliku gubią edycje — edytuj sekwencyjnie i weryfikuj `grep`em.
3. **`wasKicked` ma lukę LKI jak F3** (raport O6) — pre-existing, kandydat na
   follow-up (ten sam wzorzec: flaga do `sourceLki`).
4. **L47 dryfuje:** `copyableKeys` nie istnieje w kodzie — listy pól są
   per-ścieżka (raport O7). Przy następnej mechanice kopiującej rozważyć
   ujednolicenie albo korektę lekcji.
5. **Sheriff z plotu niećwiczony** (luka testowa z raportu) — ścieżka
   `cast from exile` czeka na test przy okazji.
6. **F2 to trzeci z rzędu dryf liczb (L92):** procedura ENVIRONMENT §7
   (odświeżanie README ze świeżego pomiaru w domknięciu) wymaga dyscypliny,
   nie nowego strażnika — liczby rosną co PR.
