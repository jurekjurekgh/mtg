# Handoff — M210 (2026-08-25)

Sesja: challenge właściciela „brązowa odznaka wyłapywacza błędów”.
Gałąź: `arena/01a038fe-mtg`. PR: **#78** (OPEN, nie mergowany — decyzja właściciela).

## Co zrobiono

Znalezione i naprawione **5 unikalnych niezgodności z CR** pozostawionych przez
wcześniejsze sesje. Każde znalezisko: potwierdzenie wobec Oracle + CR PRZED
naprawą (L57), naprawa u root cause, test zweryfikowany mutacyjnie (L61),
commit inkrementalny.

| # | CR | Rzecz | Commit |
|---|---|---|---|
| 1 | 202.2 | Landy miały `colors` opisujące produkowaną manę → animowany Swamp był czarnym stworem | `d4a2dbf` |
| 2 | 708.2a | Zakryty permanent zachowywał kolory i podtypy karty pod spodem | `d4a2dbf` |
| 3 | 303.4 / 704.5n | `ownControlOnly` ignorowane przez `isLegalAuraHost` (aura nie spadała po przejęciu gospodarza) | `d4a2dbf` |
| 4 | 707.2 | `resolve_enter_as_copy` kopiował stan bieżący zamiast wartości kopiowalnych | `a1dd05f` |
| 5 | 702.15/16a/90, 615 | Obrażenia z delirium omijały ochronę, tarcze, infect i lifelink | `ed22d2a` |

Dokumentacja: `8224f43` (L68–L70 + sekcja M210 w `PROJECT_HISTORY.md`).

## Stan końcowy

- `npm test` — **3242/3242** (było 3230; 12 nowych testów).
- `npm run build` — 54 moduły / **2656,5 kB**.
- Benchmark szybki (672 mecze) — heuristic **82,3%**, bez zmian względem M209.
- Lekcje do **L70**. HEAD = `8224f43` = origin.

## Nowy kod, o którym warto wiedzieć

- **`effectiveColors(object)`** — mieszka w `src/engine/attachments.js`,
  re-eksportowana przez `permanents.js`. Uwaga: **odwrotny kierunek importu
  dałby cykl** (`permanents.js` → `attachments.js`, nie na odwrót).
  Jedyna reguła w środku to CR 708.2a (`faceDown → []`); CR 202.2 egzekwują
  dane kart, świadomie NIE ta funkcja (patrz L70 i Genju of the Spires).
- Nowy plik testowy: `test/m210-kolory-i-gospodarz-aury.test.js` (12 testów).

## Punkty otwarte / tropy na później

1. **Podtypy zakrytych permanentów u pozostałych konsumentów.** Gałąź
   `faceDown` dodana w `effectiveSubtypes`/`effectiveSubtypesOnBattlefield`
   i podpięta w `combat.js`, ale surowe `object.subtypes` czytają jeszcze
   m.in. `abilities.js` 105/571/611/1399/1661/1844 oraz `effects.js`
   591/1177/1612/1805/2091/3181. Trzeba przejść je pojedynczo i ocenić,
   które pytają o podtyp jako REGUŁA (te wymagają `effectiveSubtypes`),
   a które o dane karty (te są poprawne).
2. **`sourceHasProtectionQuality`** (`attachments.js`) — jakości inne niż
   kolor (`quality.types`, `quality.subtypes`) nadal czytają surowe pola
   źródła; dla zakrytego źródła powinny widzieć „bezimienny stwór 2/2”.
3. Niesprawdzone tropy z tej sesji: `mana-cost.js` 13/39/263,
   `triggers.js` 15/1043, pełna zgodność z warstwami CR 613.

## Metodologia — co się nie sprawdziło

- **Sonda mierząca tylko stan końcowy daje fałszywe „OK”** (L68): brak pola
  `opponentId` w `pending` powodował ciche `unsupported_command`, a brak
  obrażeń wyglądał jak działająca ochrona. Zawsze asertować `ok === true`.
- **Mutację robić per gałąź, nie per funkcja** (L70) — inaczej nie widać
  kodu nadmiarowego.
- Filtr kandydatów celu (`legalDeliriumTargetCandidates`) **nie** sprawdza
  ochrony i nie musi: protection nie czyni stwora nielegalnym celem, blokuje
  dopiero same obrażenia.
