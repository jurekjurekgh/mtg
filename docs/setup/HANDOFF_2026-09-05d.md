# Handoff — sesja arena/01a071d1 (audyt PR #98 + pętla jakości), 2026-09-05

Kontynuacja po PR #98 (sesja arena/01a0712d). Prompt właściciela: „kontynuujemy
projekt" (brak nazwanego tematu → ADR 0021, pętla domyślna). 1 sesja = 1 gałąź
= 1 PR #99 (`arena/01a071d1-mtg`).

## Stan na koniec sesji

- **Audyt PR #98**: `docs/audits/AUDYT_PR98_2026-09-05.md` — werdykt „PR dobry",
  0 nowych błędów regułowych (klasa F), 0 regresji. Zmiany engine generyczne
  (ADR 0002 OK — grep po `cardId` w `heuristic-bot.js` = wyłącznie ogólne
  lookupy z rejestru). Bramy: `npm test` **4484/4484**, `npm run build` OK
  (59 modułów / 3284,1 kB).
- **Pętla jakości (ADR 0021 4a/4b)**:
  - Żywy Tester: 5 partii (dominaria-brg×ravnica, innistrad-brg×worek-dziki
    s11, zendikar×srodziemie s7, tarkir-wur×mirrodin-wu s3 explorer, wiedzmin×
    warhammer-wg s5 explorer). **0 zgłoszeń detektorów**, czyste etykiety,
    bez wycieków surowych slugów. Etykiety z PR #98 żywe na stole
    („Spreading Insurrection — trigger (Storm — kopie za wcześniejsze czary)",
    „Chronic Flooding — trigger (zatapnięcie zaczarowanego permanentu)",
    „Glorifier of Suffering — … Refleks po poświęceniu („when you do")…").
  - Statyczna weryfikacja (ADR 0027/0021 4b): `triggerConditionClause` (PR #98)
    pokrywa **WSZYSTKIE** klucze `condition` używane na triggerach katalogu
    (pojedynczy wyjątek `anotherOpponentExists` = karta nieaktywna w 1v1,
    wczesny return w `describeTriggered`). Pozostałe klucze z katalogu to
    warunki zdolności aktywowanych/ciągłych, nie triggerów.
  - **Nowy strażnik** `test/trigger-condition-clause-coverage.test.js` (2):
    każdy klucz `condition` na triggerze katalogu musi być reprezentowany w
    helperze (zamknięcie klasy cicho uciętego intervening-if na kaflu, L29/L31).
    Strażnik kierunku: helper faktycznie czyta używane klucze (L5).

## Commity (każdy osobno zielony, pełny `npm test`)

- `30c824c` — audyt PR #98 + plan sesji.
- `76a8ccf` — strażnik pokrycia warunków triggerów (M122-rodzina).

## Gdzie szukać dalej

- **ETB conservative** (z handoffu PR #98): `ETB_EFFECT_BONUS` w
  `heuristic-bot.js` ma typy poza tabelą → 0. Pojedyncze typy (job_select,
  amass, discover) domknąć PO realnych błędach Żywego Testera, nie na siłę.
- **Dalsza pętla jakości**: więcej matchupów Żywego Testera (explorer, inne
  zestawy talii), albo inna ścieżka CR (np. deadlock reguł L108, oferta vs
  walidacja L48/L90) — pełny B0 tylko na komendę (ADR 0018/0025).
- **Nowe karty**: TYLKO z listy właściciela w czacie (ADR 0021 4c / 0022).
- Kolejne rekomendacje z audytów (Batch53 → zamknięte w PR #98 C-R1–C-R7).

## Pułapki środowiska

- Sandbox może zresetować workspace (ENVIRONMENT §2): praca wypchnięta
  (`git reset --hard FETCH_HEAD` po resecie). GH_TOKEN może wygasnąć
  (ENVIRONMENT §3) — reconnect w Arenie, nie proś o token.
- Żywy Tester ładuje `dist/`, nie `src/` (L76): `npm run build` przed
  pomiarem; `npm i` w `tools/table-tester` (jsdom) przy pierwszym użyciu.
- Komunikaty commitów przez plik (`/home/user/msg.txt`), nie w repo
  (polskie znaki w `edit_file` potrafią się zgubić — ENVIRONMENT §4).

## Bramy (zmierzone na koniec sesji)

- `npm test` **4484/4484**, `npm run build` 59 modułów / 3284,1 kB.
- Próg regresji bota zielony (z PR #98: heuristic 84,1%, 565/672).
- `test:all` (brama PR): 4492/4492 (z handoffu PR #98; ta sesja nie ruszała
  wolnych plików — warto przeliczyć przed scaleniem).
