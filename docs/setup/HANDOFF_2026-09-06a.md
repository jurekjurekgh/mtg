# Handoff 2026-09-06a — arena/01a07682-mtg (ostateczny)

## Stan
- Gałąź: `arena/01a07682-mtg`, PR **#101** (https://github.com/jurekjurekgh/mtg/pull/101) — OTWARTY,
  wszystkie commity wypchnięte, PR body kumulatywny. Scalenie = decyzja właściciela (squash).
- Commity na gałęzi (nad `448ed55` = scalony PR #100):
  - `cd8f253` — plan sesji.
  - `b6f45b9` — audyt PR #100 (A1–A7 + weryfikacja kontraktów pól).
  - `51dbbda` — **A1**: wyceny `resolve_*` czytają payload decyzji zamiast strefy ukrytej
    + `pendingSatyrLook.cards` / `pendingRevealExile.handCards` w widoku (tylko decydent)
    + strażnik źródła (`test/m305`, 5 testów).
  - `219a3d0` — **A2**: kreator nie zwija szukania z dwiema destynacjami + etykieta wiersza
    z nazwą strefy (`test/m306`, 2 testy).
  - `addbd0d` — **A3/A5**: `effectiveTypesOf` (żywe typy > druk) + gwarancja braku odmowy
    przy szukaniu obowiązkowym po stronie silnika (`test/m307`, 2 testy).
  - `9a34458` — **A4**: okno vigilance = dokładnie `declare_attackers`, komentarz zgodny z kodem
    (behawioralnie neutralnie, gate `m221d` nietknięty).
  - `d0787ad` — **A8**: `landLossValue` — który ląd poświęcić (Springbloom/Roiling Regrowth),
    projekcja na faktach wyceny (`test/m308`, 3 testy).
  - `0166f28` — Żywy Tester: koniec partii ze `__mtgDebug.gameOver()`, usunięty drugi regex.

## Bramy zielone (zmierzone na końcu sesji)
- `npm test`: **4500/4500** (start sesji: 4488; +12 nowych testów).
- `npm run build`: 59 modułów / **3319,6 kB**.
- Benchmark szybki (672 meczów, mierzony PO A8): heuristic **84,8%** (570/672) vs aggro 26,5%
  / random 3,9% (baseline sesji 84,7% = 569/672) — naprawy wycen nie pogorszyły siły bota.
- `node tools/event-contract-audit.mjs`: czysto.
- Golden-master bota: ZREGENEROWANY świadomie po A1 —
  `test/fixtures/bot-scoring-snapshot.json` overallHash `fb8cece4b670…` → `fdb515abbacf…`
  (A8 i pozostałe commity NIE ruszyły fixture).
- Rejestr kart: 500 wpisów (`createCardRegistry().all()`), w tym 39 tokenów; bez nowych kart.

## Pętla jakości — liczby
- `bot-tie-audit` 24 partie: remisy 627 → **625** (28,4% decyzji z alternatywami),
  remisy REALNE 199 → **197** (11,1%). Najważniejsza zmiana jest w klasyfikacji:
  GROZA `resolve_manifest_dread` (1) zniknęła, `resolve_springbloom` (2) zniknęła,
  a `resolve_search_choice` (12) pozostaje w całości `rownowazne` — teraz uczciwie
  (identyczne lądy 55 vs 55), wcześniej fałszywie (różne karty wyceniane na zero).
- Żywy Tester: 4 partie (w tym para i seed ze zgłoszonego „timeoutu") —
  `== KONIEC PARTII ==` w każdej, 0 zgłoszeń detektorów.

## Domknięte wątki właściciela z HANDOFF 2026-09-05e
1. **GROZY `block` (5) i `attack` (7)** — zmierzone na przykładach (sekcja
   „Odpowiedź na otwarty wątek" w `docs/audits/AUDYT_PR100_2026-09-06.md`):
   część to symetria zamiany blokera (identyczne tokeny → uczciwy remis), reszta
   to **model addytywny** wyceny walki (suma wartości per para, bez rozliczenia
   „kto dostanie blok"), np. cztery podzbiory atakujących po 994 pkt. To NIE
   klasa L1/L132 (bot widzi dane), więc NIE naprawiane „przy okazji": zmiana
   funkcji wartości walki wymaga walidacji pełnym B0 (ADR 0018 — tylko na jawną
   komendę właściciela). Zapisane poniżej jako pierwszy kandydat.
2. **Fałszywy detektor końca partii** — naprawiony (`0166f28`).

## Do zrobienia na następnej sesji (kolejność = wartość)
1. **Wycena walki: trade-off zamiast sumy** — `block`/`attack` GROZY mają dowody
   i zdefiniowany test docelowy (`bot-tie-audit --kind=attack|block` ma spaść
   z 7/5). Uwaga: to zmiana modelu punktowego → wymaga `node tools/benchmark.mjs
   --full` NA POLECENIE WŁAŚCICIELA, nie samodzielnie.
2. **A6** (`resolve_exploit_choice` = `finish(40 - value)`, próg 20 pkt) i **A7**
   (martwy parametr `openLabel` w `renderPickerName`, `dataset.cardId` = id obiektu
   w wierszach wizardów) — drobne, odnotowane w audycie, świadomie nienaprawione.
3. **Budżet lektury startowej jest WYCZERPANY**: `AGENTS.md + 28 ADR + LESSONS.md +
   ENVIRONMENT.md` ≈ **99,9k / 100k** tokenów (test
   `test/dokumentacja-budzet-lektury.test.js`). Prógu NIE podnosimy (decyzja
   właściciela M208) — następna lekcja w `docs/LESSONS.md` wymaga kondensacji
   któregoś wpisu (numery L<nr> muszą zostać: cytowane w kodzie ~1150 razy).
   Najwięcej do ugrania: L1 (2004–2130 style opisy przypadków) i sekcje
   powtarzające te same incydenty.
4. Scalenie PR #101 = decyzja właściciela (squash), nie moja.

## Nie ruszane
- Żadnych nowych kart (ADR 0021 4c) — katalog rośnie tylko z listy właściciela.
- Pełny benchmark B0 nie odpalany (ADR 0018).
- `main` nietknięty, `--force` nigdzie nie użyty.
