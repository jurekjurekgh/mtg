# Handoff 2026-09-15 — start sesji + audyt PR #118 (PR #119, `arena/01a0a3aa-mtg`)

Zlecenie „Kontynuujemy projekt. Przeczytaj obowiązkową lekturę i zatrzymaj
się. Czekaj na zadanie.” Lektura obowiązkowa (AGENTS.md §0) ukończona.
Prompt nie nazywał tematu → obowiązkowy start ADR 0020/0021: PR sesji →
audyt ostatniego scalonego PR (#118) → STOP, czekam na zadanie.

Plan: [`docs/plans/PLAN_2026-09-15-audyt-pr118.md`](../plans/PLAN_2026-09-15-audyt-pr118.md).
Raport: [`docs/audits/AUDYT_PR118_2026-09-15.md`](../audits/AUDYT_PR118_2026-09-15.md).
PR: [#119](https://github.com/jurekjurekgh/mtg/pull/119) (scala właściciel,
ADR 0007/0020).

## Co zrobiono (w kolejności commitów)

- **`c56c8f2` — E0: plan** — push PRZED zmianami (ADR 0020 A) + otwarcie PR
  #119. Uwaga o proweniencji: wcześniejszy odcinek sesji (przed resetem
  sandboxa) wykonał lekturę i E0 (commit `0f91d46` + PR #119), ale stan ten
  nie dotarł na origin (brak gałęzi i PR) — E0 odtworzone od zera na
  `e9860cb`, pomiary stanu wejściowego są pomiarami bieżącymi (ENVIRONMENT
  §2).
- **`8a83055` — E1: raport audytu PR #118** — **APPROVE (zielone)**: 27
  plików przeczytane w całości; wszystkie 4 liczby finalnego headu
  odtworzone niezależnie; każdy poprawiony cytat CR (702.174b/j, 702.35a +
  ruling DMU, 611.3a) i każda data rulingu (BLB 2024-07-26, AKH 2017-07-14)
  zweryfikowane ONLINE z dosłownymi cytatami (ADR 0030, dodatek A);
  ścieżka mulligana sprawdzona: oferta ≡ walidacja (L48), cap 35 +
  deduplikacja nazw (L19), licznik publiczny (ADR 0017), determinizm (ADR
  0005), zero wyjątków po nazwie (ADR 0002), mutacyjność testów (L13).
- **`288368b` — E2: naprawa A1** — handoff 2026-09-14e nie był odświeżony
  po etapie 14f (deklarował stan pośredni 14e jako „finalny head": 5485/
  5485 zamiast 5496/5496, „golden master bez churnu (polityka bota
  nietknięta)" mimo zregenerowanego fixture i zmienionej polityki) —
  powtórka L92. Sprostowanie jawne (L142): liczby finalnego headu,
  „Co zrobiono" + 14f (SHA `31f51a7`/`89406a9`/`0a731ab`), dopisek z
  pierwotnym tekstem.
- **E3 — domknięcie** — ten handoff, README „Bieżący stan", plan z
  odhaczonymi etapami, opis PR #119 z werdyktem; STOP — czekam na zadanie.

## Werdykt audytu i znaleziska

**APPROVE (zielone)** — zero blokujących. Pełny wykaz w raporcie:

- **A1 (naprawione w E2)** — handoff 2026-09-14e nieodświeżony po 14f
  (L92). Skutek realny: ta sesja zaczęła od handoffu i odnotowała
  nieaktualną bazę 5485, aż świeży pomiar nie skorygował (5496).
- **O1 (obserwacja)** — w `heuristic-bot.js` trigger mulligana liczy lądy
  po `kind === 'land'`, a keepLoss dodatkowo po `types ⊇ Land` — dziś bez
  skutku, rozjazd-w-potencji (L41).
- **O2 (obserwacja)** — formuła `keepLoss` zdublowana dosłownie w
  heuristic-bot i aggro-bot (L41); pinują testy parności 14f.
- **O3 (obserwacja)** — quick benchmark random 4,2%→3,6% (14/336→12/336) w
  konsekwencji polityki mulligana; w granicach szumu próby (SE≈1,2pp).

## Bramki (finalny head, zmierzone)

- `npm test` **5496/5496**, 0 fail (177–182 s; commit E0/E1/E2 mierzone
  osobno — po każdym commit zielone, L13).
- `npm run test:all` **5506/5506**, 0 fail, 298,5 s (head audytu, przed E2
  — zmiany E2 wyłącznie dokumentacyjne; powtórzenie na finalnym headzie w
  tej sesji).
- `npm run build` **61 modułów / 3660,4 kB** (bez zmian od audytowanego
  headu — E0–E2 dokumentacyjne).
- Quick benchmark (672 mecze): **heuristic 82,9% (557/672) / aggro 30,7%
  (103/336) / random 3,6% (12/336)** — zgodne z README; bez zmian od
  audytowanego headu (E0–E2 dokumentacyjne).

## Pułapki i lekcje sesji

- **Reset sandboxa utracił E0+PR** (commit `0f91d46`, PR #119 nie dotarły na
  origin) — ENVIRONMENT.md §2 przewiduje odtworzenie; weryfikacja stanu
  GitHuba (`git ls-remote`, `gh pr list`) PRZED zaufaniem notatkom sesji.
- **L92 powtórzona u źródła**: handoff pisany w połowie sesji (po E5 14e)
  i nieodświeżony po ostatnim etapie funkcjonalnym (14f) — następna sesja
  musi traktować „Bramki" handoffu jako rzutowane na moment pisania, a bazę
  zawsze mierzyć na nowo.
- **printf w komunikatach commitów** — `%` w tekście (np. „82,9%")
  interpretowany jako specyfikator (ostrzeżenie `invalid time format`);
  komunikaty do `/tmp` przez heredoc + `git commit -F` (wzorzec znany,
  tu doprecyzowany).
- `gh pr create` zadziałał bez problemu (pułapka znana dotyczy `gh pr
  edit` — GraphQL; do edycji opisu PR-u użyć `gh api -X PATCH`).

## Otwarte (następna sesja)

- **Zadanie od właściciela — do odebrania** (sesja zatrzymana po audycie;
  pętla ADR 0021 wstrzymana do tematu).
- O1/O2/O3 z audytu PR #118 — obserwacje nieblokujące (konsystencja
  filtrów lądu, wydanie `keepLoss` do wspólnego modułu, random benchmark).
- D1/O1/O2 z audytu PR #115 — dalej nieblokujące.
- S-1 (literalne `\n` w `oracleText`, 20 kart) — bez zlecenia właściciela
  nie ruszane.
- `[Trigger:]` prefix w linii stosu — kandydat UX (nie ruszany).
- Pełne B0 wyłącznie na komendę właściciela (ADR 0018).
