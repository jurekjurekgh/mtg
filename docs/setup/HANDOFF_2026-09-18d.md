# Handoff 2026-09-18d — uwagi z testów (Dockhand C1/C2, Malamet D, reach E)

Sesja z trzema zgłoszeniami właściciela po partiach kaladesh/ravnica → plan
`docs/plans/PLAN_2026-09-18d-uwagi-z-testow-dockhand-malamet-reach.md`,
gałąź `arena/01a0b60e-mtg`, PR
[#129](https://github.com/jurekjurekgh/mtg/pull/129) (kumulatywny: audyt
PR #128 → uwagi A/B/B1 → ta tura C/D/E).

## Zgłoszenia i rozstrzygnięcia

### C1 — Merchant's Dockhand: brak wyboru X i które artefakty tapować
Właściciel: aplikacja sama zatapowała jedyny artefakt; X=0 musi być
dozwolone; przy wielu artefaktach wybór KTÓRYCH jest ręczny;
**kardynalna korekta sesji: ŻADNEJ enumeracji wariantów — uniwersalny
kreator (stepper X → zaznacz dokładnie X artefaktów → Zatwierdź).**

Naprawa w czterech commitach:
- `3691701` silnik — oferta `tapXArtifacts` zaczyna od X=0 (jawny błąd
  właściciela; CR 107.3), walidacja: pusta lista `targets` legalna tylko
  dla X=0;
- `68761ca` bot — wycena `look_top_put_one_hand_rest_bottom` dla
  `activate_ability` (X=0 → −40 jałowości; X>0 → punkt za kartę i
  opcjonalność − koszt tapów);
- `7778e8d` UI — `tapXArtifactsPlanOf` + `commandForTapXSelection`
  (multi-target.js), tryb `tapXMode` w renderMultiTargetWizard: stepper X
  (0–N), status „X = N — zaznacz dokładnie N artefaktów”, opis jałowości
  przy X=0, pełna walidacja ścieżek w testach (nie tylko klik);
- `8544b76` Żywy Tester — wzorzec grupy „przejrzyj X kart” w
  actions.mjs, gałąź kreatora tapX w sterowniku.

E2E (seed 99, kaladesh vs ravnica, transkrypt `tx-99b.txt`): kreator
przećwiczony 3× — `X=1, artefaktów w puli 1` → aktywacja → karta do ręki;
detektory: 0 zgłoszeń.

### C2 — przy X=1 modal „Weź – Forest”, Pass gubi kartę
Naprawa `6270605`: `takeSingleLookedCardToHand` w effects.js — przy
dokładnie jednej odsłanianej karcie efekt jest rozstrzygany bez pytania
(jedyna możliwa destynacja), niezależnie od rodziny efektu
(rest_bottom/rest_grave). 0 kart = pusty efekt, ≥2 = modal jak dotąd.

### D — Malamet Battle Glyph: efekty walki nie w „Rozgrywce” (ponowne zgłoszenie)
Pomiar: objaw nieodtwarzalny — po M386 (PR #128, commit `e506250`:
try/catch w showBotMoves, `rerender` ogon, `resumeOrStart` z wpisem błędu
w log) counter/obrażenia/zgony fight lądują w buforze i w przebiegu tur,
zarówno dla rzutu człowieka, jak i bota (pin `266950a`, 3/3). Podejrzenie:
u właściciela odpaliła się któraś ze ścieżek wyjątków naprawionych w M386
(np. odświeżenie z uszkodzonym save). Pin chroni przed nawrotem klasy.

### E — bot atakuje lataczem w blokera z reach
Pomiar: scoring działa poprawnie (bestow/grant reach EOT — bot nie atakuje,
pin `a9b10b1` 3/3, mutacja zdjęcia reach z `attackerCanBeBlocked` → 2 RED).
Objaw właściciela nieodtwarzalny; hipoteza z sesji: aura z reach była
widziana w panelu (opis), ale nie w danych karty (np. kopia z pamięci bez
`keywords`), albo stwór atakował przed zagraniem aury.

## Pułapki tej sesji (do LESSONS przy kolejnym uzupełnianiu)

- **TRZECI reset workspace** (po push E4): push odrzucony = sygnał;
  odzyskanie wzorem poprzednim — commit-sierota + `git reset --hard
  FETCH_HEAD` + cherry-pick.
- `gameObjectDataOf` zwraca deskryptory zdolności i parametry BEZ
  `keywords` — helper testowy musi dokładać keywordy z rejestru
  (`createCardRegistry` + `addCardToRegistry`), inaczej reach/latanie
  nie wejdą do scenariusza.
- `createGameState` startuje w `untap` — aktywacje wymagają
  `jumpToStep(state.turn, 'main', playerId)` (CR 502.4).
- Konstruktor zdolności aktywowanych odrzuca komendy bez `id`/`name` —
  w testach aktywacji zawsze `abilities: [...gameObjectDataOf(registry.get(id))]`.
- Zmiana kształtu oferty aktywacji dotyka `batch42-kart.test.js`
  (warianty Dockhand +1) — atrybucja L135.

## Stan bram (po `8544b76`)

`npm test` 5829/5829; `npm run test:all` 5839/5839; build 64 moduły /
3844.7 kB; Żywy Tester (kaladesh s21/77/99, ravnica s77): 0 zgłoszeń
detektorów, kreator tapX i kreator załogi przećwiczone.

## Na kolejną sesję

- Kierunek odwrotny oferty bloków (`Math.min(slots, 2)` — pin przy
  blokerze >2 slotów) — otwarte z poprzedniej tury.
- Jeśli właściciel poda seed/transkrypt partii D lub E z ponownym objawem
  — odtworzenie precyzyjne (bez seeda te dwie klasy pozostają nieodtwarzalne).
