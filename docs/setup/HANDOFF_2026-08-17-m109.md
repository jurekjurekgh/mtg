# Handoff — sesja M109 + M110 + M111 (2026-08-17)

Gałąź: `arena/01a00b7e-mtg` · PR **#56** (otwarty, scalenie należy do
właściciela: `Squash and merge`).

## Co zrobiono w tej sesji

**M109 — batch 33, transza 2: siedem ostatnich kart z listy właściciela.**
Każda wymagała nowej mechaniki silnika; szczegóły, ograniczenia i pomiary
w `docs/plans/2026-08-17-m109-batch33-transza2.md`.

Kolejne commity (każdy samodzielnie zielony, po każdym push):
1. `92977af` — Chill of the Grave + Diplomatic Relations
2. `edf5156` — Sagittars' Volley + Nightsnare (+ talie, przelosowane seedy)
3. `501af3b` — Tiller of Flesh (incubate, token dwustronny)
4. `8d99872` — Spare from Evil (ochrona przed jakością)
5. `ec21f6f` — Spreading Insurrection (storm)
6. `7b1f8f5` — wycena nowych efektów przez bota + prawdziwe adresy ilustracji

## Stan techniczny

- `npm run test:all` — **1993/1993**; `npm test` (szybki rdzeń) — 1984.
- Build: `node tools/build.mjs` → 51 modułów / 1781.6 kB.
- Benchmark szybki (1248 meczów): heuristic **61,9 %** vs aggro,
  **88,1 %** vs random, 0 niedokończonych. Progi regresji: 0,57 / 0,78.
  Zapisy: `tools/b5-m109-2026-08-17.txt`, `tools/b6-m109-bot-2026-08-17.txt`.
  Pełne B0 (ADR 0018) NIE było liczone — czeka na komendę właściciela.
- Żywy Tester: azorius vs innistrad, seedy 7/11/23 — bez zgłoszeń detektorów;
  seed 23 pokazał Spare from Evil w partii (to on wykrył „1 stwor\").

## Uwaga o taliach i seedach (lekcja L25)

Dołożenie 7 kart do sześciu talii przetasowało wszystkie testy grające pełne
partie. Przelosowane hunterem: devour 16, endure 2, delirium 12,
Forever Young 4, tokeny w `session-abilities-integration` 16,
`human-plays-modal` 1, surveil w `library-manipulation-modal` 8.
Po KAŻDYM batchu kart trzeba przejrzeć te testy, nie tylko testy nowych kart.

## M110 — eliminacja ograniczeń (polecenie „100 % kart wg Oracle")

Commity `f53c564` (ochrona przed jakością: celowanie + załączniki),
`8e17016` (storm jako trigger + nowe cele kopii), `1b4605b` (Willbender:
także zdolności na stosie). Trzy karty straciły `limitations`.
Plan: `docs/plans/2026-08-17-m110-eliminacja-ograniczen.md`.
`npm run test:all` 2006/2006, benchmark bez zmian.

**Uwaga o commicie `92977af` (transza 2, 1/3):** ma na PR czerwony krzyżyk.
Powód: dołożone karty były już `supported`, a jeszcze nie było ich w żadnej
talii — poleciał strażnik konwencji „każda wspierana karta nielandowa jest
w którejś talii". Lokalnie puszczono wtedy tylko testy nowych kart, nie pełny
pakiet. HEAD gałęzi jest zielony, a przy `Squash and merge` do `main` trafia
jedna zielona zmiana — historia commitów PR nie jest przepisywana.
Wniosek na przyszłość (dopisany do AGENTS.md): commitujemy dopiero po
`npm test`, nie po testach samego wycinka.

## M111 — „gotowe na nowe karty" (polecenie właściciela)

Commity: `3452a74` (obniżki kosztu przy każdym sposobie rzucenia),
`5fb31bc` (kopie czarów wielocelowych + `targetIndex` w efekcie `damage`),
`1398564` (bot wycenia tryby modalne), `6ab9013` (pole `notes` + strażnik
`limitations`).

Stan pola `limitations` po sprzątaniu: **34 karty**, wyłącznie z trzech
dopuszczonych powodów (token, tylna strona DFC, brak strefy dowodzenia
w 1v1 — świadoma decyzja właściciela, NIE kodujemy innych trybów).
Wszystko inne (58 kart) to teraz `notes`. Strażnik:
`test/limitations-guard.test.js`; zasada w `AGENTS.md`.

## Kolejka na później

1. **PlayerView `combat`** (M107) wciąż nieużywany przez `src/table/`.
2. **Sondowanie wizardów walki i scry/surveil** (budują komendy spoza
   `legalCommands`, więc oś „noop" ich nie widzi).
3. **PlayerView `combat`** (M107) wciąż nieużywany przez `src/table/` —
   render i sesja czytają znaczniki z kafli.
4. Kolejka z M104: sondowanie wizardów walki i scry/surveil (budują komendy
   spoza `legalCommands`, więc oś „noop\" ich nie widzi), sprzątanie kontraktu
   `addObject` (L21), B4/B5 z `docs/BOT_ROADMAP.md`.
5. Otwarte pytanie z M106/Z2: czy komunikat „bez efektu\" ma dostawać także
   CZAR o zerowym wyniku (dziś tylko trigger).

## Przypomnienia środowiskowe

- Node nie ma sieci — Scryfall wyłącznie narzędziem `fetch_page`
  (`https://api.scryfall.com/cards/named?exact=<Nazwa>&format=json` daje też
  prawdziwy adres ilustracji; `format=text` wystarcza do Oracle).
- Agent nie może pushować `.github/workflows/*` — wzorzec w
  `docs/setup/workflows/`, strażnik `test/ci-workflow-tiers.test.js`.
- Nowe pole karty trzeba przepuścić przez `registry.js` → `materialize.js`
  → `deck.js` → `identity.js` → `game-state.addObject`; nowy typ celu przez
  `legalTargetCandidates` ORAZ `validateTargets`; nowe zdarzenie dopisać do
  `EVENT_TYPES` w `src/protocol/types.js` i opisać w `session.describeGameEvent`.
