# Plan kontynuacji projektu — sesja 2026-09-24c (batch 59: kolekcja 126–142)

## 0. Czytanie obowiązkowe (jak zawsze, AGENTS.md §0)

1. `AGENTS.md` (zasady stałe);
2. `docs/decisions/` — ADR-y (stan: 0001–0030, 0008 w `archive/`);
3. `docs/LESSONS.md` (rejestr reguł; najnowsze **L164–L167**) + `docs/LESSONS_PRZYPADKI.md` + `docs/LESSONS_ARCHIWUM.md`;
4. `docs/setup/ENVIRONMENT.md` (sandbox, git, GitHub CLI; §2 odzyskiwanie po resecie).

## 1. Stan projektu

- **Bramy po sesji:** `npm test` **6511/6511**, 0 fail (~220 s); pełna brama
  `npm run test:all` **6521/6521**, 0 fail (~340 s, dwa przebiegi zgodne);
  build **60 modułów / 4239,9 kB**.
- **PR #136** (gałąź `arena/01a0d408-mtg`) — OPEN, base `main` (`f9bc44b`);
  opis uzupełniany kumulatywnie (sesje 24b + 24c).
- **Batch 59 zamknięty:** 10 kart / 11 wpisów arkusza (126/127 MID to przód
  i tył jednej karty). Commity: `fa8d70f` (G0: dane + plan), `44dc8eb`
  (G1.1–G1.2), `e135540` (G1.3–G1.4), `88e46ee` (G1.5–G1.6), `2987d3b`
  (G1.7), `ba5932c` (G1.8), `adcb2e5` (G1.9), `1265cf5` (G1.10 + domknięcia).
  Plan: `docs/plans/PLAN_2026-09-24c-batch59-kolekcja-126-142.md`.
- **Mechaniki ogólne dodane w tym batchu** (będą pracować dla kolejnych kart):
  `legalTargetCombos` (jeden enumerator kombinacji celów; zastąpił `cartesian`
  i duplikat w `game-state.js`), zależne pozycje celu (`graveyardOfSlot`),
  pary obrażeń „this turn" (`damageSourcesThisTurn` +
  `zones.exiledByEnchantedDamage`), bramka obrotu daybound/nightbound
  (`dayNightDriven` w `effects.transform`), efekt
  `shuffle_graveyard_cards_into_library` (event `library_shuffled`),
  `exile_graveyard_card`, typ celu `card_in_opponent_graveyard`,
  predefined token `token_mutagen`, hybrydowy pip `{2}{G/U}`.

## 2. Otwarte punkty dla właściciela

- PR #136 czeka na przegląd/merge (agent nigdy nie merge'uje — ADR 0020).
- **Kolejny batch:** lista kart wyłącznie od właściciela (artId/set), standard
  = 10 kart; karta dwustronna liczy się jako JEDNA.
- **Narzędzie `tools/cr-numery.mjs`:** nowy cytat `CR <numer>` w `src/`/`test/`/
  `tools/` wymaga weryfikacji wobec pliku CR i `--zapisz`; bez pliku CR nie
  dopisuj numeru do tabeli (w tym batchu odpadł tak `614.1a` z komentarza).
- **O-4** (CR 616.1 — kolejność efektów zastępczych) i **O-5** (`colorsFrom`)
  — nadal „KARTY SPOZA KATALOGU": implementować dopiero, gdy wejdzie karta,
  która je rozróżnia. Uwaga: O-4 ma teraz pierwszego klienta w praktyce
  (dwie aury z efektem zastępczym naraz — silnik wybiera pierwszą pasującą;
  pin dopiero przy karcie, która to rozróżnia).
- Pełny bench `--full`/macierz B0 wyłącznie na komendę właściciela (ADR 0018).

## 3. Ryzyka / watchlisty

- **Reset sandboxa zabiera katalogi poza repozytorium.** Katalog roboczy
  snapshotów (`/home/user/batch59-snapshots/`) nie przetrwał — od G1.8
  snapshoty Scryfalla pobieramy ponownie (`fetch_page`: `cards/named` +
  `/rulings`) i zapisujemy OD RAZU w `docs/cards/`. W tym batchu zdarzył się
  też **drugi reset `HEAD`**: drzewo robocze zachowało zmiany, ale `HEAD`
  wróciło do `f9bc44b` — leczenie: `git fetch` → `git reset --mixed FETCH_HEAD`
  (NIGDY `checkout`/`clean`; L136).
- **Karty dwustronne:** scenariusze mechaniczne budować z PRAWDZIWEJ talii
  (`setupCardMatch`) — tylko materializacja talii (`createCardDeck`) niesie
  `transformTo` i `frontFaceId`; ręcznie wstawiony obiekt testuje atrapę bez
  drugiej strony (L21). Dotyczy też sond O-6 (`test/audyt-pr134-…`).
- **Bramka `dayNightDriven`:** każdy nowy efekt obracający DFC musi jawnie
  zdecydować, czy jest „parą daybound/nightbound"; dziś legalne drogi to
  `setDayNight` i wejście na pole bitwy w nocy/za dnia.
- **Pola stanu i znaczniki:** nowe pole obiektu → `identity.js` (łańcuch
  registry → gameObject); nowy klucz stanu → odcisk albo
  `STATE_FINGERPRINT_EXCLUSIONS` z powodem (strażnik B2 policzył
  `damageSourcesThisTurn`); nowe pole EOT → znacznik + cleanup + reset 400.7.
- **Żywy Tester mierzy `dist/`** — po każdej zmianie w `src/` najpierw
  `npm run build`; tymczasowa talia `decks/audyt-*.txt` łamie 4 strażniki talii.
- **Mutacje testowe na kopiach** (`cp`/`/tmp`), nie `git checkout` (L136);
  po każdym zielonym kroku commit + push (sandbox bywa resetowany).

## 4. Artefakty sesji

- Plan: `docs/plans/PLAN_2026-09-24c-batch59-kolekcja-126-142.md` (G0–G1.12).
- Testy etapu: `test/real-cards-batch59.test.js` (49 testów: 10 kart + token
  Mutagen + scenariusze klas mechanik).
- Snapshoty: `docs/cards/scryfall-{charismatic-vanguard,sun-collared-raptor,
  savage-hunger,join-the-dance,waveskimmer-aven,slithering-cryptid,
  scavenging-harpy,memory-s-journey,kumanos-blessing,bird-admirer}.json`.
- Talie/CSV: `decks/*` (regeneracja generatorem), `tools/collection-art-ids.csv`
  (513 pozycji), liczniki w `README.md`.
- Wpisy: **M427** w `docs/PROJECT_HISTORY.md` i `docs/ENGINE_MILESTONES.md`.
