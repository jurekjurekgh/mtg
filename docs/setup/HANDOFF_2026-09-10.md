# Handoff 2026-09-10 — pętla jakości: Thunderstaff noop (PR #111, arena/01a08788)

Gałąź `arena/01a08788-mtg`, PR #111 OPEN (review/merge właściciela). Po F-A..F-D
sesja przeszła w **pętlę jakości Żywym Testerem do wyczerpania budżetu** (ADR 0021)
na tym samym PR (ADR 0013/0020 — 1 sesja = 1 gałąź = 1 PR).

## Co zrobiono

- **16 partii Żywego Testera** (`--steps 500-600 --tick-rate 0-0.25`, profile
  `greedy`/`hoarder`/`explorer`, seedy 1..777) → 15× DET0, 1× `noop` Thunderstaff
  w `T.16 Główna 1` przed deklaracją ataku (warhammer-wg vs tarkir-wur, seed 777,
  hoarder) — sonda `noop` (Oś 4) słusznie: koszt `{2},{T}` bez efektu.
- **Triaż:** `src/engine/effects.js:2096-2125` (CR 611.2c, early-return przy pustych
  attackers), `src/cards/card-data.js:10098` (`buff_attacking_creatures`),
  `src/engine/abilities.js:325-420` luka `effectIsNoOpOnTarget` bez case.
- **F1 (M260/F1, `fix(abilities): hide Thunderstaff outside combat`):**
  `src/engine/abilities.js` — case `buff_attacking_creatures` w `effectIsNoOpOnTarget`:
  ukrywa `{2},{T}` gdy `attackers.length===0` lub brak żywego stwora
  (U9/M103, CR 602.2b legalność zachowana). `npm run build` 61/3452,2 kB.
- **Weryfikacja:** re-run seed 777 hoarder: 1→0 detektorów; re-run 10 partii
  (6× warhammer-wg vs tarkir-wur + 4× diverse) — **10/10 DET0**, 0 niewycenionych.
  Pełny zielony przebieg bramkowy przed commitem.
- **Kontynuacja pętli (do wyczerpania budżetu, ta sama sesja):**
  `2026-09-10` — 20 świeżych partii (`--steps 400 --tick-rate 0.25`, 24 talie
  rotacyjnie `decks[(i*7)%24]` vs `decks[(i*11+3)%24]`, profile
  `greedy/hoarder/explorer/random/defensive/impatient` rotacyjnie, seedy
  `3000+i*17`) — **20/20 DET0**, **20/20 NIEWYCENIONE: brak**, 0 runtime errors
  + 10 głębokich partii (`--steps 600 --tick-rate 0.15`, seedy 5483..5690) —
  **10/10 DET0**, 0 niewycenionych + 10 celowanych warhammer-wg (`--steps 500`,
  seedy 6589..6760, 5 botów rotacyjnie) — **10/10 DET0**, 0 niewycenionych
  + 15 mieszanych (`--steps 500/600`, 22 talie rotacyjnie, seedy 8271..8705) —
  **15/15 DET0**, 15/15 `NIEWYCENIONE: brak` (log `/tmp/batch4_2026-09-10.txt`).
  Razem po F1: **65/65 DET0** (10+20+10+10+15), łącznie w pętli 81 partii (16 przed fixem +65 po).
- **BATCH5 / M348 — wyciek `discard_choice_unresolved` (INFO) → RED→GREEN:**
  batch5 56-70 (`--steps 550/600 --tick-rate 0.2`, 22 talie rotacyjnie, 6 profili, seedy `9000+i*29`) —
  przed fixem 14/15 DET0 + 1× INFO `LOG: snake_case discard_choice_unresolved` w
  `warhammer-ubr vs wiedzmin-wu seed 10885 impatient` (Fledgling Imp + pending discard_choice);
  fix `src/table/session.js` — `REJECTION_REASON_LABELS` +3 + `rejectionReasonLabel()` guard
  `≥2 _ → bez (reason)` (L6: dwa kanały — UI bez sluga, `rejectionRecords` z kodem);
  `npm run build` 61/3453,7 kB (+1,5 kB); re-run seed 10885 → DET0
  (`Ruch odrzucony: najpierw wybierz kartę do odrzucenia` bez sluga); re-run pełnego batch5
  56-70 → **15/15 DET0**, 15/15 `NIEWYCENIONE: brak` (log `/tmp/batch5_fixed.txt`);
  łącznie po obu fixach sesji: **80/80 DET0** (10+20+10+10+15+15), **96 w całej pętli** (16 przed F1 +80 po).

## Bramki (zmierzone)

- `npm test` (fast): **5055/5055**.
- `npm run test:all`: **5065/5065** (~304 s).
- `npm run build`: exit 0; 61 modułów / **3453,7 kB** (było 3451,3 kB, delta +0,9 kB za case M260/F1 +1,5 kB za M348 leaky slog).
- `node --test test/bot-benchmark.test.js`: **10/10** (via `test:all`).
- Żywy Tester: **80/80** re-runów 0 zgłoszeń po obu fixach (w tym seed 777 i 10885) — logi `/tmp/batch_2026-09-10.txt` (20×400) + `/tmp/batch2_2026-09-10.txt` (10×600) + `/tmp/batch3_2026-09-10.txt` (10×500 warhammer-wg) + `/tmp/batch4_2026-09-10.txt` (15×500/600) + `/tmp/batch5_fixed.txt` (15×550/600 batch5).

## Kolejka / otwarte (nie zapomniane)

- PR #111 scalić do `main` (review właściciela).
- Backlog jak w handoff 09-09c (język: Plot {1,W}, nazwy mechanik po angielsku,
  surowe liczniki w logu, sweep komentarzy CR — ADR 0030) — poza zakresem tej tury.
- Następna pętla jakości może wejść w kolejne 10–20 partii świeżych seedów lub
  dedykowane sondy CR dla innych efektów `buff_*` (jeśli pojawią się nowe karty).

## Pułapki środowiska (przypomnienie)

- `dist/` gitignorowany — po każdej zmianie `src/` **zawsze** `npm run build`
  przed Testerem (Tester mierzy `dist/mtg-table.html`).
- Gałąź na origin (`3f77da8` baza) — tylko przyrostowo, bez force;
  po resecie `git fetch origin arena/01a08788-mtg` + `git reset --hard FETCH_HEAD`.
- `gh pr edit` pada na GraphQL Projects — opis przez `gh api -X PATCH .../pulls/111 -F body=@plik`.
- `state.zones.battlefield` drukuje `undefined` — liczyć przez `playerView()` / `state.objects`.
- Polskie znaki w edycji — przez `write_file`/`python3` z UTF-8.

## Plany / dokumenty

- Plan pętli: `docs/plans/PLAN_2026-09-10-petla-jakosci-thunderstaff.md` (M260/F1).
- Poprzednie: `PLAN_2026-09-09d` (A–D), `PLAN_2026-09-09c` (audyt PR109), handoff 2026-09-09c.
