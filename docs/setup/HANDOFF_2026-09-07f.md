# Handoff 2026-09-07f — audyt PR #104, pętla jakości, PR #105

## Stan bieżący

Sesja `arena/01a07c4e-mtg`, [PR #105](https://github.com/jurekjurekgh/mtg/pull/105)
do `main`. Agent nie scala. Kod zamknięty i wypchnięty; po nim wyłącznie
dokumenty zamknięcia. Sprawdź bieżący HEAD/stany PR i CI na GitHub — plik nie
pinuje własnego przyszłego SHA. Plan
`docs/plans/PLAN_2026-09-07-audyt-pr104-petla-jakosci.md` wykonany E1–E4;
audyt: `docs/audits/AUDYT_PR104_2026-09-07.md` (36/36 plików diffu PR #104,
11 prób mutacyjnych → 10/10 fixów RED→GREEN, zero błędów). Historia całości
w `docs/PROJECT_HISTORY.md`, reguły w LESSONS/ADR.

## Bramki i dane do porównania

- `npm test`: **4698/4698** (start 4695; +3 strażnika).
- `npm run test:all`: **4708/4708**, w tym 10 testów benchmarku-regresji;
  pomiar końcowy ~280 s.
- `npm run build`: **59 modułów / 3369,8 kB**, plik `dist/mtg-table.html`
  (rozmiar bez zmiany względem bazy).
- `node tools/benchmark.mjs` (szybki): 10/10, ~142 s — zmierzone na bazie,
  kod produkcyjny w tej sesji niezmieniony.
- **Pełnego B0 NIE uruchamiano** (ADR 0018/0025). Żadnych nowych kart/talii/
  limitacji Oracle; nośniki syntetyczne wyłącznie w testach.

## Co zrobiono (bez napraw produkcyjnych)

- **E1 audyt PR #104**: lektura 36/36 plików, weryfikacja CR dla
  M339–M348, 11 mutacji na testach #104 (10/10 fixów RED po przywróceniu
  kształtu PRE-fix — L114; M345 podwójna próba). Raport w `docs/audits/`.
  Wnioski wyłącznie potwierdzające; jedna notka obserwacyjna (duplikat
  raportu na granicy przycięcia logu testera).
- **E3 żywy stół**: 4 partie, 4 profile, 0 zgłoszeń detektorów, 0 wyjątków:
  greedy theros|tarkir-bg s10421, explorer worek-basni|mirrodin-brg s10422,
  impatient final-fantasy|ravnica s10423 (`--snapshot-every 3`, pełna lektura
  transkryptu — triggery mill w kolejności, mana zachowana przy przerwanej
  płatności, `illegal_ability` odrzucone przez silnik, cloak + Guildscorn
  Ward), random worek-mroczny|srodziemie s10424. Obserwacja kosmetyczna:
  duplikat bloku POKRYCIE/DETEKTORY na końcu logu (bez klasy, bez naprawy).
- **E3 sonda CR 608.2b** (okolice bramki M343): czar z martwym jedynym
  celem fizzluje w całości — scry nie biegnie, brak wiszącej decyzji;
  SBA i trigger dies rozliczają się na granicy komendy rzutu (model
  triggerów silnika). Silnik poprawny; pierwotna anomalia = błąd armatury
  (L116: addObject nie stosuje liczników ETB). Strażnik wg L39:
  `test/audyt-pr105-fizzle-martwy-cel.test.js` (3 testy, w tym
  anty-over-fix z żywym celem).

## Żywy stół — dowody i ograniczenia

Transkrypty w `tools/table-tester/audyt-pr105/` są gitignorowane; dowody
wniosków w audycie i historii. Wszystkie partie: exit 0, puste stderr,
„DETEKTORY: brak zgłoszeń". Osie detektorów M346–M348 celowo nie powtarzane
(już zweryfikowane w #104). jsdom nie mierzy layoutu/obrazów/gestów;
heurystyka bota nie gwarantuje optymalnych ruchów.

## Następna sesja / wznowienie

1. Lektura zgodnie z AGENTS §0; nie powtarzać w całości po odświeżeniu
   stanu GitHub.
2. Sprawdź, czy #105 scalono. Jeśli tak, ta sesja nie koduje; nowa sesja
   pracuje na swojej gałęzi i zaczyna od audytu najnowszego scalonego PR
   (ADR 0020 B) — kandydat: ten PR #105 (audit materiał: plan, raport
   AUDYT_PR104, strażnik fizzla, dokumenty).
3. Bez listy właściciela kontynuacją jest pętla jakości (ADR 0021):
   żywy stół innymi parami dek/profilami i CR-inconsistency inną ścieżką
   (nie powtarzać osi M346–M348 ani sondy 608.2b w tym samym układzie).
   Pełny B0 tylko na jawne polecenie właściciela.
4. Ponów bramki z powyższymi wartościami. Przed testerem build; w razie
   potrzeby `npm ci --prefix tools/table-tester`. Zawsze czytaj stderr.

## Pułapki

- Budżet lektury startowej **99 904/100 000** — bez zmian (brak nowego wpisu
  LESSONS: brak nowej klasy błędu; L39 strażnik wystarczy).
- `objectId` komendy to ID OBIEKTU, nie cardId — w ofertach i testach.
- Sonda/zestaw testowy: `addObject` nie stosuje `entersWithCounters` —
  stwór 0/0 umrze od SBA na pierwszej komendzie (bywa zamiarem, jak w
  strażniku fizzla — ale musi być świadome).
- Mutacje cofaj kopią w `finally`, nie `git restore` brudnego drzewa
  (L136). Każdy zielony przyrost: osobny commit + natychmiastowy push;
  przed pushem fetch + porównanie HEAD..FETCH_HEAD i FETCH_HEAD..HEAD
  (rituał ADR 0020 D — w tej sesji raz pominięty przy d24036f, bez szkody).
- 401: poproś o ponowne połączenie GitHub w Arena; nie żądaj haseł/tokenów.
- `dist/`, node_modules i `.arena/` nie persistują po odtworzeniu sandboxa.
