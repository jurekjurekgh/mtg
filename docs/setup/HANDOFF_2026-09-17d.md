# Handoff 2026-09-17d — audyt PR #125 + domknięcie E7bis + pętla jakości (PR #126, etapy E0–E6)

Sesja „Kontynuujemy projekt." (prompt bez nazwanego tematu) → tryb obowiązkowy
ADR 0020 (PR → audyt poprzedniego PR → inkrementalne commity, każdy push
osobno) + pętla domyślna ADR 0021 §4. Gałąź `arena/01a0b0e5-mtg`, bazowy HEAD
`cf51ae9` (squash PR #125). PR: [#126](https://github.com/jurekjurekgh/mtg/pull/126).

## Etapy (commit po każdym, HEAD == origin po pushu)

- `1f5e090` — **E0**: plan sesji `docs/plans/PLAN_2026-09-17d-audyt-pr125-i-domkniecie-e7bis.md`
  (PR otwarty PRZED kodowaniem, ADR 0020 A).
- `49dda11` — **E1**: raport audytu PR #125 `docs/audits/AUDYT_PR125_2026-09-17.md`
  (werdykt APPROVE; F1 + O1/O2; weryfikacja mutacyjna V1–V6).
- `33366ed` — **E2**: naprawa F1 (`get_energy` czyta `targets[effect.targetIndex]`)
  + pin `test/m375-get-energy-target-index.test.js`.
- `fde5ed9` — **E1b**: uzupełnienie raportu (V7 dane Scryfall, V8 generyczność,
  kontrola źródeł CR, obszary bez znalezisk).
- `93b242e` — **E4/M376**: aktywacja pompy musi POPRAWIĆ wymianę (kopie na
  stosie wchodzą do wyceny) + pin `test/m376-pump-nie-zabija-swojego-stwora.test.js`.
- `684fe91` — **E4/M377**: martwe okno detektora testera musi się POWTARZAĆ
  + 2 piny w `test/table-tester-detectors.test.js`.

## Co dokładnie zmieniło się w silniku i w narzędziach

- `src/controllers/heuristic-bot.js` — nowe `pendingPumpDelta` (suma delt kopii
  tej samej aktywacji z widoku stosu: `sourceId`, `abilityIndex`, cele,
  `abilityEffects` — ADR 0017) i `pumpImprovesOutcome` (wymiana z perspektywy
  celu: śmierci wroga w górę, moje śmierci w dół, obrażenia twarzy i zyski
  życia na moją korzyść; poprawa wymagana, pogorszenie dyskwalifikuje).
  Brama używana wyłącznie w gałęzi `activate_ability` dla WŁASNYCH celów —
  cudze cele i czary zostają na `pumpChangesOutcome` (L41).
- `tools/table-tester/detectors.mjs` — bramka `frozen` w detektorze martwego
  okna: okno (puste / z samym „Poddaj partię") jest zgłaszane tylko, gdy
  `newestLogEntry` nie zmienia się wobec poprzedniego i następnego rekordu.

## Piny i mutacje

- `test/m375-get-energy-target-index.test.js` (3): targetIndex → kontroler
  celu, brak targetIndex → kontroler źródła, nieistniejący id → spadek do
  źródła. RED 1/2 → GREEN 3/3; mutacja (`effectTargets`) → 1/2.
- `test/m376-pump-nie-zabija-swojego-stwora.test.js` (4): pierwsza aktywacja
  odwracająca wymianę (kontrola pozytywna), druga kopia przy wiszącej
  pierwszej, aktywacja po rozstrzygnięciu pierwszej, dobicie większego
  atakującego (kontrola pozytywna). RED 2/4 → GREEN 4/4; mutacja bramy
  (`pumpChangesOutcome`) → 2/4.
- `test/table-tester-detectors.test.js` (+2): okno przejściowe bez zgłoszenia;
  okna powtarzające się nadal zgłaszane (mutacja: usunięcie bramki → 1 RED).
- Audyt E1: mutacje V1–V5 czerwienią piny PR #125 (`m374` 2/4, `m371` 3/5,
  `m372` 2/5, `batch56` 2/38, `m373` 3/6); sonda V6 → `ReferenceError`.

## Bramki finalne (head sesji)

- `npm test` — **5732/5732**, 0 fail.
- `npm run test:all` (bramka PR, jak CI) — **5742/5742**.
- `npm run build` — **64 moduły / 3815,6 kB**.

## Pomiary

- Quick 25 talii (25 talii, 16 seedów od 2026, oba boki, self-play):
  **5952/5952 meczów, 0 niedokończonych, 0 zacinek** (exit 0) —
  `illegal_spell: Niewystarczająca mana` (seed 2039) **nie wrócił**;
  heuristic **87,1%** (5183/5952), aggro 23,5% (700/2976),
  random 2,3% (69/2976), ~232 ms/mecz (13,8 s/1000 komend nie licząc;
  przebieg współbieżny z testami). JSON: `/tmp/quick25.json`.
- Żywy Tester: 6 partii (seedy 2027, 2031, 2035, 2037, 2044; profile
  greedy/impatient; talie z batcha 56), po fixach **0 zgłoszeń**,
  partia seeda 2031 kończy się naturalnie (przed M376: limit kroków).

## Co dalej (punkt zaczepienia dla następnej sesji)

- Pełne B0 wyłącznie na wyraźną komendę właściciela (ADR 0018) — wynik quick
  (87,1%) jest porównywalny z progiem testowym, ale to nie B0.
- Kandydaci do kolejnej pętli: dalsze partie Żywym Testerem na taliach
  `innistrad-wu` (Bonds of Faith nie pojawił się w 6 partiach — brak
  pokrycia aury warunkowej w transkryptach), przegląd `docs/backlog.md`
  (pamiętając, że to nie kolejka zadań), ewentualne domknięcie obserwacji
  z audytu (O1 token Goblin — brakuje snapshotu Scryfall tylko dla tokenu
  bez zdolności, konwencja repo).
- Otwarte pozycje procesu: brak — wszystkie etapy E0–E6 zamknięte w tej sesji.
