# HANDOFF 2026-08-20 — M161+M162 (audyt PR #67, gotowość madness, uwagi właściciela A/B/C)

## Stan

- **PR #68** (`arena/01a02093-mtg`) — sesja M161. Commity: audyt PR #67
  (9c763cd), plan (1e4a922), implementacja O1/O2 + testy (6f3fae7),
  dokumentacja (LESSONS L52, PROJECT_STATE backfill M160, handoff).
- `npm test` **2507/2507** (fast), `test:slow` 9/9, build **51 modułów /
  2137.5 kB**. Szczegóły: `docs/PROJECT_STATE.md` (M160 backfill + M161).

## Co wykonano (skrót)

- **Audyt PR #67** (`docs/audits/AUDYT_PR67_2026-08-20.md`): F1–F4,
  Z1–Z5, M160 A/B zweryfikowane poprawne (M160/A — weryfikacja mutacyjna:
  wyłączenie pętli fellows → 2 testy czerwone). Znalezisko **D1**: praca
  M160 nie istniała w PROJECT_STATE → backfill sekcji M160.
- **Zasada właściciela → reguła trwała L52** (LESSONS): ścieżka mechaniki
  zależna od przyszłych kart = zaimplementuj generycznie + wyprowadź na
  wszystkie warstwy + jawny reject na granicach zakresu + strażnik
  katalogu czerwieniejący przy pierwszej takiej karcie.
- **O1**: routing `resolve_madness_cast` po kind — czary idą
  `spells.castMadnessSpell` (suspend/rebound + płatność kosztu madness);
  oferta per cele/tryby; materialize spell-branch zachowuje `madness`.
- **O2**: bramka kolorów wg AKTYWNEGO kosztu alternatywnego
  (`altCostColors` — madness/warp), nie pipów karty; `canPayMadnessCost`
  odzwierciedla to samo.
- **Sygnał**: `test/m161-madness-spell-path.test.js` (11 testów) — pełne
  ścieżki czarowe na obiektach syntetycznych + strażnik katalogu S9.

## Pułapki dla następnej sesji

- Strażnik S9 czerwienieje przy PIERWSZEJ karcie instant/sorcery z madness
  w katalogu — wtedy: dopisz testy kartowe (batch) i sprawdź, czy karta
  mieści się w zakresie `castMadnessSpell` (additionalCost/variableTargets/X
  = świadome rozszerzenie; dziś jawny reject — test S10).
- `epicCastOffers` NIE filtruje `spell.additionalCost` (Epic Experiment
  może oferować village-rites-podobne czary, których free-cast path ich
  kosztem się nie zajmuje) — obserwacja z tej sesji, dotyczy ŚCIŻKI EPIC,
  nie madness (madness ma własny filtr w playerView). Do rozważenia przy
  pierwszym epic-czarze z kosztem dodatkowym.
- Liczby bazowe dla porównań: 2527 (fast) / 9 (slow) / 52 moduły /
  2147.1 kB.

## Kolejka

- Pętla jakości wg ADR 0021 (dopóki właściciel nie wskaże tematu):
  niewykorzystane scenariusze Żywego Testera z handoffu M159 (madness-discard
  w partii na żywo, Wrap in Flames u bota, Dire Fleet Ravager ETB,
  Wishful/Hippocamp/Mesmerist) — transkrypty w `tools/table-tester/audyt-m159/`.
- Backlog (nie kolejka): wyceny REVIEWED z L28; Gianty w katalogu
  (Invasion III czeka na pierwszego Olbrzyma).
