# Plan kontynuacji projektu — sesja 2026-09-23b (batch 58: siedem ostatnich kart arkusza właściciela)

## 0. Czytanie obowiązkowe (jak zawsze, AGENTS.md §0)

1. `AGENTS.md` (zasady stałe) — punkt 4 zawiera listy „Czego NIE czytasz na start”;
2. `docs/decisions/` — **29 ADR-ów** (0008 w archiwum od 2026-09-01);
3. `docs/LESSONS.md` (rejestr reguł) + `docs/LESSONS_PRZYPADKI.md` (narracje) + `docs/LESSONS_ARCHIWUM.md`;
4. `docs/setup/ENVIRONMENT.md` (sandbox, git, GitHub CLI).

## 1. Stan projektu

- **Bramy po tym batchu:** `npm test` (szybki rdzeń) **6215/6215**,
  pełna brama `node tools/run-tests.mjs all` **6225/6225** (0 fail),
  build **59 modułów / 4087,3 kB**.
- **PR #134** (gałąź `arena/01a0ceb4-mtg`) — OPEN; doszły commity batcha 58:
  `d2743f0` (B0a plan), `6acec6f` (B0b snapshoty), `a36d648` (B1 Boulder Salvo),
  `054753c` (B2 Grazing Gladehart), `13cba9f` (B3 Polluted Dead),
  `6b1e331` (B4 Scroll of Avacyn), `74f2025` (B5 Resurrected Cultist),
  `c52788c` (B6 Prishe's Wanderings), `507def6` (B7 Gond Gate),
  + domknięcie B8 (ten dokument, M420, README, handoff).
- **Katalog: 550 wpisów = 499 `supported` + 43 `token` + 8 `back`**, karty
  z `artId` **502**; arkusz kolekcji **502 wiersze / 499 nazw** — **każda
  pozycja arkusza właściciela ma odpowiednik w katalogu** (luka 7 kart
  z raportu M417/M419 zamknięta).
- **Nowe mechaniki silnika (M420):** surge na ścieżce czarów (B1), landfall
  „możesz” (B2), trigger śmierci celujący w ląd (B3), warunek „kontrolujesz
  stwora o podtypie” (B4), bramka `delirium` + powrót SIEBIE z grobu
  z licznikiem finality (B5), kwalifikator szukania `anyOf` + refleksyjny cel
  po przeszukaniu (B6), statyk `entersUntapped` (efekt zastępczy wejścia,
  CR 614.1d) + deskryptor `colorsFrom` („kolory, jakie może dać kontrolowana
  Brama”, CR 106.1) — B7.
- **Strażnik M193 przebudowany świadomie** (nie osłabiony): linia Oracle
  „…could produce” jest warunkowa (kolory zależą od pola bitwy), więc
  bezstanowa sonda jej nie porównuje z deskryptorem — ale wymaga
  `colorsFrom` w danych karty.
- **Seedy F3 bez reseedu** (żadna para fixture nie dotyczy zmienionych talii);
  przelosowany jeden seed E4 (`test/library-manipulation-modal.test.js`,
  11 → 35) po zmianie składu `decks/forgotten-realms.txt` — hunter po 60 seedach,
  konwencja L25. Golden-master bez regeneracji (żadna para fixture nie zawiera
  zmienionej talii).

## 2. Otwarte punkty dla właściciela (bez zmian z poprzedniej sesji)

- Pętla jakości B1 (Żywy Tester) i B2 (łowy CR) — do uruchomienia na komendę.
- Ograniczenia Z-1/Z-2 (moc efektywna vs `grantedPower` w trzech starszych
  miejscach) — opisane w `docs/setup/HANDOFF_2026-09-23.md` §6.
- Bench `--full` wyłącznie na komendę właściciela (ADR 0018).

## 3. Ryzyka / watchlisty

- **M179 (B1)** i **M255 (C1)** — zobowiązania z wcześniejszych sesji
  (nie ruszać bez pomiaru).
- **Surge** — nie reroutować Jwar Isle Avenger przez `CAST_SPELL_OPTIONS`
  (świadoma separacja: permanent vs czar).
- **`MANA_SOURCE_MAP`** — nie wolno cieniować kart z deskryptorem
  (`test/audyt-treasure-bez-id.test.js`, M200/N1).
- **`getSourceForObject`** — każda nowa ścieżka bez stanu (`state = null`)
  widzi wyłącznie produkcję bezwarunkową; kolory warunkowe wymagają stanu
  i muszą mieć fallback zgodny ze strażnikiem M193.

## 4. Artefakty sesji

- Plan: `docs/plans/PLAN_2026-09-23b-batch58-kolekcja-219-530.md` (odhaczone
  etapy B0a–B8 + zmierzony stan wyjścia);
- Milestone: **M420** w `docs/ENGINE_MILESTONES.md` i `docs/PROJECT_HISTORY.md`;
- Snapshoty Scryfall: `docs/cards/scryfall-{prishes-wanderings,boulder-salvo,gond-gate,scroll-of-avacyn,resurrected-cultist,polluted-dead,grazing-gladehart}.json`;
- PR: https://github.com/jurekjurekgh/mtg/pull/134 (base `main`, head
  `arena/01a0ceb4-mtg`).
