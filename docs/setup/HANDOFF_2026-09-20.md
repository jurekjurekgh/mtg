# Handoff 2026-09-20 — paka uwag z gry A–E (PR #130, domknięta)

PR: [#130](https://github.com/jurekjurekgh/mtg/pull/130) (scala właściciel,
ADR 0007/0020) · gałąź `arena/01a0b8fe-mtg` · tip pkga E: `7cdcc3d`.
Plan: [`docs/plans/PLAN_2026-09-20-uwagi-z-gry-a-e.md`](../plans/PLAN_2026-09-20-uwagi-z-gry-a-e.md)
(tabela stanu paczek A–E z commitami, strażnikami i bramami).
Poprzednia sesja: [`docs/setup/HANDOFF_2026-09-19b.md`](HANDOFF_2026-09-19b.md).

Zlecenie właściciela (2026-09-20, po batchu 57): pięć zgłoszeń z partii,
„napraw jeszcze w tym PR", praca paczkami, commit i push po KAŻDEJ paczce.

## Co weszło (5 paczek, każda: pin RED → fix → zielony `npm test`)

- **A `633178f`** — Time to Feed wrócił do planu „Wiedźmin": dane proweniencji
  (`tools/collection-art-ids.csv`) miały plan przepisany z nazwy setu (THS),
  a nie z arkusza kolekcji (ADR 0029). Talie: `theros` 26/9/17,
  `wiedzmin-bg` 26/9/17 (Forest 4), README z pomiaru M203/7.
  Pin: `test/zgloszenie-a-time-to-feed-plan.test.js`.
- **B `a92f982`** — bot zna WŁASNĄ talię (`ownDeck` z sesji i benchmarku)
  i nie aktywuje landcyclingu, gdy w bibliotece nie może już być celu (dolna
  granica: kopie w talii − kopie widoczne poza biblioteką, 0 → `finish(-12)`);
  typecycling i basic landcycling. Brak `ownDeck` = zachowanie sprzed zmiany.
  Pin: `test/zgloszenie-b-landcycling-bez-celu.test.js` (4).
- **C `998afc8`** — „Log partii": select wszystkich tur + „cała partia"
  (domyślnie, na bieżąco), przyciski „Kopiuj wybraną turę"/„Kopiuj całą partię",
  chronologia z najnowszymi na DOLE; zakresy liczy sesja (`logTextAll`/
  `logTextFor`). Pin: `test/zgloszenie-c-log-partii-tury.test.js`.
- **D `b9a22ce`** — bezkolorowe karty z pipami kosztów zdolności idą na stronę,
  która może je zapłacić (`abilityCostColorsOf`; Simian Simulacrum →
  `decks/dominaria-brg.txt`). Pełne wejście pipów do funkcji celu podziału
  zmierzone i odrzucone (46 czerwonych testów) — maski talii bez zmian.
  Pin: `test/zgloszenie-d-pipy-zdolnosci-podzial.test.js` (4/4).
- **E `7cdcc3d`** — wycena `declare_attackers` karze oddanie gardy
  (`crackbackPenalty: 12`), wtedy pomija premię za wyścig i zna wymuszone bloki
  przy ataku letalnym (`forcedBlockLoss`). Pin:
  `test/zgloszenie-e-oddana-garda.test.js` (6/6; RED 5/1).

Dokumentacja: `docs/ENGINE_MILESTONES.md` (M394), `docs/PROJECT_HISTORY.md`
(sekcja 2026-09-20), lekcja **L155** (+ narracja w `docs/LESSONS_PRZYPADKI.md`),
nota środowiskowa w `docs/setup/ENVIRONMENT.md` §2.

## Bramy (stan na koniec sesji)

| Brama | Wynik |
|---|---|
| `npm test` | **5997/5997**, 0 fail |
| `npm run build` | `dist/mtg-table.html`, 64 moduły / **3970,2 kB** |
| benchmark `--quick` | 672 mecze, heuristic **85,9%** (przed zmianą 86,0% — szum), aggro 26,8%, random 1,5% |
| snapshot wycen bota | `test/fixtures/bot-scoring-snapshot.json`, `overallHash` **4514c1cf65d99082…** (bez zmian po E) |
| budżet lektury startowej | 99 923 / 100 000 tokenów (zapas 77 — NOWY WPIS PŁACI SIĘ SKRÓCENIEM, progu nie podnosimy) |

## Pułapka tej sesji (zmierzona, opisana w ENVIRONMENT §2)

Środowisko odtworzyło workspace ze świeżego klona (reflog: `clone: from …`)
i cofnęło gałąź do `main` — pliki zostały, historia nie. Odtworzenie:
`git fetch --depth=1 origin arena/01a0b8fe-mtg` → `git reset --mixed FETCH_HEAD`
(NIGDY `--hard`, gdy w drzewie jest niecommitowana praca) → `git status` pokazał
dokładnie pkgi D + E, które zostały odtworzone jako commity i wypchnięte.
Uwaga: `git ls-remote`/`git fetch` działały anonimowo MIMO wygasłego `GH_TOKEN`.

## Dopisane po paczce F–I (drugie zgłoszenia z gry, 2026-09-20)

| Paczka | Commit | Strażnik (RED → GREEN) |
| --- | --- | --- |
| F — „Kreator talii” wyłączony z aplikacji | `4355c51` | `test/zgloszenie-f-kreator-talii-wylaczony.test.js` 0/3 → 3/3 |
| G — kreator many prowadzi płatność (4 lądy → 2) | `de28378` | `test/zgloszenie-g-kreator-many-brakujacy-kolor.test.js` 0/4 → 4/4 + `test/table-ui.test.js` (RED 3 tapnięcia, po: 2) |
| H — Explore nazywa i podlinkowuje odsłoniętą kartę | `eea273a` | `test/zgloszenie-h-explore-nazwa-karty.test.js` 0/3 → 3/3 |
| I — discover bez trafienia ma wpis w logu i „Rozgrywce” | `9baaf89` | `test/zgloszenie-i-discover-brak-trafienia.test.js` 0/4 → 4/4 |

Bramy po paczce: `node tools/run-tests.mjs all` **6022/6022**, `npm run build`
**59 modułów / 3948,2 kB** (przed F: 64 / 3970,2 kB), benchmark `--quick`
672 mecze: heuristic **85,9%**. Budżet lektury startowej **99 929 / 100 000**
(zapas 71 tokenów — kolejny wpis w rejestrze znowu płaci się skróceniem:
narracja idzie do `docs/LESSONS_PRZYPADKI.md`, tam budżetu nie ma).
Plan paczki: `docs/plans/PLAN_2026-09-20-uwagi-z-gry-f-i.md`.

**UWAGA (stan pushu):** cztery commity naprawcze F–I (`4355c51`, `de28378`,
`eea273a`, `9baaf89`) są WYPCHANE na `arena/01a0b8fe-mtg`. Commit dokumentacji
paczki F–I (`3130e04`) powstał LOKALNIE — w trakcie sesji wygasł token GitHub
w sandboxie (`GH_TOKEN` nieważny: `git push`/`git ls-remote`/`gh` odmawiają
autoryzacji, brak `~/.git-credentials`). Po ponownym połączeniu GitHub w Arenie
wystarczy `git push origin arena/01a0b8fe-mtg` (commit jest w historii, nic nie
trzeba odtwarzać) oraz aktualizacja opisu PR #130 plikiem
`/home/user/pr-body-130.md` (`gh api -X PATCH repos/jurekjurekgh/mtg/pulls/130
-F body=@/home/user/pr-body-130.md`).

## Kolejka i stan dla następnej sesji

1. PR #130 czeka na **decyzję właściciela** (scala właściciel — ADR 0007/0020;
   nigdy nie scalamy sami).
2. Po scaleniu: nowa sesja robi audyt scalonego PR (ADR 0020 B) i wraca do
   pętli domyślnej (ADR 0021), albo czeka na nowe zgłoszenie z gry / karty
   z arkusza kolekcji (ADR 0029 — pełny Oracle per karta, ADR 0022).
3. Otwarte, świadomie NIE naprawiane (dane poza zakresem zgłoszeń D):
   pipy zdolności w kartach KOLOROWYCH oraz pary bez podziału — final-fantasy
   (balamb UG), kaladesh (merchants-dockhand, welder-automaton), srodziemie
   (secluded-steppe), worek-dziki (immersturm-skullcairn).
