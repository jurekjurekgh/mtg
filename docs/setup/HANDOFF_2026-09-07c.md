# Handoff 2026-09-07c — PR #103 domknięty jakościowo: M336–M338 + pierwszy dokończony pełny B0

Ta sama sesja co `HANDOFF_2026-09-07b.md` (branch `arena/01a078a2-mtg`, PR
**#103** nadal otwarty — własnego PR nie scalamy). Ten plik jest punktem
startu NASTĘPNEJ sesji.

## 1. Co weszło po 07b (4 commity, wszystkie wypchnięte)

| Commit | Znalezisko | Strażnik |
|---|---|---|
| `eae8f56` **M336** (F14) | `resolve_proliferate` bez wyceny u bota; KOREKTA własnego ustalenia: `subsets()` wystawia PUSTKĘ jako pierwszą opcję (komentarz silnika „pierwsza oferta = WSZYSTKO" kłamał — przepisany), więc bot bez `case` brał pustkę ZAWSZE (przepuszczona wygrana trucizną). Fix: `case` z sumą delt liczników + `POISON_LOSS_LIMIT` eksportowany ze `state-based.js` jako JEDNO źródło progu (SBA i bot go czytają) | `test/m336-bot-proliferate.test.js` (8/8, dowody na deltach) + 2/3 mutacje |
| `3bb1970` **M337** | pełny B0 padał na 3200/5700: oferta `turn_cloak_face_up` przy cudzej otwartej decyzji (aggro mirrodin-wu vs random ravnica, seed 1001, tura 22) przy 64 bramkach `resolve_*` w `execute` — trzy ręczne kopie tej samej reguły, dwie się rozjechały. Fix: jeden predykat `optionalActionsOpen` (pętle face-up ORAZ bramka pasa; lista ~54 warunków SKASOWANA) | `test/m337-akcje-opcjonalne-blokada.test.js` (5/5; E = skan źródła: 2 predykaty, zero `if (hasPriority) {`, zero `!state.pending*` w pasie) + mutacje M1/M2 |
| `2801689` **M338** (F13) | HELP benchmarku kłamał o domyślnych (budżet „10 000", szybki „4 seedy", „22 talie → 6", watchdog dla szybkiego) — nic go nie liczyło. Fix: HELP i `DEFAULT_*` eksportowane, ADR 0025 z dateowaną „Aktualizacją liczb" (decyzja bez zmian) | `test/m338-pomoc-benchmarku-liczby.test.js` (5/5; HELP vs kod i vs `resolveMatrixShape` na realnym katalogu; 2 mutacje zabite) |
| `8906574` **L140** + kondensacja | rejestr LESSONS: L140 (ręczne listy = kopie reguły z datą ważności), L131 z dodatkiem M336 w archiwum; proza L131/L124/L136 → `LESSONS_PRZYPADKI.md`, bo budżet lektury startowej nie wytrzymywał M338 | `dokumentacja-budzet-lektury` 99 946/100 000 |

## 2. CO ZAMKNIĘTE (nie ruszać bez nowego faktu)

- **Pełny B0 DOGRANY PO RAZ PIERWSZY**: `tools/b1-final-2026-09-07.{txt,json}`
  — 5 700 meczów (19 talii, 190 par × 5 seedów × 2 strony × 3 pary botów),
  17,4 min, ~183,5 ms/mecz, 0 zacinek watchdoga. Macierz: heuristic 82,6%
  (3138/3800), aggro 59,2%, random 8,2%; heuristic-vs-aggro 70,5/29,4,
  heuristic-vs-random 94,6%. Bazą był b1-final 2026-08-29 (heuristic 80,8% na
  innej objętości próbki — JEDYNIE kierunek: +1,8 pp po naprawach F11/F14).
  Polecenie właściciela na `--full` WYKORZYSTANE — następna sesja NIE odpala
  go „przy okazji" (ADR 0018).
- **„niedokończone: 1" w raporcie to NIE usterka**: `heuristic(tarkir-wur) p1
  vs aggro(dominaria-wu)` seed 1003 tura 23 — obaj gracze tracą życie w tym
  samym rozdaniu → remis (CR 104.3a), harness liczy wynik bez zwycięzcy jako
  unfinished. Nie gonić tego jak buga; zmierzone i opisane w audycie §8 i ADR
  0025 (§Aktualizacja).
- **F0–F14 z pętli audytu PR #102 zamknięte** (F13→M338, F14→M336; reszta
  wcześniej). Rejestr finding→commit: §4 + §7 + §8 audytu
  `docs/audits/AUDYT_PR102_2026-09-06.md`.
- **Rodzina „akcje opcjonalne"**: bramkowanie predykatem jest JEDNĄ
  instytucją (pasa + face-up); execute nietknięty. Ewentualne nowe akcje
  opcjonalne (np. skok ziemi w reakcji na coś — hipotetycznie) muszą iść tym
  samym predykatem, nie obok niego — to jest przedmiot L140 i strażnika E.
- `test/m293-...` equality-pin **62** — zmiana liczby `fertile_thicket` w 8
  plikach RED-uje świadomie; procedura przy czerwieni: aktualizacja pinu +
  §18.5 audytu PR92 + MILESTONES + backlog (dług nadal NIESPŁACONY: renama
  `resolve_fertile_thicket` wymaga migracji autosave/replay — decyzja
  właściciela, nie sesji).

## 3. CO OTWARTE (w kolejności)

1. **PR #103 do scalenia przez właściciela.** Ciało: `gh api -X PATCH
   repos/jurekjurekgh/mtg/pulls/103 -F body=@plik` (`gh pr edit` zepsute);
   plik treści `scratch/pr103.md` poza repo.
2. **Budżet lektury startowej 99 946/100 000** — KAŻDY nowy wpis L141+
   wymaga kondensacji (jednomestrowy wpis rejestru + proza do PRZYPADKI;
   wynoszenie prozy MOŻE DODAĆ tokeny — mierzyć po zapisie, nie planować na
   oko). Strażnik: `node --test test/dokumentacja-budzet-lektury.test.js`.
3. **F12-bis? nie — zamiast tego:** tester UI nie ma jeszcze osłonek na
   nową asymetrię M337 przetestowaną tylko w silniku (brak przycisku obrotu
   przy wiszącej decyzji = OK, udokumentowane w `TESTER_STOLU.md`); jeśli
   następna sesja szuka żniwa na żywym stole, to jest kandydat do
   przejścia z talii celowanej (maski + ward + trigger naraz), wedle §
   „Partia celowana".
4. Katalog kart rośnie TYLKO z listy właściciela (ADR 0029/L134) — żadnego
   „dodam kartę, żeby przetestować mechanikę" na stałe; talia celowana jest
   chwilowa i kasowana PRZED bramką (M178/M203).

## 4. Pułapki tej części projektu (krótko, reszta w LESSONS/ENVIRONMENT)

- Golden-master: `--write` TYLKO na skończonym drzewie, atrybucja w commicie
  (test per-partia lokalizuje różnicę: „decyzje X vs Y, scoreSum …" — nie
  trzeba zgadywać); deryf M337-type (oferta usunięta) NIE zmienia scoreSum —
  zmienia hash przez wpis `options` w śladzie.
- Rejestr typów ofert bota: grep `case '…':` daje fałszywych pozytywnych
  (gałęzie `tieProjection`/`summarize` NICZEGO nie wyceniają); `probe-unpriced`
  liczył obserwacje w meczach — tak robić..
- Bot API: `createHeuristicBot({seed, registry, opponentDeck})` (OBIEKT),
  `createAggroBot(seed)` bez opcji; `runSimulation({state, controllers,
  maxCommands})` — wzorzec powtórki meczu w `scratch/` (repro-aggro-trigger).
- Import `tools/benchmark.mjs` z katalogu repo jest OK (decks/ względem cwd);
  sonda poza repo musi `cd /home/user/mtg` przed `node`.
- Skrypt mutacji: PRZED następną mutacją restor KOPIĄ (`cp bak/plik`), potem
  `node --check` + `git diff --stat`; `git add -A` — nigdy (zamiata cudze).
- Python splice po polsku: asercja `count(klucz)==1` PRZED zapisem; złamana
  linia komentarza bez `//` = składniowy zgon pliku (test = `# tests 1`).

## 5. Bramka końcowa tej tury

`npm test` **4636/4636** (baza 4618 + 8 M336 + 5 M337 + 5 M338),
`node --test test/bot-benchmark.test.js` **10/10** (progi regresji nietknięte),
`npm run build` 59 modułów / **3369,6 kB**, `node tools/family-audit.mjs`
czysto, `--quick` bez regeneracji progów; transkryptów testera w repo brak
(decyzja właściciela), `decks/` nietknięte.
