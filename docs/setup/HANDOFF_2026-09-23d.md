# Plan kontynuacji projektu — sesja 2026-09-23d (follow-upy: F1 v2 cloak + audyt H)

## 0. Czytanie obowiązkowe (jak zawsze, AGENTS.md §0)

1. `AGENTS.md` (zasady stałe);
2. `docs/decisions/` — **29 ADR-ów** (0008 w archiwum od 2026-09-01);
3. `docs/LESSONS.md` (rejestr reguł) + `docs/LESSONS_PRZYPADKI.md` + `docs/LESSONS_ARCHIWUM.md`;
4. `docs/setup/ENVIRONMENT.md` (sandbox, git, GitHub CLI).

## 1. Stan projektu

- **Bramy po sesji:** `npm test` (szybki rdzeń) **6304/6304**, pełna brama
  `node tools/run-tests.mjs all` **6314/6314** (0 fail, ~416 s), build **59 modułów / 4124,0 kB**.
- **PR #134** (gałąź `arena/01a0ceb4-mtg`) — OPEN, base `main`. Nowe commity tej
  sesji: `eada4cb` (F1 v2 — cloak zawsze „may”, chyba że biblioteka < 10 kart),
  `62ea637` (H — koszt odsłonięcia czytany ze stanu + piny pokrycia i wspólnych
  formaterów), plus commit domknięcia (ten dokument, M422, README, plan).
- **Zakres sesji:** dwa follow-upy właściciela po raporcie z sesji 2026-09-23c:
  1. **F1 v2** — bot grający Veiled Ascension ma ZAWSZE wybierać „may”
     (cloak w upkeepie), chyba że biblioteka jest blisko wyczerpania (< 10 kart);
  2. **audyt H** — sprawdzenie innych czarów/instantów/sorcery/aur/zdolności/
     wyborów pod kątem pokazywania kosztu: odpowiedź liczbowa + dowód, że idzie
     to jedną wspólną funkcją ofert.
- **Nowe pokrętła strategii:** `cloakLibraryFloor` (10), `cloakThinLibraryPenalty` (60).
- **Pliki strażników sesji:** `test/uwaga-z-gry-2026-09-23d-veiled-ascension-cloak-may.test.js`
  (6/6), `test/audyt-h-koszt-w-ofertach-pelny.test.js` (8/8).
- Katalog/decki bez zmian: **550 wpisów = 499 `supported` + 43 `token` + 8 `back`**.

## 2. Otwarte punkty dla właściciela

- PR #134 czeka na przegląd/merge (agent nigdy nie merge'uje — ADR 0020).
- Audyt H: liczby i wnioski w `docs/plans/PLAN_2026-09-23d-f1v2-cloak-i-audyt-h.md`
  (sekcja E2 — tabela pomiarów). Jeśli właściciel chce zejść poziom głębiej,
  naturalny kolejny krok to przegląd etykiet **wyborów** per karta (warstwa
  ofert i bramka opłacalności silnika są już pinowane).
- Pętla jakości B1 (Żywy Tester) i B2 (łowy CR) — na komendę.
- Bench `--full` wyłącznie na komendzie właściciela (ADR 0018).

## 3. Ryzyka / watchlisty

- **F1 v2 (cloak)** — próg 10 dotyczy WYŁĄCZNIE efektów zakrywających kartę
  z biblioteki (`cloak`/`manifest`) w decyzji „you may”. Strażnik
  `libraryDrainTax` (margines 20, tapnięcia 30) zostaje osobną, ostrzejszą
  drabiną dla mill/dobrania — **nie mieszać progów** ani nie dopisywać `cloak`
  do `LIBRARY_DRAIN_EFFECTS` (kara thin-20 wchodziłaby wtedy przy 20 kartach,
  sprzecznie z regułą właściciela).
- **H (koszt obrotu twarzą do góry)** — etykiety `turn_cloak_face_up` /
  `turn_manifest_face_up` czytają koszt ze STANU (`uncoverCostOf` w render.js),
  bo `playerView` nie projektuje tych pól (FoW: koszt i prawo obrotu zna tylko
  właściciel zakrytej karty). Helper ma fallback na widok; pin H/4 pilnuje
  zachowania na PRAWDZIWYM widoku (nie implementacji).
- **H/1 (pokrycie typów)** — nowy typ komendy w `COMMAND_TYPES` wymaga gałęzi
  etykiety w `commandLabel` albo świadomego dopisania do listy „tylko protokół”
  w teście (z uzasadnieniem). Test czerwienieje celowo — to nie regresja.
- **F1 (kontrakt odmowy)** — odmowa „you may” to zwykły `pass_priority`
  (etykieta wariantu w śladzie); nie wracać do modala wariantów (uwaga F1).
- Watchlisty z sesji 2026-09-23c zostają w mocy (K `installPressActivation`,
  I próg 30, A pin lokalny `syn-flash-morbid`, M pin C53/C).

## 4. Artefakty sesji

- `docs/plans/PLAN_2026-09-23d-f1v2-cloak-i-audyt-h.md` — plan + zmierzone
  tabele audytu H (pokrycie 89/90, jedna funkcja, bramka 59 wywołań, 54 testy
  warstwy kosztów).
- `test/uwaga-z-gry-2026-09-23d-veiled-ascension-cloak-may.test.js` — F1 v2
  (granica 10/9, nadpisanie parametru, brak regresji drenażu, anty-over-fix).
- `test/audyt-h-koszt-w-ofertach-pelny.test.js` — audyt H (pokrycie typów,
  jedna funkcja formaterów, bramka opłacalności, koszt obrotu w przebiegu
  produkcyjnym, FoW).
- Zmienione pliki: `src/controllers/heuristic-params.js`,
  `src/controllers/heuristic-bot.js`, `src/table/render.js`.
- Wpisy: **M422** w `docs/PROJECT_HISTORY.md` i `docs/ENGINE_MILESTONES.md`.
