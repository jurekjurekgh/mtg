# HANDOFF 2026-08-22 — M187 (audyt PR #69) + M188 (uwagi właściciela A/B/C/K)

## Stan

- **PR #70** (`arena/01a02a5f-mtg`), OTWARTY, CI **zielone** (`test:all`).
  Commity: `3ac67de` (plan), `413a0ac` (N1), `429242d` (audyt + N2),
  `3b702b5` (Z1), `97502e6` (rozpoznanie A–K), `a8a0744` (A), `889cd00` (B),
  `b0ca2d5` (C), `b8a8b49` (K), `57eb689` (dokumentacja + L55).
- `npm test` (szybki rdzeń) **2772/2772** · build **52 moduły / 2400.5 kB** ·
  `node --test test/bot-benchmark.test.js` **9/9**.
- Szybki benchmark: **85.6%** (575/672) — heuristic 80.1% vs aggro
  (przed sesją 75.3%), 91.1% vs random.

## Co wykonano

### M187 — audyt PR #69 (`docs/audits/AUDYT_PR69_2026-08-22.md`)

Poprzedni PR: 262 pliki, 15 milestonów (M171–M186). Wynik POZYTYWNY,
trzy naprawy:

- **N1** — `cantBlock` niosło DWIE zasady (L14): efekt „can't block this
  turn" (wygasa) i wydrukowaną cechę tokenu (trwała). Cleanup kasował obie
  → Phyrexian Mite i Goblin Construct blokowały po przełomie tury.
  Naprawa: `cantBlockPrinted` + centralny `creatureCantBlock()` we
  wszystkich ścieżkach (oferta, walidacja, widok, render, fingerprint).
- **N2** — brak strażnika trzeciej gałęzi `counter_spell_unless_pays`
  (auto-kontra bez many); mutacja nie czerwieniła pakietu (L13).
- **Z1** — `finishPendingSpell` dopisuje zdarzenia do stanu I je zwraca;
  2 z 21 call-site'ów robiły dodatkowy `push` → dublowane linie logu.
  Naprawione + strażnik skanujący plik.

### M188 — uwagi właściciela z testów

- **A** — badge nadanych P/T (`grantedStatBonus` → `grantedPower`/
  `grantedToughness` w playerView → badge „+1/+0"). Dotyczy każdej statyki
  warunkowej, aury, equipmentu, anthemu.
- **B** — `collectTokenNames(registry)` uzupełnia mapę nazw sesji; koniec
  `token_squirrel` w logu. Strażnik: każdy z 29 tokenów ma nazwę.
- **C** — bot nie atakuje jałowo (`futileAttackers` wyłącza premię
  wyścigu). Bot po zmianie SILNIEJSZY.
- **K** — „Przebieg tur (dla AI)": `<select>` ze wszystkimi turami
  (`turnHistoryEntries()`, `turnHistoryTextFor(n)`), kopiowanie wybranej.

## Kolejka / obserwacje dla następnej sesji

- **Pętla jakości była przerwana w połowie** (właściciel wszedł ze
  zgłoszeniem): rozegrane 8 partii, transkrypty w
  `tools/table-tester/audyt-m187/`. Warto dokończyć przebiegi na taliach
  `forgotten-realms`/`zendikar`/`worek-*` (profile defensive/random/
  impatient) — pliki `g5.txt`, `g6.txt`, `g7.txt` powstały, ale nie były
  jeszcze przejrzane ręcznie.
- **U2 z audytu PR #68 nadal otwarte:** `epicCastOffers` na ścieżce EPIC
  nie filtruje `additionalCost` — sprawdzić przy pierwszym epic-czarze
  z kosztem dodatkowym w tej samej talii co spellslinger.
- **Nowe pole `cantBlockPrinted`**: każdy nowy konsument zakazu blokowania
  ma czytać `creatureCantBlock()`, nie surowe pole (L55).
- **Nowe pola widoku** `grantedPower`/`grantedToughness`: nie wliczaj do
  nich liczników ani pumpów — mają własne badge (podwójny bonus na kaflu).
- Reszta stałych pułapek: `docs/setup/ENVIRONMENT.md` (reset workspace,
  polskie znaki w `edit_file`, `gh pr edit` → obejście `gh api -X PATCH`).

## Nienegocjowalne (przypomnienie)

ADR 0020 (PR na starcie → audyt poprzedniego PR → inkrementalne commity),
ADR 0021 (brak tematu = pętla domyślna, nie pytanie o kolejkę),
ADR 0022 (pełny Oracle albo `unsupported`), ADR 0018 (pełne B0 tylko na
komendę właściciela), ADR 0002 (zero przypadków po nazwie karty w core).
