# Handoff 2026-08-12 — M79: uwagi A/B + audyt PR #44 (PR #45)

## START TUTAJ

1. Przeczytaj `AGENTS.md`, `docs/PROJECT_STATE.md` (M79), ten plik.
2. `npm test` — oczekuj **1413** zielonych.
3. `npm run build` — 50 modułów / ~1531 kB.
4. Sesja = gałąź `arena/019ff6fd-mtg` = PR #45. Nie merguj. Nie zmieniaj gałęzi.

## Stan po sesji

- **Kod:** uwagi A/B z telefonu + audyt Batch 30 (X czarne, Epic cele,
  `enteredOnTurn`, conflict markers w PROJECT_STATE).
- **PR:** https://github.com/jurekjurekgh/mtg/pull/45
- **Bot bez zmian** — pełne B0 niewymagane. Progi 0.78/0.57, ostatni pomiar
  z #44: heuristic 79.4% ogółem, 0 crashy B0 2160.

## Co naprawiono

- **A.** Puste „Faza: …” w „Ruch przeciwnika” — nagłówek tylko przy akcji.
- **B1.** Pełny raport walki w modalu (bloki, obrażenia stwór–stwór,
  trucizna infect, śmierci, triggery) — nie tylko niezablokowany atak.
  `sourceCardId` + `combat: true` na wszystkich combat `damage_dealt`.
- **B2.** Fullscreen z wizardu ataku/bloku nie chowa `choice-request`.
- **Consume Spirit:** X tylko czarną maną (`xCost.black`).
- **Epic Experiment:** cele (i tryby) w ofercie i na stosie — bez fizzle.
- **Crew Captain:** `enteredOnTurn`, nie summoning sickness.

## Kolejka (po M79)

- Weryfikacja na telefonie (Pages, po merge): puste fazy, wynik walki
  przy niezablokowanym ataku, powrót z fullscreenu do wizardu, Consume
  Spirit X, Epic z celem, Crew Captain po kradzieży.
- Batch 31 — czeka na listę właściciela (ADR 0010: Scryfall z `set=`,
  `fetch_page`).
- Phone verify / platinum hunt / B2-w2 lookahead OFF / tester roadmap.

## Pułapki

- `edit_file` psuje polskie znaki → **python3** Path.read_text/write_text.
- Sandbox potrafi cofnąć HEAD do main — `git fetch && git rebase FETCH_HEAD`.
- Nie commituj bez `npm test`. Nie odpalaj testera bez `npm run build`.
- `PlayerView.pendingClash.cards` = **cardId**. Escape cost z **registry**.
