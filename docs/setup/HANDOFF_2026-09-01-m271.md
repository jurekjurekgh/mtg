# Handoff M271 — odznaka ZŁOTA (2026-09-01)

## Stan
- Gałąź `arena/01a058db-mtg`, **PR #91 OPEN** (base `main`, do scalenia przez
  właściciela: `Squash and merge`).
- `npm test` **4011/4011**, `npm run test:all` **4022/4022** (w tym benchmark
  botów 10/10), build **57 modułów / 3016.3 kB**. Drzewo czyste, wypchnięte.

## Co zrobiono
Trzecia odznaka tej sesji: **złota** (5 unikalnych błędów CR, #11–#15, plus
bonusowy #16). Metoda **L107** — rodziny ścieżek duplikujących wspólny helper.
Szczegóły: `docs/ENGINE_MILESTONES.md`, sekcja M271.

| # | CR | Rzecz |
|---|-----|------|
| 11 | 400.3 + 110.2a | ukradziona aura do grobu ZŁODZIEJA |
| 12 | 122.1e | aura z finality ignorowała `deathZoneFor` |
| 13 | 608.2b | czar modalny bez celów rozstrzygał się |
| 14 | 118.9 | ścieżki modalne gubiły `exileInsteadOfGraveyard` |
| 15 | 701.5a + 118.9 | to samo przy kontrze (5 kopii kodu) |
| 16 | inwarianty | kilka aur na gospodarzu wywracało partię |

Commity: `b8308ed`, `b8794cd`, `3d97468`, `e66af88`, `bf95c91`, `2ce6764`.

## Dług architektoniczny spłacony
- `deathZoneFor` i nowy `spellExitZone` → `zones.js` (najniższa warstwa grafu).
- Nowy `mover.js`: rejestr choke pointu zmian stref dla warstw niższych —
  pozwala `attachments.js` używać `moveObjectDirectly` bez cyklu importów.
- Reguła „gdzie ląduje czar po zejściu ze stosu": **8 kopii → 1**.

## Nowa wiedza trwała
- **L109** — komentarz tłumaczący, czemu kod NIE używa wspólnej ścieżki
  („żeby uniknąć cyklu"), to znacznik błędu, nie dokumentacja.
- **L110** — usunięcie duplikatu odsłania błędy, które maskował; po takiej
  zmianie uruchamiaj `npm run test:all` (benchmark botów łapie to, czego
  `npm test` nie widzi), a regresji nie cofaj — szukaj, co kopia zasłaniała.

## Sugerowany następny krok
Odznaka **diamentowa** (kolejne 5) tą samą metodą — żyła L107 wciąż nie jest
wyczerpana; naturalny kolejny trop to pozostałe rodziny wielokrotnych
emiterów tego samego zdarzenia. Alternatywnie zmiana kierunku na Wirtualny
Stół. Odrzucone tropy tej sesji (S1–S8, kasowanie tokena, echo przy powrocie
na pole bitwy) opisane w historii sesji — nie wracać.
