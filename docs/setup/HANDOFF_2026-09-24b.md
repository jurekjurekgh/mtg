# Plan kontynuacji projektu — sesja 2026-09-24b (audyt PR #135 + strażnik numerów CR)

## 0. Czytanie obowiązkowe (jak zawsze, AGENTS.md §0)

1. `AGENTS.md` (zasady stałe);
2. `docs/decisions/` — ADR-y (stan: 0001–0030, 0008 w `archive/`);
3. `docs/LESSONS.md` (rejestr reguł; najnowsze **L164–L167**) + `docs/LESSONS_PRZYPADKI.md` + `docs/LESSONS_ARCHIWUM.md`;
4. `docs/setup/ENVIRONMENT.md` (sandbox, git, GitHub CLI; §2 odzyskiwanie po resecie).

## 1. Stan projektu

- **Bramy po sesji:** `npm test` **6462/6462**, 0 fail (~225 s); pełna brama
  `npm run test:all` — patrz sekcja 4 (uruchomiona na koniec); build
  **60 modułów / 4199,5 kB** (bez zmian — sesja nie ruszała `src/`).
- **PR #136** (gałąź `arena/01a0d408-mtg`) — OPEN, base `main` (`f9bc44b`).
  Sesja **nie ruszała `src/`**: wszystko to testy, narzędzia i dokumentacja.
- **Zakres:** plan sesji (commit `ee68f2f`) → audyt scalonego PR #135 według
  ADR 0020 B (`docs/audits/AUDYT_PR135_2026-09-24b.md`, commit `558a20a`) →
  naprawy znalezisk F-1/F-2/F-3 osobno: **C1** `39b1587`, **C2** `47ec68b`,
  **C3** `5fa8759` → pętla jakości (Etap D, raport §9).

## 2. Otwarte punkty dla właściciela

- PR #136 czeka na przegląd/merge (agent nigdy nie merge'uje — ADR 0020).
- **Nowe narzędzie do przyzwyczajenia:** `node tools/cr-numery.mjs` sprawdza
  offline, czy każdy cytat `CR <numer>` w `src/`+`test/`+`tools/` istnieje
  w tabeli zweryfikowanych numerów; po dodaniu nowego cytatu uruchom
  `node tools/cr-numery.mjs --zapisz --cr <świeży plik CR>` (pobranie: nagłówek
  `test/helpers/cr-numery-tabela.js`, ADR 0030 — mirror `nwgarne/mtg-data`).
  Nowy plik z numerami jako DANYMI (jak tabela) trzeba dopisać do `WYKLUCZONE`
  w narzędziu **i** do `POMIN` strażników 701/702 (uzasadnienie przy wpisie).
- **O-4** (CR 616.1 — kolejność efektów zastępczych) i **O-5** (`colorsFrom`)
  — nadal „KARTY SPOZA KATALOGU": implementować dopiero, gdy wejdzie karta,
  która je rozróżnia.
- Pełny bench `--full`/macierz B0 wyłącznie na komendę właściciela (ADR 0018).
- Decyzje Etapu F z sesji #135 pozostają zaakceptowane — nie wracać bez polecenia.

## 3. Ryzyka / watchlisty

- **Strażnik okna nadal jest heurystyką** (F-2): para „mechanika ↔ numer"
  wymaga wpisu w `test/cr-numery-mechanik-straznik.test.js`; sama nazwa w oknie
  ±N linii nie dowodzi pary. Nowe rozjazdy tej klasy dopisuj tam jako pary.
- **Tabela CR a wydania:** tabela jest przypięta do wydania i sha256
  (CR 2026-09-25). Przy kolejnym CR część numerów może się przenumerować
  (L164: lustro bywa o wydanie do tyłu) — po pobraniu świeżego pliku uruchom
  `node tools/cr-numery.mjs --cr <plik>` (tryb bez `--zapisz`), a potem `--zapisz`.
- **Znaczniki czasu (L166/L167):** bez zmian w `src/` w tej sesji; watchlisty
  z `HANDOFF_2026-09-24.md` §3 obowiązują dalej (nowe pole EOT → znacznik +
  cleanup + reset w `moveObjectDirectly`; nowe pole obiektu → `identity.js`;
  nowy klucz stanu → odcisk albo `STATE_FINGERPRINT_EXCLUSIONS` z powodem).
- **Żywy Tester mierzy `dist/`** — po każdej zmianie w `src/` najpierw
  `npm run build`; tymczasowa talia `decks/audyt-*.txt` łamie 4 strażniki talii.
- **Mutacje testowe na kopiach** (`cp`/`/tmp`), nie `git checkout` (L136);
  po każdym zielonym kroku commit + push (sandbox bywa resetowany).

## 4. Artefakty sesji

- Plan: `docs/plans/PLAN_2026-09-24b-audyt-pr135-i-petla-jakosci.md`.
- Audyt PR #135: `docs/audits/AUDYT_PR135_2026-09-24b.md` (§9 — pętla jakości).
- Narzędzie + dane: `tools/cr-numery.mjs`, `test/helpers/cr-numery-tabela.js`.
- Strażnik: `test/cr-numery-istnienie-straznik.test.js` (4 inwarianty);
  nowe pary klasy: `test/cr-numery-mechanik-straznik.test.js` (31 testów).
- Transkrypty Żywego Testera: poza repo (`/tmp`); 6 partii, 0 zgłoszeń detektorów.
- Wpisy: **M426** w `docs/PROJECT_HISTORY.md` i `docs/ENGINE_MILESTONES.md`.
