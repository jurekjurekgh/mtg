# HANDOFF — sesja arena/01a058db (M265), 2026-08-31

## Co to była za sesja

Tryb sesji ADR 0020 + pętla domyślna ADR 0021 (brak nazwanego tematu):
(A) PR na starcie, (B) szczegółowy audyt poprzedniego scalonego PR (#90),
(C) inkrementalne commity po każdym zielonym `npm test` + `npm run build`.

**PR sesji: #91** (base `main`), gałąź `arena/01a058db-mtg`, baza `4e18fed`.

## UWAGA na start następnej sesji

**Token GitHub wygasł pod koniec tej sesji.** Ostatni commit (`ec7780c`)
jest ZACOMMITOWANY LOKALNIE, ale NIE wypchnięty. Pierwsza czynność:
sprawdzić `gh auth status`, po reconnekcie wykonać
`git push origin arena/01a058db-mtg` (nigdy force) i zaktualizować opis PR #91.

## Etap 1 — audyt PR #90

Zakres `006fcb7..4e18fed` (26 plików, +1480/−148). Metoda: pełne czytanie
diffa, sondy odtwarzające zachowanie na żywej sesji (nie raport z testów),
9 mutacji RED→GREEN.

- **Zero znalezisk regułowych/silnikowych** — sześć decyzji PR #90
  (frontFaceId, enterAsCopy vs CR 712.9, sourceId w triggerach, M261,
  ward w playerView, chooseColor) odtworzono i potwierdzono.
- **Hipoteza `deferredTurnTail` OBALONA** trzema sondami (deck-out bota,
  deck-out gracza, śmierć z obrażeń na granicy tury). Komunikat końcowy
  zawsze trafia do logu i/lub ostatniego bloku modala. Nie wracać do niej.
- **Znalezisko #1 (L99):** mutacja M8 (usunięcie `e.sourceId` z bramki
  `hiddenLive`) przechodziła cały `fow-facedown-names` — testy pokrywały
  tylko TEKST, nie SKAN karty. Dopisany test end-to-end; kod bez zmian.

Pułapka metodyczna do zapamiętania: `state.zones.library` to TABLICA id
filtrowana po `state.objects.get(id).controllerId`, nie `Map`. Skrócenie
samej tablicy łamie `assertStateInvariants`.

## Etap 2 — pętla jakości (Żywy Tester)

14 partii na taliach spoza puli M264 (alara, srodziemie, worek-basni,
tarkir-wur, tarkir-bg, innistrad-wu, warhammer-wg, worek-legend,
final-fantasy, worek-mroczny, theros, mirrodin-wu), 5 profili gracza.
0 `[STOP]`, 0 `== LIMIT ==`, 2 zgłoszenia detektorów (oba realne).

| # | Objaw | Root cause | Strażnik | Lekcja |
|---|---|---|---|---|
| #2 | log „zapłacić {2}" vs przycisk „Zapłać {W}{B}" | `optional_pay_required` bez `payColors` | `m265-optional-pay-colored-cost` (5) | L100 |
| #3 | „Rzuć za warp: … (koszt ?)" | 4 deskryptory kosztów gubione w liście pól `playerView` (ręka + exile) | `m265-hand-view-alt-cost-descriptors` (6, KLASOWY) | L101 |
| #4 | bot tapuje własnego atakującego (Halo Forager) | brak `freeCastTargetPenalty` + wpis grobu bez `spell` | `m265-grave-free-cast-target` (4) | L102 |
| #5 | fałszywy no-op sondy (Soulbright Flamekin) | `abilityResolvedThisTurn` poza `stateFingerprint` | `m265-nth-resolve-fingerprint` (3) | L102 |

Znaleziska #2 i #3 pochodzą z RĘCZNEGO czytania transkryptów — detektory
ich nie widzą. #4 i #5 z detektorów, oba potwierdzone ponownym biegiem na
tym samym seedzie (`== DETEKTORY: brak zgłoszeń ==`).

## Commity tej sesji

| Commit | Co |
|---|---|
| `8ad1c7d` | plan sesji M265 |
| `c54691c` | raport audytu PR #90 + test regresyjny bramki FoW skanu (#1) |
| `d74a962` | kolorowy koszt dopłaty w opisie zdarzenia (#2) + L100 |
| `adaf403` | deskryptory kosztów alternatywnych w widoku (#3) + L101 |
| `ec7780c` | cel darmowego rzutu z grobu + `onNthResolve` w odcisku (#4, #5) + L102 — **NIEWYPCHNIĘTY** |
| (ten) | domknięcie: README, PROJECT_HISTORY, handoff |

## Wyniki

- `npm test` (szybki rdzeń): **3912/3912**; `npm run test:all`: **3922/3922**
  (~246 s); `npm run build`: **56 modułów / 2994,1 kB**.
- Benchmark B0 pełny NIE uruchamiany (ADR 0018/0025 — tylko na komendę
  właściciela).

## Stan końcowy / następny krok

1. Reconnect GitHub → push `ec7780c` + commit domykający, aktualizacja opisu PR #91.
2. Decyzja właściciela o scaleniu PR #90 i #91 (`Squash and merge`; autor nie scala).
3. Kolejna pętla jakości: pozostałe talie nieaudytowane
   (warhammer-ubr, srodziemie jako bot, worek-dziki, mirrodin-brg z nowymi
   profilami) albo domknięcie klas z L99–L102 detektorami:
   - detektor „etykieta zawiera `?` albo pusty nawias kosztu" (klasa L101),
   - detektor „ta sama komenda, dwie identyczne etykiety" (klasa M101/B),
   - skan rodzin komend dzielących funkcję wyceny (klasa L102).
