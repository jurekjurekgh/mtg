# HANDOFF 2026-08-20 — M156 (audyt PR #65 + pętla jakości)

## Stan

- **PR #66** (`arena/01a01eb3-mtg`, M156) — OTWARTY, gotowy do przeglądu
  właściciela. 6 commitów, wszystkie zielone i wypchnięte.
- `npm run test:all` **2451/2451** (rdzeń `npm test` 2442, slow 9),
  build **51 modułów / 2075.0 kB**, próbka regresji bota 9/9 (0 crashy).
- Rejestr: 308 wspieranych kart (bez tokenów/tyłów), 12 talii — bez zmian
  w tej sesji (ADR 0021: karty tylko z listy właściciela).

## Co zrobiono w M156

1. **Audyt PR #65** (ADR 0020 B) — raport
   `docs/audits/AUDYT_PR65_2026-08-20.md`. Zweryfikowano Batch 37/38,
   M148–M155 (scry topOrder, warp, rebound, satyr, static net, living weapon,
   creature_or_vehicle, craft no-op, FoW, oba boty). Znaleziska F1–F4 + D1.
2. **Fixy F1–F3** (RED→GREEN, `test/bot-pr65-audit-fixes.test.js`):
   F1 Lotusguard friendly-flag (grant_keywords), F2 Divine Offering wycena
   (3 tabele), F3 życie niezależne od destroy (CR 608.2c; test Batch 38
   odwrócony z uzasadnieniem — L44).
3. **D1 docs:** PROJECT_STATE backfill M147–M155 + wpis M156 (poprzednia
   sesja nie zostawiła wpisu po PR #65), README liczniki.
4. **Pętla jakości:** sonda inwentaryzacji efektów celowanych → Q1 Withstand
   (prewencja na wroga), Q2 Servant (liczniki do najsłabszego);
   `test/bot-quality-m156.test.js`. **Strażnik klasyfikacji celów triggerów**
   (`triggerEffectIsHostile` + `REVIEWED_NEUTRAL`,
   `test/bot-trigger-target-classification-guard.test.js`, weryfikacja
   mutacyjna). Lekcja **L51** (klasa L50 po raz 6. — teraz pilnuje strażnik).

## Otwarte decyzje właściciela (w opisie PR #66)

- **F4:** Weftblade Enhancer „each of up to two target creatures" = 1 cel
  (notes). Polityka M111: limitations (nowy powód w strażniku) albo pełne
  Oracle (wielocelowy trigger ETB).
- Ewentualnie: pełna macierz B0 na komendę (ADR 0018) — w sesji tylko próbka.

## Kolejka (po decyzji o merge PR #66)

- Pętla jakości wg ADR 0021: (a) audyt Żywym Testerem osi nieuwzględnionych
  w M151/M152/Batch38 (np. mulligan londyński + kreator many razem), albo
  (b) polowanie na CR ścieżkami innymi niż klasyfikacja celów (np. strefy
  zamiany, kopie/zmiany tekstu, interakcje aura↔equipment).
- Nowe batchy kart wyłącznie z listy właściciela w czacie (ADR 0021).

## Pułapki środowiska (nowe w tej sesji)

- `gh pr edit` → GraphQL Projects (classic) error; obejście:
  `gh api -X PATCH repos/jurekjurekgh/mtg/pulls/66 -F body=@plik`.
- Commit message z cudzysłowami/parantezami łamie bash inline — pisać
  do `/tmp/msg.txt` i `git commit -F` (ENVIRONMENT §3).
- Anomalia FS: `docs/audits/AUDYT_2026-08-20-batch38-zywy-tester.md` — bash
  `stat`/`cat` ENOENT, python3/git czytają normalnie. Artefakt sandboxa.
- Klona był `--depth 1` — do diffu poprzedniego PR: `git fetch --depth=2
  origin main` zanim policzy się `git diff <parent>..HEAD`.

## Pierwsze kroki następnej sesji

1. `git fetch origin main && git log --oneline -3` — czy PR #66 scalony.
2. Jeśli tak: nowa gałąź sesji, PR na start (ADR 0020 A), audyt PR #66
   ( Zacznij od raportu AUDYT_PR65 i strażnika klasyfikacji — sprawdź
   mutacyjnie, że działa).
3. `npm test` (~90 s) + `npm run build` — porównaj z PROJECT_STATE (M156).
4. Czytaj: AGENTS.md → ADR-y → LESSONS (L51 nowa) → ENVIRONMENT →
   PROJECT_STATE → ten handoff.
