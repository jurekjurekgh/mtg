# HANDOFF 2026-08-18 — M146 (Batch 35 kompletny)

## Stan

- **PR #64** (`arena/01a0169f-mtg`, M146) — OTWARTY, gotowy do przeglądu właściciela.
- Batch 35 **kompletny: 10/10 kart** (4 z M145/PR #63 + 6 z M146).
- `npm run test:all` **2303/2303**, build 51 modułów / 1953.8 kB.
- Benchmark szybki: heuristic **67.5%** vs aggro / **92.6%** vs random, 0 crashy
  (`tools/b17-m146-2026-08-18.txt`).
- Audyt najnowszego scalonego PR #63: `docs/audits/AUDYT_PR63_2026-08-18.md` (czysty).

## Co zrobiono w M146

1. **Audyt PR #63** (ADR 0020 B) — 4 karty reuse zgodne z Oracle, core nietknięty.
2. **Batch 35 E3 (5 kart):**
   - Trade Route Envoy — generyczny efekt `conditional` (if/then/else);
   - Twiddle — typ celu `artifact_or_creature_or_land` + modal tap/untap;
   - Steelfin Whale — affinity for artifacts + trigger `artifact_you_control_enters`;
   - Blazing Torch — `equipment.grantedAbilities` (restrykcja blokowania
     po podtypie + zdolność nadana nosicielowi z tapHost/sacrificeSelf);
   - Basilisk Gate — `pump_by_gates` (+X/+X wg liczby Gates).
3. **Batch 35 E3b (1 karta): Mindstab — suspend (CR 702.62)** z jednorazową
   decyzją: po zdjęciu ostatniego licznika zdolność wyzwalana na stosie,
   przy rozstrzyganiu gracz RZUCA za darmo albo zostawia kartę w exile na
   stałe (uwaga właściciela — bez trzymania na kolejne tury).
4. Root cause fixes: `grantedFromEquipment` po fladze komendy (equip nie
   koliduje z granted), pola suspend/grantedAbilities przechodzą cały łańcuch
   registry→materialize→addObject→identity (L21/L48), harness panelu zbiera
   ostatnie ruchy po końcu partii, PAUSE_TYPES zna trigger_target_required.
5. Talie: green/spellslinger/mechanicy/innistrad/black +1 karta każda;
   seedy 8 testów przelosowane hunterem (L25).

## Audyt Żywym Testerem (M146, 2026-08-19)

- 4 naprawy: Basilisk Gate na stwory wroga (wycena pump_by_gates), Twiddle
  odkręcający górę wroga (wycena untap_permanent + start -1 dla czarów
  tylko-utylitarnych), stats_modified „undefined/undefined" (opisy wariantów),
  nowy detektor detectBotUntapsMyPermanent.
- Raport: `docs/audits/AUDYT_2026-08-18-m146-zywy-tester.md`. Lekcja L50.
- Ważne dla następnej sesji: **nowy typ efektu w karcie = sprawdź wycenę
  w heuristic-bocie (ścieżki cast_spell i activate_ability)** — inaczej bot
  „głupieje" na tej jednej karcie, a testy engine są zielone.

## Pułapki środowiska (powtórka)

- Egress HTTPS zablokowany — dane kart tylko przez `fetch_page` (ADR 0010).
- `GH_TOKEN` potrafi wygasnąć — `git push` prosi wtedy o hasło; poproś o reconnect.
- `git checkout <plik>` cofa własne zmiany — commituj przed instrumentacją.
- Polskie znaki: edytuj przez `python3` + `pathlib`, nie przez `edit_file`.

## Kolejka (po decyzji właściciela o merge PR #64)

- Batch 36 czeka na listę właściciela (karty tylko z listy — ADR 0021).
- Pętla jakości (ADR 0021 pkt 4): audyt Żywym Testerem / polowanie na błędy CR.
- Z backlogu (nie kolejka): kontrczar we własny czar (etykieta ostrzegawcza),
  ochrona przed jakością dla efektów nieceowanych, ergonomia dotykowa
  wizarda scry/surveil, sprzątanie kontraktu addObject (tryb twardy).

## Pierwsze kroki następnej sesji

1. `git fetch origin main && git log --oneline -3` — czy PR #64 scalony.
2. Jeśli tak: nowa gałąź sesji, PR na start, audyt PR #64 (ADR 0020).
3. `npm test` (~3 min) + `npm run build` — porównaj z PROJECT_STATE.
