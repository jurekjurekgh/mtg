# Handoff 2026-08-09f — po M67 Batch 27 (10 kart)

## Stan po sesji 2026-08-09 (PR #39 `arena/019fe7ec-mtg` — OPEN do main)

- **Kod:** `npm test` **1213/1213**, build **50 modułów / 1336.1 kB**, **198 realnych kart
  + 1 token (199 supported)**, 9 talii singleton. **Pełne B0 (9 talii / 50 seedów /
  13 500 meczów) 0 crashy**: heuristic **63.1% vs aggro / 92.3% vs random** (progi
  0.78/0.57 utrzymane; por. 65.6%/91.7% po M66 — wahanie od dodania kart).
- **Zakres M67 (Batch 27, kolejka właściciela):**
  1. **Civilized Scholar // Homicidal Brute (ISD DFC)** — {T}: draw+discard; odrzucenie
     stwora → untap+transform (`draw_then_discard` + `onCreatureDiscard` w
     resolve_discard_choice); tył: end step bez ataku → tap+transform
     (`attackedThisTurn` + condition `didntAttackThisTurn`).
  2. **Battle-Rattle Shaman (M21)** — beginning_of_combat target +2/+0 (requiresTarget
     optional — opcja odmowy).
  3. **Jeskai Devotee (TDM)** — flurry (drugi czar) +1/+1; {1}: Add {U},{R},{W} once
     (`add_mana` z kolorami z efektu → jednostka WUR).
  4. **High Stride (BLB)** — +1/+3, reach, untap (reuse).
  5. **Inspiration (8ED)** — `draw_cards applyTo:'target'` (cel-gracz dobiera 2).
  6. **Minotaur Abomination (M14)** — vanilla 4/6.
  7. **Guildsworn Prowler (CLB)** — dies „wasn't blocking" → draw (`isBlockingThisCombat`
     + LKI `wasBlocking` w extra triggera dies + condition `notBlocking`).
  8. **Giant Spider (M19)** — reach (reuse).
  9. **Scroll Thief (M13)** — combat_damage_to_player → draw (reuse).
  10. **Force Away (KTK)** — bounce + ferocious „you may draw, if you do discard"
     (`pendingOptionalDraw` tak/nie + łańcuch draw+discard).
- **Talie:** spellslinger +5 (+landy 7I/8M), red +Battle-Rattle (16M), black
  +Minotaur/Guildsworn (13S), green +High Stride/Giant Spider (18F).
- **Commity:** plan (600e8f3) → Scryfall (0b77f4a) → engine (32ea771) → cards+decks
  (124fbd4) → testy+hunter (45a29b6) → docs (następny).

## Kolejka (po M67)

- **Batch 28** — czeka na listę właściciela (ADR 0010: Scryfall ZAWSZE z `set=`;
  api zablokowane → `fetch_page`).
- **B2-w2** — infra OFF (~4× wolniej); z włączonym lookahead bot zyskuje +5/+34 p.p.;
  decyzja o koszcie i ewaluacja na pełnym B0.
- **Talie singleton** — rosną z batchami (strażnik `repo-decks`).

## Pułapki (środowisko / projekt) — aktualne

- `edit_file` psuje PL → **python3 Path.read_text/write_text**, commit msg via /tmp.
- **Sandbox potrafi COFNĄĆ lokalny HEAD do main** (zdarzyło się 2× w tej sesji) — po
  każdym commicie `git push origin arena/...`; przy rozjeździe `git fetch && git reset
  --mixed FETCH_HEAD` (working tree zachowany).
- **Scryfall:** curl i node fetch NIE działają (SSL/sieć) — wyłącznie `fetch_page` na
  `api.scryfall.com/cards/named?exact=...&set=...` (zwraca pełny JSON w markdown).
- **Nowe w M67:** `draw_then_discard` ustawia `pendingDiscardChoice.onCreatureDiscard`
  (untap+transform po odrzuceniu STWORA — sprawdzać kind, nie cardId); transform jest
  IN-PLACE (id obiektu zostaje, cardId się zmienia — testy szukają po cardId);
  `attackedThisTurn`/`isBlockingThisCombat` czyszczone w cleanup; dies „wasn't blocking"
  czyta LKI z EXTRA triggera (fireDeathTriggers), nie z żywego obiektu; ferocious
  sprawdzany przy ROZSTRZYGANIU (czar na stosie); `add_mana` z `effect.colors`
  (jednostka WUR — manaUnitKey SORTUJE kolory!).
- Talia spellslinger ma teraz 24 nielandowe — hunter seeds w testach z talią
  green/red/black (table-session, bot-pausa, audit C2) przelosowane do 2/3/4/47.
- Pełne B0 (13500) ~16 min; quick `--seeds 4` (1080) ~90 s. Zmiana talii = hunter seeds.
- Lektura nowej sesji: AGENTS.md → PROJECT_STATE (M67) → HANDOFF_2026-08-09a…f →
  ENGINE_MILESTONES (M64-M67) → WORKFLOW → plany M65/M66/B27 → ADR 0002/0003/0005/0010/
  0011/0013/0014.

## Do weryfikacji w następnej sesji

- `npm test` 1213/1213, `npm run build` 50/1336.1 kB.
- Pełne B0 13500 / 0 crashy (63.1% vs aggro / 92.3% vs random).
- Testy iPada: nowe karty w taliach (spellslinger ma 5 nowych — Scholar transform,
  Force Away ferocious, Devotee mana).
- Scryfall Batch 28: ZAWSZE `set=` + `fetch_page` (api blokada).
