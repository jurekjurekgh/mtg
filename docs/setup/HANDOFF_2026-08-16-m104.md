# Handoff — M104: sonda „noop" w modalach i skan okna + wzorzec U9/A2 dla tapnięć (2026-08-16)

Gałąź: `arena/01a00b7e-mtg` · PR **#56** · Kontynuacja handoffu M103
(„następne kroki" 1–4) · Pakiet: **1923/1923** · Build: 51 modułów / 1725.2 kB

## START TUTAJ (pierwsze kroki nowej sesji)

```bash
npm test            # szybki rdzeń (~52 s) — ADR 0019
npm run test:all    # pełny pakiet, brama PR (~3,5 min) → 1923/1923
npm run build       # 51 modułów / ~1724 kB
```

Czytaj w kolejności: `AGENTS.md` → `docs/PROJECT_STATE.md` (sekcja M104) →
ten handoff → `docs/plans/2026-08-16-m104-noop-modale-i-liczniki.md` →
`docs/LESSONS.md` (nowe L20, L21) → `docs/setup/TESTER_STOLU.md` (oś 4).

## Co było do zrobienia (z handoffu M103)

1. Pełne B0 po zgłoszeniach A–D — **NIE zrobione świadomie** (ADR 0018:
   wyłącznie na wyraźną komendę właściciela).
2. Sonda `noop` na opcje MODALI — **zrobione** (i rozszerzone o skan okna).
3. Wzorzec U9/A2 dla „połóż licznik" i „tap target" — **zrobione**.
4. Domknięcie `ci.yml` — **zrobione** (patrz „Uwaga o pushu" niżej).

## Wynik

### E2 — sonda mierzy opcje modali

`renderChoiceRequest` nadaje każdemu przyciskowi opcji `data-option-key`
(`commandOptionKey`), więc mostek `window.__mtgDebug` (tylko `?tester=1`)
znajduje komendę w `legalCommands` bez zmian w protokole. Sterownik testera
ma jeden helper `clickProbed(button, label, source)` dla panelu i modala;
rekordy niosą `source: 'panel' | 'modal'`, a raport pokrycia rozbija sondy
(`sondy noop: 103 (panel 88, modal 15)`).

Bramka fałszywych alarmów: w modalu opcja REZYGNACJI („rezygnuję",
„nie płać", „bez celów", „Bez bloków", „nie znajduj karty") ma prawo nic
nie zmieniać — to legalny wybór gracza (you may / up to one target).
W panelu bramka nie działa, bo tam „nic nie rób" to pass.

### E3 — kolejne klasy ofert bez skutku (engine)

`keywordGrantIsNoOp` (M103/A2) rozrosło się w `abilityEffectIsNoOp`
w `src/engine/abilities.js` — tablicę predykatów po `effect.type`:

| Efekt | No-op, gdy… | Realna karta |
|---|---|---|
| `grant_keywords_until_end_of_turn` | cel ma już wszystkie keywordy | Bladed Sentinel, Stirring Bard (A2) |
| `untap_permanent` | cel jest nietapnięty | **Rustvine Cultivator** |
| `tap_permanent` | cel jest już tapnięty | (brak w katalogu — klasa generyczna) |
| `cant_block` / `cant_be_blocked` | cel ma już znacznik | **Coralhelm Guide** |
| `add_counter` | `amount <= 0` | (brak — liczniki kumulują się, CR 122.1b) |

Bramka działa we wszystkich czterech gałęziach enumeracji celów;
`execute` NADAL przyjmuje komendę (CR 602.2b — świadomy rozjazd oferty
i walidacji, jak U9). **Anty-over-fix:** `onNthResolve` (Soulbright Flamekin)
oraz koszt o własnej wartości (`sacrificeSelf`, `sacrificeLand`, `discardCard(s)`,
`exileFromGraveyard`) wyłączają bramkę — Panic Spellbomb („{T}, poświęć:
cel nie może blokować" + trigger „dies → dobierz za {R}") zostaje w ofercie.

Skan katalogu (nie transkryptów) potwierdził też klasę NIEISTNIEJĄCĄ:
„połóż licznik bez skutku" nie występuje — każdy licznik katalogu kumuluje
się, także `stun` (CR 122.1b).

### E4 — najcenniejsze znalezisko: pomiar był przypięty do kliknięcia

Weryfikacja mutacyjna (L13) bramki z E3 **najpierw nie zadziałała**: po
cofnięciu bramki panel pokazywał no-opy, a oś `noop` milczała. Dwie
przyczyny, obie w narzędziu:

1. sonda mierzyła wyłącznie ofertę KLIKNIĘTĄ (polityka gracza klikała co
   innego) → `scanOffers`: każda widoczna oferta sondowana raz na partię
   (dedupe po `commandOptionKey`, limit 600; sonda i tak działa na klonie,
   więc skan nie dotyka partii). Efekt: 30 → 51–103 sond na partię;
2. koszt „Remove a counter" wyglądał w dyfie jak SKUTEK i maskował no-opa
   (klasa błędu jak L18) → `costSignature.removeCounter` + `costCounterPaid`.

Po poprawkach mutacja daje 9 zgłoszeń („odkręć → cel: Fertile Thicket"),
a przywrócona bramka — ciszę. Macierz 8 partii (6 par talii × 4 profile,
oba tryby logowania): **0 zgłoszeń `noop`**. Lekcje: **L20**, **L21**.

### E4b — odrzucenia komend jako dane strukturalne (reguła M99)

`detectRuleSmells` widział „Ruch odrzucony" tylko w linii `LOG:` snapshotu:
ten sam przebieg dawał 0 zgłoszeń pod `--quiet` i 3 ze snapshotami.
Sterownik zbiera je teraz z DOM (`.log-rejection`) po każdym kliknięciu.

### E5 — `ci.yml` domknięty

`ci.yml` i `pages.yml` uruchamiają `node tools/run-tests.mjs all` (ADR 0019)
zamiast sekwencyjnego `node --test 'test/**/*.test.js'`; kopia w
`docs/setup/workflows/` zsynchronizowana, strażnik
`test/ci-workflow-tiers.test.js`.

## E7 — ptaszek wyciszenia: rozstrzygnięcie właściciela i PRAWDZIWA przyczyna

Macierz testera pokazała trzy odrzucenia komend, wszystkie tuż po zaznaczeniu
ptaszka wyciszenia. Wstępnie zgłosiłem to jako pytanie o semantykę ptaszka —
**właściciel rozstrzygnął (2026-08-16): semantyka jest poprawna** („od tego
momentu ta opcja nie przerywa auto-passu; odptaszkowanie przywraca
przerywanie"), więc przewijanie gry po zaznaczeniu ZOSTAJE.

Prawdziwym błędem było coś innego i węższego: `toggleIgnoredOption`
renderował panel **przed** `recheckAutoPass`, a po przewinięciu gry już nie.
Na ekranie zostawał panel z minionego okna:

- kolejne tapnięcie wysyłało komendę sprzed przewinięcia → „Ruch odrzucony:
  illegal_cast: Zagranie poza main phase" / „not_priority",
- ruchy bota rozegrane w tym przewinięciu nie trafiały do modala „Rozgrywka"
  aż do następnej akcji gracza (oś 2 audytu).

Fix (`src/table/main.js`): po `recheckAutoPass` kolejność jak w `playDirect` —
`autosave() → rerender() → showBotMoves()`. Test: `test/choice-ignore.test.js`
(strażnik kolejności). Weryfikacja żywa: ten sam przebieg, który dawał
3 odrzucenia (azorius vs black, seed 7, random, `--tick-rate 0.2`), daje
**0 zgłoszeń**; macierz 5 partii z `--tick-rate 0.3` — czysta.

Detektor nie chowa już takich odrzuceń w kategorii `ui`: wracają jako `rules`
(z dopiskiem „[tuż po ptaszku wyciszenia]" w dowodzie), żeby nawrót był
widoczny.

## Benchmark

Profil szybki (ADR 0018 — pełnej macierzy agent nie odpala):
1248 meczów, 148,4 s, **heuristic 58,2% vs aggro** (bez zmian wobec M103),
**92,1% vs random** (było 92,0%), 0 niedokończonych.
Wynik: `tools/b2-m104-2026-08-16.{json,txt}`; baseline M103 zostaje
w `tools/b1-final-2026-08-16.*`.
**Pełna macierz po M103 A–D i po M104 nadal NIE była liczona** —
`node tools/benchmark.mjs --full` wyłącznie na komendę właściciela.

## Pułapki tej sesji

- **Pomiar zależny od polityki gracza** (L20) — patrz E4. Przy każdym
  narzędziu audytowym pytaj, czy pomiar obejmuje całą przestrzeń, którą
  widzi gracz, czy tylko ścieżkę sterownika.
- **`addObject` ignoruje pola spoza kontraktu** (L21): `tapped`,
  `summoningSickness`, `counters` w wywołaniu fabryki przepadają bez słowa;
  dwa testy „odkręcania lądu" były przez to martwe (asercja sprawdzała stan
  początkowy). Stan bojowy ustawiaj jawnie po `addObject`.
- Detektory: zawsze uruchamiaj przebieg w OBU trybach (`--quiet`
  i `--snapshot-every 1`) — rozjazd = błąd narzędzia (M99; w M104 znalazł
  trzeci taki przypadek).

## Stan pushu (koniec sesji)

Token GitHuba wygasł w trakcie sesji i został odnowiony przez właściciela —
wszystkie commity M104 są na `arena/01a00b7e-mtg` (PR #56).

**Jeden wyjątek: `.github/workflows/*`.** Push plików workflow z sesji
agentowej odbija się od GitHuba:

```
refusing to allow a GitHub App to create or update workflow
`.github/workflows/ci.yml` without `workflows` permission
```

Dlatego commit E5 zmienił WYŁĄCZNIE wzorzec `docs/setup/workflows/{ci,pages}.yml`.

**ZAMKNIĘTE 2026-08-16:** właściciel wgrał treść ręcznie przez UI GitHuba
(commity `e0c9c42 Update ci.yml`, `c950361 Update pages.yml` na gałęzi sesji).
Weryfikacja: przebieg CI 31968213590 zielony, krok nazywa się „Testy
jednostkowe (równoległy runner, ADR 0019)", czyli pakiet naprawdę idzie przez
`node tools/run-tests.mjs all`. Czas kroku testowego 2m28s vs 2m25s przed
zmianą — na runnerach GitHuba (4 vCPU) `node --test` i tak zrównoleglał;
zysk to spójność z ADR 0019 (CI = `npm run test:ci`), a nie czas. Realna
oszczędność (~14 min → ~3,5 min) dotyczy maszyn 2 vCPU.

Uwaga dla następnej sesji: agent nadal NIE MOŻE pushować zmian w
`.github/workflows/*` (App bez uprawnienia `workflows`). Wzorzec w
`docs/setup/workflows/` i strażnik `test/ci-workflow-tiers.test.js`
zostają — kolejną zmianę w CI też trzeba będzie wkleić ręcznie.

Strażnik `test/ci-workflow-tiers.test.js` pilnuje, że wzorzec i faktyczny
workflow nie rozjadą się w niczym poza tą jedną linią.

## M105 — brązowa odznaka (druga część sesji)

Wyzwanie właściciela: znaleźć i naprawić błędy vs CR w istniejących kartach
i mechanikach. **Sześć znalezisk, każde z testem RED→GREEN**
(`test/bug-hunt-2026-08-16-bronze.test.js`):

| # | Znalezisko | Reguła |
|---|---|---|
| B1 | Trigon of Corruption „{B}{B}" bez pipów kolorowych (2 dowolne many) | CR 202.1 |
| B2 | Goblin Picker „{R}" bez pipa czerwonego | CR 202.1 |
| B3 | Monastery Flock {2}{U} zapisany jako `manaCost: 2` + STRAŻNIK katalogu | CR 202.3 |
| B4 | Aerith Rescue Mission „up to three" bez wariantu zero celów | CR 601.2c |
| B5 | Lodestone Needle „up to one target" bez opcji „bez celu" | CR 601.2c |
| B6 | „at the beginning of THE NEXT end step" liczone jako end step KONTROLERA (Cogwork Assembler, unearth) | CR 603.7b |

Metoda i pełny rejestr FAŁSZYWYCH ALARMÓW (co sprawdzono i okazało się
poprawne — żeby następna sesja nie szła tą samą ścieżką):
`docs/plans/2026-08-16-m105-brazowa-odznaka-lowy.md`. Lekcja **L23**:
dane w dwóch reprezentacjach (string kosztu vs `manaCost`/`cost.colors`)
wymagają maszynowego strażnika.

Stan po M105: `npm run test:all` **1938/1938**, build 51 modułów / 1727.1 kB,
benchmark szybki bez zmian (58,2% / 92,1%, `tools/b3-m105-2026-08-16.*`).

## Następne kroki (kolejka)

0. **Rozszerzyć strażniki danych kart** (L23): skan pipów kolorowych zdolności
   („{koszt}: efekt" vs `cost.colors`) jest dziś jednorazową sondą — warto go
   dopisać do pakietu jak strażnik mana value.
1. ~~`.github/workflows/*`~~ — **zrobione** (właściciel, 2026-08-16; patrz
   „Stan pushu").
2. **Pełne B0** po M103 A–D i M104 (`node tools/benchmark.mjs --full`) —
   tylko na komendę właściciela; wynik do `tools/b1-final-*`.
3. **Skan `noop` w wizardach walki i scry/surveil** — wizardy budują komendy
   spoza `legalCommands` (kombinacje), więc `data-option-key` ich nie
   obejmuje; ta klasa ofert wciąż nie jest mierzona.
4. **Sprzątanie kontraktu `addObject`** (L21): strażnik rzucający na nieznane
   pole + przejrzenie ~40 plików testów, które przekazują pola ignorowane
   (kandydaci na kolejne martwe asercje).
5. Boty: B4/B5 z `docs/BOT_ROADMAP.md` (random zyskał na naprawach M103,
   aggro/heuristic bez ruchu).
