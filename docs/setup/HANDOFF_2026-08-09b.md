# Handoff 2026-08-09b — po M59–M63 (Batch 25 + UI A–F + B2-w2 + brąz/srebro po Batch25)

## Stan po sesji 2026-08-09 (PR #37 scalony jako `0afe5a4` + PR #38 `arena/019fe7bf-mtg` sync docs)

- **Branch:** `arena/019fe7bf-mtg` — PR #38 OPEN do `main` (Squash and merge). HEAD: **62ad783** (plan) + docs sync (następny commit).
- **Kod:** `npm test` **1153/1153**, build **50 modułów / 1269.6 kB**, withArt **179 wpisów supported (178 realnych + token Rat)**, 9 talii singleton, benchmark 1080 meczów 0 crashy (heuristic **87.2% vs random / 71.4% vs aggro**, progi 0.78/0.57 utrzymane). Pełne B0 (9 talii / 50 seedów / 13 500 meczów) 90.4%/61.8% — nie przeliczane w tej sesji docs-sync.
- **Docs po sync:** `PROJECT_STATE` (M59–M63), `ROADMAP` (Batch 22–25 zaznaczone, Batch 26 czeka), `ENGINE_MILESTONES` (M59 Batch25, M60 UI A–F, M61 B2-w2, M62 brąz, M63 srebro), nowy `HANDOFF_2026-08-09b`.
- **Scalony `0afe5a4` zawiera:** Batch 25 (10 kart + 10 Scryfall JSON + collection-art-ids + plan), UI A–F (choice grouping, obrazy, bot modal, badge host, Kor Cartographer), B2-w2 lookahead infra (evalView, simpleChoice, threshold, wiring), brąz po Batch25 (5 bugów protection/cleanup/Fertile), srebro po Batch25 (5 bugów plot/protection kierunki).

## M59 — Batch 25 (10 kart, 62ad783 → 0afe5a4)

Trestle Troll (RTR), Lab Rats (STH buyback), Anthem of Champions (FDN anthem), Goblin Deathraiders (ALA), Fertile Thicket (BFZ reveal top5), Reassembling Skeleton (M19), Idyllic Grange (ELD conditional Plains), Deadly Recluse (M10), Benevolent Blessing (CMR flash/protection), Springbloom Druid (MH1 sacrifice-search). Mechaniki: buyback CR 702.26, protection CR 702.16 (pendingColorChoice), minOtherPlains, pendingFertileThicket, pendingSpringbloom, all_creatures_you_control.

## M60 — UI A–F (0afe5a4)

A choiceRequestGroupKey grupuje wszystkie resolve_*; B fullscreen z menu; C 6 imageUri poprawione; D bot modal isBotAdvancing; E badge host; F Kor Cartographer grouping.

## M61 — B2-w2 lookahead (0afe5a4, OFF domyślnie)

evalView keywords/evasion/library≤5, simpleChoice polityka, threshold 2→1, helpers.simulate wiring. Benchmark ON: 84.0% vs random (+5), 80.0% vs aggro (+34) na 2 seedach; OFF: 87.2%/71.4%.

## M62 — brąz po Batch25 (5 bugów, 0afe5a4)

CR 702.16a damage protection, CR 702.16b detach, CR 514.3a cleanup doc, declareBlockers protection, Fertile optional.

## M63 — srebro po Batch25 (5 bugów, 0afe5a4)

CR 702.136 plot later-turn, CR 702.16a combat sourceId, CR 702.16a blocking kierunek, CR 702.16a non-combat protection, CR 702.16b own-exception.

## Kolejka (po scaleniu PR #38)

- **Batch 26** — czeka na listę właściciela (ADR 0010: Scryfall ZAWSZE z `set=`; api.scryfall.com zablokowane w sandboxie → `fetch_page`).
- **B2-w2** — lookahead infra gotowa, OFF (~4× wolniej); włączenie wymaga decyzji o koszcie i ewaluacji na pełnym B0.
- Talie singleton — rosną z batchami (strażnik repo-decks przetestowany).

## Pułapki (środowisko / projekt) — aktualne

- `edit_file` psuje PL znaki → **python3 Path.read_text/write_text**; heredoc z PL cudzysłowami też → pliki /tmp.
- **Nowe w M59–M63:** `effectiveProtectionFromColors` w `attachments.js` (nie `permanents.js` — cykl), `isDamagePreventedByProtection` przed tarczami/filtrem, `canBlock` kierunek (atakujący protection vs bloker kolory), `plottedAtTurn` (+ turn.number check), `pendingFertileThicket` opcjonalny skip, `pendingColorChoice` + `resolve_color_choice`, `minOtherPlains` bez self, buyback `wasBuyback` → ręka.
- Biblioteka = wspólna lista obu graczy `[0]`=top; spód własny = ostatnia własna karta (M58 fix nadal obowiązuje; scry/mulligan-bottom pushują na koniec wspólnej).
- Obrażenia: damage_dealt ZAWSZE po prewencji (po M58+M62/M63); ochrona działa w combat (sourceId) i non-combat (dealNonCombatDamage).
- Plot: `plottedAtTurn` + `turn.number > plottedAtTurn` — nowe karty z plotem muszą to respektować (CR 702.136).
- canPayTrigger: produkowalna mana (pula + nietapnięte źródła) — nie tylko pula.
- Dobranie z pustej = przegrana (także z efektów).
- Testy seedy sesji (logi) przelosowywane hunterem po zmianie talii.
- Lektura nowej sesji: AGENTS.md → PROJECT_STATE (M59–M63) → HANDOFF_2026-08-09a/b → ENGINE_MILESTONES (M59–M63) → WORKFLOW → plany Batch25 + sync → ADR 0002/0003/0005/0010/0011/0013/0014.

## Do weryfikacji w następnej sesji

- `npm test` 1153/1153, `npm run build` 50/1269.6 kB (po sync zmiana tylko docs — kod bez zmian).
- Benchmark 1080 meczów 0 crashy (87.2%/71.4%) — jeśli ruszany bot, przeliczyć pełne B0.
- Scryfall dla Batch 26: ZAWSZE `set=` + `fetch_page` (api blokada).
