# Handoff 2026-09-15b — audyt PR #120 + naprawy F1/F2/F3/F4 (PR #121, `arena/01a0a505-mtg`)

Zlecenie: „Kontynuujemy projekt." — bez nazwanego tematu → pętla domyślna
ADR 0021. Wykonano: PR sesji → pełny audyt ostatniego scalonego PR (**#120**,
ADR 0020 B) → naprawy znalezisk inkrementalnymi commitami → domknięcie.
Raport: [`docs/audits/AUDYT_PR120_2026-09-15.md`](../audits/AUDYT_PR120_2026-09-15.md).
Plan: [`docs/plans/PLAN_2026-09-15c-audyt-pr120-i-naprawy.md`](../plans/PLAN_2026-09-15c-audyt-pr120-i-naprawy.md).
PR: [#121](https://github.com/jurekjurekgh/mtg/pull/121) (scala właściciel, ADR 0007/0020).

## Werdykt audytu PR #120: APPROVE Z ZASTRZEŻENIAMI

PR #120 wdrażał znaleziska A–G właściciela (Savage Surge, Spare from Evil,
Civilized Scholar, Powerstone, Murder of Crows/Armored Skaab, Altar badge).
Wyceny napisane po deskryptorach (M212 ✓, ADR 0002 ✓), regresja botów 10/10,
golden-master 4/4. ALE:

- **F1 (naprawiony, E2)** — wycena `resolve_optional_trigger_choice` czytała
  `view.pendingOptionalTrigger.ability`; widok projektuje `{sourceCardId, effect}`
  → kara cienkiej biblioteki NIGDY nie działała (repro: Murder palił przy 4
  kartach, score 50 identycznie jak przy 25). Fix: odczyt `pending.effect` +
  `test/audyt-pr120-optional-trigger-wycena.test.js` (4 testy; RED przed fixem,
  mutacja → RED, L13).
- **F2 (naprawiony, E3)** — kreator many odcinał Powerstone (`spendOnly:'artifact'`)
  od `activate_ability`; Oracle (scryfall 2026-09-15): ograniczenie dotyczy
  wyłącznie rzutu czaru nie-artefaktu; silnik (`restrictedManaBlocked`) to samo.
  Fix: wspólna `restrictedSpellBlockedFor` (mirror silnika) zamiast trzech kopii
  (L41/L48); `test/audyt-pr120-powerstone-kreator.test.js` (3 testy, vm-wzorzec
  M348); M348/B5 doposażony o helper (część fixu, L96).
- **F3 (posprzątane, E4)** — martwy kod z #120: identyczne gałęzie `trick=-75`,
  DEBUG-log, pusty `export`, redundancja L106.
- **F4 (sprostowane, E5)** — cytaty CR: „709.2a" (CR 709 = Split Cards) → Oracle
  Altara + CR 205.2a; zamienione 702.16d↔e (prewencja = 702.16e, cytat dosłowny
  w nagłówku `protectionPreventsAnyLethal`).
- **F5 (proces, bez kodu)** — sesja #120 nie zamknęła dokumentacji: plan
  `PLAN_2026-09-15b` bez odhaczeń/podsumowania, brak handoffu i wpisu
  PROJECT_HISTORY (ostatni wpis: 14f), klaimy commitów o „zaktualizowanym
  snapshocie" (fixture identyczny przed i po) i „11 testach" (zero plików
  testowych w diffie) bez pokrycia w repo. Sesja #119 też nie dopisała wpisu
  do dziennika (jej handoff: HANDOFF_2026-09-15.md).

## Bramki (finalny head `bf8585d`+docs, zmierzone)

- `npm test` **5503/5503** (0 fail; +7 testów vs stan wejściowy 5496);
- `npm run test:all` **5513/5513** (0 fail, ~343 s);
- `npm run build` **61 modułów / 3681,4 kB**;
- `node --test test/bot-benchmark.test.js` **10/10** (próbka regresji);
- golden-master 4/4 (próbka golden-mastera nie uruchamia nowych okien wyceny —
  legalne; fixture NIE był regenerowany w #120, komunikat tamtejszy kłamał);
- quick benchmark: patrz README „Bieżący stan" (pomiar z tej sesji).

## Otwarte (następna sesja)

- **Zadanie od właściciela — do odebrania** (sesja zatrzymana po pętli audytu;
  kolejny krok pętli ADR 0021 to audyt Żywym Testerem / polowanie CR inną
  ścieżką niż poprzednie sesje).
- Obserwacje z audytu #120: O1 (drabina biblioteczna przy zapas 0 — test
  anty-over-fix), O2 (`combat_damage` w oknie Spare — krok bez priorytetu),
  O4 (badge „Altar: X" — twardy prefiks nazwy karty przy deskryptorze
  generycznym; rozważyć etykietę mechanikową przy drugiej karcie), O5.
- Komentarz M221/E (heuristic-bot ~linia 68, PR #107) cytuje 702.16e przy
  blokadzie — do weryfikacji u źródła w osobnym zadaniu (poza zakresem audytu #120).
- D1/O1/O2 (audyt #115), O1/O2/O3 (audyt #118), S-1, `[Trigger:]` prefix — dalej nieblokujące.
- Pełne B0 wyłącznie na komendę właściciela (ADR 0018).

## Pułapki / lekcje tej sesji

- **Klaimy commitów vs repo (wzorzec L56/L92/L13):** sesja #120 opisała w
  commitach regenerację golden-mastera i zielone testy, których w repo nie ma.
  Audyt zawsze: `git diff <przed> <po> --stat` + pomiar, nie opis.
- **Pole poza widokiem = martwa wycena (L1/ADR 0017):** wyceny bota wołane na
  `view.*` bez pinu kontraktu widoku milczą; test F1 pinuje teraz kształt
  `{sourceCardId, effect}`.
- **vm-extraction konsumenta wymaga jego helperów** (M348/B5): wyciągając
  `refreshManaWizard` przez vm, trzeba dołączyć `restrictedSpellBlockedFor`.
- Płytki klon sesji (`depth=1`): stan „przed PR" bierze się z
  `git fetch --depth=12 origin main` + squashe #119/#118 (0acf09a/e9860cb),
  nie z lokalnej historii.
