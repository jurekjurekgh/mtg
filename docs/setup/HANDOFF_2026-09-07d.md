# Handoff 2026-09-07d — PR #104: M339–M344 wypchnięte, M345 czeka na GitHub

## Stan i blokada

Sesja `arena/01a07b98-mtg`, PR [#104](https://github.com/jurekjurekgh/mtg/pull/104)
(utworzony jako draft). Baza `c9b884c`, lokalny HEAD i ostatni potwierdzony
push **`af0226f`**. Przed commitem M345 `gh pr view 104 --json state` zwrócił
**HTTP 401: Bad credentials**. Było pierwsze w `set -e`, więc dalsze
`git add/commit/fetch/push` NIE wykonały się. Bieżący zdalny stan PR i CI
jest nieznany. Właściciel poproszony w czacie o ponowne połączenie GitHub
w Arena — nie o hasło/token. Nie próbować obchodzić autoryzacji.

**M345 jest GOTOWE W DRZEWIE, nie w historii. Nie resetować/restore'ować!**
Pliki do opublikowania:
- `tools/fetch-card-rulings.mjs`,
- `test/m345-rulingi-bledy-api.test.js` (nowy, nieśledzony do commita),
- `docs/audits/AUDYT_PR103_2026-09-07.md`,
- `docs/plans/PLAN_2026-09-07-audyt-pr103-petla-jakosci.md`,
- ten handoff (nowy).

## Co wykonano

Pełny audyt 59 plików PR #103 (`dfbc39f..c9b884c`) PRZED implementacją;
19 prób mutacyjnych, źródła CR/rulings, sprawdzenie wszystkich komórek
raportu pełnego B0 bez przeliczania go. Raport:
`docs/audits/AUDYT_PR103_2026-09-07.md`. Plan osobnym commitem `d389d15`,
raport `536c0cc`, PR otwarty z planu przed kodowaniem.

| Commit | Naprawa | Pełny rdzeń przed pushem |
|---|---|---:|
| `5d81996` M339 / F1 | numeracja cloaków z jawnych wejść całego stołu, nie tajnego cardId; pełny test różnicowy FoW | 4640 |
| `3a12df7` M340 / F2 | manifest daje istniejącą procedurę morph/megamorph, obie ścieżki i cleanup | 4647 |
| `3844ffb` M341 / F3 | wygrana proliferate oceniana dopiero po całym wyborze (własna dziesiąta trucizna nie ginie za wczesnym return) | 4653 |
| `437cf27` M342 / F4 | odsłonięcie nie dostaje premii ETB; stary m334/C naprawiony, nie zachowany jako umowa | 4656 |
| `a659024` M343 / F5 | wspólne stateBasedActionsOpen: SBA i czyszczenie tokenów czekają do końca pendingSpell, także pustego ogona | 4661 |
| `af0226f` M344 / F6 | dawny próżny m337/C tworzy prawdziwy manifest, sprawdza blokadę/powrót i execute | 4662 |
| **lokalne** M345 / F7 | 429/błędna odpowiedź nie kasują rulingów, pierwsza pusta lista zapisuje proweniencję | **4670** |

Każdy wypchnięty przyrost miał `npm test` + `npm run build`. M343 początkowo
ujawniło jedyny stary błędny pin Rage of Purphoros (śmierć przed końcowym
scry) — przeniesiono go do właściwego momentu, dodając kontrolę stanu przed.
Dowód F5 wykracza poza liczniki: gracz z chwilowym 0 życia przeżywa, gdy
kończy ten sam czar z 2 życia; stwór z chwilową wytrzymałością 0 też może
przeżyć. Nie usuwano asercji/inwariantów ani nie luzowano progów.

M345: **8/8**, z m328 **12/12**; 3 RED + 5 kontroli przed poprawką, osobne
mutacje A (429), C (nieprawidłowe data, kontrolowany błąd), F (proweniencja).
Testy CLI podmieniają tylko fetch i zegar retry; nie ma rzeczywistej sieci
ani zmian snapshotów kolekcji. Ostatnia pełna bramka drzewa M345:
**4670/4670**, build **59 modułów / 3369,7 kB** (134,5 s testów).
Po niej dopisano jedynie notatki o blokadzie i ten handoff.

## Pomiary bota i stołu

- Bazowy `bot-benchmark.test.js`: **10/10**. Po M343 **quick 672 mecze**:
  heuristic **570/672 = 84,8%**, aggro **89/336 = 26,5%**, random **13/336 =
  3,9%**, zero dryfu; 173,3 s przy równoległej bramce rdzenia.
- Golden-master **bez regeneracji** (sprawdzany także przy M341/M342/M343).
  Progi nietknięte. **Pełnego B0 NIE uruchamiano** — brak nowego polecenia.
- Żywy Tester: trzy partie bazowe zakończone, 0 zgłoszeń:
  `innistrad-wu|warhammer-ubr` s10401 explorer/policy13/tick0.2 (74 kroki),
  `mirrodin-brg|zendikar` s10402 defensive/policy17/tick0.3 (61),
  `warhammer-wg|wiedzmin` s10403 impatient/policy19/tick0.15 (78).
  Wszystkie `--steps 600 --quiet`, transkrypty w gitignorowanym
  `tools/table-tester/audyt-pr104/g{1,2,3}-before.txt`.

## Następne kroki — po ponownym połączeniu GitHub

1. Sprawdzić stan PR #104 (merge/closure kończy sesję). `git status`, log,
   fetch gałęzi sesji i porównanie zdalnych/lokalnych commitów. NIE resetować
   brudnego drzewa, nie tworzyć innej gałęzi, żadnego force push.
2. Powtórzyć bramkę i opublikować **M345** osobnym commitem/pushem z powyższą
   listą plików; zaktualizować kumulacyjny opis PR (REST PATCH działał przed
   401; `gh pr edit` może mieć błąd classic-projects).
3. **F8 / E3 — naprawić TESTER, nie omijać.** Kod jeszcze NIETKNIĘTY.
   `run-game.mjs:snapshot` bierze `.slice(-6)` z DOM logu, który renderuje się
   od najnowszego (`src/table/render.js:4239`, `reverse()`). W kroku 74 w
   „LOG” nadal `Obie ręce zatrzymane […] Nowa partia (seed 10401)`.
   `collectRejections` ma ten sam błąd: po drugim odrzuceniu odczytuje stare
   wpisy po indeksie od końca. `collectMainLog` już poprawnie bierze początek
   i odwraca go chronologicznie. Wydzielić jeden czysty ekstraktor kolejności
   do `tools/table-tester/extract.mjs`, użyć we wszystkich trzech miejscach,
   testować bez jsdom w głównym pakiecie. Dodać strukturalny detektor
   nieaktualnego ogona: porównać ostatni wpis emitowanego ogona z najnowszym
   wpisem DOM (nie z tym samym helperem). Zbierać dane co okno, niezależnie
   od --quiet. Mutacja starego odczytu + ta sama partia przed/po, potem
   przegląd transkryptów w trzech osiach. To propozycja konkretnego testu
   klasy, nie gotowa implementacja.
4. E4: `npm run test:all` + build + family-audit, potwierdzić CI, aktualne
   liczby README, historia i plan/handoff. Cały `test:all` NIE był jeszcze
   uruchomiony na końcowym drzewie (tylko fast + quick i bazowy slow).
   Utrzymywać ten sam PR; agent nie scala.

## Rzeczy istotne przy wznowieniu

- Lektura startup AGENTS/ADR/LESSONS/ENVIRONMENT wykonana w całości. Nie
  robić ponownie w tej samej sesji. Plan i audyt są źródłem dalszego zakresu.
- Budżet startup **99 946/100 000**, LESSONS jeszcze bez wpisu tej sesji.
  Nowa reguła o pochodnych tajnej informacji potrzebuje kondensacji (np.
  zwięźlej L137/L138 z zachowaniem faktów; narracje już są w PRZYPADKI).
  Nie zwiększać progu. README nadal ma stare liczby (to element E4).
- `dist/`, node_modules i `.arena/` nie persistują między odtworzeniami.
  Przed testerem build i `npm ci --prefix tools/table-tester`. Wnioski i
  dowody są w audycie/testach, nie wymagają ocalałych scratchów.
- `.arena/` wykluczono LOKALNIE przez `.git/info/exclude`; nie dodawać do
  commita sond/kopii git archive, logów, raportów quick ani transkryptów.
- Brak nowych kart/talii/limitacji Oracle. Wszystkie syntetyczne nośniki
  mieszkają wyłącznie w testach. Żadne procesy sesji nie są już uruchomione.
