# Handoff — sesja arena/01a047a8 (2026-08-28)

## Co to była za sesja

Prompt „Kontynuujemy projekt." — brak nazwanego tematu → ADR 0021 (pętla
domyślna). Gałąź `arena/01a047a8-mtg`, **PR #85** (otwarty, jeszcze nieszcalony).

**Co zrobione:**
1. Obowiązkowy audyt poprzedniego scalonego PR (#84) wg ADR 0020 B / 0016.
2. Znalezisko **E1** (fingerprint) — **NAPRAWIONE** u root cause z testem RED→GREEN.
3. Pętla jakości Żywym Testerem (3 partie, spot-check) — czysto.
4. Dokumentacja: `docs/audits/AUDYT_PR84_2026-08-28.md`,
   `docs/plans/PLAN_2026-08-28-audyt-pr84-i-petla-jakosci.md`.

## Najważniejsza rzecz do zapamiętania

**`state.pendingEscapeExile` (decyzja „wygnij N kart" z kosztu Escape — M240/M241)
nie trafiła do `PENDING_DECISION_FIELDS` w `src/engine/fingerprint.js` (L16).**
Decyzja wstrzymuje priorytet, ale fingerprint jej nie projekował — sonda „oferta
bez skutku" i weryfikacja replayów nie odróżniały stanu przed/po otwarciu decyzji
Escape. Dowód: odcisk przed/po `cast_escape` z pendingiem był IDENTYCZNY (test RED
przed fixem). Naprawa: dopisanie `'pendingEscapeExile'` do listy + test
`test/pr84-fingerprint-escape-pending.test.js`.

**Reguła (L16, powtórzona po raz kolejny):** każda nowa struktura, która BLOKUJE
priorytet (`pending*`), MUSI trafić do `PENDING_DECISION_FIELDS` (lub mieć własną
projekcję w `stateFingerprint`), zanim zostanie użyta przez dowolny czar/zdolność.

## Zmiany w tej sesji (od `e937c63`)

| Commit | Co |
|---|---|
| `c41c579` | PLAN sesji (docs/plans/PLAN_2026-08-28-...) + otwarcie PR #85 |
| `8c90da7` | Audyt PR #84 → E1: `pendingEscapeExile` w fingerprintzie (L16) + test RED→GREEN |

## Wyniki

- `npm test` (szybki rdzeń): **3611/3611** (było 3610; +1 nowy test E1).
- `npm run build`: OK, 55 modułów / 2829.6 kB.
- `node --test test/bot-benchmark.test.js`: **9/9**.
- Żywy Tester (3 partie): `worek-dziki` vs `worek-mroczny` s=42 (wygrywa Gracz),
  `srodziemie` vs `worek-legend` s=7 (wygrywa Bot),
  `mirrodin-brg` vs `zendikar` s=11 (wygrywa Bot) — **0 zgłoszeń detektorów**,
  brak powtórzonych akcji w transkryptach. Transkrypty w `/tmp/q1..q3.txt`
  (niewypchnięte — to pliki z /tmp, przepadną).

## Co było sprawdzone i CZYSTE (audyt PR #84)

- **Escape dwukrokowy** (spells.js): walidacja kosztu przed mutacją (CR 601.2h),
  zakaz `pass_priority` w trakcie pendingu, czar wchodzi na stos dopiero po
  zapłacie, dokładnie `exileCount` innych kart (CR 702.138a). ✓
- **M242 auto-cel** (triggers.js): wyłączone dla `allowNone`/celów stałych/
  wielocelów; LKI dla źródła, które mogło umrzeć z SBA. ✓
- **M243/F statyczna niemowlność ataku** (combat.js `staticAttackPrevented`):
  wydzielenie bez zmiany zachowania `isLegalAttacker` (L48).
  *Drobna obserwacja (bez naprawy):* `isLegalAttacker` nadal dubluje `defender`/
  `detain` przed wywołaniem helpera — kosmetyczne.
- **M245 podzbiory** (`boundedSubsets`/`legalBlockerOptions`): `[[], singles,
  allButOne, all].slice(0, cap)` — dla n=15 dokładnie 32; deterministyczne (ADR 0005). ✓
- **M239/1 trample przez protekcję**, **M247 czysty ląd**, **M248 fizzle**,
  **M243 activatableAbilities**, **M244 equip** — zmiany wyceny bota, bez zmian
  reguł legalności. ✓
- **Protocol/types.js**: `resolve_escape_exile` + `escape_exile_required`/
  `escape_exile_resolved` spójne z emitowanymi zdarzeniami. ✓

## Pułapka środowiska (nie wdepnij ponownie)

- **Żywy Tester** wymaga `npm i` w `tools/table-tester` (jsdom) — zrobione w tej
  sesji, ale nowa sesja musiałaby powtórzyć (izolacja; ENVIRONMENT §1).
- `stateFingerprint` zwraca **JSON string**, nie obiekt — przy asercjach na
  `pendingDecisions` parsuj (`JSON.parse`).

## Następna sesja

PR #85 czeka na scalenie (właściciel). Po scaleniu:
- Kolejny etap pętli jakości (ADR 0021): talie spoza `BENCH_DECKS`
  (`innistrad-wu`, `tarkir-bg`, `srodziemie`, `zendikar`, `wiedzmin`…) są
  najmniej przeczesane na żywym stole; albo polowanie na niezgodności z CR
  inną ścieżką niż poprzednie sesje.
- **Nie wymyślać nowego batcha kart** — karty tylko z listy właściciela.
- Przy zmianie bota: `node --test test/bot-benchmark.test.js`; pełny B0 tylko
  na komendę właściciela (ADR 0018).
