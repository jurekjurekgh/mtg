# Handoff 2026-08-09c — po M64 Batch 26 (10 kart)

## Stan po sesji 2026-08-09 (PR `arena/019fe7bf-mtg` — M59–M64)

- **Branch:** `arena/019fe7bf-mtg` — PR #38 OPEN do `main` (Squash and merge). HEAD: **7bbc507** (cards Batch26) + docs M64 (następny commit).
- **Kod:** `npm test` **1167/1167**, build **50 modułów / 1284.3 kB**, withArt **188 realnych + 1 token (189 wpisów supported)**, 9 talii singleton, benchmark 1080 0 crashy (progi 0.78/0.57). Pełne B0 (9 talii / 50 seedów / 13 500) nie przeliczane — hunter seeds przelosowane dla logów.
- **Scalone wcześniej:** `0afe5a4` (Batch25 + UI A–F + B2-w2 + brąz/srebro po Batch25), sync docs M59–M63 (53ff016), plan Batch26 (6b34314), Scryfall 926b7c1, engine 9fc2035, cards 7bbc507.
- **Docs po M64:** `PROJECT_STATE` (M64), `ROADMAP` (Batch26 done, Batch27 czeka), `ENGINE_MILESTONES` (M64), nowy `HANDOFF_2026-08-09c`.

## M64 — Batch 26: 10 kart (Scryfall z set=, 7bbc507)

**Karty:** Kabira Vindicator (ROE) Level Up {2}{W} sorcery, Great Furnace (MRD) artifact land R, Bomat Bazaar Barge (KLD) 5/5 Vehicle ETB draw Crew 3, Index (APC) {U} look top 5 any order, Bladed Sentinel (MBS) 2/4 {W}: vigilance, Might of the Masses (2XM) {G} pump per creature, Magic Damper (FIN) {U} +1/+1 hexproof untap, Hecteyes (FIN) 1/1 ETB discard each opponent, Carapace Forger (SOM) 2/2 metalcraft +2/+2, Lurking Green Dragon (CLB) 4/4 flying cant attack unless defender has flying.

**Mechaniki generyczne (ADR 0002):**
- Level Up CR 702.86: `add_counter level` sorcery + static `minLevel/maxLevel` (2-4, 5+) w `staticConditionHolds` → self +1/+2 / +2/+4 i other +1/+1 / +2/+2 via anthem.
- Index: `pendingIndex` + `resolve_index_choice` (permutacja), `COMMAND_TYPES`/`EVENT_TYPES` rozszerzone.
- pump_by_creature_count (Might): liczy stwory kontrolera, `modifyStats` +N/+N.
- discard_each_opponent (Hecteyes): pendingDiscard dla każdego przeciwnika (1v1 jeden).
- cantAttackUnlessDefenderHasFlying (Lurking): `isLegalAttacker` sprawdza defender's flying via `effectiveKeywords`.
- Great Furnace: `MANA_SOURCE_MAP` R + types Artifact Land (metalcraft).

**Talie singleton (9):** azorius +Kabira/Bladed, green +Might/Carapace/Lurking, black +Hecteyes, red +Great Furnace/Bomat (16 landów, total 47), spellslinger +Index/Magic Damper. `art-ids` 178→188, `repo-decks` red 45→47, `table-session` hunter seeds (endure 1→2, delirium 19→1, graveyard-top 2→5).

## Kolejka (po M64)

- **Batch 27** — czeka na listę właściciela (ADR 0010: Scryfall ZAWSZE z `set=`; api blokada → `fetch_page`).
- **B2-w2** — infra OFF (~4× wolniej), hunter +5/+34 p.p. z włączonym, decyzja o koszcie.
- Talie singleton — rosną z batchami (strażnik `repo-decks` + `każda nielandowa w talii`).

## Pułapki (środowisko / projekt) — aktualne

- `edit_file` psuje PL → **python3 Path.read_text/write_text**, commit msg via /tmp.
- **Nowe w M64:** `minLevel/maxLevel` w `staticConditionHolds` (level counters ≠ +1/+1), `minArtifactsControlled` dla metalcraft, `cantAttackUnlessDefenderHasFlying` w `combat.isLegalAttacker` (defender = przeciwnik w 1v1, `hasKeyword` lower case `flying`), `pendingIndex` (jak scry, blokuje pass, `COMMAND_TYPES`/`EVENT_TYPES` rozšířone), `pump_by_creature_count` (liczy battlefield stwory kontrolera), `discard_each_opponent` (pendingDiscard per opponent), `Great Furnace` MANA_SOURCE_MAP R.
- Biblioteka = wspólna lista [0]=top; `pendingIndex` reorder: `library = [...order, ...filter(!order)]` (nie splice wspólnej).
- `effectiveKeywords` lower case `flying` — karty definiują `keywords: ['flying']` lower, nie 'Flying'.
- `IsLand` = `kind==='land'` OR types includes 'Land'; artifact land `kind:'land'` + types `['Artifact','Land']` liczy się dla obu.
- Hunter seeds dla logów przelosowane po każdej zmianie talii (green/red/black/azorius) — patrz `hunter` w `table-session.test.js`.
- Lektura nowej sesji: AGENTS.md → PROJECT_STATE (M64) → HANDOFF_2026-08-09a/b/c → ENGINE_MILESTONES (M64) → WORKFLOW → plany Batch25/26 + sync → ADR 0002/0003/0005/0010/0011/0013/0014.

## Do weryfikacji w następnej sesji

- `npm test` 1167/1167, `npm run build` 50/1284.3 kB.
- Benchmark 1080 0 crashy (jeśli ruszany bot — przeliczyć pełne B0 9 talii / 50 seedów).
- Scryfall Batch 27: ZAWSZE `set=` + `fetch_page` (api blokada).
