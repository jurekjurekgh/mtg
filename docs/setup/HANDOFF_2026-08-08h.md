# Handoff 2026-08-08h — po M56 (srebrna odznaka: 5 błędów vs zasady MtG)

## Stan po sesji 2026-08-08 (PR `arena/019fe265-mtg` — M54 + brąz + M55 + srebro)

- **Branch:** `arena/019fe265-mtg` — PR #36 OPEN do `main` (Squash and merge).
  HEAD: **8176636** (fix srebrnej odznaki). Commity 11 → 12.
- **Stan:** `npm test` **1126/1126**, build **49 modułów / 1221.5 kB**,
  withArt **168**, benchmark 1080 meczów 0 crashy. B0: progi 0.78/0.57.

## M56 — 5 błędów vs zasady MtG (srebrna odznaka, 8176636)

1. **Goad (CR 701.38c)** — wygasał w cleanup TEJ SAMEJ tury (był
   `goadUntilEndOfTurn` + `untilEndOfTurn:true`); zaczarowany stwór nie
   musiał atakować w turze przeciwnika. Fix: `goadUntilNextTurn` →
   `goadedUntilTurn = turn.number + 2`, wygaszenie na starcie tury
   (game-state.js), cleanup nie rusza goadu.
2. **Aury a hexproof (CR 702.11b)** — castAuraSpell/legalAuraCasts nie
   filtrowały hexproof (pokój Throne). Fix: `auraTargetHexproof` (resources.js).
3. **Lifelink niecombat (CR 702.15)** — damage_each_opponent /
   damage_defending_player / damage_enchanted_* nie dawały zysku życia.
4. **Curse a prewencja (CR 615)** — damage_enchanted_player ignorował tarcze.
5. **damage_dealt (CR 119.3)** — event niósł kwotę PRZED prewencją → delirium
   (Fear of Burning Alive) przeszacowywało; przy okazji naprawiony latentny
   bypass filtra Ethersworn przy infect.

Wspólny helper **`dealNonCombatDamage`** (effects.js): prewencja tarcz +
filtra „prevent all damage this turn", event damage_dealt z kwotą ZADANĄ,
infect (poison/-1/-1 po prewencji), lifelink. Wszystkie 5 ścieżek
(damage, damage_each_opponent, damage_defending_player,
damage_enchanted_permanent_controller, damage_enchanted_player) przez niego.

## Kolejka (po scaleniu PR #36)

- **Batch 25** — 10 kart (Scryfall ZAWSZE z set=; api.scryfall.com zablokowane
  w sandboxie → fetch_page).
- **B2-w2 bota** — lookahead (infra gotowa, wymaga przeprojektowania ewaluacji).
- Talie singleton — rosną z batchami (strażnik repo-decks).

## Pułapki (środowisko / projekt)

- `edit_file` psuje polskie znaki → python3 (Path.read_text/write_text).
- Obrażenia: WSZYSTKIE ścieżki przez `dealNonCombatDamage` (prewencja +
  event z kwotą zadaną + lifelink); combat damage do gracza nadal emituje
  event z kwotą przed prewencją (brak konsumenta — świadome ograniczenie).
- Goad: `goadedUntilTurn` — nie przywracać `goaded:false` do cleanup.
- Aury: hexproof w `auraTargetHexproof` (resources.js) — oferta i walidacja.
- Testy z seedami sesji (logi) przelosowywane przy każdej zmianie talii.
- Lektura nowej sesji: AGENTS.md → PROJECT_STATE (M54–M56) →
  HANDOFF_2026-08-08f/g/h → ENGINE_MILESTONES (M53–M56) → WORKFLOW →
  plany batch23/batch24 → ADR 0002/0003/0005/0010/0011/0013/0014.
