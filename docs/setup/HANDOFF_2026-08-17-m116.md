# Handoff — koniec sesji 2026-08-17 (M109–M116)

Gałąź: `arena/01a00b7e-mtg` · PR **#56** (OTWARTY — scalenie to decyzja
właściciela, `Squash and merge`). CI zielone na HEAD.

## Co powstało w tej sesji

| Krok | Temat |
|---|---|
| M109 | batch 33 transza 2 — 7 kart (storm, incubate, ochrona jakościowa, reveal+discard, obniżka po podtypie, dwa cele, fala obrażeń po keywordzie) |
| M110 | eliminacja ograniczeń: pełne DEBT ochrony (CR 702.16), storm jako trigger + nowe cele kopii, Willbender celuje też w ZDOLNOŚCI |
| M111 | obniżki kosztu przy KAŻDYM sposobie rzucenia, kopie czarów wielocelowych, bot wycenia tryby modalne, pole `notes` + strażnik `limitations` |
| M112 | sekcja `combat` z PlayerView na stole, oś „noop" wchodzi do wizardów walki i scry/surveil |
| M113–M116 | batch 34 — **10 z 10 kart** (ostatnie trzy: Chronic Flooding, Krumar Initiate, Cuombajj Witches) |

## Stan techniczny

- `npm run test:all` — **2051/2051**; `npm test` (szybki rdzeń) — 2042.
- Build: 51 modułów / 1829,5 kB.
- Benchmark szybki: heuristic **60,3 %** vs aggro, **89,3 %** vs random,
  0 niedokończonych (`tools/b10-m116-2026-08-17.txt`). Progi regresji
  0,57 / 0,78. Pełne B0 (ADR 0018) NIE liczone — czeka na komendę właściciela.
- Katalog: **zero** kart z `imageUri: null`; `limitations` wyłącznie z trzech
  dopuszczonych powodów (token, tylna strona DFC, brak strefy dowodzenia).

## Rzeczy, które warto pamiętać (poza `docs/TODO.md`)

1. **Sandbox dwa razy zresetował historię gita** (pliki zostały, `.git` wrócił
   do commita bazowego). Objaw: `git push` odrzucony „fetch first".
   Naprawa: `git fetch origin <gałąź>` + `git reset --mixed FETCH_HEAD`,
   potem normalny commit. Dlatego: push po KAŻDYM zielonym kroku.
2. **Każdy batch kart rusza seedy** testów scenariuszowych (lekcja L25).
   W tej sesji przelosowanych zostało kilkanaście; hunter to
   `/tmp`-owy skrypt odtwarzający politykę danego testu — najszybciej napisać
   go od nowa na wzór opisu w planie sesji.
3. **Nowa blokująca decyzja** = stan `pending…` + priorytet + oferta +
   bramka `execute` + łańcuchy `!state.pending…` + obie listy botów + etykieta
   w `render.js` + opis w `session.js`. Pominięcie botów zawiesza partię.

## Kolejka

`docs/TODO.md` — sekcja kart jest pusta (batch 34 zamknięty). Reszta kolejki:
ochrona jakościowa dla efektów nieceowanych, kopie czarów MODALNYCH, wycena
pozostałych decyzji przez bota (scry/surveil, cel triggera, przydział
obrażeń), sondowanie kroku kolejności w wizardzie surveil i wizarda obrażeń,
sprzątanie kontraktu `addObject` (L21), przegląd 58 wpisów `notes`.
