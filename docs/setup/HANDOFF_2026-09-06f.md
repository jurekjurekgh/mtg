# Handoff 2026-09-06f — arena/01a07711-mtg: M315 cloak + M317 bot vs triki (PR #102)

## Stan
- Gałąź `arena/01a07711-mtg`, PR **#102** (opis kumulatywny, REST PATCH).
  Head: `6d8dfa9` (wypchnięty, FF). Poprzednie: HANDOFF_2026-09-06e.

## M315 — Veiled Ascension / cloak (temat A, `963af06`)
- Zasada właściciela: „karta jest w 100% zgodna z CR i Rulings albo jest
  nieobsługiwana". Karta pozostaje OBSŁUGIWANA, luki domknięte.
- **A1:** ward {2} cloakowanego egzekwowany od M258/F3 (czary/zdolności/
  triggery); luka była w KAFLU: hardkod `keywordsNow = ['ward']` gubił jawne
  granty (licznik flying) i ward u przeciwnika. Kafel czyta keywordy z WIDOKU;
  widok: kontroler = pełna lista, przeciwnik = granty + ward (decyzja
  M258/F3: ward zakrycia jawny). Własny zakryty: badge „(Cloak)" z
  `entry.cloakReady` (tylko kontroler — CR 708.2d; cloak-vs-morph przeciwnika
  nadal ukryte).
- **A2:** NOWA specjalna akcja `turn_cloak_face_up` (CR 702.75c + ruling
  WotC 2024-02-02): any time you have priority, BEZ stosu, niereagowalna;
  tylko gdy pod zakryciem karta stwora (`cloakReady`); koszt = koszt many
  KARTY (`cloakTurnUpCost` z `faceDownOriginal` — cloak ma mana value 0);
  po obrocie pełne cechy karty i TRACI ward {2} (pole `ward` czyszczone
  ręcznie — nie pochodzi z faceDownOriginal).cloak zapamiętuje
  faceDownOriginal/cloakReady/cloakTurnUpCost (effects.js, jak manifest).
- **Bot:** `turn_cloak_face_up` → NEVER (legalność pełna; wycena „czy warto
  odsłonić" to osobny temat heurystyki — precedens M310).
- **Golden-master REGENEROWANY** (`node tools/bot-scoring-snapshot.mjs
  --write`): nowa legalna akcja w options śladu bota (ravnica|innistrad-wu
  s1000, 245→246 decyzji, scoreSum bez zmian). ŚWIADOME.
- Strażnik: `test/m315-veiled-cloak.test.js` (9). Pin m277: `cloakReady`
  na liście WARUNKOWE_SPOZA_PROBKI.

## M317 — bot vs wrogie triki obronne (temat B, `6d8dfa9`)
- Zgłoszenie: bot atakuje 2/2 „na wymianę", obrońca tapem Ghost Wardena
  robi 3/3 — atakujący ginie za darmo. „Wyceniać także inne zdolności na
  moim stole (za tapnięcie albo za manę, o ile ją mam)".
- `enemyDefensivePumpBonus(view)` (heuristic-bot): skan wrogiego stołu —
  activated zdolności z pozytywnym pumpem/bufem na cel „creature"
  (`activatableAbilities` z widoku, jawne od M243/E); dostępność: tap →
  nietapnięte źródło; mana → pula wroga + nietapnięte wrogie lądy (pipsy
  pomijane = zakładamy, że zapłaci); `detained` wyklucza. NAJLEPSZA
  pojedyncza zdolność (nie suma — jedna aktywacja na źródło w oknie bloków).
- `declare_attackers`: gałęzie first strike / przeżyje-i-zabija / gang
  (M167/I) / przeżyje-nie-zabije / wymiana / chump liczą staty blokerów
  Z BONUSEM (najgorszy przypadek). Otwarta presja i unblockable bez zmian.
- Strażnik: `test/m317-bot-triki-obronne.test.js` (6). Golden-master bez
  zmian (w partiach snapshotu trik nie występuje).

## Bramki (świeży pomiar na zamknięcie, L92)
- `npm test`: **4549/4549** (+9 m315, +6 m317); `test:all`: **4559/4559**.
- Build: 59 modułów / **3336,4 kB**. Benchmark: 84,8% (570/672) — bez dryfu.
- Żywo: ravnica|worek-legend s41 (Ghost Warden aktywny) — 0 zgłoszeń.

## Dla następnej sesji
1. **Wycena uncover bota** (M315 zostawione NEVER): wartość karty pod
   zakryciem vs koszt many vs utrata ward {2} — osobny temat heurystyki.
2. **Ruling Veiled Ascension nr 3** (uncover za morph/disguise cost, gdy
   karta ma te zdolności) — NIE zakodowane; w katalogu nie występuje para
   cloak+morph na jednej karcie; przy pierwszej takiej karcie test czerwony
   nie będzie pilnował (dopisać wtedy).
3. M258/F3 decyzja „ward zakrycia jawny przeciwnikowi" — utrzymana; kafel
   przeciwnika pokazuje „Ward {2}" na zakrytym (ujawnia cloak-vs-morph
   w zakresie ward — świadoma decyzja właściciela z PR #89).
4. L135/L92/ADR bez zmian; `gh pr edit` dalej zepsuty — tylko REST PATCH.
