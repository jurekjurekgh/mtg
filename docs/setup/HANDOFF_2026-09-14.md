# Handoff 2026-09-14 — Batch 55: 10 kart (23, 609–617) — Gift, Embalm, Duskmantle Seer (PR #116, `arena/01a09c9e`)

Druga część dnia 2026-09-14 (pierwsza: znaleziska właściciela A/M349 +
B/M350, HANDOFF_2026-09-13.md §Aktualizacja) — pierwszy plik handoffu z tej daty. Zlecenie właściciela: lista
10 kart z kolekcji + „Rozplanuj to sobie, podziel na etapy, wrzuć plan,
a potem każdy etap osobny commit".

Plan: [`docs/plans/PLAN_2026-09-14-batch55-23-609-617.md`](../plans/PLAN_2026-09-14-batch55-23-609-617.md).
PR: [#116](https://github.com/jurekjurekgh/mtg/pull/116) (ten sam PR co sesja
2026-09-13 — branch `arena/01a09c9e-mtg`; scala właściciel, ADR 0007/0020).

## Co zrobiono (w kolejności commitów)

- **`8151e3c` — B0a: plan** — push PRZED pierwszym kodem (ADR 0020 A);
  mapowanie 10 kart na plany talii, kryteria etapów B1–B7.
- **`976e3d0` — B0b: dane źródłowe** — 10 snapshotów Scryfall (Oracle +
  rulingi, `rulingsPobrano` 2026-09-14) w `docs/cards/`; CSV kolekcji bez
  wierszy FUS i LOR (zgoda właściciela: „agent MAY delete FUS and LOR rows");
  karty dopisane do katalogu jako `in-development` + wpisy planów.
- **`01f6894` — B1 (M351)**: 616 Act of Treason, 617 Douse in Gloom,
  610 Gearsmith Prodigy `supported`; nowy generyczny warunek
  `condition.controlsArtifact` (bez „another" — Oracle mówi „as long as you
  control an artifact"); pierwszy churn golden mastera w batchu
  (`107fd425…` → `3a5b59276c…`, atrybucja: tylko skład `tarkir-bg`; ten sam
  kod na starych taliach odtwarzał starą wartość). Seed w
  `bot-spell-resolution-in-modal` przelosowany 5 → 3 (ten sam świadek:
  Awaken the Bear `+3/+3` w logu I w modalu).
- **`59e1afc` — B2 (M352)**: 611 Lifecrafter's Gift (nowy `requireCounter`
  w `add_counter_to_creatures_you_control` — reguła „then" bez gałęzi po
  nazwie) i 614 Hunt the Weak (`add_counter` → `fight`); **re-podział planu
  Wiedźmina** (ADR 0024: `wiedzmin-brg`/`wiedzmin-wu` → `wiedzmin-bg`/
  `wiedzmin-wur`) — 13 plików testów + `tools/bot-tie-audit.mjs` + tabela
  talii w README; sufit audytu remisów `attack` 4 → 7 z pomiarem per para.
- **`3edadb3` — B3 (M353)**: 609 Jungleborn Pioneer (nowy wpis tokenu
  `token_merfolk`, 1/1 hexproof) i 615 Tah-Crop Skirmisher (**Embalm**,
  CR 702.128a — nowy typ efektu `create_token_copy_of_source`); przy okazji
  **naprawiony strażnik M197/K4** (klasa L23 — patrz „Pułapki").
- **`8acdcb2` — B4 (M354)**: 23 Brightwood Tracker — rozszerzenie istniejącej
  rodziny `pendingSatyrLook` deskryptorem (`pickTypes`, `restTo`, `restOrder`)
  zamiast nowej decyzji; seedowana kolejność spodu; seed przelosowany
  w `library-manipulation-modal` (34 → 11, hunter po 200 seedach).
- **`f41959e` — B5 (M355)**: 612 Crumb and Get It — pełny **Gift**
  (CR 702.174): dodatkowy koszt bez many wybierany przy rzucaniu, obietnica
  przenoszona przez rejestr → materializację → `OBJECT_FIELDS` → widok ręki
  → stos, Food PRZED innymi efektami czaru, brak daru przy skontrowaniu;
  nowy wariant komendy `cast_spell` (`gifted`, `giftRecipientId`), grupa
  w panelu `…:gift:<odbiorca>`, log „obiecuje dar", wycena bota.
- **`0762751` — B6 (M356)**: 613 Duskmantle Seer — nowy efekt
  `reveal_top_each_player_lose_life_mana_value`: odsłonięcie JAWNE
  (`card_revealed` z `cardId`), utraty życia JEDNOCZESNE (jedna seria — dwie
  przegrane naraz to remis, CR 104.4b), karty do ręki BEZ `card_drawn`
  (miracle nie widzi dobrania), mana value z kosztu DRUKU (L85); drugi
  i ostatni churn golden mastera (`3a5b59276c…` → `3d116714…`, atrybucja:
  tylko skład `ravnica`); `M337/D` skrócił się 353 → 284 komend.
- **B7 (M357) — integracja i domknięcie**: ten handoff, wpisy
  w `docs/ENGINE_MILESTONES.md` i `docs/PROJECT_HISTORY.md`, liczby w README,
  żywy tester na taliach batcha, finalne bramki.

## Bramki (zmierzone na końcu sesji)

| bramka | wynik |
|---|---|
| `npm test` (szybki rdzeń) | **5472/5472**, 0 fail |
| `npm run test:all` (brama PR) | **5482/5482**, 0 fail, 297,7 s |
| `npm run build` | **61 modułów / 3657,5 kB** |
| quick benchmark (`node tools/benchmark.mjs`) | 672 gry / 123,6 s — heuristic **82,6%** (555/672), aggro **30,7%**, random **4,2%**; kontrola na STAREJ talii `forgotten-realms` odtwarza poprzedni pomiar (84,2% / 28,0% / 3,6%), więc różnica to skład talii, nie regresja bota |
| Żywy Tester (11 partii na taliach batcha) | **0 detektorów**, 0 niewycenionych ruchów bota |

Pełne B0 **niewykonane** — wyłącznie na wyraźną komendę właściciela (ADR 0018).

## Co nowego w silniku (mapa dla następnej sesji)

- **Embalm / `create_token_copy_of_source`** — `src/engine/effects.js`;
  kopiuje wydruk źródła (P/T, typy, słowa kluczowe, zdolności),
  `effect.colors` ZASTĘPUJE kolory, `effect.addSubtypes` DODAJE podtypy,
  token ma `manaCost: 0` i `cardId` źródła (nie wymaga wpisu w rejestrze
  tokenów). Wygnanie z grobu to KOSZT — efekt czyta obiekt ze stanu
  (`state.objects.get(...) ?? sourceObject`), bo źródło jest już w exile.
- **Gift (CR 702.174)** — deskryptor `gift.effect` w `card-data.js`;
  przenoszony w CZTERECH miejscach (klasa L84): `registry.js` (freeze),
  `materialize.js` (gałąź permanentu I czaru), `game-state.js`
  (`OBJECT_FIELDS` + widok ręki), `spells.js` (stos: `wasGifted`,
  `giftRecipientId`). Efekt warunkowy: `condition.wasGifted` w `effects.js`.
  Panel: grupa `spell:<obj>:false:gift:<recipient>`, tytuł `(dar)`,
  `giftPart`; log: `gift_given` („obiecuje dar (Food) — …").
- **Odsłonięcie + mana value (613)** — `reveal_top_each_player_lose_life_mana_value`
  w `effects.js`; selektor wierzchu WSPÓLNY z tabelą strażnika
  (`libraryCardsOf`) — nie kopiuj go drugi raz.
- **Podgląd wierzchu** — `pendingSatyrLook` z `pickIds` (dawniej `landIds`),
  `pickTypes`, `restTo` (`graveyard`/`library_bottom`), `restOrder`
  (`preserve`/`random`); „you may" zawsze w ofercie.
- **Liczniki „then"/grupa** — `add_counter_to_creatures_you_control`
  z `requireCounter` (zbiór = stwory, które już mają licznik danego typu).
- **Warunki statyczne** — `condition.controlsArtifact` (bez wykluczania
  źródła) obok `controlsAnotherArtifact`.

## Golden master (stan po batchu)

`test/fixtures/bot-scoring-snapshot.json` = `3d1167140f686f7c…`
(przed batchem: `107fd425…` → B1 `3a5b59276c…` → B6 `3d116714…`).
Oba churny z atrybucją, bez zmian wag i progów; dowód izolacji: nowy kod na
starej talii odtwarza starą wartość bit w bit (workflow: `git show
HEAD:decks/<talia>.txt > decks/<talia>.txt`, uruchom
`node tools/bot-scoring-snapshot.mjs`, porównaj `overallHash`, przywróć talię,
dopiero potem `--write`).

## Żywy Tester (transkrypty w `/tmp`, nie trwałe)

Partie na taliach batcha (wszystkie `--steps 400`): `srodziemie vs ravnica`
(seed 42), talia celowana `audyt-batch55 vs alara` (seedy 50–54, duskmantle:
3× Seer, żeby trigger był widoczny), `kaladesh vs worek-legend` (7),
`worek-mroczny vs tarkir-wur` (9), `wiedzmin-bg vs tarkir-bg` (11),
`forgotten-realms vs forgotten-realms` (21), `tarkir-wur vs tarkir-bg` (23).
Wynik: **0 zgłoszeń detektorów**, 0 ruchów bota bez wyceny, żadnych `[STOP]`
ani surowych identyfikatorów; pokrycie wszystkich 10 kart batcha
(Duskmantle Seer widziany w 4 partiach celowanych — trigger w logu:
„odsłania X", „życie a → b", „biblioteka → ręka").
Skan wzorców (`scan.mjs`) bez trafień wymagających naprawy — dwa trafienia
„N karty" to poprawna polszczyzna. Dwa znane zastrzeżenia (nie regresje
batcha): `[choroba]` (znacznik summoning sickness — celowy) i `[Trigger:]`
w linii stosu (etykieta generyczna sprzed batcha, poza zakresem).

## Kolejka / otwarte (nie zapomniane)

1. **`[Trigger:]` w linii stosu** — angielski prefiks w polskim UI
   (`render.js:4375`); wzorzec zgłasza go w `tools/table-tester/scan.mjs`,
   ale etykieta jest przed batchem i dotyczy wszystkich triggerów. Kandydat
   na osobne znalezisko UX (nie ruszać „na siłę" w batchu kart).
2. **`docs/coverage`/`ROADMAP`** — batch 55 nie zmienia planów roadmapy;
   kolejny batch startuje z listy właściciela (ADR 0029).
3. Znaleziska nieblokujące audytu PR #115 (D1/O1/O2) — bez zmian, do decyzji
   przy okazji zmiany danego pliku.
4. Katalog rośnie **wyłącznie z kolekcji właściciela** (ADR 0029);
   wiersze FUS/LOR w naszych CSV usunięte zgodnie z jego poleceniem.

## Pułapki środowiska (potwierdzone w tej sesji)

- `npm test` ~3–4 min, `npm run test:all` ~5–8 min, quick benchmark ~2–4 min —
  wszystkie przez `start_process` + `wait_for: exit`; bash timeout 30 s
  ubija je w połowie.
- `gh pr edit` nie działa na tym repo → `gh api -X PATCH
  repos/jurekjurekgh/mtg/pulls/116 -F body=@plik`.
- **Nowy typ efektu = cztery dowiązania poza silnikiem** (L84): panel
  (`render.js` → M122), log (`session.js ABILITY_EFFECT_LABELS` → M255/C1),
  klasyfikacja dubli w bocie (`heuristic-bot.js STACKING_ACTIVATED_EFFECTS`
  → M179), wycena (`heuristic-bot.js` + M157/`REVIEWED_UNVALUED`).
  Dodatkowo **H7 (M256)**: typ czytający się zbiorowo (`_each_`/`_all_`/`^each`)
  wymaga wpisu w `EMPTY_RECEIVER_EFFECTS` z powodem (`no_targets` vs
  `empty_library`) albo świadomego wyjątku w teście.
- **Z1c**: każdy nowy czasownik w logu (`session.js`, wzorzec
  `${whoN(…)} <czasownik>`) musi być w czasie teraźniejszym i mieć wpis
  w `DRUGA_OSOBA`; wyjątki: i / kartę / zostają / nie.
- **Strażnik M197/K4** (od B3): skan idzie po granicach `defineCard({`
  i liczy `artId`/`plan` w każdej definicji osobno. Nowe karty pisz tym samym
  stylem co sąsiedzi (batch 55 ma `artId/plan/support` w jednej linii, tokeny
  w osobnych) — jedno i drugie jest już poprawne.
- Golden master: NIGDY `--write` bez uprzedniej izolacji przyczyny
  (workflow wyżej); churn od samych talii też wymaga dowodu.
- Talie generuje `node tools/generate-plan-decks.mjs` (ADR 0023/0024);
  po każdym uruchomieniu zaktualizuj tabelę talii w README — pilnuje jej
  `test/m203-talie-testera-i-dokumentacji.test.js`.
- Żywy Tester: `cd tools/table-tester && npm i` (jsdom) + **świeży**
  `npm run build` (lista talii jest wpinana w artefakt); talia celowana
  (`decks/audyt-*.txt`) MUSI zniknąć przed `npm test` (M331).

## Plany / dokumenty

- plan batcha: [`docs/plans/PLAN_2026-09-14-batch55-23-609-617.md`](../plans/PLAN_2026-09-14-batch55-23-609-617.md)
- milestone: `docs/ENGINE_MILESTONES.md` § „Batch 55 (2026-09-14)"
- historia: `docs/PROJECT_HISTORY.md` § „2026-09-14b — Batch 55"
- dane kart: `docs/cards/scryfall-<slug>.json` (10 plików)

## Stan końcowy sesji

PR **#116** (`arena/01a09c9e-mtg` → `main`) — commity batcha 55: `8151e3c`
(B0a plan), `976e3d0` (B0b dane), `01f6894` (B1), `59e1afc` (B2), `3edadb3`
(B3), `8acdcb2` (B4), `f41959e` (B5), `0762751` (B6), B7 (dokumenty + liczby).
Bramki na finalnym drzewie: `npm test` **5472/5472**, `npm run test:all`
**5482/5482**, `npm run build` **61 modułów / 3657,5 kB**. CI zielone dla
każdego pusha. **Scala właściciel** — agent nigdy nie scala PR-ów.
