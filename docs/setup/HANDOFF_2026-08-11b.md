# Handoff 2026-08-11b — Audyt PR #41 (M73): 9 błędów naprawionych (PR #42)

## Stan po sesji (PR `arena/019ff0e1-mtg`, #42)

- **Kod:** `npm test` **1334/1334** (było 1310), build **50 modułów / 1453.2 kB**.
  Quick B0 (1080 meczów): **0 crashy, 0 niedokończonych**. Pełne B0 (13500) —
  wynik w opisie PR #42.
- **Gałąź:** `arena/019ff0e1-mtg` (push po każdym commicie; PR #42 otwarty).

## Co naprawiono (audyt PR #41 — plan `docs/plans/PLAN_2026-08-11-audyt-pr41.md`)

1. **Fireball (JVC)** — `8c7cfca`: podział X „divided evenly, rounded down"
   (floor(X/n), reszta przepada). Usunięta machineria free-distribution
   (`resolve_damage_distribution` i spółka) — NIE ISTNIEJE już w protokole.
   Walidacja: 0 celów i X=0 legalne; protection od koloru czaru.
2. **attacks_alone (Angelic Benediction)** — `5d46cbd`: filtr kontrolera.
3. **Curiosity** — `5d46cbd`: hook `enchanted_creature_damage_to_opponent`
   (combat + niecombat).
4. **Veiled Ascension** — `5d46cbd`: flying counter dla każdego face-down
   (cloak + morph); `effectiveKeywords` faceDown = keywordy z liczników
   (flying counter daje flying zakrytemu — CR 122.1b).
5. **Oil** — `5d46cbd`: statyczny pump `oil_counters` (bez generalizacji).
6. **Protection w aurach** — `6f16628`: cast/offer/resolveAuraSpell.
7. **Zdolności na stosie** — `6f16628`: rewalidacja celów przy rozstrzyganiu;
   equip INSTANT + stos; cycling/channel na stos; ninjutsu na stos.
8. **B8** — `4019566`: sonda mechanik M72 (Necrosquito artefakt/self, Veiled
   ETB, Warmaker station) — OK.
9. **B9** — `4019566`: testy render UI M72b (E właściciel, F badge gospodarza).

## Kolejka (po M73)

- **Zgłoszone właścicielowi (pre-existing):** model priorytetu po rzucie czaru —
  engine zostawia priorytet rzucającemu (CR 117.4); auto-pass sesji maskuje,
  ale rzucający mógłby odpowiedzieć sam sobie. Decyzja: naprawić (root fix:
  `state.turn.priorityPlayerId = next` po cast/activate — wymaga przeglądu
  sesji/botów + pełnego B0) albo zaakceptować jako świadome uproszczenie.
- **Batch 30** — czeka na listę właściciela (ADR 0010: Scryfall z `set=`).
- **B2-w2 lookahead** — infra OFF.

## Pułapki (środowisko / projekt) — bez zmian vs HANDOFF_2026-08-11 + nowe

- `edit_file` psuje PL → python3 Path.read_text/write_text.
- Po każdym commicie `git push origin arena/019ff0e1-mtg`; przy rozjeździe
  `git fetch && git reset --mixed FETCH_HEAD`.
- **Zdolności aktywowane (equip/cycling/channel/ninjutsu/zwykłe) rozstrzygają
  się ze stosu** — testy muszą rozstrzygać stos (resolveStack/passBoth) po
  aktywacji; asercje natychmiastowe (np. `attachedTo` zaraz po activate_ability)
  będą fałszywe.
- **Brak `resolve_damage_distribution`** — Fireball dzieli sam; nowa karta
  „X damage divided among targets" z DOWOLNYM podziałem wymagałaby nowego
  mechanizmu (świadomie usuniętego).
- `effectiveKeywords` faceDown zwraca keywordy z LICZNIKÓW — testy zakrytych
  stwórów z licznikiem flying (Veiled) MUSZĄ widzieć flying; bez licznika [].

## Do weryfikacji w następnej sesji

- Pełne B0 (13500) w opisie PR #42.
- Na telefonie (Pages, po merge): equip działa w odpowiedzi na czar i poza
  main phase; cycling/channel/ninjutsu czekają na stosie (przeciwnik może
  odpowiedzieć); Fireball dzieli po równo bez wizarda; cudza Benediction nie
  pompuje przy moim ataku; zakryty stwór z flying counterem (Veiled) blokuje
  flyery.
<<<<<<< HEAD
=======

## Aktualizacja 2 (2026-08-11) — M73b: UX A/B + feature ptaszek + cap B0

- **A.** Panel górny: „T."/"ż."/"On", faza bez „beginning", wrap na 2 wiersze.
- **B.** Nakładka karty: każda informacja w osobnym wierszu (ovl-badges column).
- **Feature:** ptaszek wyciszenia opcji (cast/activate) — wyciszona opcja nie
  przerywa auto-passu (`hasMeaningfulDecision` filtruje po `commandOptionKey`),
  zbiór w pamięci strony, `session.recheckAutoPass()` po zmianie. Generyczne
  komendy bez ptaszka. Grupy modalne (choice-request) bez ptaszka (v1).
- **Cap B0:** maxCommands 5000→8000 (zdolności na stosie wydłużyły grind-games;
  seed 1043 wiedzmin vs azorius kończył się 2 tury po capie). Pełne B0 13500:
  **0 crashy, 0 niedokończonych** — heuristic 79.2% ogółem (64.0% vs aggro /
  94.4% vs random), progi 0.78/0.57.
- Testy: 1337/1337; build 50 modułów / 1458.7 kB.
- Pułapka: `renderTableView` — checkbox dodawany PO innerHTML (inaczej
  innerHTML kasuje dzieci); MiniEl w testach ma `prepend`.
