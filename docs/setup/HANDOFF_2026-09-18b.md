# Handoff 2026-09-18b — audyt PR #128 + naprawa 4 znalezisk + pętla jakości

Sesja „Kontynuujemy projekt." → tryb obowiązkowy ADR 0020 (PR → audyt
poprzedniego PR → inkrementalne commity) + pętla domyślna ADR 0021. Gałąź
`arena/01a0b60e-mtg`, bazowy HEAD `e0ad776` (squash PR #128). PR:
[#129](https://github.com/jurekjurekgh/mtg/pull/129).

## Stan wejściowy (zmierzony, nie przepisany)

- `git log -1` → `e0ad776 Sesja 2026-09-18: naprawa A1/A2/B (panel energii,
  seed 596891) + audyt PR #126 (#128)`; drzewo czyste.
- `npm test` → **5787/5787, 0 fail** (zgodne z `HANDOFF_2026-09-18`).
- `node --test test/bot-benchmark.test.js` → **10/10** (regresja bota).
- Klon płytki (1 commit) — audyt wymagał `git fetch --depth=20 origin main`
  (bez tego `git diff b7ee3d5 e0ad776` = `bad revision`). Odnotowane w planie.

## Audyt PR #128 (ADR 0020 B / 0016) — raport `docs/audits/AUDYT_PR128_2026-09-18.md`

Zakres: 9 plików diffu `b7ee3d5…e0ad776`. Werdykt: **PR merytorycznie
poprawny** — root cause A1/A2/B trafny, energia pozostała licznikiem gracza
(ruling WotC), 18 pinów realnie czerwienieje.

**Weryfikacja u źródła (ADR 0030, dostęp 2026-09-18):**
- ruling WotC 2024-06-07 (Shipwreck Moray) „Energy counters are a kind of
  counter that a player may have. They're not associated with any specific
  permanents." — dosłownie zgodny z komentarzem w `render.js`
  (api.scryfall.com/…/rulings; ten sam wpis w
  `docs/cards/scryfall-shipwreck-moray.json` → 9 rulingów);
- marker „Energy Reserve" = tdrc/17, layout `token`, `image_uris.large`
  identyczny z użytym (api.scryfall.com/cards/6a2c1fa5-…); jest w `all_parts`
  Moraya;
- nośniki mechanik M387 potwierdzone ze snapshotów repo (Dire Fleet Ravager
  „Menace, deathtouch"; Ember Beast „can't attack or block alone").

**Mutacje (L13) — 10 gałęzi, każda czerwieni ≥1 pin:** m387: bez menace 3/2,
bez cantBlockAlone 3/2, bez cantBlock 4/1, kopia inline w declareBlockers 3/2,
pusty predykat 1/4; m386: powrót hover.attach 5/8 (= deklarowane 8/13
czerwonych), bez try/catch 12/1, bez onEnergyCardClick 12/1, bez wpisu
o wznowieniu 12/1; mutacja gałęzi fallback → 5785/2 w szybkim rdzeniu
(m169/N1 + partia forgotten-realms).

## Znaleziska i naprawy (każde: pin RED → fix → GREEN → mutacja → push)

| # | Klasyfikacja | Znalezisko | Naprawa (commit) | Pin |
|---|---|---|---|---|
| F-1 | średnie | trzecia kopia reguł zbioru bloku w gałęzi fallback `legalBlockerOptions`; strażnik M387/D skanuje tylko `declareBlockers` (L5/L113) | fallback woła `blockAssignmentViolation` (równoważne) | `429f564` · `test/audyt-pr129-kopia-zbioru-bloku.test.js` (2) |
| F-2 | niskie | siatka bezpieczeństwa `rerender` loguje ten sam błąd przy każdym renderze (log bez przycinania) | wpis tylko, gdy komunikat się zmienił (`lastRenderLogMessage`) | `69b3947` · `test/audyt-pr129-log-spam.test.js` (1) + okno M386/H 400→700 |
| F-3 | niskie | patch M386 skleił dwie instrukcje w `buildStateOverlay` (ADR 0016 B) | podział linii (bez zmiany zachowania) | `3ae56b9` |
| F-4 | dokument. | handoff podawał „5/13 RED", pomiar daje 8/13 czerwonych | sprostowanie z adnotacją | `b845fcf` |

## Pętla jakości (ADR 0021) — Żywy Tester + naprawa luki narzędzia

- 6 partii `ixalan vs ravnica/ixalan` (profile greedy/impatient/explorer/
  hoarder) — wszystkie kończą się naturalnie (`== KONIEC PARTII ==`), 0
  zgłoszeń detektorów, brak `[STOP]`/LIMIT.
- **Znalezisko narzędziowe:** snapshot `run-game.mjs` NIE wyciągał paneli
  liczników specjalnych (#poison/#speed/#energy) — warstwy, którą naprawiał
  PR #128. Klasa A1 („panel widoczny, ale pusty") była niewidoczna dla audytu
  na żywo (L27/L33 — braki testera naprawia się w testerze).
- **Naprawa (commit `849bf80`):** snapshot raportuje każdy WIDOCZNY panel
  specjalny; panel bez treści flagowany dosłownie („(widoczny, ale PUSTY)").
- **Potwierdzenie na żywo:** energia pojawiła się z TREŚCIĄ („LICZNIKI: energy:
  Gracz: 4 {E}…") — ścieżka naprawy PR #128 działa w realnym renderze; 158
  testów testera przechodzi po zmianie.

## Bramy końcowe

- `npm test` — **5790/5790**, 0 fail.
- `npm run build` — 64 moduły / **3832,5 kB**.
- `node --test test/bot-benchmark.test.js` — 10/10.
- Pełny B0 wyłącznie na komendę właściciela (ADR 0018) — nie uruchamiany.

## Co dalej (punkt zaczepienia następnej sesji)

- Otwarta pozycja z AUDYT_PR128: „kierunek odwrotny" — oferta buduje `rounds`
  z `Math.min(slots, 2)`, więc przy blokerze z >2 slotami walidacja przyjmie
  przypisanie nieproponowane w ofercie (kierunek BEZPIECZNY: oferta ⊆ walidacja;
  pin wart dodania, gdy pojawi się taka karta).
- Żywy Tester widzi już panele specjalne — kolejna pętla może celować w talie
  z trucizną/prędkością (dotąd niewidoczne dla narzędzia).
- Katalog kart NIE rośnie z inicjatywy agenta (ADR 0029); karty tylko z listy
  właściciela.
- PR #129 czeka na decyzję właściciela (scalanie należy do niego, ADR 0007/0020).
