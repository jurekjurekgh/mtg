# HANDOFF 2026-08-20 — M159 (audyt PR #66 + pętla jakości)

## Stan

- **PR #67** (`arena/01a02025-mtg`) — sesja M159. Commity: plan (e6ad69c),
  raport audytu (17a260d), fixy F1–F4 (094338c), Z1–Z3 (4b83d98),
  Z4 Saga-kafel (d401710), Z5 materialize (ae82570), **Z5b strażnik
  (cd2b9e6) + docs — czekają na push, GH_TOKEN wygasł** (objaw: push =
  „could not read Username"; reconnect GitHub w Arenie, potem `git push`).
- `npm test` **2491/2491**, `test:slow` 9/9, build **51 modułów /
  2127.4 kB**. Szczegóły: `docs/PROJECT_STATE.md` (M159).

## Co znaleziono i naprawiono (skrót)

- **F1–F2 (madness, CR 702.34e + L48):** rzut za madness poza main phase
  był odrzucany (crash bota przy cleanup-discard); oferta cast:true bez
  walidacji płatności. Fix: wyjątek timing + `canPayMadnessCost`
  (resources.js) + oferta warunkowa.
- **F3:** Revolutionist — cel ETB obowiązkowy (usunięte `optional`).
- **Z5 (najpoważniejsze):** czysty enchantment-Saga (Invasion of the
  Giants) gubił deskryptor `saga` w materialize (kopiowany tylko w gałęzi
  stworów) — karta nie robiła NIC na stole; testy batchowe czytały
  rejestr zamiast obiektu (L5/L21). Fix + generyczny strażnik Z5b
  (wszystkie deskryptory mechanik przez materialize).
- **Z1:** fingerprint uzupełniony (regenerationShields, cantBeRegenerated,
  lostKeywordsUntilEOT, subtypesBeforeOverride, madnessReady).
- **Z2/Z3/Z4:** kafle — martwy trigger 1v1 nazwany; strażnik pustych
  opisów „: ."; Saga renderuje rozdziały (I/II/III) na kaflu i podglądzie.

## Pułapki dla następnej sesji

- Strażnik Z5b (`test/m159-zywy-tester.test.js`) — nowa mechanika-pole
  na definicji karty MUSI przejść przez gameObjectDataOf w każdej gałęzi
  kind; czerwony test przed merge.
- Testy scenariuszowe Sag: scry na PUSTEJ bibliotece to no-op — test
  rozdziału I wymaga kart w library (pułapka z Z5a).
- Madness: `resolve_madness_cast` woła castPermanent — pierwsza karta
  INSTANT z madness będzie wymagała routingu po kind (obserwacja w
  raporcie audytu, nie budować spekulatywnie).
- `hasColorManaForObject` przy madness sprawdza pipy KARTY, nie kosztu
  madness — dziś bez skutku (Revolutionist {5}{R} vs {3}{R}), przy karcie
  o innych kolorach kosztu madness trzeba zawęzić (raport audytu).

## Kolejka

- Po reconnect: `git push`, aktualizacja opisu PR #67 (kumulacyjnie).
- Pętla jakości wg ADR 0021 (dopóki właściciel nie wskaże innego tematu):
  dalszy Żywy Tester (nieodpalone na żywo: madness-discard w partii,
  Wrap in Flames u bota, Dire Fleet Ravager ETB, Wishful/Hippocamp/
  Mesmerist) — transkrypty w `tools/table-tester/audyt-m159/`.
- Backlog (nie kolejka): wyceny REVIEWED z L28; Gianty w katalogu
  (Invasion III czeka na pierwszego Olbrzyma).
