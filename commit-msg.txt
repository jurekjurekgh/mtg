fix(engine): bronze badge — 5 bugs vs MtG rules

1. CR 702.16a — protection didn't prevent damage from protected color
   (DEBT: D=damage). isDamagePreventedByProtection checks source colors
   vs target protection in markDamage.

2. CR 702.16b — protection didn't detach existing auras/equipment of
   protected color. removeIllegalAttachments now checks attachment
   colors vs host protection (except own controller's).

3. CR 514.3a — cleanup doesn't repeat when triggers/SBA fire.
   Documented as known limitation (no cards in catalog need it).

4. declareBlockers didn't validate protection — canBlock was only in
   legalBlockerOptions (UI enumeration), not in execute validation.

5. Fertile Thicket "you may look" was not optional. Player can now
   decline (skip) or look and choose 0/1 land.

effectiveProtectionFromColors moved from permanents.js to
attachments.js (eliminates import cycle).

npm test 1153/1153, build 50/1268.3 kB,
benchmark 1080 matches 0 crashes (87.2%/71.4%).
